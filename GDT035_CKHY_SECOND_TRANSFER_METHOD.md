# GDT035 second CKHY semantic transfer

GDT035 is the one authorized second prospective test of the exact GDT034
hypothesis:

> CKHY is a parallel/fused leaf-or-stalk configuration descriptor.

The hypothesis remains outside the parser.  Failure rejects only this gloss;
it cannot alter CKHY's formal residual-host status.  No alternative CKHY
meaning may be searched during or after this test.

## Why the target is outside Herbal H

GDT033 queried every Herbal-B page and GDT034's reference calculation queried
every Herbal-A page.  No Herbal-H page can honestly remain CKHY-unexposed.
The second target therefore comes from the comparable pharmaceutical botanical
fragment material rather than relabelling an already exposed Herbal page as a
holdout.

The only target is logical page **f101v**, selected from visible geometry before
any GDT035 transcription query.  The exact witness is Yale IIIF canvas 1006250,
labelled `101v (part)`, at
`https://collections.library.yale.edu/iiif/2/1006250/full/1600,/0/default.jpg`
(JPEG SHA-256 `755dd2adce5242788b6112db6deb97f0a6f0778878a4bda78130c09e4499734b`,
1600 x 2241).

The neutral `AI_DIRECT_VISUAL_OBSERVATION` is: in the lower-left botanical
fragment, an upright green central axis carries repeated narrow leaf-shaped
strokes in two near-parallel lateral rows, and their bases visibly join that
axis.  This is classified as
`COMPARABLE_PARALLEL_OR_FUSED_LEAF_STALK_GEOMETRY`.  It is geometry, not a
botanical identification, component name, or semantic conclusion.

Canvas 1006250 was previously opened in the repository's complete
pharmaceutical ownership census.  That census did not query CKHY or score this
visual class.  Thus GDT035 is prospectively fresh with respect to the CKHY
query, but not image-pristine or observer-blind.

## Frozen binary prediction

The only query is whether logical page f101v has at least one source group in
the frozen GDT016 inventory whose `residual_host` is exactly `ckhy`.  The only
admitted surface forms are the existing four:

- `ckhy`
- `chckhy`
- `checkhy`
- `shckhy`

No fuzzy match, new prefix, related host, alternative page, exact-label rescue,
or component-level reassignment is permitted.

- At least one exact host: `PASS_SECOND_SEMANTIC_GLOSS_PROVISIONAL_TRANSFER`.
- Zero exact hosts: `FAIL_SECOND_SEMANTIC_GLOSS_REJECTED`.

The query is page-level.  A hit cannot assign CKHY to the selected fragment,
leaf row, axis, label, or prose field because no authorial ownership relation
is established.  f84r remains sealed and is not opened, retained, queried,
joined, or scored.
