# GDT250 — `okaly` semantic hypothesis

## Result

**OKALY_PROVISIONAL_FIGURE_CLASS_DESCRIPTOR_TWO_FOLIO_HYPOTHESIS_OLKY_OBJECT_GLOSS_FAILED**

Among the two exact whole-host label/prose bridges, `okaly` has the first
coherent external class pattern:

| PAGE_HOST | annotated labels | physical folios | coarse evidence | result |
|---|---:|---:|---|---|
| `okaly` | 4 | 2 | four figure-tagged; three astronomical on f72, one q13 figure-position on f80 | provisional figure-class descriptor |
| `olky` | 2 | 2 | f80 figure-position, f99 plant-position | object-class gloss failed |

The exact `okaly` form labels three astronomical figures on physical folio f72
and occurs above a figure on f80r. It also occurs verbatim inside f80r prose
and has 18 published prose contexts across 15 folios, so a class descriptor or
entity-address function could naturally appear in both graphical and prose
registers.

## Provisional role

```text
okaly -> FIGURE_ASSOCIATED_CLASS_DESCRIPTOR  [PROVISIONAL, LOW INDEPENDENCE]
```

This is deliberately broader than a figure name, astronomical body, person,
or gender. It says only that the same opaque host may license a recurring
figure-associated label class.

## Why it remains weak

Three of four annotations come from one physical folio and are hedged. The
only unhedged exact-local row, f80r.3, is proximity-only rather than secure
ownership. Section ecology can explain the three astronomical labels, and the
host's broad prose distribution has no stable external referent. The result is
one two-folio hypothesis, not four replications.

`olky` is the necessary falsifier: its exact labels cross figure and plant
classes on independent folios, so no stable coarse object gloss survives.

## Frozen future prediction

On a future visual-first, independently selected graphical label, exact
PAGE_HOST `okaly` predicts a `FIGURE` association rather than `PLANT`,
`CONTAINER`, `LINE`, or `STAR_ONLY`. The prediction must not be broadened after
the target is read.

## Claim ceiling

Hypothesis generation only. No ownership, figure identity, object name, word,
language, plaintext, or translation is established. No f84 input was read or
retained and no f84 result was scored.
