# GDT081 — HPR4 context-class decomposition

Status: **YOLO source-only formal-class discovery**

The frozen HPR4 renderer class `{d, ok, yk, yt}` may conflate distinct field
functions.  Compare its four possible leave-one-host-out trios using
register-specific PAGE_HOST context distributions.  The primary context
excludes RIGHT_FAMILY, the feature that selected HPR4:

`WRAPPER × INNER_D × O/OT_FRAME × DY × B3 × POSITION_QUARTILE`.

Similarity is cosine similarity between event-count vectors, averaged over
the three host pairs and five fixed registers.  Hosts must have at least 20
occurrences, three physical folios, and three occurrences in every register.
The search correction is the maximum trio score from each of 50,000 random
four-host sets matched to HPR4's occurrence-frequency quartiles.  Report
wrapper/frame/position, wrapper-only, position-only, and RIGHT_FAMILY-included
sensitivities.  The latter is circular and cannot establish a new class.

This is postselected parser-coordinate discovery.  The frequency null cannot
match HPR4's nearly unique high-`aiin` selection path, and shared absence of
optional compiler layers can inflate similarity.  A result may freeze an
abstract formal context class, never content, a role, a morpheme, or meaning.
f84r is excluded from the source inventory and is not opened or used.
