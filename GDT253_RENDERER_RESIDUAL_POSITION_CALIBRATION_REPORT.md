# GDT253 — renderer/residual position calibration

This report is generated alongside `gdt253_result.json`.  The exact numerical
result is filled from the reproducible scorer; its interpretation is fixed:

- a max-search value near 0.05 is a borderline exploratory lead, not a
  validated positional code;
- failure to reproduce the exact member renderer in any alternate reading is
  a transcription-instability counterexample;
- the broader family `AQABAB` occurrence at f70v2 slot 3 remains a direct
  counterexample to reading the family or residual alone as position 4.

The claim ceiling is a catalogue-position association of a complete formal
construction.  No number, degree, direction, zodiac value, word, language,
plaintext, or translation follows.  No f84 input is used.

## Reproducible result

Status: **GDT252_POSITION_LEAD_MAXT_BORDERLINE_AND_RF_READING_UNSTABLE_NO_POSITION_KEY**.

The ZL3b max-search statistic is **2 folios**, with independent renderer/residual p = **0.081084**. Conditional on intact pairs, the position-shuffle p is **0.085219**. The exact member-prefix pair reproduces in ZL3b and IT2a but not RF1b.

The result is therefore a **weak borderline renderer-plus-residual positional lead**, not an executable key. The strongest next evidence would be a prospectively selected occurrence of the same full construction at the same homologous slot on a new independently inventoried folio.

No number, degree, direction, word, language, plaintext, or translation is assigned. No f84 input was used.
