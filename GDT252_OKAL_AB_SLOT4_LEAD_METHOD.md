# GDT252 — `okal + AB` slot-4 lead method

This exposed YOLO pass asks whether any repeated GDT251 right residual aligns
with an independently inventoried special-circle slot. It finds one candidate
and reports the complete comparison rather than freezing it retroactively.

Eligible targets are all eight human-inventoried 10-slot arrays. The candidate
construction is the full visible `okal…` renderer plus transferred `AQAB`
prefix and strict residual `AB`; the residual alone is also checked. Slot
indices and Kluge homologs come only from the frozen text-blind array inventory.

The search is post-hoc, the array phase is editorial/human-catalogued rather
than a proven authorial degree coordinate, and no p-value is promoted. No f84
input was read, retained, joined, or scored and no new f84 access occurred.
