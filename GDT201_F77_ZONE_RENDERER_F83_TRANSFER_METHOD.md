# GDT201 — f77 zone-renderer transfer to f83r

## Frozen prediction

GDT200 found, post hoc on f77r, that its two upper-tube endpoint figure labels
start `d` and its two middle/lower apparatus figure labels start `ot` in every
reading.  Apply that exact directional rule without repair to the already
published, geometrically comparable f83r four-label panel:

- `f83r.45/.46`: `ARCH_END_NEAR_FIGURE_AND_TUBE` → predict `STARTS_D`;
- `f83r.50/.51`: `LOWER_STRUCTURE_OUTLET_NO_LOCAL_FIGURE` → predict
  `STARTS_OT`.

The four target loci and visual classes were fixed in GDT002 CKPT012.  Their
formal values were already exposed, so this is an archived fixed-rule transfer,
not a pristine blind test.  Alternate readings remain one manuscript.

## Decision

Report all four exact predictions.  Any miss prevents transfer; no suffix,
substring, parser layer, reverse orientation, or target-specific repair is
allowed.

The test addresses only the f77 page-zone renderer hypothesis.  It assigns no
ownership, direction, process stage, word, sound, language, plaintext,
meaning, or translation.  f84r and every f84 row are excluded.
