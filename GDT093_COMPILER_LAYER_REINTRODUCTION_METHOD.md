# GDT093 — reintroducing compiler layers into PAGE_HOST content profiles

Reuse the exact GDT089 85-locus plant panel, descriptor manifest, `K=5`, and
shrinkage `4`.  Compare PAGE_HOST trigrams with the same representation after
adding WRAPPER, RIGHT_FAMILY, B3, or WRAPPER+RIGHT state tokens.  Leave one
physical folio out.  Permute complete descriptor vectors within folio 5,000
times and correct over all 8×5 tested representation cells.

The test asks whether compiler layers improve or dilute an already identified
external signal.  It does not treat dilution as proof of semantic neutrality.
f84r remains absent.
