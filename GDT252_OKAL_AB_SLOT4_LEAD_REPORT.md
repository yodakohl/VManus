# GDT252 — `okal + AB` slot-4 lead

## Result

**POSTHOC_OKAL_PLUS_AB_SLOT4_OF_10_POSITION_LEAD_TWO_FOLIOS_NOT_VALIDATED**

Two independent 10-slot astronomical bands contain the same full renderer/
residual construction at the same homologous slot:

| folio | array | label | family | slot | catalogue homolog |
|---|---|---|---|---:|---|
| f70 | f70v1 outer band | `okalal` | `AQABAB` | 4/10 | Kluge 09A |
| f72 | f72r1 outer band | `okalam` | `AQABAB` | 4/10 | Kluge 09A |

The final source member differs, but the source-native family is identical.
This supports one concrete provisional role:

```text
visible okal renderer + family residual AB
    -> POSITION_4_OF_10 / HOMOLOGOUS_SLOT_09A
       [POST-HOC, TWO FOLIOS]
```

## Counterevidence

Only two of seven formally covered slot-4 labels in the eight 10-slot arrays
use the construction, so it is not a universal ordinal. More decisively,
`otalam` at f70v2.25 has the same whole family `AQABAB` but occupies slot 3/10:
the residual/family alone cannot mean position 4. Any signal requires the full
outer renderer plus residual interaction.

The named Kluge 09A homolog is also only 2/4: f71r.5 (`oteolar`) and f71v.5
(`otchody`) occupy the same homologous slot without the construction. Thus the
pair has high descriptive precision but only half the directly comparable
homolog coverage.

The matching slot was noticed after the renderer census; conditional on the
first position, a second random position matches it with probability 1/10, but
that is not search-adjusted across renderer/residual/topology choices. The
catalogue alignment is not an authorial numbered-degree phase.

## Translation consequence

This is the first specific positional-role lead surviving the corrected q13
work, but it is too small and postselected to enter the active translation.
It suggests that complete renderer+residual combinations—not isolated hosts or
suffixes—may encode homologous diagram slots.

## Claim ceiling

Post-hoc position hypothesis only. No number value, zodiac degree, word,
language, plaintext, or translation is established. No f84 input was read or
retained and no f84 result was scored.
