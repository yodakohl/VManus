# GDT002 targeted exposed-data transfer

Status: **AQA DOES NOT TRANSFER; ACA REPEATS DIRECTION BUT IS NOT VALIDATED**.

This checkpoint tests only two formal predicates already selected and
published by CKPT009. It performs no new predicate search, opens no manuscript
image, creates no new visual observation, and does not inspect the sealed f84r
formal payload. Every visual state comes from an existing human annotation.

The target pages are not fresh confirmation data. f75v and f67r2 were consumed
by earlier label/edge investigations, while f77 and f82 have extensive prior
visual and formal exposure. Their reuse is legitimate YOLO exploration but
cannot supply a confirmation claim.

## Fixed `AQA` row/register predicate

The discovery association was 4/4 `AQA` prefixes in the formal-covered f82r
upper row versus 0/6 in its lower row. Two exposed layouts fail to reproduce
that contrast:

- f75v's ten paired upper/lower inscriptions contain `AQA` at 1/10 upper and
  1/10 lower positions. The effect is exactly zero; a within-pair swap null
  gives one-sided p=.75 over 1,024 worlds.
- f67r2 has `AQA` at 3/6 formal-covered upper isolated positions and 5/10
  formal-covered lower moon-adjacent positions. Both rates are .5; an exact
  within-page state permutation gives p=.69580 over 8,008 worlds.

Therefore the f82r `AQA` result remains a page-local row association, not a
transferable row/register predicate.

## Fixed `ACA` apparatus predicate

The source-only inclusion rule takes every non-f84r, unhedged exact human
annotation whose tags exclusively identify either `WATER_OR_APPARATUS` or
`FIGURE`, then retains source-native consensus label records (`kind=L`). It
does not select rows from their Voynich forms. One-sided pages remain in the
published join; f77r and f82r are the only pages with both states and therefore
define the exact within-page comparison.

- f82r, the discovery page: `ACA` occurs at 3/3 apparatus positions versus
  1/8 figure positions; effect +.875.
- f77r, the exposed transfer page: `ACA` occurs at 1/6 apparatus positions
  versus 0/4 figure positions; effect +.1667.

The mean within-page effect is +.52083. Preserving each page's apparatus count
gives a one-sided exact p=.01455 over 34,650 assignments. The two-page raw MDL
gain is 4.32193 bits and remains +3.32193 bits after paying one bit for the two
fixed candidates.

A two-test Holm value would be .02909, but that does not erase the earlier
808-rule exploratory postselection that produced these candidates.

That attractive pooled result is not a validation. The independent f77r page
alone has p=.6 and a negative page-only MDL gain (-.13750 bit). It supplies a
same-direction occurrence, not material independent evidence; the pooled score
is driven by the already-selected f82r page. The candidate is retained as
`INTERESTING_EXPLORATORY_DIRECTION_ONLY_NOT_VALIDATED`.

## Next decision

Do not spend the f84r holdout yet. The useful next acquisition is one
independent, provenance-clean page with multiple apparatus-associated and
figure-associated labels selected without reading their forms. Freeze `ACA`
presence and the current exact human-tag inclusion rule before that formal
payload is opened. Only a same-direction, positive held-page MDL result would
justify an f84r prediction.

Semantic roles remain `UNASSIGNED`. This checkpoint establishes no object
name, relation word, POS, sound, language, plaintext, meaning, or translation.
