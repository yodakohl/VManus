# GDT123 — Q20 exact PAGE_HOST codeword atlas

Status: `EXPLORATORY_YOLO_POSTSELECTION_CONTROLLED`

## Question

GDT122 found that a pooled PAGE_HOST trigram model does not predict archived
Q20 star morphology.  A content codebook could nevertheless be sparse: one
recurrent exact PAGE_HOST might mark a state while the global vocabulary is
mostly irrelevant.  GDT123 therefore searches exact PAGE_HOST presence without
assigning a meaning.

## Frozen panel and candidate library

Reuse the 156 provenance-bound Stolfi/Q20 units and the same 7/8-ray,
one/two-tail, and red/yellow-center axes.  For ZL3b, enumerate every exact
PAGE_HOST occurring in at least five records on at least two physical folios
in each of three scopes: OPEN, BODY, or OPEN+BODY.  The ZL3b library is then
frozen and evaluated without rediscovery in IT2a and RF1b.

Each record is one binary presence observation per host/scope.  Repetition
inside a record does not multiply evidence.  Alternate readings are
sensitivities of one manuscript.

## Association and null

For each visual axis residualize both the host indicator and binary visual
state against page fixed effects, parity, normalized ordinal, records per page,
page side, record line count, and OPEN/BODY group/member counts.  Score signed
partial correlation and its Gaussian likelihood improvement.  Charge a selector
over every host×scope×axis and both signs.

The exact null permutes complete formal-record profiles within page and exact
OPEN-member-count strata.  It preserves each host's frequency/co-occurrence,
page ecology, record lengths through nuisance adjustment, visual states, and
the full tried library.  Use 4,096 shared worlds and maxT over all candidates.

Report local/maxT p, selector-paid gain, leave-one-folio direction count,
same-host reading effects, and record-mask Jaccard.  Labels are
`INTERESTING_EXPLORATORY`, `WEAK`, `LIKELY_PAGE_CONFOUND`, `UNSTABLE`, or
`NO_SIGNAL`; they rank hypotheses rather than confirm or kill meanings.

The old exact-host external atlas failure, archived SME calibration failure,
and GDT119/GDT122 global negatives remain visible controls.  f84r is absent and
not opened, retained, queried, joined, scored, targeted, assigned, or predicted.
No host receives a visual meaning, semantic role, word value, morpheme, POS,
sound, language, plaintext, or translation.
