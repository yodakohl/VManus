# GDT129 — exact `q… | sheedy` ray-class synthesis

## Question

Does the exact adjacent two-group construction `q* | sheedy` recur in Q20
records with the same coarse 7/8-ray visual class?

This is a deliberately narrow **post-reveal synthesis** after the split GDT128
transfer. GDT128 itself was publicly frozen before its visual reveal; the
archive-wide 15-record census here was enumerated afterward. The eligible
descriptive universe is every archived GDT127 ZL3b record containing exact
group `sheedy`; the primary formal predicate requires an immediately preceding
source group beginning with `q` inside the same HPR2 field. ZL3b/IT2a/RF1b
agreement is reconstructed separately and may not be represented as three
replications.

Report exact hypergeometric diagnostics conditional on the `sheedy` universe,
the narrower two-group field control, page-conditioned contrast capacity, and
all counterexamples. The prospective target is one postselected observation,
so combined p-values are descriptive only. No alternative suffix, gap, wrapper,
ray threshold, tail rule, or color rule is selected.

The experiment can nominate a coarse primary-reading formal/visual codeword lead. It cannot
assign a number, star property, role, word, morpheme, sound, language, plaintext,
meaning, or translation. f84r remains sealed.
