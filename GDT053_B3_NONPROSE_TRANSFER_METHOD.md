# GDT053 — B3 transfer to human-annotated nonprose units

This tests frozen HPR-2 prediction `HPR2_P01` without new images. Join the
existing GDT012 exact human-annotated `DIAGNOSTIC_NONPROSE` groups to the
source-native consensus group table. Retain only loci whose complete group set
is present and whose final STA member agrees across ZL3b, IT2a, and RF1b.

Within every physical label/unit locus, permute a tested final-member class
over the observed group positions while fixing its count. Rank B3 against all
stable final-member classes with at least 20 occurrences; correct over that
complete atlas. Single-group units remain legitimate endpoint observations
but supply no permutation contrast, so informative multi-group loci and
precision are reported explicitly.

This is transfer of a formal endpoint distribution, not a claim that B3 is a
word, punctuation, label marker, or semantic role. f84r is excluded before
the source join.
