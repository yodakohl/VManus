# IDEA000560: five-group fire qualification draft

2026-09-26. Fifteen-minute bounded authoring task. **The frozen raw560 grammar is INCOMPLETE:** after `otar chepaiin`, G010 has no licensed respect value, and G011/G012 provide no declared negative operator or property. A syntax-only extension cannot supply those missing meanings; `ot(odar)` would be ill-typed. This is a missing-binding result, not UNSAT.

**Proposed extension:** a complete local C0 fire/light/nonburning hypothesis is possible with three openly guessed whole entries and one universal qualification construction. Root explicitly authorized this exploratory addition; none of its new values is already licensed by the old card or proved by the source. The five-group draft is offered for review, not selected or semantically confirmed. No full thirty-five-group continuation was written.

Frozen packet: [resemblance_fire_draft/DRAFT.json](resemblance_fire_draft/DRAFT.json); complete exact assigned-form inventory: [ASSIGNED_OCCURRENCES.tsv](resemblance_fire_draft/ASSIGNED_OCCURRENCES.tsv). The raw card, independent selection review and root source check are hash-referenced in the packet. The twelve old whole types, three literal `ot` derivations and `ot` law are retained. No meanings are inherited from the unrelated 537/558 drafts.

## The exact local hypothesis

ZL `.1 G008–G012` is `otar chepaiin otodar otodaiin opaiin`. Its four internal separators are definite spaces. Its endpoints are **not** manuscript sentence boundaries.

| Group | Written role and proposed meaning |
|---|---|
| G008 `otar` | Existing FIRE-comparison head, requiring a subject and a respect set. |
| G009 `chepaiin` | Existing explicit subject: the eleventh heaven. |
| G010 `otodar` | **New atomic whole:** respect set `{LIGHT}`. This is not derived from `ot(odar)`. |
| G011 `otodaiin` | **New written operator:** take the complete left comparison record and the right property name; append denial of that property for the record's explicitly written subject, retaining every earlier assertion. |
| G012 `opaiin` | **New atomic whole:** the property coordinate BURNS, with no truth value by itself. |

The finite construction is `QualifiedComparison := Comparison Qualifier Property`. Its semantics is uniform: the **written qualifier** projects the left record's subject, negates the named right property and preserves the comparison. It works with any well-typed comparison and admitted property; it has no special H11, FIRE, position or paragraph exception. This is an explicit new subject-reference operator, not a silent reuse of a preceding noun.

The proposed parse is:

`NegativeQualification(Compare(FIRE,(H11,∅),{LIGHT}), BURNS)`

It asserts `LIGHT(H11)=PrototypeLIGHT(FIRE)` and `BURNS(H11)=false`. With the newly declared literary prototype premise `PrototypeLIGHT(FIRE)=true`, its readable sense is: **“The eleventh heaven resembles fire in light, while that same heaven does not burn.”** Every group has one compositional role. The negative operator supplies the repeated subject; no second unwritten heaven, free NOT, time, condition or entity/predicate cast is inserted.

## Cost and the important objection

The source check supports a historical shining-like-fire comparison with an explicit nonburning qualification. It makes this *statement* plausible in the medieval source. It does not identify any of the three strings or the proposed construction. See [RESEMBLANCE_SOURCE_CHECK.md](RESEMBLANCE_SOURCE_CHECK.md).

This draft costs **three new independent whole entries**, **one new FIRE/LIGHT profile cell**, **one combinatory schema plus three terminal production extensions**, and **two authorial local boundaries**. The one qualifier entry performs three separately disclosed semantic operations: subject projection, property denial, assertion preservation. Two new atomic entries begin with `ot` but have no internal analysis under the frozen constructor. They are a conspicuous lexical exception cost, not evidence for general composition. Fifteen whole types are now assigned, still with zero independent lexical confirmation.

Review B explicitly warned against guessing `otodar` into LIGHT as though it were already licensed. **A formal parse does not prove this assignment.** Under root's clarified authoring permission, the candidate assigns the previously unbound whole a new C0 value, records the cost, and freezes its meaning at every occurrence. With the original meanings plus new syntax alone, the answer remains INCOMPLETE. After `otar chepaiin`, G010 has no licensed respect value; `ot(odar)` is ill-typed because `odar` is already a RespectSet rather than a PrototypeKind. G011/G012 have neither a declared negative operator nor a property meaning in the raw packet. Syntax alone cannot insert LIGHT, BURNS and negative polarity without paying for semantic carriers. This is an exact missing-binding gap, **not UNSAT** and not a contradiction in the manuscript.

## The strongest designation-only rival

Keep the same grammar, other fifteen-form bindings, FIRE/LIGHT premise, ownership and written negative qualifier. Change only the inherited `ot` law to record `DescribedBy(subject,kind,respects)` without property-equality entailments. The same target now says: **“The eleventh heaven is described as fiery in the respect LIGHT, while it does not burn.”** This fits the source's explicit fiery designation and preserves nonburning.

It does not itself entail that H11 shines. A model with FIRE.LIGHT true, the fiery designation true, H11.BURNS false, and H11.LIGHT false satisfies the rival's target assertions. Thus its local recovery of the selected positive-light obligation is **incomplete**. It is not false: a model with H11.LIGHT true satisfies the same rival and agrees with the source. Source background cannot be silently counted as a separately written target assertion. Conversely, the main comparison does not separately assert a speech event or NAMED predicate; if that exact source nuance is required as its own obligation, it remains unexpressed in the main.

The main therefore has stronger positive-light entailment under its added assumptions; the rival has a more direct designation reading. This is a concrete consequence difference, not a selected decipherment. No designation rival is falsified here. A later written positive LIGHT assertion could complete it, but no such continuation is supplied in this bounded task.

## Repetitions, alternates and stop

The complete owned projection was checked by literal final-field matching. `otodar` occurs only at ZL `.1 G010`; `otodaiin` is `.1 G011` in all three readers. `opaiin` occurs at ZL `.1 G012` **and G034**, and RF `.1 G012`. Its G034 meaning is frozen as BURNS:PropertyName; its operator and subject there remain unparsed. This live repeated-form obligation prevents treating it as a convenient one-off entire sentence. Every old assigned occurrence is also listed, including the unresolved comparison head `otchedy` at ZL/IT E.8. No whole-line completion is claimed from an occurrence list.

IT's five forms are `otar chepaiin otadar otodaiin opoiin`; its G010 and G012 remain unbound. RF has `otar chepaiin ot@221;dar otodaiin opaiin`; its G010 remains unbound. Neither form is aliased to ZL `otodar`, and `opoiin` is not aliased to `opaiin`. The readers are alternate transcriptions of one witness.

**Decision:** freeze this explicitly costed local candidate and the stricter raw-grammar gap for root review. Do not fill the remaining thirty groups or start a decoder. The full .1 reading, repeated G034 ownership, other exact assigned occurrences and independent meaning selection remain unfinished. The task adds no target-meaning confirmation.
