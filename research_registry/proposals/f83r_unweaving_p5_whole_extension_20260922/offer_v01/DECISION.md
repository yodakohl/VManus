# IDEA000520 — P5 whole-extension decision and capacity freeze

Freeze clock: `2026-09-22T17:42:53Z` from `date -u` in the freeze script. This is a source/capacity record, not a result.

## Ordering disclosure

The P5 projection is an already stored owned source. During metadata inspection before this note, its existing target record was printed, so `body_read_before_this_freeze=true`. No new source, image, reserve or external material was acquired. This note must not be described as a pre-body freeze; it records the actual clock and preserves the deviation.

## Frozen target and readers

- Target: `F83_P5 f83r.31–f83r.44`, complete ZL3b projection, 62 groups and 53 exact types.
- ZL metadata: 14 lines, first start=true, last end=true; two diplomatic projected forms remain unbound (`[?:s]cheol` and `so[r:s]`).
- IT scope: the enclosing f83r.25–44 paragraph has 92 groups; the corresponding .31–.44 lines have 60 groups and are not an independent complete IT paragraph. RF has no complete target reader.
- Source and primary hashes are in `DECISION.json`; the owned projection source is P12 `READING_L1.md`. Line boundaries are source/report boundaries, not independently recovered historical sentence boundaries.

## Fixed material before P5

The preceding unweaving account remains its full 96 positions with all 64 values, constructions, bindings and rivals unchanged. The exact 24 parent values and 40 added values are preserved in `DECISION.json` under `all_64_fixed_values`; no P5 repair may change them. The new obligations are four `lchedy` KNOWLEDGE occurrences, two `shey` epistemic negations, and two `chedy` FINISHED occurrences. One uniform negation scope and one uniform AFTER_COMPLETION/RECAP_E scope are required.

| class | count |
|---|---:|
| fixed parent values | 24 |
| fixed new unweaving values | 40 |
| fixed total | 64 |
| old positions retained | 96 |
| P5 projected groups | 62 |
| P5 projected types | 53 |
| P5 frozen positions | 22 |
| P5 unassigned positions | 40 |

## Caps and prior music exposure

- At most 38 new exact whole values, 4 genuinely reusable productions, and 6 nondefault bindings; aliases and packing remain zero; no default phase escape.
- The P5 record was previously exposed as a music/rota account in RAW373/RAW364/ROTA. GDT578 corrects RAW440’s music description and GDT970 closed a concatenated full-part writer. This prior project material remains a disclosed rival/source context, not a dictionary or semantic evidence. No new score, image or source is admitted.

## Retained counterexamples

- GDT1020 leaves both old schedules coherent; discovery does not entail a nighttime action.
- GDT1025 contradicts the reversed shared-frame case only under an added same-action identity; its separate-action diagnostic remains coherent.
- The strongest P5 countercase is that the four `lchedy` occurrences may be quoted knowledge topics rather than asserted epistemic conditions.
- No music account may be selected or silently merged into the unweaving meanings.

## Development rule

The whole account must distinguish a new unmarked assertion after completion (`AFTER_COMPLETION`) from one explicitly written recap scope (`RECAP_E`). It must preserve every informative edge and nonglossed case, use one uniform negation rule for both `shey` cases, and make all 158 positions (96 inherited plus 62 P5) visible. If four productions or six bindings cannot carry those dependencies without one-off patterns or arbitrary per-occurrence phases, stop with exact debts. No numeric replay or semantic result is authorized.
