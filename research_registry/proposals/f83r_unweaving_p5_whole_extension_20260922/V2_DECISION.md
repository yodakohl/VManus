# IDEA520 V2 revision note

V1 stopped at a positional ledger with 38 P5 forms and 40 positions left unbound, even though the authorized cap allowed 38 new whole values. That was an incomplete use of the budget: it exposed the uncertainty but did not attempt the requested content account.

V2 preserves V1 byte-for-byte under `offer_v01/`, then assigns every one of the 38 forms a distinct provisional whole value. It composes all 62 groups with four reusable typed productions and six explicit bindings. The selected global context is AFTER_COMPLETION. Present `lchedy` knowledge and both `shey` negations are separated by an explicit inherited earlier time E, so the negation is a retrospective claim rather than an accidental reversal of the completed state.

These are fitted, unconfirmed meanings. The complete IT2a 92-group unit and RF1b availability status are retained separately; alternate-reader data does not receive new semantic assignments or increase the cap.
