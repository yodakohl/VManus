# IDEA527 capacity stop

Clock: `2026-09-22T18:19:32Z`. The exact cached target is f31r.6–.9, 27 groups in each complete reader. The inherited parent is 37 groups, so the whole would contain 64 positions.

The fixed parent has 30 candidate-local whole values. Exact-form matching gives:

- ZL3b: 24 forms; inherited exact forms `aiin, chedy`; **22 new exact forms**.
- IT2a: 23 forms; inherited exact forms `aiin, chedy, r`; **20 new exact forms**.

The declared cap is 14 new whole values. Both readers therefore fail before semantic construction. Assigning the excess forms to old values would violate exact candidate-local values; aliases, packing, normalization, fallback paragraphs, and cap increases are prohibited. No semantic continuation, consequence, or execution is claimed. The exact two-reader source, 37-group parent, all eight quantity×knowledge×purity candidates, and hashes are preserved in the adjacent JSON files.

The nomination also requires an exact `qokeey` KNOWS occurrence and at least one exact old person/proposition form from `{daiin, [s:r]air, chey}`. Neither selected reader contains `qokeey`; neither contains one of that required set. This independently closes the gate.
