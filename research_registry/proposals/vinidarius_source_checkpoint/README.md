# Vinidarius source checkpoint

Complete source capture, **incomplete semantic compiler; no target fit**.
The independent A/M packets preserve all six inventory segments, 31 electronic
recipe segments and the storage postlude. Their 1,206 nonordinal content tokens
and 11 postlude tokens agree exactly. Original annotations remain unchanged.

The malformed I.2 continuation is recovered. A native 1922 edition confirms
its boundary, but joins XXI–XXII editorially: 31 is Harsch's electronic
segmentation, not an edition-independent recipe count. Its exclusive base
edition is unknown; neither edition silently substitutes for the other.

M's reconciliation identifies twelve material compiler decisions. In particular,
five written amulo ingredient occurrences must not merge with the verb amulabis;
addes/addis and adicio forms are not merely inflections of one verb. Atom identity,
part/state attachment and the serialization of overlapping quantity/clause
spans are still undeclared. Missing fraction bases may stay explicitly unbound.
No literal-copy shortcut or partial-ingredient fit follows this checkpoint.

Both readers support the local malva/beta alternative distinction and the
surviving unordered amygdala/aballana symmetry. This is not a full orbit result.
B's original policy receipt missed literal AVT in a regex; its separate correction
preserves the original and accounts for nine coordination operators plus three
Alias/ALITER markers. No semantic reading was changed by that correction.

Download the two source URLs in SOURCE_BINDINGS.json as pantry.html and
recipes.html in a chosen cache. Run `python3 validate_local_witness.py CACHE`
and `python3 align_sources.py CACHE` from this directory. Both verify the exact
source hashes; the latter reproduces ANNOTATION_ALIGNMENT.json byte for byte.
SOURCE_EDITION_B.json gives the native edition URL, hashes and page provenance.
These checks certify source accounting and the local witnesses, not complete
semantic annotation, a manuscript hypothesis or any translated word.
