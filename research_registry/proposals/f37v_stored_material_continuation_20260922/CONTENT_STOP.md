# IDEA000522 — complete source alignment and lexical-capacity stop

The exact nominated cache records were retrieved only after `DECISION.md` and
`INHERITED_FREEZE.json` froze the metadata, old values, reader coverage and
capacity. `SOURCE.json` retains the complete ZL3b and IT2a records; `GROUPS.json`
retains every source ID, line, offset, separator boundary and whole form.

The cap fails before a new semantic construction can be honestly written. ZL3b
contains 22 exact types, of which six are inherited old values and one (`dor`)
is already retained from RAW512. Sixteen additional exact ZL forms would need
new entries. IT2a contains 20 exact types, seven inherited/retained and thirteen
additional exact forms. The union has 19 additional exact forms. The frozen
maximum is 12. The forms remain listed in `LEXICAL_INVENTORY.json`; none is
assigned a repair gloss, alias, split, or packing rule. No new production or
binding is added to evade this stop.

## Complete raw continuation

The physical line partition is a source alignment, not a claim that line ends
are semantic punctuation. The working whole-unit lines are:

| locus | ZL3b exact groups | IT2a exact groups |
|---|---|---|
| f37v.8 | `qotor choiin chetchy daiin` | `qotor choiin chetchy daiin` |
| f37v.9 | `dor chor sho daiiin daiin` | `dor chor sho daiiin daiin` |
| f37v.10 | `soiin {ch'}ey o koiin chey tom` | `soiin shey otoiin chey tom` |
| f37v.11 | `qotoiin choror cthol daiin` | `qotoiin choror cthol daiin` |
| f37v.12 | `chor sh[y:o]ly sheaiin do tody` | `chor sholy sheaiin dotody` |
| f37v.13 | `sotoiiin` | `sotoiiin` |

The inherited terminals visible in this unit are `chor=POWDER`, `sho=WATER`,
`daiin=MASS_UNIT_Q`, `cthol=PASTE`, `sheaiin=OVERNIGHT`, and IT-only
`shey=THEN`; `dor=UNTIL` is a retained RAW512 exact entry. Their presence does
not determine the unknown neighboring arguments. For example, `dor` begins
line .9 without a target-local operation that can be safely supplied, and
`sho` does not by itself create an operation or a new free water stock.

## Conditional material account and rivals

The inherited incoming object is one lineage `M` in a stored paste carrier `T`.
Its plant-solid mass is `Q`, with uncomminuted and coarse grades zero and fine
grade Q. Physical retained water is conditionally zero after the predecessor's
DRY step, while the Boolean `DRY_STATE` result is unresolved. ROOT/LEAF
visibility after CRUSH, GRIND and WORK_INTO_PASTE is also unresolved. There is
no free powder stock and no fresh plant stock in this state ledger.

Under `CARRY_STORED`, a compatible target reference must consume or describe
this same `T/M` lineage. Any withdrawal, transformation and resulting state
must be written by a complete construction; the repeated `daiin` token cannot
create a second Q supply. `chor` and `cthol` remain distinct POWDER and PASTE
kinds, and the known `sho=WATER` token does not establish that a water operation
occurred in this unit.

Under `NEW_RECIPE`, the paragraph could begin another preparation only if an
explicit input constructor supplied an initial quantity and state. No such
constructor can be identified without assigning the 19 over-cap exact forms.
Under `TYPE_DESCRIPTION`, the unit could make a preparation-kind assertion
without withdrawing material, but that requires a reusable assertion
construction whose arguments are likewise unavailable within the frozen cap.
These are retained rivals, not selected readings.

The result is `CAPACITY_STOP` with complete source coverage and explicit
material debts. It is not a semantic failure or a compatibility pass. No
numeric replay, simulator, image access, source-only substitute, registry
update, ledger edit, route edit, reserve access or new word assignment was
performed.
