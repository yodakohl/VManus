# W31 registered joint chey polarity and patient reparse
Before reparse/run; exposed development. Budget25minutes including implementation validation publication. W30 found8scopes7commands1quality and stopped; no W30 result changed. W13 supplies existing requal, W11/W05 existing argument solver; W28 current lexical baseline. Unknown: after removing chey as a check, does unchanged grammar still bind the negative quality, and what full-paragraph consequences follow from all8negative scopes?
Only whole chey changes ACTION to NEGATION (neither material, action nor new transparent-role member). Reapply existing W05/W11 arguments and W13 qualities to ALL17paragraphs1045groups; no new fallback patient or line boundaries. Recalculate existing W14 input timing. Keep W30 rightward scope with literal sol/qokal barriers, same executable predicate inventory except removed chey and parity. Every chey remains in full word alignment and scope table even though no check event is emitted. Q/A shol, D/H chol, B/J/M, O/I all retained. All other words, SAME accounts and effects fixed; no aliases.
N scope: odd action target is a written prohibition applying only to that instruction; it does not execute, and unbound patients/debts remain unresolved. Odd standalone quality asserts exclusion of that exact axis value, not its named opposite. With no patient: MISSING_PATIENT, no state update. With existing equal positive value or explicitly forbidden positive requirement: CONFLICT; compatible known different value: MATCH; no positive value: record exclusion as NEGATIVE_CONSTRAINT, never synthesize warmth from not-cold. Later valid action on that axis replaces earlier positive/exclusion state at its own time. Nonnegated positives keep legacy statuses, and detect prior exclusion before updating. Other axes unchanged. No persistence of a command ban to different later written instructions.
Expected issue from W30: oty f86v5.19:6 may lose its old chey-mediated patient when existing requal is rerun. Preserve missing binding if so; do not replace it with nearest previous material or subset away this quality. Display unresolved object explicitly. Reparse may also change other actions/qualities; report ALL changes and downstream requirements, not just seven successful prohibitions. Compare N with stored W28 C on all17paragraphs and all24variants. Record loss of an assertion vs evidence for a reading separately.
Decision: no N selection if its negative quality loses binding or new contradictions; retain N as incomplete explicit rival. Fewer actions/conflicts alone never selects N. No physical or semantic independence, significance or plant name confirmation. All source content already exposed; f84/f84r and reserves closed. No new data, raw TSV, images, contacts or alternative transcription.
