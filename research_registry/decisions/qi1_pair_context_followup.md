# Exact pair context follow-up

2026-09-08. User-directed descriptive continuation of QH, not a semantic experiment.

## Decision before lookup

Motivating observations: shol kaiin shol kaiin at f8r.19 and cheor chey
cheor chey at f30r.11. Unknown here: whether their component pairs also occur
without doubling and whether their immediate surroundings recur. GDT820 already
shows that repetition alone cannot identify word class or force twice/quantity;
GDT575 inventories a working renderer, not independently translated repetitions.
The QH within-line exact length4–6 census already limits longer exact recurrence.
Exact-source duplicate review also located GDT851: both ABAB spans and their
full lines were already published there. These are rediscoveries, not new anchors.
IP055/058/049 already retain competing explanations; the producer adds no
duplicate. The old IP055 contract is native two-lane geometry, not an invented
parity-context prediction; IP058 requires an independently fixed pair phase.
The existing root triage leaves both unselected.

Smallest lookup: retrieve these two exact pairs and their immediate contexts
through the existing 179-selector guarded source. Keep raw groups, seams and
alternate readings. No normalization, cross-line repair, new image or meaning
score. Budget: eight minutes for extraction, review and publication; stop without
expanding to a decoder or null study. Recurring surroundings would nominate a
specific parallel passage for review; isolated surroundings leave these examples
as local repetitions and do not justify a formula-decoding follow-on.

## Full context correction from the preceding user lookup

All three readings agree on these full lines:

```
f21v.4 sho chodaiin choty chol daiin daiin chty chtol
f21v.6 sho tsho chotshol chol todaiin daiin
```

Only the selected local frame differs by daiin/todaiin; the complete lines differ
elsewhere and in length. These are not otherwise identical sentences. At f8r.19
the recorded drawing interruption precedes the whole ABAB run; at f30r.11 the
interruptions follow it. Neither run is assembled across an internal drawing
gap. This is transcription context, not native visual confirmation.

## Result

The guarded replay yields six reading-specific shol kaiin starts, all inside
one ABAB locus. cheor chey yields eleven starts: its ABAB locus plus f5r.6
(ZL/IT, ZL uncertain seam) and f82r.33 (all three, definite internal seam).
At f82r.33 the common ending is `cheor chey`; the preceding group is `ikhey`
in IT and `{ikh}ey` in ZL/RF. Neither singleton shares the ABAB immediate
external neighbours. Thus there is a reliable single/double contrast for one
pair, but no recurring longer immediate formula in these selected occurrences.
This descriptive comparison does not distinguish grammatical repetition from
scribal duplication or establish any meaning. No semantic route selected.

Reproduction: `python research_registry/decisions/qi0_pair_context_extract.py`
emits the complete compact occurrence artifact. Root replay and assertions check
6/11 reading-specific starts, the reader-specific definite-seam counts and all
three internal seams of both ABAB anchors. No images were viewed. The agent's
initial hand-written result wrapper was replaced by the actual script output.

