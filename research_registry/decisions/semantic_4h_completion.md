# Four-hour semantic research continuation — 2026-09-07

Session interval: 2026-09-07 12:57:35 UTC through
2026-09-07 16:57:49 UTC. The requested four-hour minimum is
complete; elapsed seconds and the report hash are in `jf_four_hour_time_receipt.json`.
This report concerns research-memory consolidation and method review. No new
manuscript experiment, native image observation, independently bound meaning,
word translation or clause translation was obtained in this session.

## Verified change from the committed starting state

Baseline: `e76e60a255f6c3ddf1d83c971d035fb995f1428c`, before HU.
Counts compare identical units: [root baseline audit](jc_four_hour_root_baseline_audit.json).
Reproduce this read-only audit from the repository root:

```bash
PYTHONPATH=. python research_registry/decisions/semantic_4h_audit.py
```

| Distinct unit | Before | After |
|---|---:|---:|
| Public semantic hypothesis variants | 3,854 | 3,859 |
| Separate formal-role cards | 265 | 266 |
| Active plus archived source cases | 4,640 | 4,647 |
| Current scoped question dossiers | 70 | 114 |
| Semantic variants directly bound to these dossiers | 89 | 136 |
| Reviewed multi-member equivalence groups | 65 | 81 |
| Duplicate display rows removed by equivalence | 77 | 94 |
| Related, rival or specialization links | 43 | 53 |
| Authored raw proposals | 35 | 52 |
| Authored proposals with a method review | 11 | 27 |
| Authored proposals still unreviewed for method | 24 | 25 |

Seven historical hypotheses were recovered as complete source-bound cards. One
mis-extracted card was archived as a source error and one semantic card was
reclassified as formal; hence the net gain is five semantic variants. All 4,163
original active-plus-archived cards survive. Their original case payloads and
evidence objects are preserved. Equivalence affects display grouping, not source
record retention. The display reduction includes one formal-role row.
All seven append-only decision/proposal/ledger files also retain the complete
baseline byte prefix; reproduce with `python research_registry/decisions/semantic_4h_history_audit.py`.

The seven recovered hypotheses concern the shared qokeol/okeol heat/middle-stage
field, lkaiin entry/level scope, local qochey dryness/stage, the inherited ol oeeal
field, the complete chorcholsal candidate, the AQABBA local hybrid, and the
three-field bracket model. They were already proposed in historical sources;
recovery is not a new semantic discovery. IP/IQ and IV contain their primary spans.

## What the audits changed

- **False extraction:** lowercase `dy = Schluss` was extracted from a passage
  denying that reading. That card is now archived as a source error. The distinct
  uppercase DY historical hypothesis remains. Genuine hypotheses are not deleted
  merely because later work withdrew them.
- **Scope of failure:** GDT753's failed directional q/base comparison does not
  refute every possible relation between qokeol and okeol, nor independently
  identify their shared heat/stage hypothesis. GDT804's aggregate field result
  and individual-bracket controls answer different questions; inherited anchors
  do not turn either into independent meaning labels.
- **Constructed evidence:** fixed renderer choices, scripted teaching/replay,
  inherited carrier assignments and analyst-created semantic axes were separated
  from observations that could discriminate meanings. A successful replay or
  selected display is not another decipherment success.
- **Compatible rivals:** material class and individual reference, temporal and
  causal relations, state and action, and reset/reload versus continued ownership
  can coexist. Proposed tests now state the missing contrast instead of calling
  an overlapping prediction a falsifier.
- **Historical relations:** two new links explicitly preserve GDT666→753 and
  GDT796→797 development without equating the propositions or transferring their
  scientific verdicts. Relatedness is not confirmation or reopening permission.

There are 44 additional question dossiers, not 44 new experiments. Their direct
semantic target coverage rose by 47 variants. Source correction decisions comprise
one archive, five scope restatements and 37 explicit retentions. Raw proposal intake
added 17 records; method review added 16 assessments. These are different units.

JG also checked the complete designs of the previously suggested shortlist
items IDEA31/32. Their restricted exclusive-branch and shared-predicate list
models were preserved; out-of-scope language examples were not accepted as
falsifiers. The actual blockers are the unjustified generic-connector/item-noun
null and possible selection on the outcome being tested. Both remain untested
with specific design and independent-binding requirements.

## Remaining coverage and next decision

The operative inventory contains 3,859 public semantic variants, 266 formal-role
cards and 45 archived source-error cards. Reviewed semantic equivalences reduce the
public semantic display to 3,766 groups. The 95 local variants are a separate scope.

Of the 3,859 public semantic variants, 136 have direct scoped question bindings.
Combining these with 27 directly prioritized targets gives 140 distinct covered
variants because 23 overlap. Thus 3,719 still lack either type of direct assessment.
Extraction coverage, method review, scientific failure assessment and meaning
confirmation must not be conflated. Global semantic deduplication is not complete.

The nine conditional scientific shortlist entries remain unready. The queue has
25 explicitly unreviewed proposals, enough surplus for bounded selection. Do not
start another broad decoder, control corpus or raw-idea generation cycle merely to
increase output. Start a manuscript test only after the complete design and
primary predecessors support different, independently scoreable predictions
between the stated rivals. A bounded method review can still be useful when
bindings are missing; choose one whose outcome changes the next research decision. If the only missing premise is a semantic binding, more page counts
alone do not repair it. If a historical failure is solely a documented capacity
shortfall, record exactly which changed input would meet its original gate.

The final JH navigation screen did not produce an adopted next candidate. It
revisited four already method-reviewed records and considered only four unreviewed
designs. Its exclusions are not scientific assessments. The root navigation
receipt lists the actual 25 remaining IDs with current input hashes; select from
that bounded queue rather than repeating reviewed items.

## Validation and reproducibility

Current source/card/case preservation, identity consistency, required review-file
hashes, scoped question bindings, priority bindings and registry freshness passed.
The final identity manifest requires 100 source-review inputs. The existing 54
registry/identity/lifecycle tests passed, including bounded navigation with 10,000
records; no new test was added just to mirror a reversible edit.

The peer audits JA/JB are retained with explicit root corrections in JC: they mixed
active-only versus archived case counts, log rows versus display groups, or repeated
a stale method-review count. The root uses actual baseline and current records.
JD acceptance covers three documented source comparisons, not an exhaustive claim
about all eighteen reports named by the peer. Exact spans and hashes were checked.

This session did not repair the known global GDT600/index debt or claim those checks
pass. Publication uses the exact staged-tree privacy and scope check. No GPU job,
external LLM key or public decipherment-approach search was used.
