# LA: distinguish score references from actual hypotheses

2026-09-08 02:53 UTC. Budget20minutes including primary scope review and any validation/publication.

KY found three result-only fragments counted beside complete V4 definitions. A navigation filter found40single-row numeric model candidates before those three archives; this is not40errors. V76 local alternatives explicitly name different proposed interpretations and cannot be rejected merely because the table also gives counts/scores.

Inspect only two V4 visual-owner rows and one qokaiin discrimination row. Unknown: is each a bare score reference with an existing separately evidenced full hypothesis, or the only source-bearing candidate proposition? Outcomes change archive versus retain/recover; no hypothesis may vanish merely because its evidence uses a table. Verify counterpart identity/scope and dependencies before archival. Preserve all source cases and historical bytes. No model rescore, new observation or semantic verdict.

Root disposition after peer comparison: preserve hypotheses through full-source bindings, rather than let score-only references stand alone. M2/M3 require two proper full-definition recoveries before their old score representations can be archived atomically. Q already has a full exact-card model; its score shorthand also narrows the unit incorrectly to qokaiin. Archival classifies the standalone representation, not the underlying hypothesis as false; score rows with actual new semantic content remain outside this decision. Record explicit archive-to-full-model maps.
