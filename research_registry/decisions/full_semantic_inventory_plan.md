# Full semantic idea inclusion

User correction: include all semantic-analysis ideas in the same operational list,
not only IP001-IP082. Scope: every surviving imported historical record plus
explicit proposals and hypothesis components in safely readable tracked research prose.
Missing historical files cannot be reconstructed; no source record is omitted for
lacking a semantic keyword or for having a structural top-level classification.

Decision changed: the operational queue must expose older candidates instead of
reporting 26 raw IP sketches as the available idea stock. Preserve distinct idea,
attempt, event and unresolved-source units; deduplication links never remove evidence.

Smallest adequate work: two independent source extractions, root unified paged queue,
independent exact-coverage and source-fidelity checks. Total budget20 minutes with
10-minute checkpoint. No new experiment, decoder, manuscript-image access or semantics
scoring. Preserve all legacy source bytes, sealed material, existing statuses and gates.

All uncertain semantic scope stays visible and labelled needs_scope_review. Extraction
of an explicit proposed gloss preserves a historical guess, not a translated lexeme.
