# QI0 pair-context lookup

This bounded pass queried the admitted 179 selectors through the guarded
`query-tsv` projection, preserving raw groups, source indices, edition and
separator classes. It checked exact adjacent `shol kaiin` and `cheor chey`
pairs, their immediate complete line contexts, and whether the pair repeats as
ABAB.

`shol kaiin` occurs twice at f8r.19 in each alternate reading, as the exact
3/3-supported span `shol kaiin shol kaiin` at groups 5–8. All three internal
seams are `DEFINITE_SPACE`; both pair starts belong to that ABAB span, leaving
zero singleton pair starts or singleton loci after excluding the two ABAB
constituent starts.

`cheor chey` occurs twice at f30r.11 in each reading as the exact 3/3 span
`cheor chey cheor chey` at groups 2–5. It also occurs singly at f5r.6 (ZL3b
and IT2a; ZL has an uncertain internal seam) and f82r.33 (all three readings,
ending the line). Thus the second pair has both ABAB and singleton examples:
after excluding the two ABAB constituent starts, two singleton starts remain at
two loci. Seam-qualified reading counts are ZL3b 3/4, IT2a 4/4 and RF1b 3/3;
the ZL3b occurrence at f5r.6 is the one uncertain seam.

Both ABAB anchors and their full contexts were already recorded by GDT851;
this is a scope-bounded recurrence check, with no semantic or decoder claim.
