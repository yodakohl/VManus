# Correction and direct visual question: f2r

**Superseded location claim:** the direct follow-up in
`qe2_f2r15_wrong_region_correction.md` shows the Aug11 correction's detail is
f2r.14, not the faint f2r.15. The following mistaken recovery is preserved as
history. Its paint-chronology caution remains; its claimed f2r.15 boundary
overlap must not be used as the current conclusion.

2026-09-08. Source-memory correction, not a new decipherment result.

The published `qb0_joint_constraint_anchor_audit.json` repeated superseded
claims that the complete f2r.15 phrase was inside a leaf and physically under
green paint. Withdraw that asserted conjunction. The 2026-08-11 primary
`experiments/semantic_assumptions/results/f2r15_native_visual_ownership_correction_report.md`
already corrects it to boundary overlap with unresolved ink/paint order.
The original qb0 receipt is preserved as history. The proposed GREEN directive
had no established paint chronology and remains unsupported.

The user's subsequent question concerns the distinct isolated entry f2r.14.
After recording admission, root personally viewed the verified whole original
Yale1006078 with native vision. Writing stands separately at the right, near a
leaf tip and away from the upper and lower prose blocks. A local caption is a
plausible function, not an established plant name or part/property translation.
The exact-locus human note says `East of plant.` and grades proximity only.
Both available separator-atlas readings, ZL3b and RF1b, give `ytoail` at f2r.14;
f2r.15 instead contains `ios an on`. These are alternate readings, not two
independent manuscript occurrences. No new native glyph reading is asserted.

Reproduce the two selector-first projections with `vmanus-exp query-tsv`:
source `experiments/semantic_assumptions/results/source_separator_transcription.tsv`,
selector `page`, allow `f2r`, columns
`edition,locus,kind,code,source_group_index,ivtff_group_raw`; select .14/.15
only after the guarded projection. For
`experiments/semantic_assumptions/results/existing_human_exact_locus_annotations.tsv`,
use the same selector/allow and columns
`locus,local_comment,object_tags,local_relation_tags,certainty`.
Image identity and pre-access admission:
`docs/visual_overview/F2R_USER_QUESTION_ADMISSION.md`.

User's priority correction is accepted: this repairs our evidence and answers
a direct question; it does not justify a new paint, ownership or hole experiment.
The proposed whole-model comparison also needs a genuinely different prediction:
GDT809 already contains competing complete-paragraph readings. See
`qd0_model_disagreement.json` and `qd1_semantic_model_identification_critic.json`.
No new experiment is selected by these reviews.
