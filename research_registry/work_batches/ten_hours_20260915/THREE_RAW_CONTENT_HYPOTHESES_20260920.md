# Three bounded raw content hypotheses

Status: **RAW_UNREVIEWED, unselected.** These are three distinct exploratory
content systems over already exposed report-owned excerpts. They do not revise
IDEA417, use its f106r.46 relation, open new target material, or inherit old
word glosses. The exact predecessor checks were GDT412, GDT769, GDT792, GDT943,
IDEA000379, IDEA000408, IDEA000410 and IDEA000417; their limitations remain.

## A — f80v.8–.9: repeated application under one condition (strongest)

Already exposed lines:

```text
f80v.8 olteedy qokaiin shedy qokain sheol qokchdy qokchdy qoty dy
f80v.9 tchdy qol tol tal taldain chckhy qokal dol checthy qokal ly
```

The content hypothesis is a two-clause process record. Clause 1 introduces an
active item and a changed item, states the intermediate state, applies one
operation twice in sequence, and closes under a written condition. Clause 2
records the resulting item through an ordered path, repeats the same target
field, checks the result, and closes the record. The repeated `qokchdy` is one
operation applied first to the input and then to its output; it is not a
second free operation or a doubled quantity. The repeated `qokal` is one target
identity reused in the second clause, not two silently invented destinations.

The finite shared rule is:

```text
qokchdy(A)              -> APPLY(OP, current=A) = A'
qokchdy(A')             -> APPLY(OP, current=A') = A''
qoty                    -> CONDITION(K0, current=A'')
qokaiin                 -> INPUT(I0)
qokain                  -> OUTPUT(O0)
qokal                   -> TARGET(T0), reused if repeated in one clause
dy / ly                 -> CLOSE(current record)
```

The remaining groups are ordered typed fields of the two clauses: `olteedy`
opens the record, `shedy` carries the first intermediate state, `sheol` joins
the second application, and the .9 groups retain path, step, target, check and
closure fields. They are not assigned singleton English words. The worked
reading is therefore:

> An input item is acted on; the intermediate state is retained. The same
> operation is applied again to that result under a stated condition. The
> resulting item then follows an ordered path to one target, whose result is
> checked and the record closed.

Unknowns are the identities of input/output, the operation, condition, path,
step, target, check, and whether the two lines are one record boundary. The
reading does not import a material, vessel, treatment, quantity or historical
source. Its concrete unchanged-rule consequence is that the second
`qokchdy` must consume the first output and the second `qokal` must reuse the
same target under the same global constructor. A rival independent-entry
model predicts two unrelated operation cards and two targets. The smallest
discriminator is an already exposed complete line containing a repeated
`qokchdy` or `qokal` with a written intermediate/result field: the process
model predicts sequential state/target carry; the independent-entry rival
does not. No such held semantic observation is claimed here.

Closest predecessor: IDEA000382 also uses a repeated maneuver, but its content
is a salt/sponge success-failure narrative on a different f82r paragraph. A
failure branch and material change are not imported here. GDT412 supports only
that CHD-family process readings can remain open beyond closure; GDT769 warns
that contextual process fields do not identify a material or product; GDT792
warns that repeated whole forms do not establish participant identity.

## B — f106r.43/.47: one item assessed, then revisited with a counterpart

Already exposed lines in the same W80 f106r.42–47 paragraph:

```text
f106r.43 daiiral sheol daiin otedy qokain okar cheor al taiin chekal otaras
f106r.47 ycheoar okain qokain char oky cheokam
```

This hypothesis treats the repeated whole `qokain` as one typed item across two
clauses. The first clause introduces it into an assessment field: an origin,
item, criterion and evaluated target are all retained. The second clause
reopens that item alongside a distinct counterpart `okain`, records a changed
comparison/state, and closes. The shared content is **reassessment of one
item**, not containment, transfer, quantity or a source-to-result path.

Finite rule: an `ITEM(qokain)` introduced under `otedy` remains the current
item until an explicit counterpart field is written; `cheor` opens an
assessment, `chekal` commits its criterion/result, and `cheokam` closes a later
reassessment. `okain` is a typed counterpart parameter, not a translated noun.
All other groups are ordered assessment fields and remain unknown values; no
singleton receives a free English gloss.

Unchanged-rule consequence: if the same item is revisited, a later assessment
must refer to the prior item and preserve its identity while allowing a new
criterion/result. A rival occurrence-independent model predicts two unrelated
items. The smallest test is one already exposed complete clause in which
`qokain` recurs after a written assessment closure; the two models predict
identity carry versus a fresh item. No such semantic result is currently
observed. This branch remains weaker than A because the cross-clause identity
is an explicit hypothesis rather than a written pronoun or reference marker.

Closest predecessors: GDT792 explicitly withholds semantic identity from
reused complete forms; GDT769 keeps contextual fields and products tied; IDEA417
uses the same paragraph for a different typed containment proposal and is not
altered by this alternative.

## C — f82r.14: reference, operation, verification, and return

Already exposed complete local line:

```text
f82r.14 r olchy qokal chey qokain deeedy qokeey qokaiin olchedy
```

This hypothesis reads one compact clause as a reference/check construction:
one record is opened, a target field is identified, an item is acted on, an
intermediate result is checked against a second typed item, and the relation
returns to the original record. It does not assign qokain/qokaiin as
container/content and does not claim two routes converge. `deeedy` and
`qokeey` are the shared check/result operators; `olchedy` closes the reference
back to the active record. The other groups are typed source, target and
argument fields with unknown values.

Unchanged-rule consequence: the second typed item is an operand of the check,
not automatically a new destination or a repeated source. A rival
same-result/convergence model predicts a downstream shared result; this model
predicts a verification edge returning to the active record. The smallest test
is a complete exposed line where the same `deeedy–qokeey` sequence occurs with
one operand changed: verification predicts a changed comparison while the
record endpoint remains fixed. No such semantic observation is claimed.

Closest predecessors: IDEA000379 proposes coupled temporal rulers; IDEA000408
and IDEA000410 propose convergence/same-result relations. C is different in
keeping one active record and a verification edge rather than two routes or
two derivations. The target line is not a new admission, and no old scalar or
temporal gloss is inherited.

## Selection and common limits

A is the strongest raw branch because its repeated `qokchdy` supplies a
nontrivial compositional consequence without requiring cross-clause identity
or a singleton English dictionary. B and C remain useful diverse alternatives
because they test persistent item identity and verification scope. None has an
independently observed meaning binding, source-language proof, or held semantic
outcome. The expected consequences above are therefore untested predictions,
not manuscript findings. No decoder, experiment, ledger, route or publication
was changed.
