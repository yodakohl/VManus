# Decision — complete typed latitude worksheets

2026-09-15, root, before any candidate count or fit. Inclusive wall-time bound
11:55 UTC covers preparation, registration, implementation, independent checking
and publication. First perform only fixed-length literal-capacity preflight;
if either global model lacks three distinct physical leaves, stop that model
without a decoder. No large implementation unless the remaining finite scope
can change the research decision.

## What is unknown and what would change

GDT967's both lossless bath-term carriers failed; those contracts stay closed.
GDT607/608 establish directed composition, substantial whole-form residuals and
entry context, not a semantic parser. GDT880 lacks immediate same-head triple
capacity. GDT882 excludes its universal additive whole-line rule. GDT898 rejects
its fixed February string code. GDT902 rejects a copied44-equation decimal table;
GDT909 rejects repeated equal-width division steps inside complete paragraphs.
Their primary reports have been read. No automatic repair follows from them.

The new unknown is whether different complete paragraphs can be three typed
numerical calculations under one compositional number notation. Each is ONE
observation/calculation. Shared numeric cores must work across different quantity
roles even when their opcode prefixes differ. Explicit stored intermediate and
final values must agree with executed arithmetic; execution cannot manufacture
an unwritten result. No region identity or common latitude is inferred.

A full witness would supply complete conditional readings of three paragraphs
and shared code alternatives for every written component, including operative
north/south/equinoctial distinctions. It would justify examining those complete
readings and their unresolved equivalent interpretations. It would not confirm
an English word, degree unit or source identification. A necessary or exhaustive
failure closes only this fixed worksheet notation. A cap leaves UNKNOWN. If
all outcomes merely renamed formal slots, implementation would not be justified;
here changing decoded quantities changes arithmetic validity under one key.

## Source support and explicit assumptions

Chaucer, Astrolabe II.25, complete rubric and lines1–60 in the Skeat edited text,
SHA2568a6691961db5dea8ef02046e3bc2ff5b0acd138f6f3656d501799d9c654bafd1,
was read in full. It supplies equinoctial90−38=52 with Oxford qualification,
approximate north56−18≈38, an unnumbered south addition followed by the90-degree
complement, and a fixed-star alternative. These are conditional branches, not
three successive observations or three exact worked equations. Source image
previously called II.25 actually shows the zodiac/rete description; no native
confirmation of the edited computation is assumed. Keep that provenance correction.

The proposed notation is conjectural and is NOT a copy of all60lines of prose.
Generate every whole-degree illustrative worksheet allowed by the algorithm:
Q=90; h,e,phi in1..89; N and S have positive d in1..24; E hasd=0.
The24-degree envelope is a declared illustrative grid limit, not a newly read
source calibration. Define e=h−d forN, e=h+d forS, e=h forE; phi=90−e.
No sign branch can be satisfied by an N/S zero-declination shortcut. This yields
1836N,1836S and89E complete numerical programmes. Q=90 and degree interpretation
are fixed hypotheses; scaling and geographic/quantity-role alternatives do not
vanish merely because a calculation is internally consistent.

## Fixed language and two global spellings

Every programme has exactly seven instructions in this order:
OBS_ALT(h), OBS_DECL_BRANCH(d), CORRECT, ASSERT_EQ(e), QUADRANT(90),
COMPLEMENT, ASSERT_LAT(phi). Paragraph boundaries supply closure. There is no
extra END word, invisible input, optional filler, renamed region or gallows null.
A single programme cannot change spelling mode mid-paragraph.

FUSED: each numeric instruction is one raw group, a nonempty opcode prefix plus
its numeral. The two operand-free instructions are each one raw group:7groups.
SEPARATE: opcode and numeral are separate whole groups for the five numeric
instructions, with the two operand-free groups unchanged:12groups.

One global table gives distinct nonempty literal strings to the nine opcodes:
OBS_ALT, DECL_N, DECL_S, DECL_E, CORRECT, ASSERT_EQ, QUADRANT, COMPLEMENT,
ASSERT_LAT. This is a typed-instruction-code hypothesis, not a claim that the
source uses nine different ordinary words: e.g. source abate occurs in multiple
roles. Its injectivity is a strict model assumption. Full grammar positions
resolve compound parsing; prefix distinction alone is not global decodability.

All ten decimal digits have distinct codewords of one common positive width k
in literal[a-z] characters. Numerals use ordinary base10 with no leading zeros;
0 is one digit. k is not selected after fitting: enumerate every feasible width
from the full group lengths, with Q=90 necessarily two codewords. Both formats
use the SAME rule within a model; the models have separate complete searches.
This is no one-EVA-character-to-Latin-letter claim. All finite opcode-prefix
splits and digit assignments enter. Missing digit codewords remain unresolved
completions, not recovered values. Fixed-width strings give an exact finite
completion count over the declared26-letter transcription alphabet.

Require three complete paragraphs on three different physical leaves, oneN,
oneS and oneE, sharing one width, opcode table and digit table. All candidates
and equivalent observations are retained; no favorable paragraph or restart
chooses a key. Different paragraphs can describe different locations, so no
unwritten same-place or same-latitude identity is imposed.

## Scope, exposure and first preflight

Use all complete paragraphs of the unchanged GDT928 loader over its six
already guarded GDT915 caches and179 admitted selectors. Preserve raw words,
source IDs, literal[a-z]+, definite internal seams and original completeness.
Every line must meet its original literal eligibility rule. All groups count.
Only complete7/12group paragraphs can satisfy the respective language; record
all other lengths and nonliteral cases as outside that exact scope. Alternate
readings are one manuscript, not replication. All cached material has project
exposure; zero independent meaning confirmation, no new image or reserve.
f84/f84r and f116v remain closed.

Before public registration, inspect only this metadata capacity and the external
source/algorithm. If a model has capacity, freeze full source programmes,
serialization, numeric/code assumptions, finite algorithm and run limits before
extracting target candidate strings or fitting. No appropriate control covers
the whole project search: no significance claim. Source corrections do not
become manuscript progress; a serialization failure is not arithmetic evidence.
