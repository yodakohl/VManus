# GDT078 — HPR4 formal paradigm lexicon

Status: **YOLO generative synthesis and lexicon freeze**

Convert GDT076's leave-register-out propensity evidence into an explicit,
reproducible PAGE_HOST class atlas.  A host is `AIIN_STABLE_HIGH` only when it
has at least three eligible held-register folds, training-high and held-high
fractions at least `.60`, and training/held binary agreement at least `.60`.
No host is added by inspecting surface attractiveness.

For every resulting host, inventory all five explicit HPR2 RIGHT_FAMILY cells
(`aiin/air/ain/ar/al`) in each of the five fixed registers, preserving counts,
wrappers, examples, and absences.  Freeze HPR4 with a host-specific renderer
propensity vector and the weak directional dependency from GDT077.  The atlas
is formal: `PAGE_HOST` and `RIGHT_FAMILY` are parser coordinates, not asserted
words or morphemes.  Preserve GDT003's conclusion that transformation
prediction did not beat string baselines.  Freeze one narrower prospective
non-f84 enclosure prediction without running it.
