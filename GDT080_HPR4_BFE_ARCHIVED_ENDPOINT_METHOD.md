# GDT080 — HPR4 archived enclosure-endpoint audit

Status: **YOLO hypothesis stress test; not prospective validation**

Test the frozen GDT078 `AIIN_STABLE_HIGH` PAGE_HOST set `{d, ok, yk, yt}`
against the already acquired BFE001 visible enclosure states after excluding
f84r.  The predicted direction is more `INDIVIDUAL_BOUNDED` observations among
class-positive labels.  This is not HPR4_P01: the BFE endpoint predates the
class freeze, was already included in GDT002 exploration, and has only one
mixed page.  HPR4_P01 remains `FROZEN_NOT_RUN` and requires genuinely fresh
non-f84 evidence.

Join the 30 non-f84 BFE rows in
`gdt002_exploratory_visual_formal_join.tsv` to the frozen HPR2 parse in
`gdt059_hpr2_external_inventory.tsv`.  Missing HPR2 parses remain explicit and
are never treated as class-negative.  Report pooled prevalence, exact-host
composition, parser coverage by state, leave-folio-out direction, and the
only identifiable conditional contrast: exact state-label permutations on
f82v preserving its three bounded and two open observations.

The historical BFE capacity stop remains unchanged.  No fresh image or formal
holdout is opened.  f84r is filtered before the join and is not retained,
queried, scored, targeted, or inspected.  No role, gloss, word, morpheme, POS,
sound, language, plaintext, meaning, or translation may follow.
