# GDT122 — Q20 PAGE_HOST versus archived visual states

Status: `EXPLORATORY_YOLO_FIXED_MODEL_FAMILY`

## Question

GDT119 found no corrected association between visible star rays/tail/color and
raw record strings or HPR2 compiler profiles.  GDT122 tests the remaining HPR2
localization claim directly: does stripping wrappers, O/OT, RIGHT_FAMILY, DY,
and B3 expose visual signal in PAGE_HOST strings that raw strings obscure?

Reuse the same 156 provenance-bound Stolfi star units and three frozen binary
axes: 7/8 rays, one/two tails, and red/yellow center.  The color axis is an
ordinal/parity-confounded sensitivity, not an independent content label.  The
archived SME target-free calibration failure remains binding.

## Models and controls

Hold out one physical folio.  The nuisance model knows parity, normalized
ordinal, records per page, side, record line count, and OPEN/BODY group/member
counts.  At fixed logistic ridge 10 compare:

1. OPEN PAGE_HOST character trigrams in 32 SHA bins;
2. BODY PAGE_HOST trigrams;
3. combined OPEN+BODY PAGE_HOST trigrams;
4. OPEN compiler profile;
5. raw combined record trigrams.

Within each held folio, permute complete formal profiles only within page and
exact OPEN-member-count strata.  This preserves page ecology, the visible
state multiset, record lengths through nuisance adjustment, and all formal
profiles.  Use 4,096 shared worlds and max-15 correction.

A useful PAGE_HOST lead must be selector-paid positive, max-15 p at most .05,
positive in all readings, positive on at least five of seven folios, and beat
both compiler and raw-string controls.  The purpose is exploratory localization,
not semantic confirmation.

f84r is absent and is not opened, retained, queried, joined, scored, targeted,
assigned, or predicted.  No star attribute meaning, PAGE_HOST meaning, word,
morpheme, POS, sound, language, plaintext, or translation is inferred.
