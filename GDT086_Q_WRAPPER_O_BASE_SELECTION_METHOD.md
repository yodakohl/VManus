# GDT086 — q-wrapper selection of the O-class PAGE_HOST base

Status: **YOLO formal-operator generalization test**

Extend GDT085 beyond `k/t`.  Use every supported two-sign PAGE_HOST pair
`oX/yX` for terminals `k,l,p,s,t` (minimum one occurrence, both bases
present).  Predict binary q-wrapper presence on completely held physical
folios.  The baseline is terminal-specific; the alternative adds the first
PAGE_HOST sign (`o/y`) with Dirichlet shrinkage `{1,4,16,64,256}`.  Report all
terminal and register gains, every cell, and every q+y counterexample.

As a look-elsewhere diagnostic, scan all first-sign pairs with at least two
matched terminal strata and at least two occurrences per cell using the
stratified q-enrichment z score.  This is a formal wrapper-licensing test, not
semantics or linguistic morphology.  Preserve GDT003.  f84r remains excluded.
