# GDT320 — fresh-surface `d` dual-entry transfer

GDT318 gave `d` small positive coefficients for both physical line start and
immediately preceding physical-group DY. Test that post-selected dual-entry
lead prospectively after excluding every exact source surface present in the
GDT318 panel. Freeze remaining opaque
`PAGE_HOST × frame × inner-D × right × DY × B3` cells with at least two `d`
and two non-`d` events, each class on at least two physical folios.

The frozen panel contains seven cells, 46 events, 21 `d` choices, and 30
folios. In leave-one-physical-folio-out folds compare ridge-10 exact-cell
priors against line-start, preceding-DY, and joint selectors. Report held log
loss, cell/register-matched directions, fold coefficients, section
contributions, and an 8,192-world fixed-crossfit max-three cell/register
alignment diagnostic. The diagnostic is not an exact retrained null.

Call `D_DUAL_ENTRY_EXTENDS_TO_FRESH_SURFACES` only if the joint model saves
more than the fixed two-bit four-model selector charge, both matched deltas
are positive, both joint feature coefficients are positive in at least 23/30
folds, at least two powered B/H/S sections contribute positive joint gain,
and max-three diagnostic p is at most .05. Failure rejects only a reusable
dual-entry `d` selector.

At most this establishes stochastic `d` choice after an opaque compatible
cell is already known. It predicts no new license and assigns no prefix,
morpheme, POS, meaning, sound, language, plaintext, or translation. No f84 row
may be opened, parsed, retained, joined, or scored.
