# GDT271 — frozen Q20 transfer of the q outer-layer association

## Chronology

This method and `gdt271_frozen_prediction.json` are frozen and published before
GDT271 computes any conditional q/record-stage association.  GDT268 previously
exposed only marginal q and bare early/late rates.  GDT271's exact
PAGE_HOST/non-wrapper-compiler conditional endpoint is new, but the source
panel is not observer-blind.

## Frozen prediction

Transfer the GDT270 direction without alteration:

> Within exact page, PAGE_HOST, and non-wrapper compiler strata, `q` will be
> allocated toward early rather than late Q20 records.

The non-wrapper compiler tuple is exactly:

```text
(O/OT_FRAME, INNER_D, RIGHT_FAMILY, DY, B3)
```

The ZL3b reading is primary. IT2a and RF1b are alternate-reading
sensitivities, never independent replications.

## Q20 record split

Use the existing f84-free `gdt127_q20_field_inventory.tsv`, containing 170
star-defined records on thirteen Q20 pages per reading.  On each page, sort
records by human-defined star ordinal.  Select the first and last equal-sized
halves, excluding the middle record on odd-sized pages, exactly as GDT268.
This yields 162 selected records per reading.

Expand every field into aligned PAGE_HOST and compiler cells. Retain only q and
bare/`NONE` wrappers.  No target outcome is selected from visual content.

## Frozen tests and null

Three nested conditionings are frozen from GDT270:

1. `PAGE_HOST_PAGE_OTHER_COMPILER` — primary;
2. plus `WITHIN_FIELD_POSITION` — position sensitivity;
3. plus joint `WITHIN_FIELD_POSITION:FIELD_END` — fine sensitivity.

Within every stratum, hold wrapper counts and early/late margins fixed.  The
exact occurrence diagnostic convolves the corresponding hypergeometric
distributions.  Its directional tail is frozen as `q EARLIER`.

The clustered test sums conditional scores within each of thirteen pages and
enumerates all `2^13` page sign flips.  Use one shared max-three statistic
across the three frozen conditionings.  The primary transfer gate requires:

- positive primary conditional score;
- at least 9/13 positive page scores; and
- primary max-three page-sign p <= 0.05.

The exact occurrence tail and alternate readings are reported corroborating
diagnostics, not substitutes for a failed primary page gate.

## Capacity frozen before scoring

The capacity freezer may count strata and margins but does not calculate the
directional association.  It establishes ample primary capacity: 128 movable
ZL strata, 668 occurrences, 33 hosts, and all thirteen pages.  Corresponding
IT/RF capacities are retained in the frozen JSON.

## Claim ceiling

A pass would establish only that q's separable outer record-stage association
transfers from q13 to Q20 under the frozen HPR2 representation.  It would not
make q a spoken prefix, word, morpheme, semantic operator, universal ordinal,
language feature, plaintext, or translation.  A failure leaves GDT270's q13
result intact.  No f84r access is authorized or performed.
