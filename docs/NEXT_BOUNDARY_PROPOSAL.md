# Proposed next question: association across source-group gaps

2026-09-05. NOT_ACTIVE_PENDING_INTERNAL_REASSESSMENT. No experiment executed.
No new experiment number, manuscript extraction, image, fit or target score.

## Subsequent user correction

The user instructs us not to search public approaches, stating that those
have already been thoroughly explored. The public-paper motivation below is
therefore withdrawn as a basis for selecting a fresh route. The earlier
proposal is retained as history, not an active experiment instruction. A next
recommendation must be justified from our own data and primary reports.
This scope correction does not scientifically reject the cross-gap hypothesis.

## Earlier recommendation

Set aside the l/m reflow route. Test whether the last source atom of one group
and the first of its immediate neighbour remain associated across definite
internal gaps after preserving measured line position. This does not require
the extended identical passages absent in GDT829. It asks for a conditional
structural constraint, not a translation or a newly chosen cipher.

Use the existing source-group atlas and explicit 179-selector allowance.
Keep definite spaces, uncertain small spaces, drawing interruptions and line
ends distinct. Preserve opaque extended entities and reading uncertainty;
do not assume the historical 98-unit inventory is an identified alphabet.
ZL is primary; other readings are sensitivity channels of one manuscript.

## Concrete next work unit

1. Specify a source-faithful endpoint representation and a position-preserving
   null before scoring. Primary: definite gaps between internal groups.
   Uncertain gaps form a separate declared diagnostic, never a pooled rescue.
2. A candidate null permutes whole right-neighbour groups across boundary
   observations within page, line-group-count and exact slot. This preserves
   relevant slot-wise whole-group marginals and page/hand context. It does not
   preserve each individual line's inventory, or create a complete rewritten
   manuscript. Line-specific content can remain a confound. Length controls
   and sensitivity strata must be made executable before the test, not merely
   promised in prose.
3. Check donor mobility before interpreting the test: movable observations,
   distinct partners and physical-folio support. Exact conditioning can be
   sparse even when the total corpus is large. Do not relax it after seeing
   the target result. Numerical capacity/effect thresholds are not yet frozen.
4. Validate the measurement on controls with known independent group generation
   and known cross-group dependence, retaining the measured layout effects.
   Include line-specific composition as a possible false-positive control.
   Then test whether any extra association survives the fixed null and whether
   it transfers beyond the folios used to select a particular transition.

A positive result rejects only the stated conditional-independence model.
It does not establish causal direction, grammatical agreement, shared sounds,
cryptographic state, meaning or false word boundaries. Mutual information is
symmetric. A null result would limit this particular asserted residual effect;
it would not refute language. A capacity stop would decide neither.

The intended deliverable is one compact, controlled answer about where the
observable dependency lies. A later inverse model would have to reproduce a
surviving effect; no inverse model or language is selected now.

## Basis and limits of the planning review

[Rozanova and Temerev's 17 August 2026 preprint](https://arxiv.org/abs/2608.17096)
reports weak adjacent whole-token order alongside stronger glyph-edge
association and differing separator behaviour. Those are the authors' claims,
not findings independently reproduced here. The abstract, repository README
and [edge-order driver](https://github.com/lrozanova/voynich-units/blob/main/analysis/reproduce_edge_order.py)
were read. The driver uses whole-token random permutations within each line;
these change endpoint positions. That observation motivates a stricter
conditional test, not a refutation of the preprint's other claims. No external
transcription, coordinate table, image or manuscript-result row was opened.
The preprint's shared conventions and any dependency on sources used in our
older work must not be counted as independent manuscript evidence.

GDT605's real positive is an association with uncertain separators. Its
composite collapses and 64 BPE merges constrain the learned inventory; coverage
of the resulting 98 types does not identify authorial symbols. It is not
entirely tautological, since residual final units can differ after later merges.
GDT283 localizes an opaque wrapper/host association inside a group. Neither
result supplies the new cross-gap conditional test by itself.

Three independent planning reviews considered physical writing width,
inverse-writing/segmentation, and external anchors. Actual writing-width
measurements could add evidence, but target-independent right boundaries and
sufficient matched visual cases are not established. GDT155/157/207 already
explored authentic abbreviation/expansion pairs extensively. GDT617–621
supplies real historical herbal parallels without a Voynich plaintext link.
These are not selected as immediate substitute routes.

Targeted duplicate screens and primary reads covered GDT283,605,608–609,
612–616,800–803,829,155/157/207 and the historical source chain. Lexical search
does not establish an exhaustive novelty claim. No old failed route is silently
reopened; GDT829 remains closed at its registered capacity stop.

Read-only driver snapshot SHA-256: `dc021e2ee1355faef77d9cceabc24687abdb7c44157ebdb25dfdfb21658da34c`.
