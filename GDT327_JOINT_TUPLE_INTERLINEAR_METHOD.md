# GDT327 — executable joint-tuple interlinear

Build one f84-free, translation-ready formal observation layer from the
settled GDT321–326 architecture. This is an instrument checkpoint, not a new
semantic experiment.

For every Voynich reference group define the opaque joint tuple

`J = (PAGE_HOST, local-frame, inner-D, right-family, DY, B3)`.

Assign stable SHA-256-derived IDs to J, PAGE_HOST, and the five-field
coordinate. Preserve page/folio/locus, physical group order, record/field
coordinates, register/section/Currier/hand, observed wrapper, line entry, and
preceding-DY state. Assign no semantic category or translation.

For the exact 126-cell GDT322 lexicon only, reconstruct the wrapper
probability

`softmax(log(n[J,w]+1/2) + I[w=s]×1.0021314958853849×LINE_START +
I[w=q]×0.8920380870887143×PREV_DY)`.

For every other observed tuple emit `UNLICENSED_OR_UNKNOWN` and no generated
probability. GDT325 forbids coordinate fallback, and GDT326 forbids treating
PAGE_HOST as an independently recombining payload. The interlinear therefore
never fabricates an unseen license.

The output is a formal alignment substrate for future whole-field/record
external grounding. `semantic_state` and `translation_state` remain
`UNASSIGNED`. No word, morpheme, POS, sound, language, meaning, plaintext, or
translation is inferred. No f84 row may be opened, parsed, retained, joined,
or scored.
