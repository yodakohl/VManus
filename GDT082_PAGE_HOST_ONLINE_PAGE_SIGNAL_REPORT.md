# GDT082 — PAGE_HOST online page-local identity signal

## Outcome

**PAGE_HOST_IDENTITY_HAS_PAGE_LOCAL_SIGNAL_BEYOND_WRAPPER_BUT_F86V5_DOMINATES**

Across 958 `ok/yk/yt` occurrences on 127 pages and
76 physical folios, the
register×WRAPPER nuisance model saves 96.090 held-folio
bits over register alone.  After same-line updates are prohibited, the selected
online page shrinkage 32 saves another 11.610 bits
(8.803 after its seven-way selector).  A 5,000-draw
register×WRAPPER-preserving max-grid shuffle gives p=0.00120.

The gain is localized: `f86v5` contributes +10.384
bits, 89.4% of the total, with
`ok/yk/yt=6/6/8`.  It is a human-catalogued
text-only page.  `OTHER_B` contributes +10.674 bits;
Stars/Recipe contributes -2.090.  Therefore
PAGE_HOST identity carries a real local-page vocabulary signal beyond wrapper,
but it is not a stable manuscript-wide semantic class.  The strongest lead is
the f86v5 local inventory contrast, not a gloss.  The null does not redo HPR5
or wrapper-alpha selection, so its p-value is exploratory.  f84r was excluded
and not opened or used.  No semantic class, role, gloss, word, morpheme, POS,
sound, language, plaintext, meaning, or translation is assigned.
