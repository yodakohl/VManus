# GDT002 contact/gap visual acquisition report

Status: **STOP — PREREGISTERED CAPACITY GATE FAILED; NO FORMAL COMPARISON**.

This exploratory acquisition obtained a genuinely new physical observable from
12 exact official-Yale image regions. A source-aware localizer identified each
frozen inscription without making the target judgment. A separate reviewer,
shown only randomized context and target crops, classified visible
writing-to-drawing geometry as `CONTACT`, `CLEAR_GAP`, or `UNCERTAIN`.

## Result

| Physical folio | CONTACT | CLEAR_GAP | UNCERTAIN | Gate |
|---|---:|---:|---:|---|
| f89 | 2 | 0 | 1 | FAIL |
| f99 | 1 | 3 | 0 | PASS |
| f100 | 1 | 4 | 0 | PASS |

The frozen gate required every folio to contain at least one `CONTACT`, at
least two `CLEAR_GAP` calls, and no unresolved call. f89 therefore stops the
route. Its three-item array cannot supply the required within-folio contrast:
two labels were judged `CONTACT` and the third is too faint and
mottled for a secure state.

This failure is informative. The reviewer assigned both `CONTACT` and
`CLEAR_GAP` on f99 and f100, but the registered
three-folio panel does not support the balanced discovery-plus-transfer design.
The visual states were not joined to formal families, and no role solver was
run. The previously observed source transcriptions and formal tables were
available to the source-aware localizer, but not to the randomized-crop
reviewer; this is procedural separation, not pristine observer blinding.

Exact IIIF region URLs, full-image and crop hashes, coordinates, localization
notes, and all twelve review calls are recorded in the accompanying TSVs. The
validator checks record integrity and gate arithmetic but does not substitute
for independent visual inspection.

## Claim ceiling

The registered panel contains `CONTACT` and `CLEAR_GAP` crop-review calls, but
fails its predeclared per-folio capacity gate. No association with a Voynich
family, construction, semantic role, object, word, part of speech, sound,
language, plaintext, meaning, or translation was tested or inferred.
