# GDT251 — `okal*` renderer cluster

## Result

**OKALY_FIGURE_GLOSS_DEMOTED_AQAB_RENDERER_CLUSTER_SECTION_CONFOUNDED**

The complete annotated `okal*` family contains 20 groups, ten surface forms,
and seven physical folios:

```text
okal, okaly, okalal, okaldal, okaldy,
okalar, okalam, okalair, okalo, okalchy
```

All 20 begin with the transferred source-family prefix `AQAB`. Their strict
residuals vary among `EMPTY`, `A`, `AB`, `BAB`, `BA`, `AC`, `AF`, `KA`, and a
boundary-bearing form. This is a productive graphical-label construction
family, not one invariant opaque host.

## Why the apparent figure meaning collapses

Eighteen of 20 rows are figure-tagged and 19/20 are figure-or-sky tagged, but
every occurrence lies in the figure-heavy Zodiac or q13 sections. In the
GDT235-covered comparison, Zodiac is already 178/178 figure-tagged; q13 is
55/73. The `okal*` rows are 14/14 and 4/4 respectively. Thus the attractive
purity is almost entirely predicted by section ecology plus a known label
renderer.

The two non-strict rows matter: f67r2.22 is sky-tagged without a figure tag and
f67v1.3 has only a label tag. More importantly, no residual has an independently
repeated owned referent. The GDT250 `okaly -> FIGURE_ASSOCIATED_CLASS_DESCRIPTOR`
hypothesis is therefore demoted to **likely renderer/section confound**.

## Generative update

The corrected theory is:

```text
AQAB / visible okal-     = graphical-label renderer family
right residual           = unresolved local code/rendering material
whole form in prose      = renderer family is not label-exclusive
```

This explains the broad right-side variants, the repeated identical labels,
and the exact label/prose bridges without requiring `okal` to mean “figure.”

## Claim ceiling

Renderer-family census only. No figure word, suffix meaning, object name,
language, plaintext, or translation is established. No f84 input was read or
retained and no f84 result was scored.
