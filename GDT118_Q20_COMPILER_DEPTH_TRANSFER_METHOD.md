# GDT118 — Q20 OPEN compiler transfer by BODY depth

Status: `INHERITED_GDT115_DEPTH_DIAGNOSTIC`

Does the GDT115 OPEN-compiler signal reach the whole record, or only the first
following line?  Predict three fixed BODY targets on held physical folios:

1. `BODY_LINE_1` — first line after OPEN;
2. `BODY_LINE_2` — second line after OPEN;
3. `BODY_TAIL_3PLUS` — pooled third and later BODY lines.

For each depth compare OPEN `COMPILER12` with `RAW_CHAR3_HASH32`.  Ridge 1000,
the eight whole-folio folds, three reading sensitivities, record-shape nuisance
and same-page/exact-OPEN-length 4,096-world null are inherited.  The nuisance
also receives the leave-one-record-out mean target-depth profile of the other
records on the held folio.  Models are corrected across all six fixed
depth×representation tests.

f84r is excluded before retention and receives no query or prediction.  BODY
depth is physical layout, not discourse or semantics.  No heading, recipe,
role, word, morpheme, POS, sound, language, plaintext, meaning, or translation
is assigned.
