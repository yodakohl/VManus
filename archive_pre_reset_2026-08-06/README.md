# Frozen pre-reset investigation

This directory is the intact local snapshot retired on 2026-08-06.

- `semantic_assumptions/` is the former active experiment tree, including its
  complete ledger, reports, scripts, caches, registrations, and failed routes.
- `root_documents/` contains the former large root README, state/handoff/worklog,
  chat takeovers, and structural “translation” presentation.
- `ARCHIVE_MANIFEST.tsv` binds every regular archived file by size and SHA-256.

The archive is read-only by policy, not by filesystem permissions. Do not run a
route from it or import an old claim into active state without first consulting
the compact closed-family index and stating genuinely new evidence. Archived
runners remain at a directory depth that preserves their original repository-
root resolution.
