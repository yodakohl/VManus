# GDT317 — `q` post-DY control calibration

Freeze the GDT316 Voynich instrument unchanged. For every other already frozen
GDT278 observation-layer control, form opaque
`PAGE_HOST × frame × inner-D × right × DY × B3` cells. A cell is eligible when
it contains at least two `q` and two non-`q` choices, with each class present on
at least two physical folios. A panel is powered at five cells, 100 events,
five folios, and 20 `q` choices.

For each powered panel, compute whether the immediately preceding physical
group is DY-closed and run ridge-10 leave-one-physical-folio-out exact-cell
versus exact-cell-plus-preceding-DY scoring. Keep the same 8,192-world
fixed-crossfit cell/register alignment diagnostic. Do not rescale, relabel, or
tune a panel.

Rank Voynich by held gain and exact-cell/register-matched post-DY delta. Call
the behavior Voynich-enriched only when Voynich ranks first on both axes. Call
it non-specific when at least two controls equal or exceed the Voynich gain;
otherwise report mixed.

This calibrates an observation-layer surface-choice behavior. A control `q`
class is an analogous parser class, not a common prefix or linguistic unit.
No morpheme, POS, meaning, sound, language, plaintext, or translation is
assigned. No f84 row may be opened, parsed, retained, joined, or scored.
