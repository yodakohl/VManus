# SNPL001 — public repeated-plant source-native capacity

## Question

Can four publicly documented Herbal--pharmaceutical copies support one new,
source-native label-to-prose test without reusing the failed parsed-root score?

## Public source admission

The relations must be recovered from the public `voynich.nu` catalogue, not
from a prompt or an archived VManus claim. The associated label must also be
bound by the public manual transcription and the published old-to-current
locus crosswalk.

Frozen relations:

- f48v -- f89v2 fragment 54 -- current label f89v2.6;
- f18v -- f102r2 fragment 212 -- current label f102r2.21;
- f23r -- f102r2 fragment 213 -- current label f102r2.22;
- f19r -- f102v1 fragment 240 -- current label f102v1.17.

The f89v2 manual says its label is partly under plant `[1,4]` and next to
plant `[1,5]`. Preserve this as an explicit ownership ambiguity. The other
three manual entries directly identify the associated plant position.

## Score-blind capacity

Use the validated source-STA consensus-group table. Read only the four label
rows and non-target Herbal confirmed-prose rows. Do not read or score prose on
f48v, f18v, f23r, or f19r.

For each ZL3b/IT2a/RF1b label reading, inventory its contiguous four- and
five-symbol STA member motifs and their page document frequencies in the
target strata `A/hand 1` and `B/hand 5`. This is a capacity/rarity inventory,
not a semantic score. The readings are alternate descriptions of the same
ink, never independent samples.

Proceed to a separately frozen target only if all four public relations and
label mappings bind, every label-reading has at least one non-universal
four-or-five-symbol motif in both strata, both strata retain at least five
background pages, and the four-way assignment orbit can attain one-sided
`p <= .05` (`1/24`).

Even a future pass can license only manuscript-internal same-plant reference
signal. It cannot by itself identify a plant name, English word, sound,
language, cipher, plaintext, or translation.
