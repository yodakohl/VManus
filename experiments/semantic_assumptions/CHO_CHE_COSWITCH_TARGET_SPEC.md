# `cho/che` independent co-switch single target

Status before execution: **REGISTERED_SINGLE_RUN**

Bind the validated 5,012-row masked panel, v2 capacity, stopped v1 preflight,
passing v2 blockwise preflight and its independent reconstruction, both frozen
scoring cores, and the source-native group-alignment table plus its independent
validation.  Result and report must be absent and installed without clobbering.

Read the source family table once and join exactly the masked source-group IDs.
Require zero STA alternatives and exact stored formal length.  Apply the
representation and nuisance-cell rules in
`CHO_CHE_COSWITCH_SYNTHETIC_PREFLIGHT_SPEC.md` exactly: at least two groups on
both sides, equal cell means, low-to-high difference, and the frozen 24/48/576
family, endpoint, and ordered-bigram blocks.  Run the v2 scorer once.  Store no
event-level family sequence or feature vector; store only aggregate score,
block/reading/leaf-vector hashes, counts, gates, and bindings.

The already validated `.675/.155`, `.673/.157`, and `.683/.160` defining-site
mixtures are the positive control.  Recto/verso is controlled by within-leaf
pairing, both 5/3 state orientations, the mandatory cross-orientation cosine,
and the preflight's zero-passing side-only worlds.  The diagnostic/circle
subgroup is controlled by the mandatory prose/diagnostic cross-cosine and its
two corresponding zero-passing adversarial families.

No threshold, block, nuisance cell, state label, leaf, reading, or direction may
change after registration.  A pass establishes only that at least two distinct
off-site formal feature blocks share a distributed held-leaf direction beyond
the defining construction.  A nonconfirmation rejects that broad co-switch
model at the calibrated resolution and permits a separately preregistered
canonicalized-form representation test; it does not prove that only `o/e`
changes.  Neither result identifies meaning, sound, wordhood, language, cipher,
plaintext, or translation.
