# Second-member incremental opening capacity

Status: **FROZEN_TARGET_LABEL_MASKED_CAPACITY_ONLY**

## Question

After fixing the complete coarse family remainder and the exact first member
state that already explains the confirmed local `qo` orthotactic relation, is
there enough repeated structure to test the exact second remainder member on a
held physical folio?  This is an incremental longer-template question, not a
rerun of the first-onset result.

Use the frozen 1,207-row mixed-cell panel.  Derive the second remainder member
triplet only after the same longest `DAQKJ`, `DAQK`, `DAQ`, `DA`, else `NONE`
split.  Output no operation row label, prefix code, complete remainder member
sequence, unit surface, locus, page, or English field.

## Eligibility

A row is preliminary-eligible only if:

1. its remainder has at least two STA members;
2. its exact `(base, first-onset)` baseline occurs with at least two different
   second-member triplets in the complete masked panel;
3. its exact `(base, first-onset, second)` refinement occurs on at least two
   physical folios.

After those rules, score only preliminary-eligible rows on folios containing
at least three such rows.  The folio threshold is fixed before any operation
label or score and prevents single-row folios from controlling equal-folio
inference.

Capacity passes only with at least 600 final rows, 40 folios, 15 bases, 25
varying baselines, 35 reusable refinements, 12 second-member triplets, 600
all-reading-consensus second states, at least 100 rows in each Currier register,
and maximum row share of one folio<=.10.

## Ceiling

A pass authorizes target-free synthetic calibration of a base-plus-first versus
base-plus-first-plus-second held-folio predictor.  It does not establish a
longer dependency and supplies no morphology, sound, word function, POS,
syntax, language, cipher operation, meaning, plaintext, or translation.
