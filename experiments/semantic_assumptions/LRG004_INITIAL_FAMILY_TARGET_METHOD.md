# LRG004 one-shot initial-family target

Status: `REGISTERED_UNSCORED_ALL_24_TARGET`

Join the exact 2,767-row LRG001 B/P target in its frozen cell/class order and
replace each source-native family sequence by only its first official family.
Evaluate all 24 families simultaneously with the validated LRG004 fixed-quota
max-statistic null and every effect, folio, B/P, parity, balance, deletion, and
concentration gate unchanged.

Emit all 24 aggregate family metrics and every registering family code and
direction. Do not emit source sequences, rows, forms, member codes, feature
weights, EVA strings, or post-target subsets. Zero families is a valid final
result and closes this exact discovery design.

A registering positive family is only more frequent at the start of manual
label groups than matched prose; a negative family is less frequent. Even a
stable family is not thereby a prefix, classifier, morpheme, word, POS, name,
identifier, sound, meaning, plaintext, or translation. Independent exact
reconstruction is mandatory before integration into the structural reading.
