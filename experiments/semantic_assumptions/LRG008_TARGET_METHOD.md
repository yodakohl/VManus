# LRG008 one-shot label-versus-diagram profile projection

Status: `REGISTERED_UNSCORED_SINGLE_TARGET`.

## Fixed target

Reconstruct the two independently confirmed LRG001 648-column profiles from
its exact 2,767-row B/P training panel and official STA alphabet
`ABCDEFGHJKLMNPQRSTUVWXYZ`. The profile learned on odd physical folios scores
only even LRG008 folios; the even-trained profile scores only odd LRG008
folios. No profile is relearned from the LRG008 target.

Join all 286 hash-bound LRG008 capacity rows to the complete consensus-group
table by `consensus_group_id`, verifying page, folio, section, symbol count,
and manual role. Reconstruct the same 648 source-native family features, emit
no sequence or feature weight, and compute one scalar opposite-parity profile
score per row internally.

Apply the validated LRG008 v3-R1 statistic once: average-rank scores inside
each of the 40 exact page-by-length cells; contrast 146 manual `L` rows with
the 140 pooled `C/R` rows; average cells within pages, pages within folios, and
six folios equally; use the frozen 8,192 fixed-quota null. Apply every frozen
effect, p, z, role, section, parity, folio-support, deletion, concentration,
and `.50` parity-balance gate without change.

The output may contain only aggregate effects, p/z, robustness summaries,
matrix/vector/profile/null digests, gates, status, and decision. It must not
emit row scores, sequences, family identities, feature weights, favorable
forms, or EVA.

## Decision ceiling

On pass: the already confirmed label-associated construction profile also
distinguishes manual `L` labels from other circular/radial diagram writing on
this fixed six-folio panel. This supports a distinct label structural register
rather than generic short diagram text.

On failure: the profile does not establish label specificity beyond prose.

Neither outcome establishes identifier, name, noun, owner, object, part of
speech, word, sound, language, meaning, plaintext, or translation.
