# `cho/che` co-switch capacity v2 — inferential-unit correction

The v1 score-blind capacity result is preserved at SHA-256
`5f1ae292148f27d31aa02e14b7b766b3019bd98ff5b552207485b73f28b0ecce`.
It stopped only because RF retained 1,597 groups rather than the rounded 1,600
required by one gate.  No STA family sequence, feature/state association,
score, effect, or p-value had been computed.

That total-row gate is not an inferential capacity test: groups are nested
measurements inside physical page sides.  The frozen independent units are the
eight physical leaves, and the already frozen relevant capacity gates are:

- eight opposite-state all-reading-agreed leaves;
- both high-side orientations represented at least three times;
- at least 30 retained off-site groups on every reading/leaf/side;
- a synchronous 256-state leaf-flip orbit with attainable one-sided floor
  `.00390625`;
- at least five prose and two diagnostic leaves;
- exact metadata-cell overlap and zero defining-site/target fields.

V2 therefore removes only the arbitrary 1,600-total-groups-per-reading gate.
The observed per-reading totals remain mandatory descriptive checkpoints.  No
power claim replaces it: target access remains forbidden until a frozen
synthetic calibration demonstrates null control and power on this exact eight-
leaf geometry.  All other v1 definitions, exclusions, inputs, gates, and claim
ceilings are unchanged.

This is a prospective target-free design correction, not a score rerun or a
weakened manuscript threshold.  It supplies no co-switch result, meaning,
sound, wordhood, language, cipher, plaintext, or translation.
