# Post-ETR001 route and evidence-recovery audit

Date: 2026-08-10

Decision: **HOLD — no currently identified nonduplicate target statistic**.

## Rejected internal successors

1. Cross-record shared-form orientation is not identifiable beyond existing
   evidence. A raw folio-reversal score would rediscover the confirmed exact-
   form FIRST/LAST/EDGE/CORE tendencies, the line gradient, and IL010-like
   directional assembly. Pair-order residuals cannot be independently
   permuted because all pair orders in one record must form one transitive
   order. Exact conditioning on record bags, form-position margins, and local
   transitions reduces to the already stopped IL026 pairwise-preserving route.
2. A target-centered matched-offset kernel has nominal label-free geometry:
   1,784 movable targets, 445 target-bearing records, 453 donor records, 118
   strict cells, 21 folios, 6,655 cross-folio exact `(L,j,m)` target pairs,
   1,738 eligible targets, 239 pair strata, 174 folio pairs, 39,691 same-offset
   terms, 213,408 cross-offset terms, and maximum folio endpoint exposure
   0.1482. It is nevertheless not identified beyond first-order grammar. If
   the conditional offset mean is `mu[y,r] = T^r phi(y)`, same-offset minus
   cross-offset similarity is generally positive even for a pure first-order
   chain. The existing whole-record donor null breaks target-neighbour
   dependence, so the mandatory pure-first-order negative control cannot pass
   in principle. This would be a nonlinear LRS001 representation rescue, not
   a new falsifier.

## EAS001 artifact recovery

The five reset-manifest SHA-256 values and exact filenames were searched as
source-only recovery keys. No match was found in:

- indexed exact-hash or exact-filename web search;
- any public Git branch or tag (the repository exposes only `main`);
- GitHub releases, Actions artifacts, or forks for `yodakohl/VManus`;
- exact-filename search across the local home and temporary-storage trees.

This extends, but does not change, the existing Git-object provenance audit:
EAS001 remains historical and unreconstructable. Recovery would establish
provenance only, not manuscript meaning.

## Boundary

Do not score either rejected internal route, weaken ETR001 exact strata, or
restore EAS001 as confirmed. The next substantive manuscript test requires a
new provenance-clean author-visible relation, hash-matching artifact recovery,
or a statistic with a demonstrated null that preserves the already-confirmed
local grammar. No word, POS, language, meaning, plaintext, or translation is
established here.
