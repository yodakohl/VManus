# RPE001 radial endpoint-polarity capacity

Status: **PASS_UNSCORED_60_STRICT_RADIAL_LOCI_5_FOLIOS**

The public IVTFF convention explicitly distinguishes inward radial text (`Ri`) from outward radial text (`Ro`). The complete public/manual inventory has 142 radial loci: 75 `Ri` and 67 `Ro`. Requiring at least two complete, zero-alternative consensus source groups leaves **60** loci on **11 pages and 5 physical folios**: 23 `Ri` and 37 `Ro`. Each direction spans four folios, with a three-folio bidirectional core.

This permits a falsifiable test that reverses textual start/end while holding physical center/outer position fixed. Endpoint family identities were not aggregated and no score was computed. Capacity alone supplies no direction word, semantic label, plaintext, or translation.
