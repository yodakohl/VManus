# Semantic reset complete

Status: **RESET_COMPLETE_NO_ACTIVE_SEMANTIC_ROUTE**
Date: 2026-08-06

Historical-state notice (2026-08-08): the archive sizes and hashes below
correctly describe the reset-time snapshot, not the current workspace. The
superseded bulk was later removed; only the curated primary-evidence subset and
compact route registries remain. See `WORKSPACE_CURATION_RESULT.json` and the
current archive manifest.

The complete pre-reset investigation was moved intact to
`archive_pre_reset_2026-08-06/`. The old semantic directory contains 105 ledger
entries, 16,268 files, and 4,629,172,562 bytes before addition of archive
metadata. No semantic result was promoted: zero English lexemes and zero
plaintext clauses remain established.

The final archive contains 16,280 regular files and 4,630,559,982 bytes outside
its two self-manifest files. Its complete SHA-256 inventory is 2,355,383 bytes
with digest `dade5b48caa64acf557020f96d52675bf6f37ad03f304a879eb6bc5e4c990271`.
The archive was then independently re-read against all 16,280 entries with no
drift. All eleven retained grammar runners import read-only from their archived
location, and the canonical ZL parse remains 5,376 loci/38,988 tokens.

At reset time the active directory contained only a compact grammar baseline,
primary-evidence index, closed-route family index, empty hypothesis queue, and
integrity tools. Later post-reset work is recorded in the active state and
ledger; this report does not supersede them.
