# SME001 calibration independent validation

**PASS — 17/17 nonimporting checks validate the underpowered decision.**

A separate implementation reconstructed both 8,192-assignment phase ensembles, all 64 complete 84-feature null worlds, all 96 target/strength power worlds, every stored planted-row statistic and gate, requested-material bisections, joint pass counts, calibration gates, and the two high-power failures. The seven frozen manuscript-side inputs were read only as opaque bytes for SHA-256; no target row or anonymous feature value was parsed or joined, and no target artifact exists.

Coverage was complete: 21,504 null and 32,256 power target-feature/ensemble rows were rescored. The null artifact stores only final page/folio pass counts and joint sets; those matched exactly. For power worlds, all full-family maxima were rebuilt and all 5,472 stored planted-row field comparisons matched.

The reconstruction confirms 0/64 null worlds with a joint pass, but only 5/8 ray and 0/8 tail worlds passing at requested material .500, below the frozen 7/8 and 6/8 requirements. SME001 therefore closes as underpowered without a manuscript score.

This validates synthetic calibration behavior only and supplies no manuscript association, marker function, meaning, lexeme, plaintext, language, or translation.
