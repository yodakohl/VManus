# CRE001 control validation — attempt 1

Status: **INCOMPLETE_INVARIANCE_RECONSTRUCTION_TARGET_UNOPENED**

The first nonimporting validator exactly reconstructed all six numeric control
records and construction fixtures, but it trusted the producer's two stored
invariance booleans instead of independently rebuilding the affine and
serialization fixtures. The manuscript target remained absent. This artifact
does not authorize target access; the corrected validator must reconstruct the
invariances and full top-level gate object.
