# CCT002 one-shot marginal-null canonical-transfer target

Use the same exact 2,223 masked IDs and exact single-site raw-to-`X`
canonicalization registered for CCT001.  CCT001's association was never
scored.  The sole CCT002 change is its independently capacity-validated and
synthetically calibrated merger null: bijectively permute all `e` members
inside exact `(length,ch/sh-prefix,site-index)` shells while every `o` member
remains fixed.  Every draw preserves every member and member frequency exactly
once.  Score 8,192 deterministic draws and the unchanged exact 256 leaf-state
orbit.

All CCT001 state-excess, reading, folio, deletion, concentration, prose,
diagnostic, prefix, materiality, p-value, and decision gates remain unchanged.
No output may contain a raw type, canonical template, pair identity, page
score, or folio score.  Require a public exact-file freeze and absent target
and validation artifacts before the single run.

Decisions are unchanged: all gates pass confirms only useful general formal
canonical transfer; core gates with domain/prefix failures are a general
domain/prefix-limited nonconfirmation; every other failure is
`NONCONFIRM_CCT002_CANONICAL_TRANSFER`.  Production-free reconstruction is
mandatory.  No decision establishes wordhood, sound, language, cipher,
meaning, plaintext, or translation.
