# LRG001 one-shot source-native label-register target

Status: `REGISTERED_UNSCORED_SINGLE_TARGET`

The target uses the exact 101 B/P mixed cells, 2,767 rows, two parity
directions, 648-column feature map, 8,192 held assignments, and all decision
gates validated in LRG001 calibration v2.

For each capacity cell, join every strict `kind=L` group followed by every
strict `kind=P`, `grammar_scope=CONFIRMED_PROSE` group, sorting within class by
`consensus_group_id`. The only target feature is the complete
`family_surface`; its length must equal `symbol_count` and every member must be
one of source-native families `A`--`X`.

The production result may contain only aggregate matrix/profile digests,
direction effects, p-values, folio/section summaries, deletions,
concentration, gates, and the final decision. It must not emit individual
family weights, form rankings, source sequences, member codes, EVA strings, or
favorable row identities.

If all frozen gates pass, the claim is:

> A source-native construction profile learned from manual label rows on one
> physical-folio parity transfers to exact-page/exact-length manual labels on
> the other parity in both directions and both B/P sections.

This may be called a `label-associated structural profile`. It may not be
called an identifier, name, noun, object owner, word class, part of speech,
language feature, lexeme, plaintext, or translation. Projection into prose is
permitted only after production-free exact reconstruction and requires a new
frozen descriptive artifact.

If any gate fails, close this exact 648-column B/P held-parity representation
without mining features, forms, folios, pages, sections, or thresholds.
