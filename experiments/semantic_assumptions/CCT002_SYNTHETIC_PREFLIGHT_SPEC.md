# CCT002 target-blind marginal-null calibration

Use the exact masked 2,223-event geometry, synthetic generator, held-folio
state-excess estimand, 256 state orbit, thresholds, robustness blocks, 64 null
worlds, partial-strength grid, and nine negative families frozen for CCT001.
The sole scientific change is the separately capacity-validated CCT002 merger
null: collision pairs are permuted bijectively inside exact
`(length,prefix,site-index)` shells without pair-frequency bins.

Every draw must retain every `o` and `e` member exactly once.  Thus member
frequencies and all additive member-specific effects are fixed, while the
surrounding-string counterpart pairing is broken.  Score exactly 8,192
deterministic SHA-ranked draws.  The capacity floor remains at least 24 pairs,
16 movable pairs, all eight leaves, and all three readings.

Acceptance is unchanged: at most 1/64 null passes; 8/8 full distributed worlds
pass; at least 6/8 worlds pass at the weakest registered partial strength with
primary state excess at least .05; every side-only, one-folio, one-reading,
prose-only, diagnostic-only, one-prefix, one-side, generic-compression, and
unique-surrounding world gives 0/8; state complement, malformed inputs, and
exact member-marginal invariance pass; independent code reconstructs all 176
worlds and decisions.

A pass authorizes only separately frozen CCT002 target execution.  It supplies
no manuscript association, canonical form, word, sound, language, cipher,
meaning, plaintext, or translation.
