# Source-native within-group interior-position capacity

Status: **REGISTERED_SCORE_BLIND_CAPACITY**

## Question and distinctness

The confirmed `FIXED_5` result compared complete groups with an exchangeable
family baseline. It may therefore be driven entirely by the already-confirmed
special behavior of group edges. Before calling it a five-position interior
grammar, test a stricter question: after removing the first and last family
from every group and conditioning on Currier plus exact original group length,
does relative position still predict held interior families?

This is not the closed internal-locus order route, which tested order across
successive source groups inside a physical locus. The new unit is a family
position inside one complete source group. It is also not a rerun of the
complete-group stage target: endpoints are absent from every fit and score,
and exact original length is a baseline condition.

## Score-blind capacity

Start only from the frozen 21,899-row stage capacity panel. Retain groups of
original length at least three. Preserve its already frozen physical-folio
TRAIN/CAL/TEST split. The derived public table may contain only the existing
identity and metadata, original length, interior length, and split. It must
contain zero family sequences, member codes, endpoint values, interior values,
positions, scores, fitted parameters, or English glosses.

Continue only if the derived table has exactly 19,203 groups / 45,867 interior
symbols on all 94 folios, split as:

- TRAIN: 9,364 groups / 22,260 interior symbols / 47 folios;
- CAL: 4,887 / 11,693 / 23;
- TEST: 4,952 / 11,914 / 24.

Both Currier registers must retain at least 1,000 groups in every split, and
original lengths 3--11 must be preserved without consulting any family value.

## Next allowed action and ceiling

Capacity authorizes only a separately frozen target-free calibration of an
interior-only length-conditioned positional model. No source family sequence
may be opened during calibration. No result establishes an affix, root,
suffix, sound, word, part of speech, language, meaning, plaintext, cipher, or
translation.
