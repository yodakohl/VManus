# Drawing-segment exact-form position reanalysis

Recompute the published exact STA-family `FIRST` versus `LAST` and `EDGE`
versus `CORE` descriptive atlas using confirmed drawing-segment positions while
preserving the original physical-locus position labels for comparison. Use the
unchanged eligibility and label rule: at least 20 occurrences, 10 physical
folios, 20 contrast observations, absolute corrected log-odds at least 1.0,
and at least 90 of 94 leave-one-physical-folio coefficients in the same
direction.

Report every old-to-new label transition. This is a correction audit of
structural position tags, not a confirmatory test. Segment positions are not
words, POS, sentence roles, meaning, plaintext, or translation.
