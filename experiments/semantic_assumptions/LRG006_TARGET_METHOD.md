# LRG006 one-shot A1-member target

Status: `REGISTERED_UNSCORED`

The target uses the exact 677-row, 69-cell LRG006 A-initial panel. Its only
feature is `A1_ALL_READINGS`: the first structural member is exactly A1 in
ZL3b, IT2a, and RF1b. Every other exact A-member triplet is the complementary
state. ZL3b, IT2a, and RF1b remain alternate readings of one manuscript item.

The target reconstructs the 163 manual-label and 514 confirmed-prose roles
once, preserves every page-by-symbol-count cell quota, and applies the frozen
v2 two-sided statistic and 8,192 fixed-quota assignments. A result must pass
all predeclared folio, B/P section, odd/even folio, deletion, concentration,
material-effect, z, and p-value gates.

Positive effect means A1 is more frequent in labels than prose within the
conditioned A family. Negative effect means the complementary other-A state is
more frequent in labels. A powered nonconfirmation retains only the already
confirmed broad A-family label register and closes exact A1 localization at
this resolution.

The producer and clean validator may emit only aggregate metrics, hashes,
gates, status, and decision. They may not emit unit IDs, loci, pages, surfaces,
member triplets, or row roles.

A pass establishes only an exact member substate inside the structural A
family. It cannot establish a sound, normalized spelling, word, part of
speech, function, language, cipher operation, English meaning, plaintext, or
translation.
