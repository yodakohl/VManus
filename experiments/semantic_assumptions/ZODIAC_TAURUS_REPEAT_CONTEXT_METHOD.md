# ZODIAC_TAURUS_REPEAT_CONTEXT — post-result masked context audit

Date: 2026-08-10

This audit follows the already observed exact `AQJABABA` / nearest-basic-EVA
`okeodaly` repeat on the two public Taurus half-pages.  It is explicitly
post-result and cannot provide a confirmatory p-value.

## Fixed scope

- f71v.1 and f72r1.1, the circular loci containing the two frozen witnesses;
- ZL3b, IT2a, and RF1b scored separately;
- every zero-alternative source-native group in each locus;
- the exact target family surface `AQJABABA` is removed from every context,
  including contexts centered on other positions.

## Context and reference

For each group position, combine the preceding and following cyclic windows at
radii 1, 2, 3, 5, and 8.  Direction is deliberately discarded, making the
context invariant to rotation and reversal of a circle.  Score weighted
Jaccard in six source-native views: STA-family n-grams of length 2--4 and exact
STA-member n-grams of length 1--3.

For each reading/radius/view, compare the masked candidate-position pair with
all cross-locus position pairs.  Store the inclusive rank, exact tie count, and
tie-aware midrank percentile.  A homologous diagram field would require broad,
cross-reading high rank rather than one selected view.  Report the complete
90-cell grid, its median midrank percentile, and top-decile counts by reading.

## Claim ceiling

This can test only whether the two occurrences occupy similarly patterned
local circular contexts under these source-native representations.  It cannot
prove or disprove that the form is a freely positioned content item, and it
cannot establish TAURUS, a sign name, a word, meaning, sound, language,
plaintext, or translation.
