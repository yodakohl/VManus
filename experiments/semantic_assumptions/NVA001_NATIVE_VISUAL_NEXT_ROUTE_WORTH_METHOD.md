# NVA001 native-visual next-route worth audit

Status: **PREREGISTERED_SOURCE_ONLY_WORTH_AUDIT**.

## Question

After the complete f70v2 star-tail census and the pharmaceutical container
extension stop, does any currently proposed native-visual continuation supply
a genuinely new, adequately replicated author-visible relation before another
image body or Voynich filler is opened?

## Inputs and access boundary

Use only the active ledger and these already published source projections or
results:

- `results/existing_human_exact_locus_annotations.tsv`
- `results/existing_human_locus_roles.tsv`
- `results/public_voynich_nu_page_annotations_v2.tsv`
- `results/dri001_paired_document_role_inventory_selection.json`
- `results/dri001_paired_document_role_inventory_result.json`
- `results/dri002_discordant_cell_role_capacity_result.json`
- `results/translation_anchor_acquisition_registry_v1.tsv`

Do not open a manuscript image, transcription surface/family/member/root,
parser role, OCR output, embedding, or semantic score.

## Frozen candidate checks

1. **Held-star point count.** Stop as a duplicate if the ledger already has a
   final complete-panel result for 52 f68 loci and 17 frozen tests.
2. **Herbal leaf-margin state.** Stop as a consumed route if the complete
   residual visual census passed but the prespecified formal successor stopped
   at synthetic calibration before target access.
3. **Strong exact-local relation class.** Reconstruct unhedged exact-local
   attachment, enclosure, and overlap/contact rows. Require at least one human
   annotation unit containing two strong relation types; otherwise a relation
   classifier is page/register-confounded and stops.
4. **Retracing-feature recurrence.** Count all exact-locus comments mentioning
   retracing, then count the strict feature-difference statement `original
   hook ... Retracer did not`. Require a second feature-difference locus on a
   different folio; generic retracing comments do not qualify.
5. **f66r marginal columns.** Stop as a duplicate if the ledger already
   retains the physical hierarchy and reports the prior marker/heading/order
   tests as failed.
6. **Compact text-only role cell.** Reconstruct the exact section `T`, kind-run
   `P` cell. The two DRI001 pages are already `PROSE_DOMINANT`. Human catalogue
   prose says f1r is a text-only page whose four paragraphs fill the page, so
   only f86v6 remains capable of adding another role. Stop because no second
   role can reach two physical folios.
7. **Translation-anchor registry.** Require at least one currently admissible
   row among all eleven registered evidence families. If there is none, direct
   geometry cannot be promoted to a readable anchor.

## Decision

`AUTHORIZE_NEW_NATIVE_VISUAL_PANEL` requires a passing strong-relation unit,
recurrent feature-difference locus, compact role cell, or admissible anchor.
Otherwise return
`STOP_NO_GENUINELY_NEW_CAPACITY_QUALIFIED_NATIVE_VISUAL_ROUTE` before image or
filler access.

The stop does not claim that the manuscript is untranslatable. It says current
ordinary-image and human-annotation data do not supply a new one-to-one
readable contrast. Reopening requires new readable authorial ownership, a new
independent physical folio satisfying a named reopen condition, or new
physical-layer/multispectral evidence. Structural records and reset boundaries
may be used to prepare matching against such future evidence, never as English
glosses by themselves.
