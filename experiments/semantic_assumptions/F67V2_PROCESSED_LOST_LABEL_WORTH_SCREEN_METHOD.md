# f67v2 processed lost-label worth screen

Date: 2026-08-12

## Fixed target

The target was selected from the existing human exact-locus annotation before
viewing the processed page. `f67v2.21` is the missing southwest member of the
four-corner-circle label register. The annotation reports that the vellum was
curled in the 2004 scan, is unfolded in the current scan with some paint loss,
and retains possible brown-ink traces of a sideways label that is "quite
unreadable." The same human source separately notes faint smudges in the large
annular band but considers reverse-side bleed-through more likely than an
erased text ring.

Inspect exactly the official Yale f67v canvas and the corresponding public
December-2024 `oshfdk` paint-removal rendering. The mapping is Yale manifest
item 121 to processed `image.122.jpg`. Exact URLs, image hashes, dimensions,
canvas ID, annotation hash, and prior special-circle screen hash are frozen in
`f67v2_processed_lost_label_worth_screen_observations.tsv`.

## Question and stop rule

Ask only whether the processed witness yields either:

1. a stable multi-character segmentation for the southwest corner label; or
2. a distinct recoverable text sequence in the ghost annulus.

Stop if neither appears. Paint suppression is an algorithmic display
transform, not physical-layer imaging, and disappearance or survival under the
filter cannot prove erasure, layer order, or reverse-side bleed-through.

This is direct source-bound native AI visual inspection recorded as
machine-authored, not human annotation. It uses no OCR, automated
transcription, glyph classifier, CLIP, embedding, similarity score, proposed
reading, decoder, or language fit.

A pass could add only an anonymous Voynich-script surface to an already known
diagram register. A stop supplies no direction, corner name, object, word,
sound, language, cipher, plaintext, meaning, or translation.
