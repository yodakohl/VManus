# LRG002 one-shot prose-slot target

Status: `REGISTERED_UNSCORED_SINGLE_TARGET`

Reconstruct the already confirmed LRG001 label/prose profiles with the official
24-family alphabet. For each B/P confirmed-prose row in the fixed 5,824-row
LRG002 panel, compute its 648-column family profile and score it only with the
profile trained on the opposite physical-folio parity. Convert scores to
average ranks within exact page x symbol-count cells exactly as calibrated.

On the fixed 5,769-row, 705-segment primary panel, compute the frozen
FIRST-minus-CORE and LAST-minus-CORE vector, both 8,192-rotation nulls, B/P and
odd/even transfer, folio support, deletions, concentration, and all v2 gates.
No statistic, threshold, segment, row, normalization cell, or gate may change.

The target result may contain only aggregate counts, array/profile/null
digests, position-vector summaries, p-values, gates, status, and decision. It
must not emit row scores, ranks, sequences, individual family weights,
favorable forms, member codes, EVA strings, or row identities.

If all gates pass, the claim is limited to:

> Relative likeness to the confirmed manual-label structural profile occupies
> a distributed and repeatable corrected-segment position in B/P prose.

FIRST, LAST, and CORE remain structural coordinates. A pass does not make the
favored coordinate a name, identifier, noun, subject, object, opening, closing,
POS, word, meaning, plaintext, or translation. If any gate fails, close this
exact projection without mining forms, rows, folios, pages, thresholds, or
subsets. Independent clean reconstruction is mandatory before interpretation.
