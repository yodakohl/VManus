# LRG006 A1-versus-other-A member capacity

Status: `FROZEN_ASSOCIATION_UNOPENED_CAPACITY`

LRG004 confirmed family A as label-associated, but official A contains several
member spellings. LRG006 asks whether that result is specifically an A1 state
or remains a family-level effect. This is a conditional post-confirmation
localization, not a new semantic route.

Use only the exact B/P LRG001 page-by-length cells. Keep strict A-initial manual
label and confirmed-prose rows, then keep only cells containing both roles.
The sole feature is `A1_ALL_READINGS`: the first source-native member is exactly
A1 in ZL3b, IT2a, and RF1b. Every other A-member triplet is the complementary
state. Alternate readings remain one triplet.

The capacity artifact may emit only opaque unit/cell IDs, folio, section,
per-cell role quotas, aggregate feature totals, and a feature-vector hash. It
must not emit row roles, member codes, surfaces, pages, loci, or any
role-by-feature contrast. Capacity requires at least 650 rows, 150 label rows,
500 prose rows, 65 cells, 13 folios, 50 feature-variable cells containing 575
rows, and both sections.

The D-family member branch stops at capacity: fewer than ten label rows, fewer
than five folios, or only one exact D-member triplet forbids a D-member target.

A future target requires target-blind two-sided calibration with distributed
positive and negative power plus one-folio, one-section, one-parity, random,
page/length/cell-constant, and concentration controls. A pass can identify only
an exact member substate. A powered nonconfirmation can say only that no
material A1-versus-other-A contrast was detected at the frozen resolution. No
sound, word, POS, function, meaning, plaintext, or translation follows.
