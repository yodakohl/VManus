# Exact-member onset compatibility capacity

Status: **FROZEN_TARGET_LABEL_MASKED_CAPACITY**

## Question

The exact-member safeguard identifies `D1 A1 + remainder` as the dominant
formal `DA` construction.  The next distinct question is whether use of that
construction is conditioned by the exact STA member immediately following the
prefix.  This is an internal compatibility test, not a rerun of the failed
locus-position or neighboring-group context target.

## Label-masked panel

Use exactly the 1,207 rows in the existing 197 mixed family-remainder/physical-
folio quota cells.  For each row strip the already frozen family operation and
retain only:

- existing opaque unit and family-remainder IDs;
- physical folio and Currier register;
- an opaque ID for the ordered first-remainder member triple
  `(ZL3b, IT2a, RF1b)`;
- whether that triple is identical in all three readings.

Store no `NONE`/`DA` row label, prefix member code, full remainder signature,
locus, page, neighbor, score, or English gloss.  The existing quota table may
provide aggregate label counts only.

## Capacity gates

Before any model is defined or target label is joined, require:

- exactly 1,207 rows, 197 quota cells, 59 folios, and 44 family remainders;
- at least 20 distinct onset triples;
- at least 25 family remainders with two or more onset triples;
- at least 75 quota cells and 500 rows in cells with onset variation;
- at least 35 folios represented by onset-varying cells;
- at least 1,000 rows, 50 folios, 25 family remainders, and 40 family/onset
  pairs whose same family/onset combination occurs on another physical folio.

These are feature/cross-validation capacity checks only.  A pass may authorize
target-free calibration of one leave-physical-folio-out onset-compatibility
statistic.  It cannot authorize a real target without a separate frozen
calibration and target specification, and cannot establish detachment,
allography, morphology, sound, wordhood, syntax, language, cipher operation,
meaning, plaintext, or translation.
