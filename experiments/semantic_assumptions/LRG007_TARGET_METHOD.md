# LRG007 one-shot A/D prose-edge transfer target

Status: `REGISTERED_UNSCORED`

Join the exact 4,911-row opaque LRG007 panel to the source-native family table
once. Encode only the first family as A=`+1`, D=`-1`, other=`0`, and require the
vector to match the capacity hash and every frozen per-cell A/D/other margin.

Apply the unchanged calibration statistic, exact-margin 8,192-assignment
maximum null, and all FIRST-minus-CORE and LAST-minus-CORE gates. Both channels
must pass, including separate A-enrichment and D-depletion contributions,
folio support, B/P, odd/even, deletion, and concentration requirements.

The result may emit aggregate channel, folio, section, parity, component,
null, gate, hash, status, and decision fields only. It may not emit unit IDs,
loci, pages, source strings, family sequences, or row assignments.

A pass establishes only that the already confirmed A-positive/D-negative label
register transfers as an A-over-D opposition at both independently defined
prose edges. FIRST, LAST, and CORE remain structural coordinates and do not
mean opening, closing, subject, object, word, part of speech, sound, language,
cipher operation, English meaning, plaintext, or translation. If either
channel fails, close this exact transfer without subset or threshold mining.
