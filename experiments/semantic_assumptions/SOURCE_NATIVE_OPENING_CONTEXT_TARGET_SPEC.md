# One-time `NONE` versus `DA` structural-context target

Status: **FROZEN_ONE_TIME_TARGET**

After the independently validated synthetic preflight, the runner may open the
strict source-family groups once and reconstruct the real `NONE`/`DA` labels for
the 5,826 masked rows. It must score only the 1,207 rows in the 197 mixed exact
remainder–folio quota strata, preserving the calibrated row order, features,
8,192-assignment null orbit, statistic, and gates byte-for-byte.

The target reports `POSITION_PASS` and `NEIGHBOR_PASS` separately:

- `POSITION_PASS` means the presence of the `DA` opening on an otherwise exact
  family remainder has a transferable locus-role association across folios and
  both Currier registers.
- `NEIGHBOR_PASS` means it has a transferable immediate adjacent-group context
  after locus role and exact group count have been removed from the neighbor
  features.

The four frozen decisions are:

- both: `RETAIN_POSITION_AND_EXTERNAL_CONTEXT_OPERATION`;
- position only: `RETAIN_LOCUS_POSITION_OPERATION_ONLY`;
- neighbor only: `RETAIN_EXTERNAL_NEIGHBOR_CONTEXT_OPERATION_ONLY`;
- neither: `RETAIN_OPENING_CHAIN_CAPACITY_ONLY`.

No subgroup, remainder, folio, section, register, neighbor family, feature,
threshold, null, or label may be selected after the join. Store only aggregate
summaries and label totals, never row assignments, remainder identities, loci,
pages, or event contexts. Even a pass establishes only a neutral structural
association for the source-family `DA` opening. It cannot establish physical
detachment, wordhood, a prefix name, part of speech, syntax label, sound,
language, cipher operation, English meaning, plaintext, or translation.
