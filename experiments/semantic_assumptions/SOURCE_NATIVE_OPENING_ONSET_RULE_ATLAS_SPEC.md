# Complete opening/onset rule atlas

Status: **FROZEN_POST_CONFIRMATION_DESCRIPTIVE_DECOMPOSITION**

## Purpose

Explain the independently confirmed within-base, cross-folio onset result
without running another hypothesis test.  Reconstruct all 1,207 rows in the
frozen mixed `NONE`/`DA` panel and publish the complete aggregate table for
every coarse family remainder and exact first-member `(ZL3b, IT2a, RF1b)`
triplet.  Do not select a subset before construction and do not assign a sound,
word class, function, or English meaning.

## Exact rows and fields

Use the same longest family-opening rule `DAQKJ`, `DAQK`, `DAQ`, `DA`, else
`NONE`; retain only the already frozen 1,207 panel rows.  Join by the same
opaque unit and base identities used in the confirmed target.  Hard-stop
unless there are exactly 892 `NONE`, 315 `DA`, 197 base/folio quota cells, 44
coarse bases, 95 exact base/onset pairs, 25 onset triplets, and every quota is
preserved.

For every one of the 95 pairs, publish:

- coarse family remainder and exact ordered member triplet;
- all-three-consensus flag;
- row and physical-folio counts;
- `NONE`, `DA`, and raw `DA` fraction;
- expected `DA = sum_cell(n_pair_cell * DA_cell / n_cell)`;
- observed-minus-expected residual;
- numbers of base/folio cells with positive, zero, and negative residual;
- whether the pair occurs on at least two physical folios and its corresponding
  row count in the confirmed leave-folio-out score.

Also publish all 25 onset-triplet aggregates with base, row, operation,
expected, residual, and base-level positive/zero/negative counts.  Order every
table deterministically.  The report may name the twelve pair rows with
largest absolute quota residual, breaking ties lexically, but this is
descriptive localization only.  Store no unit, locus, page, or full remainder
member sequence.

## Ceiling

The atlas may show whether the confirmed compatibility is concentrated in a
small alternation or dispersed across base-specific rules.  It adds no new
confirmation and cannot identify allomorphy, harmony, orthography, morphology,
pronunciation, wordhood, POS, syntax, language, cipher operation, meaning,
plaintext, or translation.
