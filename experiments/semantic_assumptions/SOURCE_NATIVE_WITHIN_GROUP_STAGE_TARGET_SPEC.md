# Complete source-native within-group stage one-time target

Status before execution: **REGISTERED_SINGLE_RUN**

## Authorization and frozen inputs

This target is authorized only by the target-masked capacity, the unchanged
model in `SOURCE_NATIVE_WITHIN_GROUP_STAGE_TEST_SPEC.md`, the v2 reversal
amendment, and the independently reconstructed target-free calibration.

- masked panel SHA-256
  `16d7395ae0410c8fc72b5e5462d6d425cd3a2685e7ea70eee0677bd936106ae5`;
- capacity validation SHA-256
  `2a95ce3183b72540f39a8ef0f68129d1f7ccf2e688683a9f2989360f84c20007`;
- model core SHA-256
  `ce1cd0854426b34e8b3e9ba0e6057352f9a5b99737e9e148e791e02979bc65dc`;
- model specification SHA-256
  `e3758d2a4c8d5d306b38602e8a1663ebc42a78db2abecd5905fe191a5d983d47`;
- v2 amendment SHA-256
  `b0b42cc092c2b97ac919d5ecc471d890a09a7c5e0b21fe10548efb543c02bc80`;
- v2 preflight result SHA-256
  `a619c087692b27dd3dac062412238388d717fcfe7c5f213fbfa28b0fe0c586c2`;
- v2 independent validation SHA-256
  `6d6dcd56ae68e06f42977d1ca7e754bbddd2235e9fe1d19962f1bde731db4533`;
- complete source-family table SHA-256
  `a202d93498e8a350a5d7e0ca46e831dcc37ea5c0182dc404d63cb797a98b1225`;
- source-family validation SHA-256
  `fcb6a53461b4f9df36f34161ed1d42087f4395988bea0d71f74a7dd635b68b76`.

The result and report must be absent before invocation and are installed with
paired no-clobber semantics. Numeric library thread counts are pinned to one
before NumPy import. No model, split, threshold, subset, direction, or target
analysis may change after this registration.

## Single target join

Read the 26,184-row complete source-family table once. Join each of the exact
21,899 masked unit IDs to one strict-zero-alternative `CONFIRMED_PROSE` source
row with an ordinary physical folio. Require exact equality of locus, page,
folio, section, Currier, hand, kind, symbol count, and frozen split, and require
the joined ID set to equal the entire eligible source ID set. Encode each
`family_surface` losslessly in the official 24-family alphabet. Do not store
event-level sequences in the result.

Fit every candidate only on the 10,753 TRAIN groups, select once on the 5,516
CAL groups, and compute the frozen summaries on the 5,630 held TEST groups.
Also compute the same frozen run after reversing every complete group. This is
the predeclared robustness view required by the v2 amendment, not a choice of
reading direction.

## Decisions

`LATENT_STAGE_TARGET_PASS` requires both directions to satisfy the frozen
`LATENT_STAGE_PASS`, the same `LATENT_K` to be selected in both, and their
complete selected-minus-K1 TEST summaries to agree within `1e-10`. A pass
authorizes a later descriptive atlas using only neutral stages `S0...S(K-1)`.

If that stronger gate fails, `POSITIONAL_TARGET_PASS` requires both directions
to satisfy the frozen `POSITIONAL_PASS`. It establishes only complete source-
native within-group position structure; it does not authorize a unique latent
stage atlas. Otherwise the exact model is a nonconfirmation and closes without
retuning.

Store only aggregate candidate diagnostics, TEST summaries, 24-family total
counts, bindings, gates, and access counters. Store zero event-level family
sequences, paths, or English glosses.

## Claim ceiling

Even a latent-stage pass establishes only a transferable neutral ordered-stage
construction inside complete source-separated groups. `S0`, `S1`, and `S2`
would be structural states, not translated morphemes. No result identifies a
prefix, root, suffix, sound, word, part of speech, language, cipher operation,
meaning, plaintext, or translation.
