# STA-member surface interpretation audit

Status: **FROZEN_PUBLIC_RULE_PROVENANCE_AUDIT**

## Question

What do the formal member labels in the confirmed opening/onset result encode?
Use the official cached STA1-to-EVA and STA1-to-EvaT bidirectional rule files,
whose provenance and lossless alignment are already independently validated.
Do not infer sounds, authorial letters, morphemes, or meanings.

Parse every exact code rule.  Require `D1 -> q` and `A1 -> o` in both rule
systems.  For all 25 onset triplets in the complete rule atlas, publish the
exact ZL3b, IT2a, and RF1b transcription surfaces obtained from their
edition-appropriate rules and whether the three strings agree.  Bind the
twelve predeclared largest absolute pair residuals from the complete atlas and
report how many use all-reading-identical member codes and surfaces.

Also count how many represented STA families contain at least two distinct
exact onset codes and at least two distinct mapped surfaces.  This identifies
whether the confirmed result distinguishes surface spellings that had been
collapsed into one coarse family.

## Ceiling

The audit may reclassify the confirmed relation as literal transcription-level
orthotactic compatibility rather than a latent feature or semantic agreement.
EVA and EvaT remain transliteration systems.  No physical-letter identity,
allography, sound, morpheme, word function, POS, syntax, language, cipher
operation, meaning, plaintext, or translation follows.
