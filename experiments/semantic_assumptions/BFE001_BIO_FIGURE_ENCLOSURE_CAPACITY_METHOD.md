# BFE001 biological-figure enclosure capacity method

## Question

Can the existing human figure-label layer support a source-bound comparison
between figures drawn in an individualized bounded apparatus and figures in an
open or communal setting, without first inspecting Voynich label identities?

This is a capacity and ownership screen only. It is not a bathing, medical,
sex, action, water, or semantic interpretation.

## Frozen selection

Start from one row per `source_physical_location_id` in the existing-human
current-locus crosswalk. Retain rows that are primary eligible, belong to the
human `bio` section, and have the human object guess `nymph?`. Exclude f75:
its surviving records are paired pod-line or bottom-pool group sequences, not
individually positioned figure-label candidates. Do not read the ZL3b, IT2a,
RF1b, source-text, alternate-text, normalized-text, or formal-analysis fields.

The fixed screen contains 40 physical locations on eight pages and five
physical folios. Inspect only the exact official Yale images. Record the
physical setting, not an interpretation:

- `INDIVIDUAL_BOUNDED`: the candidate's figure area is an individualized
  vessel, tube end, or bounded apparatus compartment;
- `OPEN_OR_COMMUNAL`: the candidate's figure area is a shared/open band,
  basin, or pool rather than an individualized compartment.

Ambiguity about whether a nearby string belongs to a figure or apparatus is
not resolved here. That is precisely why no filler may be opened unless the
state contrast first has adequate independent within-folio mobility.

The fixed state projection is:

- f77r, f77v, f83r, f83v: `INDIVIDUAL_BOUNDED`;
- f80r, f82r, f84r: `OPEN_OR_COMMUNAL`;
- f82v.2, f82v.39, f82v.40: `INDIVIDUAL_BOUNDED`;
- f82v.41, f82v.46: `OPEN_OR_COMMUNAL`.

## Capacity gates

All gates must pass before any label surface or formal feature is accessed:

1. at least 30 physical candidate locations;
2. at least five physical folios overall;
3. each state on at least three physical folios;
4. at least three physical folios containing both states;
5. at least three pages containing both states;
6. maximum physical-folio candidate share at most .35.

Gate 4 is the essential transfer gate. With one mixed physical folio, a
paired folio sign orbit has only two assignments and a one-sided floor of .5.

## Claim ceiling

A stop says only that this existing annotation/visual panel cannot separate
the enclosure contrast from page and folio ecology. It does not establish a
figure owner, apparatus owner, bath, medical procedure, word, name, POS,
sound, language, cipher, plaintext, meaning, or translation.
