# GDT1028 executable method

Read PREREGISTRATION.md and byte-exact src/SOURCE.json for all meanings, source
coverage, selection history and exposure. src/SPEC.json fixes36 candidates and
src/CASES.json17 complete conditional fixtures. src/model.py compiles each whole
49-group sequence into five typed clauses and evaluates separate D process,
E norm/desire, capability and comparison components. No decoder is fitted.

src/independent.py validates by reversed source-production consumption,
declarative constraints and persistent trace invariants. src/preflight.py uses
invented forms only and writes predictions before target use. src/bind.py --lock
binds every scientific input/code byte; public registration precedes src/run.py.
Run and then validate using experiment.json commands. Reproduction must retain
the original registration receipt; neither rerun overwrites source/code locks.

Outputs: ALL_POSITIONS maps every raw group to its assumed value; GRAPHS stores
all36 complete interpretations; CASES and OUTPUTS retain228 outcomes including
all17 fixtures for each serious semantic candidate; CANDIDATES and EQUIVALENCE
summarize only those observed model consequences. DIPLOMATIC_SCOPE includes the
complete unchanged alternate transcription. RESULT and VALIDATION are compact.

An initial preflight SyntaxError from a missing bracket was corrected before
successful synthetic execution and public lock. Source-rival denotation costs
were audited before lock; target code has never executed during development.
