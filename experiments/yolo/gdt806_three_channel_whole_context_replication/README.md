# GDT806 — post-data whole-context deck-concordance test

Status: `PASS__652_GLOBAL__577_RESIDUAL__967_TARGET_EVENTS__0_CONDITIONAL__0_CROSS_DENOMINATOR__6_UNRESOLVED__0_NEW_ROLES__ZERO_LEXEMES`

GDT806 compares twelve fixed complete-whole role rivals for
`cheol, otal, okal, ol, qokeol, qokol` after target-specific K12 centering,
exact rank, raw/stable LOCO, stable LOFO and an unfiltered all-opportunity
denominator. The fail-closed deck contains 652 global surfaces, of which 75
form the narrow projection and 577 the disjoint residual; the six targets
contribute 967 occurrences.

None of the six duels survives the complete gate chain. `okal` is the only
target with a strong residual-deck preference, favoring the opaque/system-entry
rival, but its narrow-deck ranks and all-opportunity margin fail. GDT806 therefore
installs zero new roles, meanings or renderer licences. Seven repeated frames
and twelve passages remain display-only with zero decision weight.

The analysis is transparently outcome-aware: approximate results were visible
while the adversarial rules were corrected. The three decks descend from
related German working renderers and measure deck breadth, not independent
semantic replication.

Primary interpretation: `REPORT.md`

Method and correction record: `METHOD.md`, `PREREGISTRATION.md`

Reproduce with:

```bash
python3 experiments/yolo/gdt806_three_channel_whole_context_replication/src/run.py
python3 experiments/yolo/gdt806_three_channel_whole_context_replication/src/validate.py
```
