# GDT805 artifacts

- `SOURCE_LOCK.tsv`: hashes and purposes for all builder inputs and guards.
- `GDT805_GUARDED_QUERY_STATS.tsv`: explicit selector, column and sealed-prefix
  audit for both mixed transcription sources.
- `GDT805_131_GDT739_SURFACE_PROJECTION_AUDIT.tsv`: all deduplicated GDT739
  neighbour surfaces with radius, quarantine, target-mask and projection gates.
- `GDT805_1086_EXTERNAL_CONTEXT_ATLAS.tsv`: exact discovery-subtracted
  occurrence atlas with L2/L1/target/R1/R2 identities, cell/projection evidence
  kind, GDT804 local candidates and alternate-reader stability.
- `GDT805_11_CONTEXT_CAPACITY.tsv`: external occurrence, folio, stability and
  mapped-axis capacity for each target.
- `GDT805_NEIGHBOUR_IDENTITY_PROFILE.tsv`: complete-surface neighbour counts by
  target and side.
- `GDT805_ROLE_CONTACT_PROFILE.tsv`: raw and pair-stable projected-axis profiles.
- `GDT805_K12_ROLE_COMPARISON.tsv`: all 264 target-side-axis comparisons against
  the fixed twelve-control pools, with ties counted against the target.
- `GDT805_ROLE_LEADS.tsv`: the 21 projected-axis profile leads; every row carries
  zero semantic credit and zero renderer licence.
- `GDT805_13_REPEATED_FRAME_TYPES.tsv`: seven real two-sided and six
  boundary-anchored multi-folio frame types.
- `GDT805_CANDIDATE_SCORECARD.tsv`: twenty width-scaled, axis-correlated rival
  diagnostics with no selection authority.
- `GDT805_11_CANDIDATE_ADJUDICATION.tsv`: one capacity-aware decision card per
  target, including the two retained priors and zero new roles.
- `GDT805_45_PASSAGE_CARDS.tsv`: five real, distinct-folio context displays for
  each target with sufficient capacity, separating exact-cell skeleton,
  projected axes and concrete rivals.
- `GDT805_GDT388_CONTEXT_EDGE_PACKET.tsv` and
  `GDT805_GDT388_EDGE_INTAKE.json`: mandatory relation packet and its explicit
  invalid/not-score-ready intake result.
- `GDT805_STRUCTURAL_CARD.tsv`, `RESULT.json` and `VALIDATION.json`: compact
  outcome, machine-readable result and 5,272-check/two-replay validation record.
