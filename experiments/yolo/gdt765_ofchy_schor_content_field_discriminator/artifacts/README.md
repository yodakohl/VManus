# GDT765 artifacts

- `TARGET_6_EXACT_OCCURRENCE_ATLAS.tsv`: all exact `ofchy` and `schor`
  positions with shortest usable spans, neighbors, roles, and both renderers.
- `TARGET_RAW_EXACT_AUDIT.tsv`: four raw/three exact `ofchy` and three/three
  `schor`, including the excluded f39v.5 reading.
- `OFCH_25_EXACT_FAMILY_ATLAS.tsv` and `FCHY_13_EXACT_FAMILY_ATLAS.tsv`: the
  full formal analogy neighborhoods; neither exports a substring meaning.
- `FORMAL_FAMILY_21_WHOLE_PROFILE.tsv`: family-qualified profiles for thirteen
  `ofch*` and eight `*fchy` complete forms.
- `CHOR_VALUE_67_PAIR_ATLAS.tsv`: all admitted direct value contacts for the
  nine selected `chor`-like complete heads.
- `H_HEAD_X_DAIIN_12_ATLAS.tsv`: the twelve exact H-head field triples.
- `F22R_4_VALUE_GRID.tsv`: the four value cells on f22r.4–5.
- `TARGET_HYPOTHESIS_SCORECARD.tsv`: content, quality, unit, item, and action
  alternatives with an explicit working score.
- `TARGET_WORKING_DICTIONARY_REVISION.tsv`: role confidence, identity
  confidence, evidence, counterevidence, and two rivals for both targets.
- `TARGET_6_CONCRETE_RENDERER.tsv`: portable and bold readings for all six
  exact observed spans.
- `F22R4_9_TOKEN_WORKING_READER.tsv`: a nonempty local default for every token
  and one complete bold line reading.
- `CFHY_6_EXACT_TRANSITION_AUDIT.tsv`: the six exact positions supporting the
  local field-transition reading and retiring inherited action prose.
- `HISTORICAL_CONTENT_FIELD_COMPARATORS.tsv`: seven period-register
  architectures with zero target spelling or identity credit.
- `RESULT.json` and `VALIDATION.json`: compact result and byte-replay audit.

The 67-row value-pair table is retained because it is the complete comparison
set behind the `schor` field decision and is far below the artifact-size cap.
