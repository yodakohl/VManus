# GDT765 working alternatives

## Question

Do the three exact `ofchy` occurrences behave as a learned material name, a
preparation, a quality/index, a unit, or an action? Does the exact
`schor daiin` cell on f22r belong to the same nominal inventory, and can the
two forms receive concrete defaults that remain usable until a better reading
replaces them?

## Starting alternatives

For `ofchy`, the leading alternatives are a learned drug/preparation head, an
open attribute, a quality/index, a unit, and an action. For `schor`, they are a
plant-part item, a generic subentry, a reproductive plant part, and an action.
The practical identity candidates are allowed to be bolder than the role
classification: `ofchy = Blütenmasse` and `schor = Blütenstand` may remain as
defaults if all observed spans stay coherent, even though neither is proven.

The selected role should explain every exact target position. A concrete
identity may use a weak complete-whole family analogy, but no individual EVA
character or substring receives that meaning. A contradiction at one target
position demotes the identity; a better coherent identity replaces it.

## Fixed checks

- enumerate raw and reader-exact `ofchy` and `schor` occurrences;
- compare the complete `ofch*` and `*fchy` neighborhoods without treating them
  as automatically compositional;
- enumerate the selected `chor`-like heads directly followed by a written
  value and all exact `H-head X daiin` triples;
- inspect the full f22r.4–6 value grid and give all nine f22r.4 tokens a local
  default;
- preserve GDT738's failed generic transfer into `schor` as counterevidence;
- use the historical material only for record architecture, not EVA spelling.

No new page, image, transcription, f84, or f84r enters the pass.
