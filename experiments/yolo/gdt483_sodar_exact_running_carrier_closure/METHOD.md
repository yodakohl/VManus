# GDT483 method

## Question

Does GDT482's sole unique functional residue, f89r `sodar`, have exact carriers
in the already admitted running edition, and what concrete local sentence does
that support?

## Inputs

- GDT413's 4,576 running events and 46-component dictionary;
- GDT416's 4,576 owner-local imperative clauses;
- GDT473's 183 local-address working events;
- GDT482's 45 residual-event component tiles.

The page-bearing GDT413, GDT416, and GDT473 sources are materialized only
through `./vmanus-exp query-tsv`, with all 26 or six admitted page values named
explicitly and only required columns returned. The guard reports zero forbidden
rows.

## Construction

The fixed local target is `P1008-E1297`, surface `sodar`, recipe
`S+O+DA+R`, literal reading
`WÄHLEN · AUSFÜHRUNG · ZWEITE STUFE · MARKIEREN`.

The builder then:

1. selects every running event whose exact surface is `sodar`;
2. requires exact recipe and literal-reading agreement;
3. joins its GDT416 imperative and verifies exact portable back-projection;
4. inventories all running `DA` events and every contiguous `DA+R` block;
5. counts support for `S`, `O`, `DA`, `R`, their adjacent pairs, trigrams, and
   the complete four-component recipe;
6. reclassifies only the one GDT482 `sodar` row from unique functional residue
   to exact running-carrier closure.

The preferred sentence removes repeated “den Eintrag” from the old local
default but changes no component value:

> Wähle den Eintrag und markiere ihn – als Ausführung auf der zweiten Stufe.

## Claim ceiling

This is an exact-carrier bridge between already admitted artifacts plus one
fluent paraphrase refinement. It adds no root, component meaning, syntax,
plaintext, language, surface, recipe, event, or page.
