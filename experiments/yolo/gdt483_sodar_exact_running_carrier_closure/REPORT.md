# GDT483 `sodar` exact running-carrier closure

## Outcome

The final functional residue from GDT482 was caused by the deliberately narrow
183-local-event donor pool, not by a genuinely isolated card. The complete
running edition contains two exact `sodar` carriers:

| event | page | register | recipe |
|---|---|---|---|
| `G407-E0930` | f67r2 | CELESTIAL | `S+O+DA+R` |
| `G407-E2712` | f77r | BIOLOGICAL | `S+O+DA+R` |

Together with local target `P1008-E1297` on f89r/PHARMA, this gives three
identical visible surfaces, recipes, and literal component readings on three
pages and in three registers. There is no surface-recipe or literal-reading
conflict. Both running imperative clauses back-project exactly.

GDT473 had already retained the target as a full formula with evidence
`EXACT_SURFACE_HAS_ONE_RUNNING_RECIPE`; GDT483 connects that older fact
explicitly to GDT482's residual ledger.

## Concrete reading

The stable literal is:

`S=WÄHLEN · O=AUSFÜHRUNG · DA=ZWEITE STUFE · R=MARKIEREN`.

The two running contexts inherit different arguments—`AIN` on f67r2 and `Y`
on f77r—while retaining the same action/control frame. The compact generic
reading is therefore:

> **Wähle den Eintrag und markiere ihn – als Ausführung auf der zweiten
> Stufe.**

For f89r's owner: “Wähle den Drogen- oder Zutateneintrag und markiere ihn –
als Ausführung auf der zweiten Stufe.”

## Internal support

The running deck contains `S` in 300 events, `O` in 683, `DA` in 35, and `R`
in 114. The adjacent fragments occur as follows:

- `S+O`: 13 events/nine recipes;
- `O+DA`: six events/five recipes;
- `DA+R`: ten events/four recipes on seven pages and three registers;
- full `S+O+DA+R`: two events, one surface, two pages/registers.

The four `DA+R` recipe families are `DA+R+Y` (five),
`DA+R+A_ADDR+AM_ADDR` (two), `S+O+DA+R` (two), and `L+DA+R` (one).
Thus “zweite Stufe markieren” is a recurrent ordered block, not an ad hoc
meaning invented for f89r.

## Residual ledger after closure

- 42/45 events are fully explained by recurrent components inside the local
  event atlas;
- `sodar` is closed by two exact running surface+recipe carriers;
- `cheosdy` and `saloiinsheol` retain only expected learned lexical slots;
- zero functional residue remains, and all 45 events keep concrete defaults.

No atom, model, surface, recipe, event, or page changes. One German paraphrase
is smoothed without altering its back-projection. Validation passes 92/92.
