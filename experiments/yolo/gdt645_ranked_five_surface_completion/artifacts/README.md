# GDT645 artifact map

- `TARGET_DECISION_DECK.tsv`: the five ordered meanings, parses, rivals and
  exact-whole scope rules.
- `FORM_FAMILY_ATLAS.tsv`: 68 observed or explicitly held family cells.
- `COMPONENT_BINDING_AUDIT.tsv`: component readings and their exact-whole
  licensing boundaries.
- `ALL_OCCURRENCE_SEMANTIC_AUDIT.tsv`: all 115 target positions with reader and
  context verdicts.
- `READER_VARIANT_AUDIT.tsv`: the ten non-exact target positions.
- `SEQUENTIAL_DECISION_LEDGER.tsv` and `ROUND_COVERAGE_COUNTS.tsv`: cumulative
  V21→V22 insertion and dictionary hash chain.
- `NEWLY_COMPLETED_LINES.tsv`: six complete literal and smoothed readings.
- `NEWLY_EXPOSED_ONE_HOLE_LINES.tsv`: the new `s` and `tcheey` frontier rows.
- `V22_EXACT_TOKEN_GLOSSARY.tsv`, `WORKING_DICTIONARY_V22.tsv`,
  `ALL_LINE_CONCRETE_COVERAGE_V22.tsv`, `COMPLETE_PASSAGES_V22.tsv`, and
  `ONE_UNKNOWN_PASSAGES_V22.tsv`: the executable V22 edition.
- `RESULT.json`: hash-bound machine result.
- `VALIDATION.json`: independent replay and invariant checks.

All TSV artifacts are generated by `../src/run.py`; do not edit them by hand.
