# Sidequest V15 Biological value-deck selection

Date: 2026-08-21

Status: **speculative YOLO visual-role synthesis, not a GDT result or confirmed
visual grounding**.

## Four-perspective comparison

All four perspectives froze a 38-occurrence visual-role map before joining the
value families. Their role granularity differs, which is itself informative.

| perspective | score / 100 | disposition |
|---|---:|---|
| R3 technical register writer | **95** | **selected latent-value architecture and control analysis** |
| R2 medical/Herbal scribe | 93 | supplies the concrete medical content bets |
| R4 chancery corrector | 92 | supplies the conservative ownership ceiling |
| R1 workshop master | 90 | supplies the teachable taxonomy; freeze imperfectly blind |

The shared decision is:

```text
the four families are probably latent application/configuration values
licensed by a page/record stencil,
not names of visible bodies, pools, pipes, junctions or outlets
```

The generic exemplar/stencil/cadence model remains close and is now the
strongest rival.

## What the blinded maps show

No family is visually pure. The more differentiated freezes produce an
attractive raw role×value association, but it weakens after page conditioning
and is fully entangled with paragraph/stencil identity:

- raw role×value mutual information: .551 bit, descriptive p=.0097;
- page-conditioned diagnostic: p=.1003;
- stencil proxy mutual information: .770 bit;
- R4's conservative source-aware freeze assigns all 38 to the general
  page/configuration owner because exact line→picture-part ownership is not
  secure.

These diagnostics are exploratory and their role maps are not identical. The
correct reading is not “the image has no effect.” The image/paragraph selects a
configuration stencil; the stencil constrains which latent values are legal.
Exact values need not name the visible station itself.

## f82r.27

The repeated `VAL-QE` at cells 3 and 6 is the strongest retained content event:

```text
slots:  S1 | S2    | S3     | S4 | S5 | S6     | S7
values: A  | (B,b) | VAL-QE | D  | E  | VAL-QE | F
```

The differentiated R1/R2/R3 freezes place both occurrences under the same broad
pool/configuration owner. They do not establish two separate visible stations.
The best reading is therefore:

> The same latent condition or application setting is selected twice within
> one pool/application configuration.

This is stronger than a generic punctuation reading and weaker than naming a
specific pipe, pool, temperature or action.

## Selected value taxonomy

The four agents disagree on literal actions but converge on a useful latent
state taxonomy:

| family | selected working class | confidence | concrete subordinate glosses |
|---|---|---:|---|
| `VAL-Q` | `BASE_CONFIGURATION` | .50 | standard/ground setting; immersion only .20 |
| `VAL-QE` | `MARKED_MEDIUM_OR_APPLICATION` | .48 | circulation/throughput; tempered/warm .27 |
| `VAL-S` | `SET_OR_RESULT_STATE` | .45 | standard/as usual or hold/rest .30 |
| `VAL-L` | `LOCAL_HELD_OR_CARRIED_APPLICATION` | .43 | local/body-end use; pour/rinse .25 |

The labels describe different roles within one technical answer deck. They do
not assert that Q/QE are authorially related morphemes, nor that the source
language used these English words.

At deck level, the medical scribe's most concrete application sequence remains
useful but low-confidence:

```text
STANDARD -> TEMPERED/WARM -> POUR/RINSE -> IMMERSE/SOAK
```

This should be treated as a content palette, not a fixed order or dictionary.

## Page-level readings

### f81v

> Maintain the active application relation. Select a general/set state for the
> current figure/configuration; in two adjacent cells repeat the same base or
> immersion-like state; commit each local selection.

### f82r

> For the pool/apparatus configuration, fill a sequence of inherited slots.
> Select one marked medium/application condition twice at nonadjacent positions,
> with other local states between them; commit every cell.

### f83r

> Progress through several figure, vessel and path configurations. Choose base,
> marked, result or locally carried states as licensed by each record stencil;
> use L/O to continue the active relation and commit each selected value.

These are source-class readings. No body, sex, disease, pipe, liquid or action
is identified by a card.

## Why medical/application content remains ahead

The direct visual-role dictionary fails, but an application/configuration deck
still explains:

- exact value identity beyond generic DY closure;
- one-card inherited slots;
- nonadjacent repeated values in one configuration;
- movement of a value among field ordinals and visual zones;
- the page-level figures, containers, pools and conduits;
- register-local vocabulary under a shared B production system.

Working confidence is .62 for a latent application/configuration deck and .52
for a generic stencil/cadence deck. Direct visible-station naming is only .10.
The medical interpretation therefore remains leading but narrows from
“pictured role name” to “latent state selected for the pictured configuration.”

## Strongest rival and replacement rule

The generic exemplar/stencil model predicts that family identity follows
copied paragraph template, wrapper and field ecology without stable practical
content. It gains from the repeated `1C|3C|1C|4O` stencil on f81v.17/f82r.7,
which preserves `VAL-S` at the same field while most cards vary.

It should overtake the medical deck if future fixed-page parses show that:

1. values follow stencil identity after page/configuration changes;
2. identical values do not preserve any state-like relation across repeated
   records;
3. concrete application glosses systematically contradict their other
   occurrences;
4. paragraph/template prediction explains f82r.27's repeated value without a
   repeated latent condition.

## Claim ceiling

V15 supports latent configuration values, not direct picture labels. The four
family meanings remain exploratory. No treatment, temperature, body part,
liquid, direction, word, language or plaintext is confirmed. `f84` and `f84r`
remained sealed.
