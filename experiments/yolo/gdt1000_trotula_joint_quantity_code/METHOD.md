# Complete two-recipe quantity/part compatibility test

This is a fixed compatibility test inside exploratory reading. The source
meanings are hypotheses. No previously translated Voynich word is required;
no resulting string fit alone can establish the source or a word meaning.

## Source and complete content

`src/SOURCE.json` owns every span of Trotula IV15 and the entire V19 named
experiment, heading through asserted return to its own place. The source-only
packet, exact official image URL and image digest are bound there. Root viewed
the complete printed page8 natively before selecting the writer. No target
image is opened. The owned witness is a1544 print; pre1420 wording/order is
unproved. This is content serialization, not phonetic or directly inflected Latin.

IV15 has22 content atoms; V19 has33. The ownership table retains the
alternative, five ingredient identities, white pepper, each-one-drachm amount,
grinding, both conjunctions, tempering, white-or-sweet wine, evening, and
two-drachm administration. V19 retains the superlative tested-experiment claim,
condition, deer-heart powder, laurel leaves, equal drachm amounts, myrrh and
scruple amount, tempering, participial grinding state, wine, drinking, both
conjunctions, consequence marker, repeated uterus, asserted future return,
and its own place. There is no free background or unassigned character.

Quantity abbreviations become QD1, QD2 and QS1. Nonquantity inflection is
abstracted to content concepts; GRIND RESULT_STATE preserves the explicit
participial distinction and RETURN ASSERTED_FUTURE preserves the outcome
claim. Scope uncertainties are source uncertainties, not target-fit switches:
IV15 wine mass and dose material referent, V19 grinding scope and dose/time,
unknown unit conversion and unknown actual efficacy. `5D` is the quantified
IV15 dry subtotal only. No2/5 finished-mixture fraction and no per-ingredient
2D dose is inferred. Source ingredient names are not established Voynich names.

## Fixed writers and code

Two candidates use the same rules: FORWARD follows the source content sequence;
REVERSE_ATOMS reverses each complete atom sequence. A joint pair has one shared
orientation. Internal character strings are never reversed. Every ordinary
content atom has one globally shared nonempty string. Distinct ordinary atoms
have an injective prefix-free code. Any consecutive ordinary atoms may share
an original word, but no atom may cross an original word boundary. Every
character and every word of both paragraphs must be generated exactly once.
The prefix-free condition is a writer assumption, not established Voynich fact.

Quantities are a separate word namespace. Let I be a nonempty unknown count
string, and let (L_D,R_D),(L_S,R_S) be different unit frames with nonempty
combined lengths. Individual left/right halves may be empty. Then:

- QD1 = L_D I R_D
- QD2 = L_D I I R_D
- QS1 = L_S I R_S

These three complete words must differ. Each occupies exactly one original
word; their full strings cannot also occur as ordinary-content words in the
same pair. No ain/aiin values are supplied. Every compatible frame split is
retained, including multiple splits of one numeral spelling. Unit conversion,
physical quantity scale and ingredient identity remain unbound.

## Population, exposure and complete accounting

Use all1349 complete paragraphs in the unchanged GDT928 cache: ZL3b659,
IT2a690, RF1b0. Reader layers are alternative readings of one manuscript.
Each paragraph is considered for each recipe and orientation. Each ordered
IV15/V19 pair must use different physical leaves within one reader and one
orientation. Same-leaf pair exclusions are counted. Every remaining pair is
included in the compressed candidate table, not only successful pairs.

The cache is already admitted and exposed. Its earlier DISCOVERY/EVALUATION
labels do not create a new blind division. Here both complete recipes jointly
select a hypothetical code; the second leaf is an exposed compatibility
constraint, not independent confirmation. There is no third-leaf confirmation
in this experiment and no independent meaning capacity. f84/f84r remain sealed,
f116v is not admitted, no reserve is opened. RF supplies no complete-paragraph
capacity, not a negative result. No new source data admission is inferred.

A paragraph is fully literal for this test only if every cached line has
`anchor_eligible=true` and every raw group matches `[a-z]+`. The cached flag
also requires definite seams and at least two groups on that line. Failure
of this qualification is UNKNOWN_SOURCE. Do not repair uncertainty or discard
the line. The exact source groups remain in the candidate records.

## Exhaustive necessary stages and bounded joint solving

For each literal paragraph, whole-group count cannot exceed atom count and
character count cannot fall below atom count. Enumerate every possible pair
of whole quantity-word positions, retaining the number of source atoms and
whole target groups in each intervening chunk. Empty source chunks require
empty target chunks. Each nonempty chunk requires between one and its atom
count groups and at least one character per atom.

IV15 enumerates every unit-frame decomposition producing the observed QD1 and
QD2. V19 requires different QD1/QS1 words with a nonempty proper common count
substring. Joining the two independently enumerates all three distinct words
and both frames with exactly the same count string and drachm word. These are
necessary filters only. Complete source equations, prefix-free constraints,
all original seams and the separate quantity namespace remain mandatory.

Every numeric-compatible paragraph pair receives a stable SHA256 ordering of
`edition|IV15_id|V19_id|writer`, with literal indices breaking ties. At most4096
such pairs receive a joint cvc5 check:3seconds per initial check,1second for a
different-code check after SAT,12seconds external worker limit,32workers.
Unvisited eligible pairs are UNKNOWN_JOB_CAP; timeouts/errors remain unknown.
No first witness narrows the population and no additional restart is allowed.
A different code query covers every ordinary atom and all three numeric words;
unit-frame alternatives with the same words are separately exhaustively retained.
Solver-negative results close only their exact finite pair/code model.

All potential codes are finite because nonempty atoms cannot cross words,
so their lengths cannot exceed the longest observed whole word. A bound in
elapsed time is not exhaustive search. Do not claim code uniqueness from one
SAT witness, restart agreement or an unresolved different-code query.

Pair decisions first retain a proved IV15 necessary contradiction, then a
proved V19 contradiction; otherwise source uncertainty, then shared-numeral
contradiction, then the joint solver result. A known necessary contradiction
in one member excludes that pair even if its other member is unreadable.
The table and underlying paragraph rows preserve that distinction.

## Validation and decisions

Before target access, synthetic full source-code positives test both writers;
known broken word boundaries and whole strings fail. A separately implemented
validator checks900 short numeric string pairs and1024 placement cases.
These are implementation checks, not search controls or semantic evidence.

After execution, the validator reconstructs all paragraph and pair decisions
from the fixed inputs using separate quantity and placement enumeration. It
checks every SAT reading by consuming its atom strings word by word. Every
cvc5 UNSAT receives a separately formulated Z3 replay with1second solver and
8second process limits. Z3 unknown is disclosed, not a second proof. A contrary
SAT is an error requiring a correction, not a model repair. All computation
remains inside the inclusive19:00UTC preparation/publication checkpoint.

A full shared witness advances this specific conditional two-recipe reading,
with complete words/parts and all ambiguity displayed. Necessary exhaustion
can close the writer; computational or source unknowns prevent a global
exclusion. No resulting case counts are independent trials. Arbitrary content
renamings, another source with the same repetition structure, numeric scale,
and code/frame ambiguities can preserve fits. There is no suitable whole-search
null and no significance claim. No confirmed plant names or translated words
are claimed without an independent meaning test. No automatic source reorder,
elision, allomorph, numeric rule repair or reserve test follows any outcome.
