# GDT625 artifacts

- `PAGE_ALLOWLIST.tsv` — canonical 179-page safe selector set.
- `TERMINAL_QUALITY_OCCURRENCES.tsv` — all 1,162 terminal quality-family tokens.
- `SUCCESSIVE_QUALITY_PAIRS.tsv` — 535 same-line or next-line successive pairs with order, interveners, anchors and stability.
- `SUCCESSIVE_TRANSITION_MATRIX.tsv` — complete 4x4 state matrices by locality and frame strictness.
- `MOISTURE_DIRECTION_SUMMARY.tsv` — dry/moist direction counts and conditional operation glosses.
- `THREE_STATE_CYCLES.tsv` — all six local A-B-A moisture paths.
- `INTERVENING_TOKEN_CANDIDATES.tsv` — surfaces between same-line state pairs, including the `cthy` rejection as a verb and low `otar` lead.
- `CANDIDATE_TERM_ROLE_SUMMARY.tsv` — section, edition, stability, position and current defaults for seven practical terms.
- `CTH_ROOT_FAMILY.tsv` — all 408 occurrences across 69 `cth-` surfaces.
- `CTHY_PART_CONTACTS.tsv` — 32 local `cthy` contacts with `shor`, `chor` or `dair`.
- `ANCHOR_QUALITY_CONTACTS.tsv` — every strict immediate terminal-quality contact for the current carrier candidates.
- `ANCHOR_QUALITY_SUMMARY.tsv` — carrier-level contact totals; `cthy` has 12 contacts, 8 stable.
- `CONCRETE_LOCAL_READINGS.tsv` — five practical readings on previously opened Herbal pages.
- `HISTORICAL_PROCESS_COMPARATORS.tsv` — nine quality-versus-operation mechanisms from fourteenth- and fifteenth-century sources.
- `MANUAL_VISUAL_JUDGMENTS.tsv` — eight bounded judgments on already opened images; no new image page.
- `RESULT.json` — canonical synthesis and bound hashes.
- `VALIDATION.json` — deterministic replay and safety certificate.

The occurrence, pair, matrix, cycle, bridge, family, contact, reading and result
tables are regenerated by `src/run.py`. The historical and manual visual TSVs
are source observations and are hash-bound as inputs.
