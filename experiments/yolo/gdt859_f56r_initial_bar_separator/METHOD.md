# GDT859 — f56r initial upper bar and source separators

This is a post-discovery source audit. Root has already natively observed an
extended upper line near the first two tall uprights of f56r physical text
line1 in the published remaining-plants orientation. This protocol neither
pretends that discovery was held out nor tests a selected meaning. Existing
admission f56r, Yale1006184,2793×3707, source hash in SPEC. No new admission.

After public GO extract exactly page=f56r AND locus=f56r.1 from each of the
three GDT851 safe179 source caches. Keep one complete stored line losslessly,
all metadata, raw groups, indices, entities and both separator fields. Missing
or duplicate matches yield SOURCE_INCOMPLETE; save all exact matches and do
not broaden to another locus. Schema errors also prevent a complete result.
No cleaning, BPE, text model, score, cross-reader vote or scope extension.

Root supplies native VIEWER_A.json and VIEWER_B.json, each with page/canvas_id,
targets AB and BC, and note. A/B/C mean the first three tall forms or
ascender clusters left-to-right in physical line1. A/B each contain an upright;
C may have multiple vertical strokes. A double-legged form is one target.
These are image-only ordinals, not assumed transcription group indices.
Each target has localized boolean, connection CONNECTED/NOT_CONNECTED/UNCERTAIN,
and note. A_SEAL.json records VIEWER_A byte SHA256 and sealed_at_utc before B
is obtained. Optional extra localization description is permitted. A then B
is a recorded workflow assertion, not chronology proved by a self-signed hash.
Both inspect the same photograph, not independent manuscript witnesses.
Software checks original image bytes/SHA256/dimensions against frozen metadata,
schemas and seal, not native vision. Missing original image hard-fails validation.

Report AB/BC viewer judgments separately from full-line reader segmentation.
List every adjacent stored-group pair with original indices/raw strings and
left-group right_separator / right-group left_separator; index gaps remain
explicit. Definite group separators do not prove authorial word boundaries.
Software does not establish which upright belongs to which transcription
group. Any ordinal visual/text alignment is a separately qualified manual
observation, not an executable identity claim. No exact glyph identity,
allography, word, stroke order, pen motion, sound or semantic conclusion.

Fixed native descriptor: AB is supported as connected only if both viewers
localize AB and mark CONNECTED; BC disconnected support analogously requires
both localized and NOT_CONNECTED. Otherwise retain the complete judgments
without forcing consensus. This does not turn the local comparison into a
population test. Pending observers yield SOURCE_COMPLETE_NATIVE_PENDING.
Final source-complete packet is LOCAL_NATIVE_AND_TRANSCRIPTION_AUDIT_COMPLETE.

Independent validator re-extracts exact target lines from original safe caches,
verifies frozen hashes and saved metadata/line equality, reconstructs adjacent
pairs separately, checks A seal/schema and fixed native descriptors. It does
not import the runner. Cached --check must reproduce source/result artifacts
byte-for-byte. Pre-data controls cover missing, duplicate and uncertain seams;
none reads manuscript cache bodies. Source-file hashing before GO is permitted.

Budget15min07:42:20–07:57:20UTC on2026-09-06, including public registration and
publication. Root owns observations, globals and git. Freeze this METHOD,
PREREG/SPEC and code before target cache interpretation; wait for public GO.
