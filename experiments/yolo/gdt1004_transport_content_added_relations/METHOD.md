# Frozen grammar projection versus complete world projection

The exact two paragraph pairs and all190existence/marginal queries come from
GDT1003PREDICTIONS. The world domains/24tuples are its immutable observed output.
The comparator is GDT1002's complete grammar: one unknown whole-form terminal
per pair, all47terminals, mandatory INITIAL/GOAL/SAFETY/CAPACITY/CONCLUSION,
17patterns including PAIR/COPY, every written group covered exactly once.
No physical movement, reference resolution, goal or danger condition is imposed.

Run the old Z3 exact-position and old cvc5 boundary-flow encodings with only a
build/interface extraction; old files stay byte-identical and are hash-bound.
Each marginal query pins the specified value. Enumerate up to256distinct complete
shared-value tuples by blocking each tuple across full dictionaries/parses; limits
remain UNKNOWN, and no exhaustion is inferred from repeated solver samples.
5seconds/query,600seconds/system inclusive construction,2parallel systems.
For each syntax SAT store the complete code/parses; do not replay it as if it
were a coherent reading. All existing1003tuples must be syntax-possible.

Independent validator checks every source/hash/grammar ownership and190query
answers using cvc5. It then tests every enumerated primary tuple separately and
one query blocking all those tuples if primary claims exhaustion. A contrary
SAT/UNSAT is ERROR; timeouts remain unresolved. Full word maps within tuples are
not exhausted. Reuse1002synthetic complete shared-code checks before registration;
fixture result is implementation validation only, not source meaning evidence.

Compare complete marginal domains and tuple projections only where both sides
are exhaustive/independently verified. Report syntax-only values, tuple exclusions,
retained worlds, source uncertainty, inherited singleton roles and unseparated
variant/name/synonym alternatives. Post-run short logical explanations may clarify
an observed difference but cannot change the fixed query census or old semantics.
No statistical significance, source-language claim, new word glossary or reserves.

Preparation disclosure: the initial synthetic joint fixture wrongly expected
blocking T/U to exhaust x while its other word values were all free. It actually
allows x=GO/SAFE/ONE in other complete parses. The expected outcome was corrected
to SAT before registration/target execution; neither grammar builder was changed.
The pinned single-paragraph fixture still checks true T/U exhaustion.
