# Method

Freeze local C/W content choices and all source/model hashes before the broad
inventory. Retain all21 R/T base values and add only three whole values. Enumerate
all exact targets from the unchanged179-selector GDT915 snapshots, preserve all
raw line boundaries/entities, and bind every available whole paragraph in GDT928.
Render four fixed candidates over those complete contexts and all1408 GDT930
groups. Do not infer meanings from role counts or fluent output. Group identical
local predictions. R/T and prior F/P remain open.

A manual post-output audit covers every target-containing run of at least two
known hypothetical values. Runs are navigation aids, not discovered clause units.
All single-target cases and unread groups remain in full output. ASSESSMENT.json
retains every run-type assessment and explicitly conditional conflicts. Separate
source/replay validator checks completeness, fixed values and limits, not semantics.
