# GDT784 method

## Question

Can `chorcholsal` receive a concrete complete-whole default without treating
EVA substrings as proved words, and what is the least overcommitted reading of
the already admitted span `ol chorcholsal`?

## Frozen inputs

`src/SOURCE_LOCK.tsv` binds fifteen local inputs. The guarded GDT782 loader
reconstructs the admitted 179-page cache and rejects f84/f84r selectors before
row materialization. No new page, image, OCR or transcription is accessed.
The visual finding is a prebound observation of Yale canvas 1037112 and its
locked crop/full-image hashes.

## Construction

1. Count exact target and constituent surfaces in the guarded cache.
2. Compare ZL3b, IT2a and RF1b with Stolfi at f88r.22 while the target meaning
   is masked.
3. Audit every available f84-free Stolfi line among the fourteen modern GDT759
   `chor chol`/`chol chor` contact loci: thirteen comparison loci are present,
   each with a definite dot at the relevant boundary.
4. Preserve the visual result: a clear external gap after `ol`, with no equal
   internal gap in `chorcholsal`.
5. Import GDT759's eight forward and seven reverse pair occurrences, the
   current whole-role provenance for `chor`, `chol`, `sal` and `cheor`, and the
   exact same-register frame twin f100v.20 `cheor ol chockhar`.
6. Score four predeclared complete-whole readings by surface fit, slot fit,
   PART+DRY echo, historical architecture and semantic-overreach penalty.
   Scores are diagnostic throughput weights, not probabilities.
7. Patch exactly one GDT783 renderer row and inherit all other rows and token
   ownership unchanged.

## Decision and ceiling

The selected surface analysis is the lexicalized whole with a non-exporting
PART+DRY echo. `chorcholsal` receives C0 **`trockene Blütendroge`** and the
complete span receives C0 **`Ansatz: trockene Blütendroge`**. Surface wholeness
is C2; the internal echo is C1; `sal`, exact botanical identity, language and
plaintext remain C0/open. `ol` is not assigned the prepositional meaning “aus”.
No component value, specific substance, confirmed lexeme or plaintext clause
is exported.

The two boundary relations are emitted in GDT388 format as valid acquisition
evidence but remain ineligible for scoring and fail the capacity, fold and
mobile-null gates by design.
