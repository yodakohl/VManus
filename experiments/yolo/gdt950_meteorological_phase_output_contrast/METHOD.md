# GDT950 method

The immutable PREREGISTRATION.md, src/SPEC.json and src/CANDIDATES.json define
this exposed test. PREREG_LOCK.json binds them and the already guarded parent
source. No transformation of source groups, entities or uncertain separators.

The runner conserves the source IDs, separately scans each physical P line and
retains every literal definite-space operator/result pair. All other operator
positions are explicitly unresolved. Every candidate is evaluated at every
qualifying position, separately by edition and physical-leaf partition.

Candidate families contain two atomic operations and four distinct named
phenomena. Their frozen possible-pair tables are generated before the full
census. Compatibility compares required output phase with the stipulated
phase of the result name. This is a consistency check of those assumptions,
not an observed liquid/solid classification of the manuscript or its image.

Full possible-pair equivalence and equivalence on actual source cases are
reported separately. No counterfactual B/C case is counted as observed. All
nominal permutations, alternate-reader differences, unknown operation outputs
and the empty additional-leaf test remain visible.

Independent validation reconstructs the source selection and model predictions
without using the runner as its oracle. Publication binds sources, declarations,
code and compact results. No significance or semantic truth test is claimed.
