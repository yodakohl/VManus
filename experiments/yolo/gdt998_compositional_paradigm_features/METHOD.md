# GDT998 — complete compositional grammatical tables

Decision2026-09-20 15:38UTC; preparation, implementation, validation and public
closure checkpoint16:20UTC. This is one bounded question inside the user's
ten-hour block to22:52:35UTC. No automatic expansion after any outcome.

## Unknown and decision

Can two entire six-person, three-channel present/imperfect paradigms share a
single compositional written code? GDT900 excludes a different conjunction:
literal source letters or disjoint pairs copied under a prefix-free code.
This hypothesis instead encodes grammatical content. GDT419's attested formal
neighbors and GDT608's directed composition motivate testing reusable parts;
their guessed action/grade meanings are not inherited. Whole-form residuals
are known: this deliberately exact construction may fail even if some real
language has inflection. GDT892 stopped before control generation and supplies
no recovered architecture. IDEA317's two triples do not establish a paradigm.
IDEA412 has no executable feature grammar; this is a separate stated writer,
not an unchanged use of its incompatible reference to GDT993's transport rules.

A full witness retains a concrete candidate feature reading, all spellings and
all equivalent codes. Zero whole paired witnesses closes only this exact table/
serialization/concatenation/scope conjunction. No added allomorphs, cell deletion,
reordered persons, merged target groups, source-letter rescue or runtime repair.
The smallest test is finite substring factorization, not a learned decoder.

## Source content and unavoidable symmetry

Use both complete18-cell subtables in GDT900 SOURCE_INPUT, Kuun1880 printedp1,
already collated from the edition. Person rows are1SG,2SG,3SG,1PL,2PL,3PL;
columns Latin, Persian, Cuman. Two tenses PRESENT and IMPERFECT. These are
grammatical subunits, not the complete audio entry extending to printedp5.
Headings and apparatus are excluded exactly as an explicit unit hypothesis.
The edition is not our fresh collation of the medieval manuscript.

Each cell means HEAR(channel,person-number,tense). The lexical predicate HEAR
is a hypothesis supplied by this nominated source: replacing it by SEE, KNOW
or any other predicate with the same paradigm changes no tested consequence.
Likewise renaming channels and grammatical feature labels preserves the string
equations. In particular the test cannot distinguish PRESENT from IMPERFECT
without an independently identified temporal construction. Print these
symmetries even for a unique concrete string factorization. No confirmed word.
Source spelling variants have identical feature projections; report one class
covering all four GDT900 apparatus envelopes, not four independent confirmations.

## Writer frozen before opening the target packet

There are three nonempty distinct stem strings R0,R1,R2; six pairwise distinct
person-number strings P0..P5; two distinct tense strings T0,T1. P and T strings
may be empty (at most one per family by distinctness). Values in different
families may coincide or be prefixes of one another: no global prefix-free or
letter-level premise. All are literal substrings, with lengths bounded only by
their actual complete target groups. No previous EVA/BPE division is imposed.

For one global order selected from all six permutations of R,P,T, every cell
is exactly the concatenation of its three selected strings. The same R and P
values serve both tenses. No singletons, extra letters, deletions, stem changes,
context-specific spelling, homographic persons, fused values or free filler.
This is a schematic grammatical notation, not a claim that the three historical
languages share inflectional endings or that Voynich is directly Latin.

Each complete table is serialized either row-major or column-major. One mode
and one of six channel-column permutations must apply to both. Person order
stays fixed. Target order of the two complete tables is unrestricted. Both
must occur in the same alternative transcription panel, with no shared physical
group ID. They may share a physical leaf. All cells must be represented.

The six channel permutations are exact label symmetries: solve canonical column
indices once, then report all six equivalent channel assignments for every
result. All six field orders remain tested. A single-table fit is a necessary
intermediate, never a complete two-tense reading. A temporal-order swap supplies
another labeled interpretation, never independent confirmation.

## Exhaustive exposed population and predictions

Use unchanged GDT900 artifacts/TARGET_INPUT.json: every18-group consecutive
window wholly within one complete admitted odd-leaf paragraph from GDT893.
Its original counts are CONS0,IT6710,RF73,ZL142. Preserve every original group,
source ID, paragraph and physical leaf; require exactly these counts/hash.
All windows and both traversals participate. No favorable candidate selection.
The prediction for EACH possible pair is equality to all36 writer cells,
shared roots/persons, different tense strings and disjoint physical groups.

No new text source, mixed TSV, image, page admission or reserve is used.
f84/f84r remain sealed; f116v unadmitted. All targets are historically exposed.
The old odd-only frame is selection data, not new blindness. Independent meaning
confirmation capacity is zero; do not open even-leaf reserves after a local fit.
Any later fixed predictions on other leaves require a separate prospective
decision and the user's nearly-complete-overall-reading threshold.

## Finite complete calculation

Necessary length condition: for every matrix cell, its length equals the
length at(row,0)+length at(0,column)-length at(0,0). A failure excludes all
field orders for that window/traversal. Save a concrete violating rectangle.

For each surviving matrix and each field order, enumerate EVERY split of
cell(0,0) into three substrings, including empty grammatical components but
requiring the root nonempty. This fixes R0,P0,T. The other two roots are
uniquely obtained from(row0,column) by stripping known prefix/suffix parts;
the other five persons are similarly obtained from(row,column0). Enforce
within-family distinctness, then check ALL18 exact concatenations. Keep every
factorization; no best score, beam or restart. This is exhaustive because any
valid code yields one enumerated split and its other values are forced.

Join all local factorizations on panel, traversal, field order, ordered roots
and ordered persons. Every unordered pair of local witnesses with different
tense strings and disjoint group IDs supplies both tense assignments. Preserve
all pairs and equivalence classes. A global600-second execution ceiling, checked
between windows/joins, gives UNKNOWN_REMAINDER rather than negative coverage;
no witness or pair cap. Stop expansion at the inclusive wall checkpoint.

Validate source feature projection, every selected window and full population,
all negative length certificates, all local factorizations and all pair joins.
Independent factorizer enumerates integer field lengths, derives length vectors,
then extracts every field from every cell and checks agreement; it does not
import the runner. Short exhaustive and known-generated positive examples are
mandatory before target execution. They validate the algorithm, not significance.

## Ceiling

No project-wide search null or numerical significance. No exact source identity,
confirmed hearing/person/tense translation, component semantics or independent
confirmation from alternative readers. A successful structural feature code
does not explain the rest of the manuscript, identify historical languages,
justify reserves, or supersede GDT900 or any failed older grammar. New scored
relation evidence would still need the GDT388 packet gates.
