# GDT998 — no complete shared-feature table

**Decision: CONTRADICTED_COMPLETE_FEATURE_TABLES.** All6,925 exposed18-group
windows fail a necessary consequence of the registered shared stem/person/tense
writer under both table traversals. No local table factorization or complete
paired reading survives. No component or word meaning is identified.

## Concrete prediction and complete result

The candidate expresses every one of36 source grammatical cells by the same
three stem strings, six person-number strings and two tense strings, in one
shared field order. Grammatical values may be empty; roots must be nonempty.
For each18-cell table, lengths must satisfy the same additive rectangle:

`len(cell[r,c]) = len(cell[r,0]) + len(cell[0,c]) - len(cell[0,0])`.

This follows from the frozen concatenation rule independently of field order,
channel naming or the chosen tense. It is a necessary consequence, not a
statistical resemblance score. Every window has a concrete contradictory
rectangle in each traversal.

| Alternative reading | All windows | Window/traversal cases | Length contradictions | Local codes | Paired codes |
|---|---:|---:|---:|---:|---:|
| IT2a | 6,710 | 13,420 | 13,420 | 0 | 0 |
| RF1b | 73 | 146 | 146 | 0 | 0 |
| ZL3b | 142 | 284 | 284 | 0 | 0 |
| CONSENSUS | 0 | 0 | 0 | 0 | 0 |
| **Total** | **6,925** | **13,850** | **13,850** | **0** | **0** |

The length proof excludes all six field orders and all six channel permutations
for every case:83,100 canonical field-order representations,498,600 including
channel labels. These are equivalent model representations, not independent
experiments. There are no timeouts or untested cases in this fixed packet.
CONSENSUS has no window capacity, not a positive or negative manuscript finding.

For a concrete trace, the first IT row-major window starts with pcheol/shol
and has sheol/shey in the next row. The four lengths are6/4/5/4; the writer
requires the last length to be5+4−6=3, observed4. This is one illustrative
certificate chosen as the first case after the full result; all13,850 are in
the complete table. No favorable isolated sequence was selected as a reading.

## What has and has not been tested

This is a new semantic-feature construction, not GDT900's literal-letter or
pair code and not IDEA412's incompatible reference to the transport grammar.
GDT900 and its original failure remain unchanged. IDEA317's selected triples
are not confirmed or generally refuted. The result does not rule out ordinary
inflection, allomorphy, grammatical feature bundles, multilingual content, or
compositional Voynich writing generally. It rules out this exact complete
three-channel/two-tense/table-order/concatenation/window conjunction.

No phonetic values, old glosses or fixed EVA segmentation were imported. The
hearing predicate, persons, tenses and language channels were assumptions from
the nominated source. Even a positive code would have been invariant under
substituting another lexical predicate and renaming grammatical values. Here
there is not even a structural candidate to carry those hypothetical meanings.
No automatic allomorph, table fragmentation, row-order or boundary repair follows.

All targets are already exposed odd-leaf selection data. The alternatives read
one manuscript. No new text/image admission, reserve, f84/f84r, f116v, even-leaf
confirmation or outside contact was used. Independent semantic confirmation
capacity is zero. No search-wide control exists and no significance is claimed.

## Reproduction and validation

Public preregistration4760273b04d8beaa44b4ed609381126595689335 preceded target
execution15:46:11.738–15:46:11.991UTC20September2026 (0.252seconds). Independent
validation15:46:19.423UTC checked all13,850 negative certificates, complete
population,36 feature cells, exact witness and pair inventories;PASS.

Before opening the packet, the two implementations agreed on1,296 exhaustive
short matrices under all six orders,144 generated full6×3 positive matrices
and144 one-cell mutations.62 positives include an empty person value and10 an
empty tense. These are algorithm checks, not a search null or meaning evidence.
Both implementations have the same author; independent means separate algorithm.

- `artifacts/CANDIDATES.tsv`: every window/traversal and its contradiction.
- `artifacts/CASES.json.gz`: complete records, witnesses and pairs (both empty).
- `artifacts/PREDICTIONS.json`: all36 feature predictions and label symmetries.
- `artifacts/FIRST_CERTIFICATE_EXAMPLES.json`: first deterministic certificate
  per panel/traversal, with all four original group IDs and spellings.
- `artifacts/RESULT.json`, `VALIDATION.json`, `PREFLIGHT.json`: exact receipts.
- `METHOD.md`, `PREREGISTRATION.md`, `PREREG_LOCK.json`, `src/`: frozen contract
  and executable reproductions. Original target cache remains hash-bound in900.

Exact staged privacy/scope and new experiment checks pass. The global checker
retains the eight pre-existing GDT600/GDT953 errors; no globalPASS is claimed.
Preparation began15:38UTC; closure publication is recorded in Git before the
16:20UTC inclusive checkpoint. The active user research block continues to
at least22:52:35UTC. Next is another full content construction, not a repair of
this failed table or a claim that experiment count itself is translation progress.
