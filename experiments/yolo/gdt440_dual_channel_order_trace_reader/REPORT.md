# GDT440 — keep the root order and the fluent sentence

## Result

The full reader no longer needs a wholesale sentence rewrite. It now exposes
the two readings an apprentice actually needs:

1. the exact ordered portable meaning trace;
2. the fluent clause after owner and carried state are supplied.

For example:

- `AIR+OL`: `BAHN · FORTSETZEN` plus the fluent continuation clause;
- `OL+AIR`: `FORTSETZEN · BAHN` plus the same fluent clause.

The sentence may sound identical, but the ordered meaning trace does not.

## What this fixes

Adding the ordered channel raises the full catalog from 1,449 to 1,543
signatures:

- 87 of GDT439's 104 groups resolve completely;
- six split partly, leaving only co-valued channel subgroups;
- eleven remain wholly co-valued local-channel groups;
- all 76 same-atom order groups disappear;
- all five contacts involving a main future card resolve;
- none of the 49 main cards remains in a dual-channel collision.

The remaining 18 groups contain 38 recipes. They differ only in learned exact
channel identity while sharing the same ordered values—mostly alternative
`HIER` signs, plus `OS`/`RESUME_CARD = VORBEZUG`. Giving these signs different
German words would be cosmetic overtranslation. The reader instead shows the
exact recipe alongside the common meaning trace.

## Current-edition replay

All 4,576 current events keep exactly the GDT438 state and fluent clause. Each
now also receives its catalog-bound ordered trace, yielding output of the form:

`Kernfolge: BAHN · FORTSETZEN. [fluent state-aware clause]`

All 29 checks pass, including deterministic regeneration. No working meaning,
surface or page changed. This dual output is now preferable to forcing every
card into a unique polished sentence.
