# GDT886 — Fixed symbol collation excluded in both f88r rows

No total order of literal EVA characters or exact STA members can make either
selected six-inscription row lexicographically nondecreasing left to right.
The contradiction survives the all-three exact-consensus subset (five inscriptions
per row), so it does not depend on the disputed locus2 or RF1b's annotated locus17.

The shortest middle-row witness is `oram → dary → okol` (f88r.13–15):
its first letters require both `o < d` and `d < o`. The top-row consensus sequence
`otorchety → orald → oldar → otoky` (loci1,3,4,5) requires `t < r < l < t`.
Exact STA yields the same contradictions as `A1/B1` and `Q2/C1/B2` respectively.
Deleting an unreadable inscription preserves a necessary sorted-subsequence rule.

Native root and independent observation confirm the two physical horizontal
orders. The arithmetic validator independently reconstructs the pair constraints,
checks acyclicity by topological elimination, replays the guarded source projection,
and agrees with exhaustive alphabet-permutation checks on512 small word triples.
Validation PASS. Since row1 is already inconsistent, row2 supplies no successful
held prediction. It is the same physical folio in any case.

This closes the declared exact sorting model, not arbitrary abbreviations,
rearranged names, language or cipher hypotheses. No word meaning, botanical
identification, new page or alphabet has been recovered. No sorting-convention
or segmentation rescue follows. See METHOD.md for the frozen protocol and
artifacts/RESULT.json for every reading, exclusion and witness.
