# GDT875 — native source units behind two exposed bridges

Decision and budget:2026-09-08 09:18–09:40UTC including preparation, native viewing, validation and publication. GDT874 found only two raw multigroup bridge candidates; the old R3 report assigns f67r2.56 to a left outer star station and .64 to a right wheel station. These cached assignments do not establish that the two written groups are visually co-owned. The smaller unresolved question is whether the alleged local complete entries can actually be localized as contiguous shared inscriptions, and which visible reading ambiguities affect their running counterparts.

This is retrospective exploratory source inspection, not unseen confirmation. Root has already seen all twelve transcription records for the four targets and the GDT874 results, and viewed the whole source canvases in earlier orientation work. No target image has been opened in this interval before this registration. Do not represent agreement as an independent manuscript witness or as a statistical result.

Fixed sources: the two cached Yale originals in src/SPEC.json, already admitted. Only the four targets f67r2.56, f67r2.64, f89r2.8 and f89r2.28 are evaluated. Whole-image context and neighbouring text may be used for localization, with no new candidate mining. No new page admission, OCR, enhanced reconstruction or external decipherment search. View actual source pixels; if a localized region is unavailable or too uncertain, retain unresolved. Do not infer exact location from an EVA string alone.

Root first records an independent native observation for each target: localization evidence and coordinates if supportable; whether the local pair occupies one spatial inscription field or visibly separate owners; group-boundary evidence; for the running endpoints only the disputed span's visible shapes and uncertainty. Classification for local entries: ONE_SPATIAL_ENTRY / SEPARATE_VISIBLE_OWNERS / UNRESOLVED. Running entries: LOCALIZED_READING_DESCRIPTION / UNRESOLVED. Neither category proves a linguistic phrase, syntactic constituent or shared referent. No forced EVA adjudication.

Seal root's observations before sharing them. A second native viewer receives only the fixed sources, four target readings and protocol, not root's observations. Preserve both originals. Record agreement and disagreements descriptively; do not use software to claim vision validation. An ambiguous locus remains unresolved even if one viewer proposes a reading. Stop at four targets or the budget; no additional corpus census, reading relaxation or null panel.

Research decision: a visibly split or unlocalizable local entry cannot serve as a supported complete-caption bridge; a clear shared spatial entry retains a source-grounded candidate but still needs stable written identity and meaning evidence. A running glyph disagreement may explain a reading-specific match without licensing corpus normalization. Every outcome changes the provenance/claim ceiling of these two candidates only. The stopped general orientation route and old semantic proposals remain stopped.

Reproduction: src/run.py creates the four-target reading packet from GDT874's hash-verified guarded projection; src/validate.py checks source hashes, target coverage and the root observation seal. Native judgements require viewing the cited original images and cannot be reproduced by the schema validator.
