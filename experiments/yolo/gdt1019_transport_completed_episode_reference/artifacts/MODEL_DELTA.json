{
  "world.py": "--- GDT1013/world.py\n+++ GDT1019/world.py\n@@ -18,6 +18,8 @@\n         fresh=OR([z3.And(x==num[name],z3.Not(seen[k][i])) for k,name in enumerate(CARGO)])\n         s.add(first[i+1]==z3.If(first[i]>=0,first[i],direct),recent[i+1]==z3.If(fresh,direct,recent[i]))\n         for k,name in enumerate(CARGO):s.add(seen[k][i+1]==z3.Or(seen[k][i],x==num[name]))\n+    fresh_episode=[z3.Bool('fresh_episode'+str(i)) for i in range(n+1)]\n+    s.add(z3.Not(fresh_episode[0]))\n     agent=[z3.Bool('agent'+str(i)) for i in range(n+1)]\n     loc=[[z3.Bool('cargo'+str(k)+'_'+str(i)) for i in range(n+1)] for k in range(3)]\n     last=[z3.Int('last'+str(i)) for i in range(n+1)];final=[z3.Bool('final'+str(i)) for i in range(n+1)]\n@@ -58,6 +60,9 @@\n             if kind in ('PAIR','COPY'):\n                 for (i,j) in hazards:causes[(i,j)].append(z3.And(b,z3.Or(z3.And(left==i,right==j),z3.And(left==j,right==i))))\n             trip=kind in ('WITH_OUT','EXCLUDE','FERRY','WITH_RETURN','CONVEY','ALONE','FINAL_TRIP')\n+            if trip:cond.append(fresh_episode[hi])\n+            elif kind=='THEN':cond.extend([fresh_episode[lo],z3.Not(fresh_episode[hi])])\n+            else:cond.append(fresh_episode[hi]==fresh_episode[lo])\n             if trip:\n                 if kind=='ALONE':load=z3.IntVal(-1)\n                 elif kind=='EXCLUDE' and variant['exclude']=='EXCLUDING':\n",
  "independent.py": "--- GDT1013/independent.py\n+++ GDT1019/independent.py\n@@ -28,6 +28,10 @@\n             after=AND([appearance[k][stop]>appearance[j][stop] for j in range(3) if j!=k])\n             e=c.If(c.And(present,before),k,e);l=c.If(c.And(present,after),k,l)\n         earliest.append(e);latest.append(l)\n+    completed=[c.Int('completed'+str(i)) for i in range(n+1)]\n+    certified=[c.Int('certified'+str(i)) for i in range(n+1)]\n+    solver.add(completed[0]==0,certified[0]==0)\n+    for value in completed+certified:solver.add(value>=0,value<=n)\n     positions=[[c.Int('loc'+str(k)+'_'+str(i)) for i in range(n+1)] for k in range(5)] # M,B,W,G,C\n     last=[c.Int('load'+str(i)) for i in range(n+1)];final=[c.Int('final'+str(i)) for i in range(n+1)]\n     pair=[c.Int('pair'+str(i)) for i in range(n+1)];stay=[c.Int('stay'+str(i)) for i in range(n+1)];staybank=[c.Int('staybank'+str(i)) for i in range(n+1)]\n@@ -74,6 +78,9 @@\n             if kind in ('PAIR','COPY'):\n                 for a,b in hazards:causes[(a,b)].append(AND([edge,OR([AND([left==a,right==b]),AND([left==b,right==a])])]))\n             moving=kind in ('WITH_OUT','EXCLUDE','FERRY','WITH_RETURN','CONVEY','ALONE','FINAL_TRIP')\n+            cond.append(completed[end]==completed[start]+(1 if moving else 0))\n+            if kind=='THEN':cond.extend([completed[start]>certified[start],certified[end]==completed[start]])\n+            else:cond.append(certified[end]==certified[start])\n             if moving:\n                 if kind=='ALONE':load=c.IntVal(-1)\n                 elif kind=='EXCLUDE' and variant['exclude']=='EXCLUDING':\n"
}
