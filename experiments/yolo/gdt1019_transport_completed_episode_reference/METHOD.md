# Complete event-reference meaning

Keep the whole1013source population and every original47value dictionary/settings.
Reinterpret the existing THEN terminal uniformly as a fresh completed-episode
anaphor. Let n count successful written voyages and c the last certified count,
initially0. Every THEN requires n>c, certifies interval(c,n], then sets c=n.
No nontrip clause resets this reference. A return to an old physical state still
counts as a real voyage. No hidden movement or maximum voyage count is added.

This is a distinct hypothetical meaning, not a fact about ordinary discourse
connectives. Every spelling mapped to THEN obeys the same rule. There is no
sol-specific exemption or new production. The added f50paragraph has no literal
sol shared position; if a complete extension uses zero THEN terminals, it passes
this component vacuously and is not an independent second attestation of sol.
Original sol positions and all assumed aliases remain exposed, not confirmation.

Primary implementation augments the frozen full Boolean-state world with one
fresh-event bit per selected boundary. A trip sets it, THEN requires and clears
it, other clauses preserve it. Independent integer/boat/STAY encoding uses
separate cumulative completed/certified counts. Both leave the exact17patterns,
all reference rules, local hazard accumulation, physical legality, result and
final-goal constraints unchanged. Exact model diffs are bound.

Ground replay combines the unchanged993/1006complete physical executions with
an independently computed interval predicate on the complete chosen parse.
On a physically successful whole path every parsed trip is an actual successful
trip, so this conjunction is exact. Failed partial paths are not used to invent
completed actions or semantic successes. Two interval implementations compare
running counters versus written event-index sets. All markers and intervals
are retained, including empty failed intervals.

Census first: all156original settings and all125saved1013/1014positive maps.
If any original fails, stop before new extension search. If all pass, search
all20original content classes (312old dictionary/setting/reader member cases).
Only new whole-word assignments already free in1013 may vary. Both engines
execute each complete query, distinguish SAT/UNSAT/UNKNOWN, independently replay
all positives, and lift every positive code to every original member/inverse.
A proved exclusion requires both complete encodings UNSAT; one timeout remains
UNKNOWN unless a separately verified positive exists. No restart unanimity claim.

Preflight: exhaustive finite event strings plus complete synthetic narratives,
including certificates before any trip, after one trip, after several trips,
after nonaction clauses and repeated certificates without an intervening trip.
No manuscript certificate census is run before public registration.
No1014wrapper,1017shared-type danger or1018universal-choice constraint inherited.
Confirmed words0;independent meaning capacity0;no significance or reserved access.
