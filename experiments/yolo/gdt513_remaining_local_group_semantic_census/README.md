# GDT513 — Remaining local-group semantic census

Status: `ALL_510_REMAINING_LOCAL_GROUPS_RECEIVE_DEFAULTS__MIXED_RECORD_MODEL_SELECTED`

GDT513 supplies a current default reading for the 510 local groups that were
outside GDT459–GDT473's 183-event address edition. It compares five creative
working architectures, retains the mixed formula/record/nomenclator model, and
publishes five expectations for the next four-page intake.

Run:

```bash
python3 experiments/yolo/gdt513_remaining_local_group_semantic_census/src/run.py
python3 experiments/yolo/gdt513_remaining_local_group_semantic_census/src/validate.py
```

The readable output is
`artifacts/GDT513_REMAINING_LOCAL_READING_BOOK.md`; the complete normalized
edition is `artifacts/gdt513_510_remaining_local_working_edition.tsv`.
