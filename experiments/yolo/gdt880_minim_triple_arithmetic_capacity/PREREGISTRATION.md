# GDT880 — adjacent same-head written-tail capacity

Registered2026-09-08 before new target access. Total wall budget25minutes,
14:50–15:15UTC including preparation, implementation, validation and publication.

## Decision note

GDT626 establishes a four-cell written tail and prints f38v.6 as 3,4,2,2 under
its provisional run+1 convention. That is exposed motivation, not new arithmetic
evidence. Prior AII-before-AI ordering and calendar tests do not test addition.
NUMBER_CALENDAR_AND_COUNTING stays closed: a numerical coincidence is not an
authorial equality. QA4 records exact predecessors and the scope distinction.

Unknown: does immediate raw adjacency supply enough same-head three-term units
on other physical leaves to justify designing an order/frequency-aware arithmetic
test? A total-first v1=v2+v3 restriction would be stronger than weak descending
grade, but this pass does not score that restriction or fit a numerical key.
Both4,2,2 and4,3,1 are weakly descending; their occurrence alone would not select
addition. No numeral or imagery inference is authorized by capacity alone.

## Fixed source and construction

Use the179-selector source allowlist. Remove every physical leaf named by a
locus in GDT626 REPORT.md: exact extracted loci/leaves/selectors frozen in
src/SPEC.json. The larger historical626 panel has been analyzed before; the
remaining material is not claimed never seen or an independent replication.
No new image/page. f84/f84r remain forbidden before payload materialization.

Read the source-group atlas only through query-tsv with repeated explicit allows
and projected columns in SPEC. For each edition/page/locus, order by raw group
index and preserve every row including empty, uncertain and annotated groups.
Only kind P lines. A candidate is THREE IMMEDIATELY ADJACENT raw groups, not
three occurrences found after filtering intervening material. Both internal
separators must be DEFINITE_SPACE. Each group must be one literal lowercase
ASCII whole: raw == clean_ascii_fragments, fragment_count==1.
Each whole must match ^([a-z]+)a(i{0,3})n$ with the SAME nonempty residual head H
before the final a. This differs from626's head spelling including that a but
is a one-to-one head relabeling. Keep all sliding windows, including same-value
ones and overlapping windows; overlapping windows are not independent units.

Primary inventory starts from ZL. To join an IT/RF view, the exact three raw
whole strings must occur uniquely as an eligible immediate window in that same
locus in that edition. Require uniqueness in ZL too. Otherwise retain the ZL
candidate as missing/ambiguous sensitivity material, not a consensus case.
This is exact same-locus string concordance, not independent glyph adjudication.

## Capacity, exposure and next decision

Publish all ZL candidate triples and cross-reading locators plus compact count
summaries; total-first equality is NOT computed. Exact tuples expose potential
arithmetic patterns even without a statistic, so any subsequent test must disclose
this discovery exposure and freeze its own contract before score access.
Report distinct all-reading source lines, physical leaves and heads. At least
20 lines on5leaves with2heads permits DESIGN REVIEW ONLY. It does not establish
power or arithmetic; otherwise stop this immediate-triple endpoint for current
scope. These minima are a resource triage threshold, not a significance gate.
No automatic wider window, longer paragraph, head merger, offset0 convention,
new control corpus, decoder or calendar/number reopening follows either outcome.

Validator independently reconstructs eligible windows and consensus joins from
guarded sources, checks source projections and frozen bytes, and verifies counts.
Source images and complete broad raw projections remain local runtime only.
