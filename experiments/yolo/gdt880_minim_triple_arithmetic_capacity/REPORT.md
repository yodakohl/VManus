# GDT880 — only three eligible immediate triples

**STOP_INSUFFICIENT_IMMEDIATE_TRIPLE_CAPACITY.** The fixed163-selector projection
contains three ZL immediate same-head written-tail triples, all uniquely concordant
in IT2a and RF1b. They occupy three lines on three physical leaves under three
heads. This misses the predeclared resource threshold of20lines on5leaves with
2heads. No arithmetic statistic or decoder is run.

| Source locus | Three literal raw groups | Raw group indices in all readings |
|---|---|---|
| f103v.24 | otan otain otain |7,8,9|
| f40r.9 | okaiin okaiin okaiin |6,7,8|
| f86v3.3 | ytaiin ytaiin ytaiin |2,3,4|

These are source observations, not authorial equations. Repeated whole groups
were already known; two AAA cases are not newly discovered repetition behavior.
The new contract is exhaustive immediate same-head triple capacity outside the
fixed printed-example leaves. Complete tuples are disclosed here, so they cannot
be called unseen targets for a subsequent score.

The source projection preserves92,344 raw-group rows from the163selected source selectors;
2,122 f84-prefixed rows are rejected before payload materialization. Only87rows
covering candidate loci in all readings are retained publicly. Empty/annotated
rows remain in the adjacency stream; no filtering first joins separated terms.
The8physical leaves named by loci in626's report were excluded prospectively,
including the already printed f38v.6 four-group example. The historical larger
panel has been used before: this is a new bounded exploratory question on old
material, not independent manuscript replication or numerical confirmation.

The provisional run+1 interpretation motivated a possible total-first law.
Addition is stronger than weak descending grade, but both4,2,2 and4,3,1 are
weakly descending. A later numerical test would need to address order and value
frequencies; one apparent equality would not license numbers or reopen the
closed calendar/counting family. Current capacity does not justify that test.
No wider window, head merger, value offset change, new data or control corpus
is selected as an automatic rescue. General numeral meanings remain untested.

The preregistration and source spec were locally frozen in b54e7669 before new
source access. The producer and independent validator query the same guarded
source separately; the validator reconstructs all windows, joins, source receipt
and counts without importing producer logic. Eight synthetic checks cover raw
interruption/annotation, interstitial groups, overlapping repeats, empty heads,
out-of-range tails and different heads. ValidationPASS concerns source/contract
fidelity only. Run and validate commands are in experiment.json; broad source
projection stays in ignored runtime, with exact digest in SOURCE_RECEIPT.json.
