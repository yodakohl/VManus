# Artifacts

`SOURCE_ROWS.json` contains only projected rows at candidate loci, including
interstitial negatives across all three readings. `SOURCE_RECEIPT.json` binds
the guarded projection, source statistics, SPEC, and preregistration.
`CANDIDATES.json` contains the ZL candidate windows and exact same-locus joins;
`RESULT.json` reports capacity triage only. No broad 163-selector projection is
published.
