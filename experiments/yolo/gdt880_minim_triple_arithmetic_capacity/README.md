# GDT880 — adjacent same-head written-tail capacity

This producer performs the preregistered guarded source inventory only. It
keeps raw adjacency and writes compact candidate/source receipts; it does not
run an arithmetic test or select a numerical interpretation.

```bash
python3 experiments/yolo/gdt880_minim_triple_arithmetic_capacity/src/run.py
```
