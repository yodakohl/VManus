# GDT508 — Source `CH+CH` repeated-package bridge

Status: `SOURCE_CHCH_GAINS_LOCAL_REPEATED_PACKAGE_BRIDGE__ALL_FOUR_CONTEXT_TARGETS_HAVE_LOCAL_SUPPORT`

Two local Source package repetitions supply the missing local support for the
last cross-register GDT507 context card. The exact duplicate
`G407-E0019→G407-E0020` repeats `CH+T+AR` on inherited `AIIN`; a second pair
repeats the `CH+T+OL` prefix around one visible argument card. Projecting one
`CH` from each package yields the already fixed two-slot `CH+CH` default. The
bare target recipe remains unobserved.

See `REPORT.md`, `METHOD.md`, and
`artifacts/GDT508_SOURCE_CHCH_REPEATED_PACKAGE_BRIDGE.md`.
