# Shared original personal orientation and source boundary

Root will view the complete official original Yale1006199 natively, after public
registration, source acquisition and metadata recording. This is a personal
orientation/source-scope record, not a hypothesis test or rediscovery claim.
The cached official manifest and F69VSD001 METHOD identify a joint f69v/f70r1/
f70r2 canvas, not a photograph restricted to f69v. GDT791 admitted f69v only.
Register f70r explicitly as one additional physical key and two selectors before
acquisition: 42 keys/48 selectors total; eight discretionary keys remain.

Describe broad visible arrangement, the relation of circular writing to other
text blocks, and page/fold divisions only where directly visible. Preserve
uncertainty about panel identity. Do not turn the old whole-canvas description
of three wheels into a claim that all three belong to the f69v selector.
Root has read that old V70 description and is not blind to it.

No slot recount, starting-point/direction judgment, manuscript transcription,
glyph identity, LM traits, label ownership, diagram semantics or translation.
F69VSD001 and all number/mansion/start-direction routes stay closed. No text
payload is opened. TGC001 query selects zero rows for all three selectors,
with 30 nonallowed rows skipped before payload; no f84/f84r or reserved
f67r2/f68r1 is accessed. No crops, enhancement, OCR or computer vision.

Runner --acquire saves only the official full JPEG and records its hash, bytes
and header dimensions; unexpected dimensions stop before native viewing.
Root then manually records OBSERVATION.json after the actual image view.
The default runner packages the observation status. An independently authored
validator checks source bytes, registration, schema and recorded completion;
software does not validate the visual content. No second viewer is claimed.

Selection source: internal known canvas provenance, not text or promising
numerical/semantic content. Acquisition failure ends the pass without a substitute.
Budget: start10:01:29UTC; intended public result by10:18UTC. The ten-hour research
goal remains active. Native original size may constrain precise detail; uncertainty
is retained rather than resolved by a crop or resized duplicate.
