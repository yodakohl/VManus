# Compact evidence

IMAGE_METADATA records retrieved official original bytes; OBSERVATION is a human-authored native judgment. RESULT packages completion; VALIDATION checks provenance and schema, never visual truth.
