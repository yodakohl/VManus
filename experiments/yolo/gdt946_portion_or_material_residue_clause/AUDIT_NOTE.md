# Post-census necessary-condition audit — GDT946

Written after the first fixed census and inspection of all16 olal positions.
The frozen protocol classifies any unknown neighbor as UNBOUND before examining
a known wrong neighbor. That rule is preserved in RELATION_CASES and registered
counts; it cannot justify treating the whole relational package as surviving.

Observed examples already available at this note: f107r21 has right okain=REMAIN
in ZL/IT; f108r43 has left ar=COORDINATION in IT/RF; f113v22 has right okaiin=VESSEL
in all readers. The opposite neighbor is unknown in each case. These are necessary
condition failures under the unchanged MODEL role lists, regardless of how the
unknown neighbor is eventually read. The first-pass UNBOUND status masks them.

Add a separately labelled post-census audit for ALL32candidates and ALL16positions:
retain boundary-ineligible cases as NOT_TESTABLE; if any KNOWN side violates its
fixed required role, label KNOWN_SIDE_CONTRADICTION; otherwise if either side is
unknown label UNBOUND; otherwise COMPATIBLE. No new word, role, boundary, source or
selection change. Preserve the original table and decision in separate fields.
A known-side contradiction rejects the same specific typed-construction package.
This is a transparent audit of logical consequences, not a preregistered discovery
success, independent validation of meaning or a repair that rescues the reading.

No general-preposition conclusion follows: in particular a material-only coexistence
contract is narrower than every possible German meaning of neben, and a material-only
partitive contract is narrower than all possible genitives. Widening either now is
not part of this trial. The German drafts and the eight values remain hypotheses.
