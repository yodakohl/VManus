# Sidequest Pass 811: CFH is the pressing verb

The only `CFH` card is `cfhy = CFH+Y` in the long f11r plant-processing
statement. It follows holding the operation at the target and precedes measured
holding and inward filling.

The former reading AUSWRINGEN added an unseen cloth or twisting action.
ENTNEHMEN would merely duplicate `CH`; ABGIESSEN would add an unseen liquid
layer. The shortest distinct workshop verb is:

> `CFH = AUSPRESSEN`

The local chain now reads:

> Den Ansatz am Ziel halten; diesen auspressen; ihn nach Sollmaß halten; in den
> lokalen Empfänger einfüllen; länger halten; anwenden und den Vorgang schließen.

Although only one cell is observed, the verb fits the same grade/endpoint board
as other operations. `CFH+Y` is attested; seven closed or graded cells receive
collision-free prediction surfaces (`cfhdy`, `cfhey`, `cfhedy`, `cfheey`,
`cfheedy`, `cfheeey`, `cfheeedy`).

`CFH` joins specialist core32. Next, resolve the singleton `S` inside
`ches = CH+E+S`: test TEIL, MENGE, REST, and ABGETRENNTER_ANTEIL against its
complete statement and the existing `AIN=PORTION` value.
