# GDT771 artifacts

- `SELECTOR_461_GUARDED_LOCUS_INVENTORY.tsv`: complete explicit selector and
  admission state for all 461 cached target loci.
- `COMPLETE_203_TARGET_CONTEXT_ATLAS.tsv`: all 203 target occurrences on the
  176 admitted loci, including strict-exclusion flags.
- `OL_LEFT_BRANCH_ATLAS.tsv`: every conservative or broader local left
  amount/value candidate, its right-cell exactness, role provenance and full
  GDT770-branch status.
- `DISCRIMINATOR_OCCURRENCE_ATLAS.tsv`: all positive matches for the eight
  fixed discriminator predicates, including incomplete and excluded matches.
- `DISCRIMINATOR_SUMMARY.tsv`: thresholds, page holdout and PASS/FAIL decision
  for each discriminator.
- `OTAR_IDENTITY_COVERAGE.tsv`: sequence, nominal and endpoint support sets.
- `NEXT_SCORE_DECK.tsv`: exact targets and controls proposed for GDT772.
- `GDT771_4_WORKING_DICTIONARY.tsv`: current replaceable four-form defaults,
  confidence and counterevidence.
- `RESULT.json`: compact machine-readable result and source hashes.
- `VALIDATION.json`: independent guard/count checks and nine-artifact byte
  replay.

All semantic identity, plaintext and component-export credit fields are zero.
