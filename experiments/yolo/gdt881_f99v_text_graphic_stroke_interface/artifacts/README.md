# Artifacts

SOURCE.json binds the official image and protocol. ROOT.json and B.json are
independently frozen native observations with normalized source locators.
RESULT.json packages the decision; VALIDATION.json checks source/packet integrity
and decision accounting, not perception. No image or transcription is published.
