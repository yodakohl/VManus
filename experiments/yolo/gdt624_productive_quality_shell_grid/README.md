# GDT624 — productive quality shell grid

Status: `COMPLETE_48_CELL_SURFACE_LATTICE__COMPOSITIONAL_QUALITY_CORE_WORKING_READER`.

GDT624 turns the four GDT623 quality corners into a complete word reader:

```text
P + {k,t} + {ch,sh} + [e?] + [d?] + y
P = bare, o, or qo
```

All 48 predicted surfaces occur. They supply 829 exact tokens; every surface
has at least one locus on which ZL3b, IT2a, and RF1b preserve the same token.
The short working values are `k=hot`, `t=cold`, `ch=dry`, `sh=moist`, `e=a
forward-bound/attributive form`, and `d=a grammatical DY binding or state
closure`. No grid form is read as an operation by itself.

The primary report is `REPORT.md`. `artifacts/PRODUCTIVE_READER.tsv` contains a
default for every one of the 48 complete words, and
`artifacts/CONCRETE_LINE_READINGS.tsv` applies them to fifteen actual lines.
