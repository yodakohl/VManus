# GDT1039 public registration

This public registration precedes primary-runner execution and the357-fixture
numeric replay. The target and numeric results were already exposed in GDT1015; the manual
predictions and known counterexamples are openly declared in METHOD.md. No blind
discovery is claimed. PREREG_LOCK.json binds the old sources, frozen author/critic
contracts, machine specification, both independent implementations and this method.
The public commit is recorded in artifacts/PUBLIC_REGISTRATION.json before run.

Disclosure before public freeze: by16:44:55 UTC the independent validator author
had executed its complete target-derived twelve-construction event oracle for
all six policies, with five synthetic pairs (0,0),(3,3),(0,1),(6,2),(-1,99):41
checks passed. This was called source-independent self-testing too broadly; it
was target-derived trace execution. A technical Counter(dict) assertion error
was corrected to Counter(keys). No primary-runner output or357-fixture replay was
read or executed. The complete manual prediction/eligibility inventory had already
been frozen before either implementation. Public registration therefore does not
precede every implementation of the reference trace, and no such claim is made.

All80 normalized positions and all357 old numeric rows must appear. No subset,
additional policy, repaired source, truth-dependent publication, extra identity,
post-result alias or new word value is admitted. The six candidate predictions
are the METHOD table, not fitted output. The seven explicit reference fields are
replaced while global/function/local bindings and old nonreference costs remain.
No result supplies independent meaning confirmation or search significance.
