# GDT1039 — whole hour/day reference composition

The complete source is GDT1015's already exposed normalized f82r.11–19:80 positions,
60 guessed word values and12 authored constructions. All old files are immutable.
The authority for the new event inventory is
`research_registry/proposals/raw477_full_reference_trace_review_20260922.md`,
including its final preregistration clarification. The independent preflight is
`research_registry/decisions/idea477_independent_reference_preflight_20260922.md`.
The machine specification and both implementations are sealed before primary
execution and357-fixture replay. Validator pre-freeze trace self-tests are fully
disclosed in PREREGISTRATION.md; this is not a blind reference prediction test.

## Data boundary and supplied semantics

Only existing GDT1015 SOURCE, MODEL and357 ROWS fixtures are used. No held-out text,
pixels or new source is opened. These are exposed diagnostic fixtures, not357 new
manuscript tests. No independent physical leaf remains in this comparison. Original
SCOPE_CORRECTION applies: positions37 and54 were normalized from `qok[ee:ch]dy`
and `ra{cty}`. No diplomatic coverage or alternate-reader replication is claimed.

Seven dynamic reference sites are replaced: C04 subject; C07's two THIS arguments;
C09 DAY_COUNT; C11 SAME result; C12's two METHOD arguments. The former
SAME_METHOD_REFERENCE choice is also replaced, expressly, by each new policy.
All old numerical results and nonreference lexical/scope costs are preserved.
Global cycle H and initial phase, local hour variables, method-result projections
and the unit/instance distinction remain supplied dependencies, not FETCH successes.

## Resolver and finite candidates

FETCH selects the most recently mentioned visible record of the required type
whose identity is unused under a consuming policy. It receives no desired ID,
result truth or intended comparison. A successful fetch updates recency and marks
the identity used where appropriate. Re-mentioning never creates a fresh identity
or releases an already consumed one. Results RA/RB have distinct provenance even
if numerically equal; methods A/B and their result records are separate identities.

The primary policy resets consumption at each of the12 fixed construction
boundaries and publishes B/RB after C11. EAGER publishes both, query-visible,
after REACH74 before SAME75. Publication is structural and independent of truth:
a false C11 is retained while C12 is still evaluated. NONCONSUMING permits a
second selection of the same identity. UNTYPED uses the same inventory without
type filtering, then checks the unchanged required type. GLOBAL omits local reset.
An empty or type-wrong fetch stops that policy's trace; later comparisons are
unbound, not false or silently repaired. A wrong-type candidate is logged, but no
mention, used-set or successful-selection update is committed. The six policies
are fixed in the table.

| Policy | Seven selections or first stop | C11 when A differs from B | C12 when A differs from B |
|---|---|---|---|
| DEFERRED_CONSUMING | D,D,U,N,RA,B,A | false | false |
| DEFERRED_NONCONSUMING | D,D,U,N,RA,B,B | false | true |
| EAGER_CONSUMING | D,D,U,N,RB,B,A | true | false |
| EAGER_NONCONSUMING | D,D,U,N,RB,B,B | true | true |
| UNTYPED_RECENT | C04 selects U, wrong DAY type | unbound | unbound |
| GLOBAL_CONSUMPTION | C04 D; C07:40 no unused DAY | unbound | unbound |

All four completed policies yield true,true when A=B. Thus only the joint change
removes both cross-method equalities. Main policy agreement with old bindings is
a conditional factorization, not unique recovery of historical reference syntax.
Only C12 provides same-type competition; most other choices have one eligible
record. A larger comparison-acceptance count is not evidence for the weaker rule.

## Execution, validation and decision

Publish the frozen inputs, predictions and code first. Then evaluate all six
traces and all357 numeric pairs without choosing a favorable subset. Retain all
original fixture fields, per-fetch visibility/recency/used state, both comparison
truths, and complete80-position coverage. Report grouped truth patterns and each
old candidate family's outcome, including its old changes and unresolved duties.
The validator independently reconstructs references and truth values without
importing the runner. Check lock hashes, coverage and original-input immutability.

Accept only the narrow conditional interaction if the table is reproduced.
Reject any main reference mismatch; retain observational equivalence where it
occurs. There is no search-wide null, significance assertion or independent
meaning binding. Confirmed words remain0. A technical PASS is not a semantic
decision. No automatic new policy, registry type, alias, boundary or arithmetic
repair follows an inconvenient result. Budget and decision changes are in DECISION.
