# GDT953: complete purpose headings and their components

Selection: 2026-09-15. The source checkpoint found a consistently delimited
introductory purpose field in Picatrix IV.ix, paragraphs29–56, unlike the earlier
I.iv multi-effect register in IDEA132. Row22 is an explicit lacuna. Root read the
complete passage and independently checked the repeated headings3/7 and lacuna
against the page images. This permits a small content test, not a decoder build.
Budget:45minutes from00:36UTC through01:21UTC, including implementation,
validation and publication. Stop expansion at the checkpoint.

F69M001's failed mansion-name spelling route and GDT898's impossible February
mnemonic code remain closed. This tests a different field: the complete intended
purpose, with source-defined recurrent actions and participants. No word-gloss,
prefix, transcription or old decoder is changed. Latin is the source language of
our historical evidence, not an assumed plaintext language of the manuscript.

Exposure: all28 f69v labels were previously exposed to the project. Root read the
GDT898 report's common complete `okeod` collision at f69v.14/.18 before selecting
this source. The repeated source purpose at rows3/7 motivated this hypothesis.
That match is discovery-selected, not confirmation. Full fresh target projection
and component inspection occur only after the attached hashes are frozen and
publicly committed. Existing catalogue order is not independently certified glyph
by glyph: cyclic conclusions are conditional on that order.

Scope: exactly f69v.4 through f69v.31, all28 labels, in ZL3b, IT2a and RF1b.
These are alternative readings of one exposed physical leaf. No additional
confirmation leaf is available in this test. All reserves and f84/f84r remain
closed; f116v is not admitted. The input uses the existing selector-first guard.

Candidates: all56 cyclic alignments, both directions and28 starts. Candidate
F00 maps source row1 to f69v.4 and increasing rows to increasing loci; F01 shifts
that start by one. R uses decreasing locus direction. Mapping formula:
target_index = (start + direction*(source_row-1)) mod28, locus=4+target_index.
No alignment selected after seeing partial matches replaces this complete set.

Model P: every complete known purpose has one deterministic, injective whole-label
rendering. Purpose IDs preserve each source heading's full meaning; only rows3
and7 are identical. Row22 is UNKNOWN. Distinct known purpose IDs require distinct
whole labels, equal IDs require equal labels. Every pair of known source rows is
tested; a target uncertainty makes that pair unresolved rather than absent.

Model C: P plus11 fixed recurrent semantic atoms in SOURCE.json. Every atom must
have a different, globally invariant cached root unit whose presence and absence
exactly follow that atom across all27 known source rows. There is no fitted error
tolerance, synonym substitution or new segmentation. Source22 is UNKNOWN for all
atoms. Presence is Boolean; repeated occurrences within one inscription are not
additional evidence. A candidate unit must have at least two observed positive
inscriptions and two observed negative inscriptions. These are capacity counts,
not independent semantic confirmations. The11 simultaneous distinct-unit demands
are decided by exact bipartite matching; all unit mismatches/domains are exported.
Also retain domains without the2/2 capacity requirement. If an atom has no known
positive inscription, an unobserved anonymous unit remains logically possible.
Only failure of this generous matching is a component contradiction; failure
only of the2/2 matching is NO_CAPACITY. Anonymous units never support a reading.

Representations: complete literal ASCII lower-case groups in original order;
internal seams must equal the source enum DEFINITE_SPACE. External line edges do
not impose a space. A nonliteral group or ambiguous internal seam makes the whole
literal inscription UNKNOWN. The component view requires this literal check plus
exact equality of the legacy surface split on whitespace with those raw groups.
Legacy root_sequence has one whitespace item per raw group; each item is split
on literal +, preserving all other characters/case. Nonalphabetic or empty root
units invalidate the whole component inscription. No fragments of an uncertain
inscription are scored. The old parser is not available here; this is a test of
the existing cached representation, not proof of correct morphology.

Evaluation: export all56x28 source-to-locus purpose predictions before intake;
then every pairwise P contradiction, every C atom/unit mismatch, complete target
records and all168 candidate/reading outcomes. P survives only if it has no
observed contradiction; unknown cells remain unresolved. C is feasible only if P
survives and all11 domains permit an injective matching. If P fails, its entire
source/meaning/constant-rendering conjunction fails. If only C fails, the weaker P
possibilities remain as equality-compatible guesses, not translated words. If C
survives, export every atom's domain and whole-table predicted reading for later
exploratory development; do not claim uniqueness from one matching or software
agreement. No reserve is opened on this result alone.

Decision value: this tests whether the tempting `okeod`/all-good alignment supports
an actual compositional heading reading beyond one repeated pair. Failure stops
this fixed heading-plus-unit model, without excusing mismatches through new
synonyms, source versions or error rules. Unknown-only survival supplies no
positive evidence. No suitable whole-search null is supplied, so no significance
claim. No independently bound meanings, hence no confirmed word even if feasible.
