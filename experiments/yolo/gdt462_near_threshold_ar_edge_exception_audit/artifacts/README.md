# GDT462 artifacts

- `gdt462_residual_edge_channel_inventory.tsv`: all eleven running edge
  channels that touch the thirteen residual labels;
- `gdt462_ar_complete_running_extensions.tsv`: all seven `ar-` extensions;
- `gdt462_ar_repeated_relation_package.tsv`: the exact `arary`/`parar`
  package evidence;
- `gdt462_two_promoted_residual_labels.tsv`: the two bounded revisions;
- `gdt462_107_revised_hybrid_dictionary.tsv`: complete revised label deck;
- `gdt462_6_page_summary.tsv`: per-page reconciliation;
- `gdt462_result.json` and `gdt462_validation.json`: compact result and checks.
