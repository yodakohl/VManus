# GDT462 — near-threshold `ar-` edge exception audit

Status: `SCORED_COMPLETE`

GDT462 audits every running-text prefix or suffix that touches GDT461's final
thirteen whole labels. Only `ar-` enters the fixed 0.75–0.90 review band. Its
sole mismatch is an already established two-page `P+AR+AR` package, so `ar-`
is admitted narrowly as `AR=AUSGANG` on `arom` and `arody`.

See `REPORT.md`, `METHOD.md`, and `experiment.json`.
