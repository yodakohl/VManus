# Fixed operation and population

For each inherited joint wrapper F and all47 terminals x, require
F(sigma(x))=sigma(F(x)) for each permutation sigma of W,G,C. All other44
terminals are invariant descriptors. One total F serves every observed pair.
Equivalent finite form: invariant inputs have invariant outputs; the cargo orbit
is either identity or one constant invariant output. This permits noninjectivity
and does not reduce arbitrary invariant-input cells to known meanings.

Primary encoding uses this finite orbit form plus equal-input congruence.
Independent cvc5 uses a total function table, range bounds and all six explicit
permutations on every terminal. Ground checking separately saturates each
observed cell under all permutations and rejects inconsistent completions.

All52 cases,312 member lifts and complete two-reader scope are inherited exactly
from1014. All36 old codes remain fixed. The original-only precursor uses known
cells of EACH JOINT inventory. Original-copy and f50 uncertainty is unchanged.
No new reading is selected by best witness or restarts. A SAT witness is checked
against every wrapper equation, complete grammar and both frozen world executors;
all cargo-renamed members are replayed. UNSAT requires both implementations;
time/resource limits remain UNKNOWN. Original precursor is a transparent exposed
candidate test, not independent confirmation. No whole-search null is supplied.
