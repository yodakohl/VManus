# GDT587 — action-conditioned carrier nouns

Status:
`PASS_1243_ACTION_CONDITIONED_CARRIERS__953_EXACT_ACTION_HOSTS__136_OBSERVED_ACTION_ROOT_CELLS__793_STATEMENTS__744_LOCAL_CARDS__ZERO_GLOBAL_ROOT_CHANGE`

GDT587 gives every written `Y/AIIN/AIN/OR` carrier under a GDT584
`T/SH/CHD/S` action an occurrence-specific German noun and case form. It also
composes a small set of exact multi-carrier packets such as body+part,
position+segment+value, and amount+basin-content instead of rendering their
members as unrelated objects.

The portable roots remain `POSTEN/WERT/ANTEIL/EINHEIT`. No new page, surface,
segmentation, name, root, or parser is introduced. See `REPORT.md` for the
result, `METHOD.md` for reproduction, and `artifacts/` for the complete reader.
