# GDT839 — no exact boundary-conservation candidate

**CAPACITY_STOP.** The preregistered cross-folio screen found zero qualifying pairs among 4,169 eligible adjacent-group occurrences. No image audit or held-prediction experiment follows from this screen.

## Manuscript result

Of 13,634 within-line adjacent pairs in the bounded paragraph scaffold, 12,227 have a definite intervening space. 10,398 meet the fixed minimum of two atoms per group and eight joined atoms; 4,169 also satisfy exact local two-group agreement and equal full-line group counts in ZL3b, IT2a and RF1b. None shares its complete ordered atom string with a different physical folio at a split shifted by one to three atoms.

The projection uses the same 88 odd admitted selectors as GDT838. Its scaffold contains 339 bounded paragraphs, 368 segments and 1,853 paragraph lines. Even folios and sealed f84/f84r payloads were not materialized. Readers are alternate readings of one manuscript, not three witnesses. No new images, language model, fit or public decipherment search.

This rules out candidates under these particular strict criteria. It does not establish that spaces are lexical boundaries, reject other segmentation mechanisms, or estimate chance frequencies. Exact conservation differs from prior approximate root-shape and working-parser relation tests. It supplies no meaning or translation. Agreement filtering excludes many occurrences; this scope limitation is preserved rather than repaired after seeing zero.

## Registration and checks

Public registration commit `9a3a8df2` preceded the first GDT839 extraction. Three invented validator fixtures pass. An independently implemented raw-concatenation pair census agrees with the saved atom-bucket result. Extraction uses shared unchanged GDT829 helpers; the guarded source replay reproduces all three output artifacts byte for byte. The validator is independent at pair enumeration, not at source extraction. Source and protocol hashes are fixed in src/PREREG_LOCK.json.

Three parallel read-only idea reviews preceded selection. Label/body register comparison and authorial correction dependencies supplied no new executable discriminator; their triage rejection is not a new scientific negative. The selected conservation proposal was tested without choosing a glyph after inspection.

The scientific screen and replay finished about eight minutes after the 02:02:40 UTC start; the ten-minute budget includes publication. Stop here, without threshold changes or a larger pipeline. Repository-wide preflight retains the previously known GDT600 binding and historical TSV-index debt; focused staged checks govern this publication.
