# GDT530 compact superform-peel working book

## New working recipe

```text
ychekchy = Y+CH+K+Y        (exact old, f17r)
- y/Y at the left edge
chekchy  =   CH+K+Y        (current, f66r)
ckhy     =   CH+K+Y        (exact old, two events)
```

Working reading: **“Nehmen, geben und posten.”**

The previous extra `CH` is removed. Repeated `CH` remains possible elsewhere;
it is simply not needed in this form.

## Reusable peel rule

An exact old form one visible character longer may support a current candidate
when its exact recipe is one atom longer, both deletions are position-matched,
and that visible/atom edit signature already recurs in the old deck. Revise a
working reading only when the route points to the current rank-one candidate
and no equally strong route supports the old working recipe.

Across all 159 forms: 25 routes, 14 surfaces, 24 confirmations, one revision.

## `chy` split

Visible `...chy` has two real compositions:

- old: 28 types / 69 events end in `Y` without `CH`;
- old: 26 types / 34 events end in `CH+Y`;
- current: `chekchy` uses `Y`; six other forms use `CH+Y`.

Do not write a global `chy` card. Choose the tail from the larger composition.

## Score and queue

Accumulated working rank one: `152→153`; rank sum: `174→173`. Remaining:

```text
aiicthy  dairykodas  dalcheeeky
dsholdaiir  qef  saiis
```

Next: decompose `dsholdaiir` through stable `SH+OL`, address/value tails, and
its local sentence neighbours. No new page.
