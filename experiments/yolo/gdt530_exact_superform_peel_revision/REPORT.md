# GDT530 — `chekchy` loses the unsupported repeated action

## Result

`PASS_EXACT_SUPERFORM_PEEL_WORKING_REVISION`.

The best current composition is now:

```text
chekchy = CH+K+Y
         NEHMEN · GEBEN · POSTEN
```

Short working reading: **“Nehmen, geben und posten.”**

The old exact form `ychekchy` supplies the decisive larger pattern:

```text
ychekchy = Y+CH+K+Y
chekchy  =   CH+K+Y
```

Removing the initial visible `y` removes the initial recipe atom `Y`; nothing
inside the remainder changes. This is not an invented segmentation: the old
deck contains `ychekchy=Y+CH+K+Y` exactly on f17r, and the left `y→Y` edit
pattern occurs 54 times among 59 comparable old pairs. The remainder
`CH+K+Y` also occurs independently as old exact `ckhy` twice.

## What changed

The previous current-page reading was:

```text
chekchy = CH+K+CH+Y
         NEHMEN · GEBEN · NEHMEN · POSTEN
```

Repeated `CH` is possible elsewhere in the manuscript, so it was not absurd.
But here it has no direct carrier: the exact old superform requires no second
`CH`, the finite compiler already ranks `CH+K+Y` first, and no matching
superform peel points back to `CH+K+CH+Y`. The leaner reading is now the better
working theory.

## Whole-deck comparison

The same construction was tried on every one of the 159 current forms, not
only on `chekchy`:

- 25 licensed old-superform routes were found across 14 current forms;
- 24 simply reconfirm the existing rank-one working recipe;
- exactly one supports a different rank-one candidate: `chekchy`;
- no current working choice becomes worse.

| Working edition | Rank 1 | Top 2 | Top 3 | Top 5 | Rank sum |
|---|---:|---:|---:|---:|---:|
| after GDT529 | 152 | 157 | 158 | 158 | 174 |
| after GDT530 | **153** | 157 | 158 | 158 | **173** |

Six unresolved working discrepancies remain:

```text
aiicthy  dairykodas  dalcheeeky
dsholdaiir  qef  saiis
```

## The useful `chy` lesson

`chy` cannot be assigned one unconditional recipe. In the old inventory, 28
invariant `...chy` types end with `Y` alone and 26 end with `CH+Y`; by event
count the split is 69 versus 34. In the current seven `...chy` forms,
`chekchy` now takes the first pattern while the other six keep the second.

So the practical rule is compositional context, not “`chy` always means X”.
For `chekchy`, the surviving components are `CH`, `K`, and `Y`.

## Next move

The exact-superform route has now been exhausted across all 159 targets; none
of the remaining six has such a carrier. The next useful target is
`dsholdaiir`, whose disagreement lies around the stable `SH+OL` and `AIIN/R`
packages and can be attacked through larger shared recipe blocks and local
line syntax without opening another page.
