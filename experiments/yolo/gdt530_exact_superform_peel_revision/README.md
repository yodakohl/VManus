# GDT530 — Exact-superform peel revision

Status: `PASS_EXACT_SUPERFORM_PEEL_WORKING_REVISION`

GDT530 revises the working composition of `chekchy` from `CH+K+CH+Y` to
`CH+K+Y`. The direct route is the exact old carrier
`ychekchy=Y+CH+K+Y`: peeling its initial visible `y` and initial recipe `Y`
leaves the current form and the already top-ranked candidate exactly.

The resulting short working reading is “Nehmen, geben und posten.” The rule is
checked across all 159 current forms and does not turn visible `chy` into a
global suffix. See `REPORT.md`, `METHOD.md`, the compact working book under
`artifacts/`, and the executable scripts under `src/`. No new page is used.
