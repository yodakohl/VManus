# GDT409 method

## Question

Do the nineteen portable German working values retain one compact role across
all five running registers, and which values still need a broad rival meaning?

## Inputs

GDT407's complete 4,576-event, 715-statement, and 5,051-attachment editions,
plus the already fixed GDT405 atom classes. No new page or image is used.

## Method

Inventory every mention of each root by page, register, recipe position,
surface, focus role, governed focus family, and exact substitution frame. Build
a complete 19×5 register matrix, one real context example per populated
root/register cell, and all 171 root-pair frame overlaps. Compare the current
short value with two concrete rivals while keeping local technical expansions
out of the portable dictionary.

## Decision rule and claim ceiling

Every root must retain one short nonempty default. A value is `KEEP` when its
broad slot/order contrast is the useful minimum; it is `KEEP_PROVISIONAL` when
the structural role is stable but the exact German verb or relation remains a
live rival choice. This is a working-dictionary refinement, not a confirmed
lexical translation.
