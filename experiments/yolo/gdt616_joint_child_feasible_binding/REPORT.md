# GDT616 report

## Outcome

GDT616 terminates at Stage A as
`NO_JOINT_CHILD_FEASIBLE_BINDING`. The prospective contract and both complete
solver implementations were published at commit `11017354` before execution.
The primary and independent solver each return exact UNSAT on the frozen
TRAIN-only instance and the orchestrator records agreement.

The decisive formula jointly ranged over the complete same-role permutation of
all 34 primitive output cards and all assignments of the eight named paid cards
to eight distinct merge nodes. At every one of the 64 directed merges it
required both the recursive unoverridden child concatenation and the selected
effective render to be nonempty members of the frozen 28,101-entry TRAIN
substring relation. Paid output had to differ from its child; the exact `qok`
node could not receive a macro card. The primary encoded 23,295 allowed
left/right/child relation rows; the independent solver used a separately built
finite-domain table model.

The strict decision is bound by three canonical terminal artifacts:

- primary: `d87d925fff5c7e185a256dacf53619b72a8fe430e2db21ce6f6232f2e906faef`;
- independent: `38b7e7741850791731946d9bc963f3ad44d5147eb267e0387d6c57b71f601361`;
- comparison: `e098d63da66b49134e2277e5646639a20cc3b6a8c840b22394da284a1f14aa2c`.

The terminal validator checks those exact files and both frozen solver-source
hashes. It also binds both corrected post-terminal diagnostics described
below; neither diagnostic nor any supplementary test claim contributes to the
strict dual-solver decision.

## Decision consequence

Stage B cannot run. No W0 mapping or paid assignment exists to freeze, and no
W1/W2 world, Held partition, `lm_confirm`, oracle panel, recovery target,
Voynich target, page, f84, or f84r was opened. The stricter grammar, macro-host
licenses, ordered traces, nonoverlapping 98-unit tilings, transitions,
frequency thresholds, and recovery machinery cannot rescue an UNSAT necessary
relaxation.

This closes only the registered synthetic FST34/V2 configuration. It does not
show that primitive composition, abbreviation, Latin, ciphering, or natural
language is impossible in the manuscript. It assigns no Voynich glyph, unit,
sound, word, language, plaintext, object, operation, or meaning.

## Post-terminal diagnosis

The corrected subset-core diagnostic first replaces every registered
paid-card `exactly once` constraint by the weaker `at most once` constraint.
Even that relaxation is UNSAT. A 23-group subset-minimal core needs only the
literal-carrier bijection, merge ranks
`2/3/4/6/9/10/11/14/23/38/43/45/51/52/60/61/62/63/64`, and the at-most-once
caps for short outputs `de`, `di`, and `ent`. The core is UNSAT and every one
of its 23 one-group deletions is SAT.

In plain language, even if every other paid output may be reused without a
global limit and `de/di/ent` may each be absent, at least one of those three
short outputs must occur at two or more of the retained merge ranks. The
registered model instead requires each card at exactly one rank. Removing the
exact-use constraints without the logically implied at-most-once replacements
is SAT, so that looser deletion is not evidence for the contradiction.

The first provisional role labels were withdrawn before publication after an
emission-order audit; the corrected 23-group artifact was rebuilt, all drops
were replayed, and its bytes were then checked exactly. This diagnostic
explains a failure boundary but does not alter the frozen terminal decision.

### Minimum adjacent relaxation

A second post-terminal diagnosis keeps the complete same-role binding, all
eight distinct paid locations, every paid card exactly once, every effective
output in TRAIN, paid/output inequality, and the `qok` macro prohibition. It
relaxes only this one condition: at a paid node, the unoverridden child
concatenation may be absent from TRAIN although the paid effective output must
still be present.

Complete 64-rank participation sweeps are UNSAT at exactly zero, one, two, and
three such child-gate violations and SAT at exactly four. Only ranks
`14/18/45/47/49/59` (`Ey/Edy/Sol/qokEdy/qokedy/qokEy`) can participate in a
minimum repair. One fixed representative uses ranks `14/45/47/49`, replacing
the out-of-TRAIN child strings `hoa/hire/tinhora/tinura` by the paid effective
outputs `ere/runt/con/erunt`. All 64 effective outputs remain in TRAIN and the
registered eight-card budget is unchanged.

Those strings are internal products of the synthetic Latin training world,
not readings of Voynich forms. The result locates the strict model's nearest
failure: four paid nodes must genuinely override child compositions that the
strict gate had forbidden. It neither rescues GDT616 nor recovers a key,
plaintext, or meaning.

## Next route

Do not repair the internal synthetic generator again. The next useful route is
an externally identified multi-folio plaintext relation: at least five
independently paired physical Herbal folios from a historically documented
entry/image tradition, with four discovery entries and one untouched complete
folio. A single global key must then predict concrete words on the held entry;
generic structural tags or another single-page pseudoreading do not count.
