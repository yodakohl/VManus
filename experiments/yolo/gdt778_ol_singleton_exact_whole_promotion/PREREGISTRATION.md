# GDT778 route declaration — exact-whole promotion for the legacy singleton deck

Declared: 2026-09-03, after the GDT777 runner result and before the GDT778
runner is written.

## Fixed form deck and corrected cohort

Start from all 376 rows of the final GDT777 renderer. The authored deck contains
29 complete right-token surfaces selected from the legacy GDT734 singleton
cleanup. A row is admitted solely when its complete `right_surface` is in that
deck and `right_reader_exact=1`. Every exact occurrence of an admitted surface
must be selected; neither corpus order nor an occurrence-ID allow-list may
choose one representative.

The first scout estimate of 29 loci was rejected before implementation because
it silently kept only the first reader-exact occurrence of each form. The fixed
form-wide rule instead predicts 41 raw candidates and 39 exact loci on 31 page
labels and 25 physical folios. `keey` at G769-T0284 and `dal` at G769-T0391 are
the two expected nonexact exclusions. The occurrence IDs are an audit
fingerprint only, never selection input.

The expected exact cohort has 32 GDT777 fallbacks and seven already contextual
rows. Therefore contextual coverage should rise from 163 to 195 and fallback
count should fall from 213 to 181. Five existing displays are expected to gain
meaning (`ar`, two `kain`, two `chy`); two `chol` rows already say `Zustand:
trocken` and should be confirmed rather than counted as new information.

## Meaning rule

Each deck entry supplies exactly one short exploratory German default, two
rivals, confidence, positive evidence, counterevidence, and its source-card
status. The complete exact right word is the smallest licensed unit. The
runner may not strip an initial, export a body substring, or infer a free EVA
character meaning.

Five entries (`ar`, `chor`, `chol`, `dair`, `ols`) use later complete-whole
evidence. The other 24 are deliberately new `EXACT_OL_PLUS_WHOLE_ONLY`
promotions from GDT736/GDT737 body candidates: those old cards are candidate
sources, not previously proven bare-word translations. `ols=Produktposten` is
a new C0 throughput default under explicit source conflict; the superseded
Filtrat/abgeseihte-Zubereitung reading is only a rejected rival and receives no
support from GDT759 after GDT769/GDT772.

## Renderer and consumption rule

Precedence is GDT778 exact whole, GDT777 inherited contextual span, inherited
fallback. A selected fallback consumes its complete right token. A selected
already-contextual row retains its structural frame and composes the new whole
meaning into it. Four expected right tokens (`chol` twice and `chy` twice) are
already consumed by the same inherited target row; ownership may be transferred
to GDT778 without counting the token twice. Any cross-row consumption collision
is fatal. The expected unique consumption total is 120 + 39 - 4 = 155.

## Claim ceiling

GDT778 may publish replaceable exact-`ol + whole` meanings and their composed
line displays. It confirms no free EVA component, lexeme, historical spelling,
language, plaintext sentence, specific recipe identity, disease, treatment, or
substance. No new page, image, OCR, transcription, `f84`, or `f84r` may be
opened. All defaults are exploratory working meanings, not a decipherment.
