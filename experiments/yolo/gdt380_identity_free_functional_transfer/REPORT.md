# GDT380 — identity-free functional-operator transfer report

## Result

`NO_IDENTITY_FREE_SIGNATURE_PASSED_COMPARATOR_GATE`

The target stage was not run. No Voynich target row was read, retained, or
scored, and f84 remained sealed. GDT379's F1 was not used. It remains only the
exposed formal recurrence anomaly `F1 X F1`; its failed semantic route and
stability gate are unchanged.

## Comparator calibration

The frozen detector replaced exact opaque form identity with local equality,
recurrence, pre/post diversity, reconvergence, return horizon, deletion-bridge,
and boundary behavior. Models were trained leaving out an entire readable
domain. The four families and all behavior components were charged together
in a 1,024-world conditional held-score maxT null.

Candidate membership was not fixed by the null strata. Mobile capacity was
83,050 rows/131 mixed strata for CMP01, 101,724/265 for CMP02, 80,623/344 for
CMP03, and 100,661/2,164 for CMP04. Thus the stop is not another deterministic-
slot identifiability failure like GDT378.

| anonymous family | held-domain AUC floor | positive full-gain domains | positive deletion-gain domains | PCEEC2 AUC / gain bits | maxT p | decision |
|---|---:|---:|---:|---:|---:|---|
| CMP_FUNCTION_01 | 0.677016 | 2/5 | 1/5 | 0.604941 / -4.624 | 0.000976 | FAIL |
| CMP_FUNCTION_02 | 0.779770 | 4/5 | 4/5 | 0.779770 / -47.227 | 0.000976 | FAIL |
| CMP_FUNCTION_03 | 0.597290 | 1/5 | 3/5 | 0.686222 / -0.911 | 1.000000 | FAIL |
| CMP_FUNCTION_04 | 0.896572 | 3/4 | 3/4 | 0.896572 / -3823.761 | 0.000976 | FAIL |

The low permutation p-values for three families mean their held score rankings
are nonrandom under this fixed structural instrument. They do **not** satisfy
the frozen transfer criterion. The criterion also required positive held
codelength gain in PCEEC2, at least three positive-gain domains, a procedural
pass, and survival after removing pivot-recurrence features.

The clearest near-misses are informative:

- `CMP_FUNCTION_02` ranks its comparator endpoint well in PCEEC2 and three
  procedural domains, but reverses below chance in CoReMA (AUC 0.453801) and
  loses 47.227 held bits in PCEEC2. The behavior is domain-dependent rather
  than a single transferable transformation.
- `CMP_FUNCTION_04` has excellent held ranking in all four available domains,
  but its full model is catastrophically miscalibrated in PCEEC2 because the
  endpoint prevalence and recurrence regime differ sharply from the
  procedural corpora. Removing pivot recurrence makes PCEEC2 positive, but
  Quinte Essence then becomes negative. This is a rank-level closed-class
  resemblance, not a stable identity-free predictive channel.
- `CMP_FUNCTION_01` adds information in only Curious Cures and Harleian; the
  pivot-recurrence-deleted model is positive only in Harleian.
- `CMP_FUNCTION_03` fails the frozen AUC floor and joint correction and adds
  full-model information in only Harleian.

## Consequence for Voynich

No anonymous behavioral class is authorized, so there is no
`CMP02_BEHAVIOR_CLASS_A` or analogous candidate, no realization inventory, and
no operator-pairing, scope, valency, or coordination prediction. This is not a
negative score on Voynich; it is a comparator-stage instrument failure. The
experiment therefore avoids repeating GDT378's mistake of searching Voynich
identities with a comparator label whose supposedly invariant behavior has not
itself transferred.

The useful conclusion is narrower: the original GDT378 signatures depended
substantially on corpus-specific identity/recurrence and calibration regimes.
Replacing identity with local behavioral transformations did not yield one
robust cross-domain detector under the fixed end-to-end gate.

## Claim ceiling

GDT380 establishes no AND, OR, NOT, UNTIL, function word, POS, morpheme,
sound, language, plaintext, meaning, or translation. Semantic state remains
`UNASSIGNED`. No f84 file, row, image, text, or formal payload was accessed.
