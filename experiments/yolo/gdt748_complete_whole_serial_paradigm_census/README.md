# GDT748 — complete-whole serial paradigm census

GDT748 tests every cached written three-/four-word window with exactly one
non-W2/W3 position. Serial context alone is too loose: it recovers an axis at
only 189/429 known leave-one-out positions. Requiring one complete known whole
within edit distance two raises the recovery to 96/126; two such whole-form
bridges recover 18/20.

The combined rule leaves sixteen recurrent role leads: thirteen reinforce weak
cards, two replace retired literal identities with axis-only roles, and
`okechy` receives the only new open-whole lead (`HOT`, two pages). `qochey`'s
strongest frame remains `DRY|MIDDLE_STAGE`, but two weaker conflicting frames
block a fixed global value.

Primary result: [REPORT.md](REPORT.md). Machine scope:
[artifacts/RESULT.json](artifacts/RESULT.json).
