# GDT748 artifacts

- `ELIGIBLE_ONE_OPEN_FRAME_CENSUS.tsv`: every structurally eligible window;
- `PREDICTIVE_SERIAL_FRAME_CENSUS.tsv`: windows with a shared quality/stage axis;
- `COLLAPSED_POSITION_EVIDENCE.tsv`: one best frame per physical target;
- `FORM_BRIDGED_POSITION_EVIDENCE.tsv`: the complete-whole edit≤2 subset;
- `SURFACE_PREDICTION_CENSUS.tsv`: surface aggregation, conflicts and decisions;
- `HELD_12_SERIAL_AUDIT.tsv`: direct audit of the GDT747 candidates;
- `KNOWN_LEAVE_ONE_OUT_CALIBRATION.tsv`: hidden-known-target self-test;
- `FORM_BRIDGE_CALIBRATION_CENSUS.tsv`: unconditioned and bridge-class rates;
- `GDT748_COMPLETE_WHOLE_SERIAL_READER.md`: compact human reader;
- `GDT748_GDT388_SERIAL_EDGE_PACKET.tsv` and intake JSON: expected invalid,
  not-score-ready relation intake;
- `RESULT.json`: machine summary and artifact hashes;
- `VALIDATION.json`: independent invariant and byte-replay certificate.

The full frame tables are retained because every apparent new role must remain
traceable to exact whole surfaces, positions, prior cards and counterframes.
