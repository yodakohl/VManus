# GDT535 — `qef` follows the q-role of its sentence

## Result

`qef` keeps the existing working recipe:

```text
qef = E + LOCAL_CHAR_F
      GRAD I + HIER
```

The short working phrase is **“Hier auf Grad I.”** The change is not a new
atom split. It closes the remaining ambiguity against
`CARRIER_Q+E+LOCAL_CHAR_F` by using the exact statement around the card.

## Six local votes, one answer

G515-S010 contains seven q-initial cards. Removing `qef`, the other six are:

```text
qokees  qokeey  qokeey  qotar  qokey  qokeor
```

All six recipes omit `CARRIER_Q`. Among the four finite `qef` candidates,
`E+LOCAL_CHAR_F` is the only noncarrier recipe; all three rivals begin with
`CARRIER_Q`. The local vote therefore promotes the global rank-two candidate
to local context rank one.

## This is a recurrent sentence habit

The same simple neighbour vote works well beyond this one sentence:

| deck | q events | multi-q statements | scored neighbour votes | correct |
|---|---:|---:|---:|---:|
| old 26 pages | 768 | 166 | 383 | 339 (88.5%) |
| current four pages | 78 | 17 | 47 | 41 (87.2%) |

This does not make every statement uniform: the exceptions remain in the
atlas. It makes a six-to-zero local vote useful when the target candidates
differ exactly in carrier presence.

Two older mechanisms point the same way. Initial visible q adds no recipe atom
in 75/84 old edit pairs, and the old first-atom-E context is 1/1 noncarrier.
GDT523 already supplied the exact target path `qe→e~E`; its stronger combined
setting could put `qef` first but was too broad elsewhere. GDT535 uses no such
global weight. The exact sentence supplies the missing selector.

## Working-edition effect

No recipe changes: `qef=E+LOCAL_CHAR_F` was already the provisional working
choice. Its status changes from unresolved to locally resolved, and it gains a
short phrase. Candidate metrics therefore remain 155/159 rank one and rank sum
174. The semantic working layer is now resolved for 158/159 surfaces.

The runner is byte-stable across two executions; the independent validator
passes 38/38 checks and preserves every GDT531–GDT534 revision.

## Next grip

Only `aiicthy` remains. Its right side is easy (`cthy=CH+T+Y`); the unresolved
part is `aii`. Compare three concrete readings—`AIIN`, `A_ADDR+IIN`, and the
visible `A_ADDR+LOCAL_CHAR_I+LOCAL_CHAR_I`—through exact `...aii...` carrier
families and the same f31r block. No new page is needed.
