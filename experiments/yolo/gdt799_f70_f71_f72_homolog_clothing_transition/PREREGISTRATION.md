# GDT799 preregistration

Date: 2026-09-04

Status: `FROZEN_AFTER_FULL_PAGE_ORIENTATION_REVIEW_BEFORE_BLIND_REVIEW_RETURN`

The analyst had already viewed the two full admitted canvases to localize the
outer figures.  This is therefore not called an analyst-blind acquisition.
The two independent crop reviewers are blind to page, position, label, and
alignment.  Their 18 random IDs and crop rectangles were frozen in
`src/BLIND_CROP_KEY.tsv` before their calls were opened.

The endpoint is the strict visible upper-torso garment boundary defined in
`METHOD.md`; it is not a social or astrological interpretation.  The primary
alignment is exactly f70-R0/f71-F9/f72-R0.  No state regrading, alternative
visual axis, ring deletion, label feature, or better-looking transform may
replace it after unblinding.

Primary decisions:

1. both new rings mobile, fixed transform top-40/400, tie count <=40, and
   better than identity: retain a C0 position-specific image-grammar rival;
2. either new ring nonmobile: select page/ring facies only;
3. mobile rings but fixed transform misses: no fixed-alignment image support;
4. reviewer agreement below 14/18 or any unresolved decisive conflicts: stop
   on acquisition ambiguity.

All 400 transforms and all ties are reported as diagnostics, even when the
mobility gate stops relation reuse.  The GDT388 relation packet remains empty
because cross-page analyst homology is not an authorial directed edge.
