# GDT780 — ol two cardless whole bridge

Status: `PASS__2_EXACT_CARDLESS_WHOLES__2_FORMS__2_LOCI__247_CONTEXTUAL__129_FALLBACKS__207_CONSUMED__NO_COMPONENT_EXPORT`

The preregistered occurrence-free rule tests exactly two independently
supported complete-whole roles, `eees=Mengenfeld` and
`sheeol=Endzustand`, against all 376 GDT779 rows.  It selects exactly two
fallback spans, raises contextual coverage 245→247, lowers the fallback count
131→129, and raises unique right-token consumption 205→207.  Eleven frozen
sources now reconstruct the target-excluding evidence row by row; all fourteen
runner outputs replay byte-identically.  See `PREREGISTRATION.md`, `METHOD.md`,
`REPORT.md` and `experiment.json`.
