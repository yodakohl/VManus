# GDT780 preregistration — two cardless complete-whole bridges after `ol`

## Fixed question

Can two complete surfaces in GDT779's reader-exact cardless residual acquire
short practical role defaults from evidence that predates and does not depend
on their target `ol` positions, and can those defaults replace every matching
generic fallback without changing any other row?

## Frozen deck

Exactly two complete surfaces are admitted before the runner is written:

| surface | short default | rival 1 | rival 2 | confidence |
|---|---|---|---|---|
| `eees` | `Mengenfeld` | `Einheitenfeld` | `Wertkopf` | C1 role / C0 identity |
| `sheeol` | `Endzustand` | `Feuchtzustand` | `kalte Zustandsform` | C1 role / C0 identity |

`eees` is selected because GDT758 independently records seven reader-exact
occurrences, four exact right contexts and three `aiin` followers: conditional
rate .75, ordered-value baseline .021613 and lift 34.702083. Removing the later
`ol eees` target still leaves two `aiin` followers among three exact right
contexts. The signal selects an amount/value-field role, not a number or unit.

`sheeol` is selected because GDT745 records ten cache occurrences/nine exact;
GDT746's whole-form and distribution intersection retains `END_STAGE`; and
GDT747 supplies four local END contacts on three pages. Moist and dry qualities
conflict. GDT748's visible cold frame has no whole-form bridge and remains a
counterframe, so only the end-state axis is spoken.

The other 23 reader-exact cardless right surfaces are not in the deck. No edit
neighbor, substring, page, locus, occurrence ID, frequency, or immediate
neighbor may admit them.

## Frozen selector and precedence

For every one of the 376 GDT779 rows, select exactly when:

```text
gdt779_renderer_contextual == 0
AND right_reader_exact == 1
AND right_surface IN {eees, sheeol}
```

Every match is selected. Existing contextual rows always retain precedence.
Each selected `ol + right complete whole` consumes the right token exactly
once. The two defaults are rendered as whole-span values; neither the right
surface nor `ol` is decomposed.

## Predicted result

- selected spans/forms/loci/pages/folios: 2/2/2/2/2;
- selected surfaces: exactly `eees` and `sheeol`, one each;
- parent contextual/fallback: 245/131;
- new contextual/fallback: 247/129;
- unique consumed right tokens: 205→207;
- fallback replacements: 2; sharpenings, confirmations and takeovers: 0;
- residual partition: 23 exact cardless, 49 nonexact card-bearing,
  20 nonexact cardless and 37 line-final;
- no component, lexeme, number, unit, material, liquid or substance identity.

The two expected target fingerprints are retained for validation only:
`G769-T0208` at `f43v.16` and `G769-T0487` at `f88r.21`. Neither identifier is
an input to selection.

## Failure conditions

The promotion fails if the pure selector yields anything other than the two
complete matches; if either source evidence row cannot be reconstructed; if a
protected contextual row changes; if consumption collides; if one of the 23
other exact cardless forms is promoted; or if any substring, specific unit,
number, substance, language or plaintext claim is exported.

No new page, image, OCR or transcription may be opened. `f84` and `f84r`
remain forbidden.
