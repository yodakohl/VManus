# GDT976: declaration before projection execution

All inputs, the exact source projection, runner and validator are frozen in PREREG_LOCK.json and published before target execution. Target pages and older eligibility/length results were already project-exposed. This is a registered follow-up, not fresh independent confirmation.

Question: does the unchanged GDT963 four-whole-record code admit even its necessary shared IRIS/XIPHION projection? Do not change its333 source atoms, aliases, lexical order, nonempty prefix-free code, source selection, target frame or separators. Original GDT963 UNKNOWN remains unchanged.

Read only frozen963 SOURCE.json, TARGET_FRAMES.json.gz, CANDIDATE_TABLE.tsv and the three older primary contract/result documents needed to verify origin. All357 frames and all four record domains are retained. Literal old contradictions are excluded as previously recorded; unknown solver cases remain candidates. Nonliteral sources remain UNKNOWN_SOURCE. Editions are separate readings, not independent samples. Four physical leaves remain required.

For each eligible ordered I.1/IV.20 whole-page pair on distinct leaves, enumerate every possible nonempty prefix v/w of the respective whole strings. They must be distinct and prefix-incomparable. Their finite maximum lengths follow solely from source occurrence counts and at least one character per remaining atom; no arbitrary code-length or word-boundary limit. In I.1, v occurs first and w follows after at least4 other characters, with at least259 trailing characters. In IV.20, w occurs first and v follows after at least14 other characters, with at least110 trailing characters. These values must be derived from the frozen atom streams, not hand-edited after results.

Every I.2 candidate must admit v after at least6 initial characters, again after at least92 intervening characters, with at least20 trailing characters. Both occurrences have the SAME v. Every other source atom occurrence is relaxed to an arbitrary nonempty string; repeated omitted atoms need not agree. This permits incidental additional v/w occurrences inside those unknown strings, so no absence/count-equality condition is imposed. Require all four selected physical leaves distinct; Meum retains its full minimum length but no name value is inferred. Every admitted completion of the full model satisfies this relaxation.

The earliest legal match construction is complete for these positive lower-gap/positive-tail constraints: any earlier match leaves at least as much suffix for subsequent matches. Prefix events with zero initial gap must start at0. A separate regular-expression whole-string matcher and synthetic exact brute-force partitions check the construction. No general decoder, codebook optimization, opaque token deletion or silent source correction.

Emit all page-pair outcomes, all surviving v/w pairs and every supported Acorus page/occurrence witness, with every eligible fourth Meum page factored by its leaf exclusions. Grouping is storage only; no page ranking, first-match semantic selection or omitted contrary case. A pair with no completion is a projection contradiction; positive rows are PARTIAL_SHARED_REFERENT_ONLY. If no capacity-bearing edition survives, close only the literal963conjunction. Otherwise retain every partial binding and its exact untested obligations. No automatic solver extension follows either result.

Independent confirmation capacity0; no semantics, plant names, significance, probabilities or score-ready relation packet. No raw TSV/corpus/image/reserve is read; f84/f84r sealed and f116v unadmitted. Complete-context/source-code translation remains untested by this projection. Parent inclusive budget15:16–15:46UTC.
