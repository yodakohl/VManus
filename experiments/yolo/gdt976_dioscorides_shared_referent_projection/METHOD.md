# Necessary shared-name projection

The frozen963source has I.1: IRIS at0 and XIPHION at5 of265 atoms; I.2: IRIS at6 and99 of120; IV.20: XIPHION at0 and IRIS at15 of126. Meum has102 atoms and neither selected name. SPEC derives these positions and all omitted-atom gap counts. All other content still belongs to the original hypothesis; relaxing its code equalities supplies a necessary test, not a translation that deletes it.

Every actual IRIS/XIPHION code must be a nonempty prefix of its corresponding source-entry page. Enumerate those finite prefixes, preserve their prefix-incomparability, and apply the whole-string minimum-gap projection in all three linked records. Any full four-record code would yield one of the retained rows. An absent row therefore excludes that particular relaxed page/code case. A retained row does not prove a full code or known meaning.

Acorus support depends on v and physical leaves; cache that calculation without changing candidates. Meum support is a factored list excluding the three selected leaves. Every partial code is stored with every supported page, not a winning example. GDT963 local length contradictions retain their original status; source-unknown pages do not become contradictions. GDT963 itself is not mutated. The separate validator uses regex whole-string matching and rebuilds every prefix domain and table.
