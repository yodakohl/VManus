# GDT961 registration — exhaustive contiguous-component incidence bound

Exploratory follow-up after GDT960 exposure. Freeze all22 original complete
15-paragraph windows, both directions, both source models, and every32/34
plant/material name. No change to sources, names, paragraph ownership,
transcription, keys, separators, GDT888, GDT913 or GDT960.

For every alphabetic whole group classified KNOWN by GDT960, enumerate EVERY
nonempty contiguous substring, including length1 and the whole group. No
frequency, length, position, affix, BPE, language or semantic filter. A piece's
observed mask is the union of every paragraph containing it anywhere inside
such a group. Identical masks define indistinguishable occurrence predictions;
retain every piece in each class and every rejected mask in the full inventory.
The tested marker claim is biconditional: the source plant occurs in a row if
and only if that exact context-independent piece occurs inside a group there.

For every source-name/window/direction/model case, KNOWN_MASK_MATCH lists all
pieces with exactly the required fifteen-bit observed mask. This is a necessary
observed condition, not proof of absence in unread portions. UPPER_POSSIBLE
lists every observed piece with no known occurrence outside its required rows
and at least one whole UNKNOWN group in every missing required row. A fresh,
previously unseen piece is a separate unbounded completion class when every
required row has an UNKNOWN group. Length/glyph plausibility of such additions
is not asserted. Known substrings within groups classified UNKNOWN remain
unconstrained, making the upper bound conservative.

Report every plant's concrete rows, known pieces, upper pieces, needed unknown
rows and identical-prediction classes. Summarize cases with all domains nonempty,
cases missing a known plant domain and cases missing even an upper domain.
No joint allocation, plant identity or lexicon uniqueness is claimed: several
markers may share a word, and independent nonempty domains need not form a
single compatible completion. An empty required domain is sufficient to
contradict this fixed marker model; a nonempty domain does not confirm it.

No new raw TSV, image, selector, reserve, f84/f84r or f116v access. The179-selector
admitted cache and all prior exposure are inherited from GDT960. Five exposed
physical leaves;0 independent confirmation leaves per candidate. Alternate
transcriptions and overlapping windows are dependent. RF1b lacks paragraph
capacity. No GDT388 relation score and no significance claim. All concrete
predictions are saved before running component enumeration. Budget and research
decisions: src/DECISION.md.
