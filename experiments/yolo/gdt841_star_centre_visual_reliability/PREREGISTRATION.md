# GDT841 — native star-centre reliability pilot

IP001 next decision: is the potential carried-star visual contrast reproducibly readable before any label test? Root previously viewed this whole image and f72r3; incidental text is visible. The pilot is outcome-aware discovery, not blind image acquisition. No star-label relationship, pigment chemistry, lexical value or independent manuscript witness is claimed.

Freeze all five stars in the inner ring of admitted f70v, Yale canvas1006201, existing1000x1242 image. SPEC.json gives approximate centres clockwise from top and exact image bytes. No new fetch, page, OCR, enhancement, crop or ray counting. The source selector within the admitted page is not inferred from the cache filename. Root and a second native viewer classify all five independently; neither reads the other's current judgments before both files are written. Both see the same photograph and task, so agreement is not independent physical evidence.

Classes: PIGMENTED_CENTRE = central region visibly filled with ochre/yellow/brown tone distinguishable from nearby parchment; OUTLINE_CENTRE = central region visibly parchment-like within an outline, without discernible fill; UNCERTAIN = resolution, stain, fading or overdraw prevents choosing. These describe the photograph, not original paint absence. Add a short per-target reason. Do not grade rays, clothing, labels, names, or ownership. Save exact source SHA in each record file and fixed five IDs. All cases retained; no threshold repair.

Technical capacity PASS requires at least4/5 exact category agreements AND at least one agreed PIGMENTED_CENTRE and one agreed OUTLINE_CENTRE. UNCERTAIN agreement counts in overall agreement but cannot supply either clear class. Otherwise STOP_VISUAL_CAPACITY. A pass permits considering a larger predeclared annotation panel only; it does not authorize a text correlation, semantic edge, or generalization across pages. A fail stops this exact pilot; no cherry-picked higher-resolution rescue within it.

Comparison is deferred until both annotated files exist. The public source protocol and targets precede annotation. Run verifies own frozen inputs and computes the simple cross-tab; independent validator recomputes from a confusion matrix. No model training or hypothesis p-value. Budget5min total from02:45:37 UTC, including pipeline work and publication; record overrun if necessary. Existing gates on page/ring confounding and singular ownership remain untouched. Duplicate audit already distinguishes GDT796 clothing/barrel/facing, paragraph-marker color, ZST002 tails and direct-ray ownership in STAR_FILL_PILOT.md.
