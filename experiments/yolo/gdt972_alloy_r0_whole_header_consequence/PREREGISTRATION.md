# GDT972 — necessary whole-account header under the existing R0 writing rule

2026-09-15, fixed test. Inclusive budget14:09–14:35UTC, including design,
registration, the complete census, independent validation and publication.
The S0/R0 card was public in185207d72 before selection. No R0 target-header
inspection has occurred. R0 is unchanged; the producer's independently drafted
within-group proposal is not an after-result repair or an input to this test.

## Decision and exact unknown

GDT971 establishes conditional source number identifiability with known roles;
its second path adds zero eliminations. R0 now supplies a finite complete
variable-account grammar with a shared38-atom code. Before paying for a general
tree/code search, does any complete literal paragraph satisfy R0's unavoidable
header conditions? Zero survivors rejects this fixed R0 representation over
the declared literal scope. Survivors justify considering a full grammar/code
test but do not establish a complete parse, numerical meaning or any word.
No full decoder, source-copy count, larger control corpus or automatic repair
is authorized by this small screen.

The relevant primary reviews were read: GDT902/969 fixed arithmetic formats,
GDT970 whole-part consequence and intake, GDT882/903 line-action limits,
IDEA124 original linear-material register proposal and its OPERATORS_RESULT,
and the live composition baseline. R0 is a separately proposed variable
arithmetic account, not a changed SOURCE/word-count/input-prefix branch of those
failures. Known whole-form/entry effects remain unexplained by R0, not erased.
Reusing a code-like formal role does not identify historical arithmetic.

## Whole-text contract and necessary consequences

Exactly S0/R0 as defined in the bound ALLOY_FINITE_GRAMMAR.md/JSON/code, including
the stated source-prose projection and finite bounds. One whole paragraph is
one complete account. Every group would have to be consumed by its grammar.
The header, with zero-based group positions, is always:

```
0 GRADES  1 A  2 NUM(a)  3 B  4 NUM(b)  5 C  6 NUM(c)
7 TARGET  8 NUM(t)  9 TOTAL  10 NUM(m)  11 FIRST
```

Every atom has one global nonempty lowercase code of length1–8; all38 codes
are prefix-free and distinct. NUM(n) concatenates h(NUM) and the globally
shared decimal digit codes. Header literals are positive integers at most400,
with a<b<t<c. No source numeral values or exact program lengths are imposed.

This test computes all the following necessary conditions, in this fixed
first-failure order, for each eligible complete paragraph:

1. At least14 whole groups:12 header groups plus at least YIELD and a recipe.
   This is a deliberately weak lower bound, not a claim that14 can be valid.
2. Seven atomic header words at0,1,3,5,7,9,11 each have length1–8.
3. Those seven words are distinct.
4. No one of the seven atomic words is a prefix of another.
5. Numeric header words at2,4,6,8,10 each have length2–32: one NUM atom and
   one to three decimal digit atoms, each length1–8.
6. The four grade/target words at2,4,6,8 are distinct. The total may equal one.
7. At least one candidate h(NUM), of length1–8 and shorter than every numeric
   word, is a common prefix of all five numeric words and is prefix-incomparable
   with all seven already fixed atomic words. Retain every such candidate.

All applicable conditions are recorded even if an earlier one fails. This
screen does not yet parse the remaining digit strings, assign numerical
values, enforce arithmetic, fit all38 atom codes, parse the rest of the body
or require a common key across several paragraphs. Its positive label is
NECESSARY_HEADER_ONLY, never a whole reading. All failures are logical necessary
contradictions of the unchanged complete model within its literal scope.
The full body remains in the bound input; no favorable internal start or
single matching line is selected.

## Scope, exposure and outputs

Use exactly GDT970 artifacts/PARAGRAPHS.json, itself reconstructed and validated
from the guarded GDT915 caches with the frozen GDT928 paragraph fences.
Retain all1349 complete paragraph rows, including561 literal eligible rows
(ZL32,IT529). The788 nonliteral complete rows remain unknown, not failures.
RF lacks complete paragraph fences. One-group lines are allowed as in970;
literal eligible means all groups [a-z]+ with definite interior seams and
consecutive group indices. No new transcription normalization or regrouping.
Pages must belong to the bound179-selector SPEC and must not start f84 or be
f116v. Alternate editions are one manuscript, not independent replication.
All this text was already exposed in earlier work, including GDT970. No
independent manuscript confirmation capacity, reserve access, new image or
external contact is available or used. f84/f84r remain explicitly forbidden.

Output one prediction/result row for every complete paragraph: edition, full
paragraph ID, physical leaf, eligibility/defects, group count, full fixed header,
each necessary-condition result, all candidate NUM prefixes, every contradiction
and first decision. Summaries give whole denominators, leaves and first/all
failure counts; survivors are listed exhaustively. Report remaining grammar/
code/meaning ambiguity and scope outside the literal cache. No significance
or source identity/metal name claim. No GDT971 success is counted as a target hit.

Before target access, validate the necessity checks on the three complete
source-generated accounts using one explicit synthetic prefix-free38-atom code,
plus fixed corruption cases for each condition. Synthetic ciphertext is only
a software fixture, not manuscript evidence. An independent validator must
reconstruct the header rules from R0 without importing the primary checker,
then compare every row and denominator on the same fixed whole-paragraph cache.
Freeze/publish before either target-header enumeration. No source/key/prefix,
header-deletion or other rule adjustment follows the observed outcome.
