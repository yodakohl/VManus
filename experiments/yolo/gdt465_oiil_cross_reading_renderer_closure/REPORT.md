# GDT465 — the final whole-label residual stays whole

## Result

`oiil` remains `[PFLANZENNAME:oiil]`. This closes the current nomenclator
decomposition pass at eighteen full formulas, 87 function-shell plus learned
core labels, one owner-family-only label and one wholly learned label. The
function-character total remains 440/713; no earlier reading changes.

## What the readings show

At f17r.13, ZL3b and RF1b both read the complete line fragment as
`oteeeon oiil`. IT2a has no reading at this locus. Thus the two present readings
agree exactly on `oiil`, but they do not provide three independent witnesses or
an internal boundary.

The admitted f17r image associates the labels with one pictured flowering
plant. It supplies a useful owner class but no separately labelled sub-objects
that would map onto `o|ii|l`.

## Why the attractive split fails

The running deck makes the temptation understandable:

- `O+IIN` occurs in sixteen surface types, 28 events and thirteen pages;
- exact `o=O` and `l=L` cards both exist.

But the necessary bridge is absent:

- `IIN+L`: 0 types;
- `O+IIN+L`: 0 types;
- exact `oii`, `iil` and `oiil` cards: absent.

The only running surface containing `iil` is
`cphodaiils=CH+P+O+D_ADDR+AIIN+L+S`. It supports `AIIN+L` in that longer card,
not `IIN+L`. Conversely, the twelve surfaces visibly containing `oii` preserve
their old running recipes and never supply the missing following `L` package.
Visual adjacency therefore cannot complete the proposed parse.

## Closure

Keeping one learned whole label is the stronger working dictionary: it permits
the productive technical shells to remain productive without forcing every
pictured name through them. `oiil` can be memorized as a local plant-label
surface until genuinely new material supplies a better family or package.

Validation passes 66/66 checks, including the guarded transcription query and
a byte-identical rebuild. No page, core meaning, object identity, spelling
prediction, plaintext, language or confirmed lexeme is added.
