# GDT902 — complete multiplication-register model excluded

All four panels fail necessary conditions, independently reproduced from the
second native-source compiler. No numeral-code solver or code search was needed.

In IT2a, all38 individual word domains are nonempty. However, both distinct
operator words have exactly the same singleton domain: `chedy`. Only this raw
wholeword supplies the required occurrence-count profile across blocks of
9,8,7,6,5,4,3,2 equations. The two operators therefore cannot receive distinct
wordvalues. There are zero ordered distinct candidate pairs; this is an
injectivity contradiction, not an observed alternating-order mismatch.

RF1b and ZL3b each have11empty domains, including both operators and numeral
values0,2,3,4,5,6,7,8,9. CONSENSUS has one paragraph, below the mandatory eight;
the independent checker also finds all38domains empty. These are alternate
readings of one manuscript, not four independent replications.

The source is fully preserved: all44printed equations, eight blocks,220fields,
36realized numeral values, two literal operators and eight zero-product rows.
Two independent native readings agree cell for cell. No ×1 rows, commuted rows
or an unwritten81-cell table were added. Source arithmetic and the exact old
GDT893paragraph copy were independently checked. The complete model contract
and both necessary-domain programs were published in2a2bd8f5 before execution.
Primary/independent execution took0.37/0.17seconds; neither exhausted its budget.

This excludes the fixed44-equation table represented by one global word per
realized numeral/operator, shared decimal digit composition, global background
and eight complete target paragraphs. It supplies no Voynich numeral meaning
and does not exclude arithmetic generally. It does not identify dependence on
the later1478printed book. No heading constraint was used. No operator merger,
zero-row removal, source replacement or other automatic repair follows.

`artifacts/RESULT.json`, `INDEPENDENT_DOMAINS.json` and the compressed complete
domain certificates reproduce the exclusion. `src/validate.py` checks their
full domain parity and the exact IT2a singleton contradiction. No held text,
new Voynich image, positive meaning or translation was obtained.
