# GDT902 — complete multiplication register

See METHOD.md for the fixed70-minute decision and source/code/target contract.
The source contains44equations in8complete blocks; no unstated table cells.

Run `python3 experiments/yolo/gdt902_treviso_complete_multiplication_register/src/run.py`.
Validate `python3 experiments/yolo/gdt902_treviso_complete_multiplication_register/src/validate.py`.
Native-source receipts are checked independently by `src/validate_source.py`;
its optional native directory verifies the original cached reproduction bytes.
`src/selftest.py` preserves a full220-field known numeral-code witness and
compares operator feasibility against an exhaustive finite oracle.

The first runner applies complete necessary constraints. If any panel survives,
the unchanged complete decimal-code model must be implemented and solved within
the registered remaining budget before any numerical interpretation.
