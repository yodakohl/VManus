# GDT902 — complete multiplication register with shared decimal composition

Decision2026-09-10 02:57UTC. Total70-minute budget through04:07UTC includes
source collation, preparation, implementation, validation and publication.
Source/contract checkpoint03:17; maximum20minutes for complete fits/projections.
If a necessary complete-domain proof already excludes a panel, do not build or
run a larger solver for that panel. No repair, replacement source or extension
of the fixed limits follows any outcome.

Unknown: can a complete arithmetic system identify a global numeral code and
its target records jointly? GDT880 found too few immediate same-head additive
triples; no arithmetic score was run. NUMBER_CALENDAR_AND_COUNTING and the f69v
start/mansion routes do not provide a numeric key or authorial coordinate here.
GDT899's condition programs timed out. GDT901's full music network was excluded
even by counts/headings alone. None tests this multiplication/decimal-composition
law. This experiment supplies no inherited numeral, stem or word meaning.

Primary source: public Centro Morin facsimile of the Treviso1478 Larte de
labbacho, PDF page16 (zero-based15), all44 printed equations in eight visibly
separated blocks. Two independent native readings must agree on every cell.
Each block has a fixed first operand a=2..9, second operands a..9 followed by0,
and exactly five fields per row: a, fia, b, fa, product. The rowcounts are
9,8,7,6,5,4,3,2; total220fields. Preserve both literal operator words and all
eight terminal zero products. Do not add ×1 rows, reversed products or an
unwritten full9×9table. Printed decimal digits may be spaced within one aligned
numeric cell. Numeric transcription is independently checked against the digit
primer and exact products. This is a fixed operational projection of complete
printed blocks, not a claim about copying every surrounding word.

1478 is later than the usual Voynich dating. The hypothesis concerns the same
mathematical register, not dependence on this particular printed book. No claim
that this exact triangular layout occurred earlier is supplied by the source.
The public scan has no independently identified physical-copy provenance here;
the exact reproduction is byte-bound. Original zero/i-like glyphs are not EVA.

Target: only the unchanged GDT893 already-exposed odd wholeparagraph packet:
ZL3b14, IT2a259, RF1b11, CONSENSUS1. All eight sourceblocks must map injectively
to eight distinct wholeparagraphs within one panel. The complete sourcefield
order is fixed. Every realized numeral value and the two operator words has one
globally shared target wholeword value. These realized values are all distinct.
Every other raw wordtype is background everywhere. Removing background from
each selected wholeparagraph must reproduce the entire source block, not a
chosen subwindow. No per-occurrence deletion, fuzzy matching or source omission.

There is no raw-first-word heading condition: these source blocks have no
lexical headings; their first field is an operand of the first equation. The
source sequence must still be the entire foreground projection, including its
first and last fields. This is an explicit new arithmetic-register hypothesis,
not a relaxation or rerun of GDT901's named-pitch heading model.

Ten nonempty globally shared literal digit strings encode0..9 and form a
prefix-free code. Each realized whole numeral is the concatenation of its
ordinary decimal digit codes, without leading zeros. Digit1 is not a standalone
source cell and must not be assigned a fabricated wholeword occurrence. Its
finite possible strings are nonempty proper prefixes of candidate words for10
(the remaining suffix must be the code for0). The operator wordvalues are
independent wholewords, distinct from every realized numeral; they need not be
prefix-free with digit codes. All numeral cells and all multicharacter decimal
compositions must share the same digit strings. This is a representational
hypothesis, not an established phonetic interpretation of the transcription.

Complete inference is finite. First compute sound individual wholeword domains
from exact count histograms across all eight source blocks, including zero
counts, with no heading filter. Then enumerate every ordered possible fia/fa
pair: each complete targetparagraph's two-word projection must equal (fia,fa)
repeated n times, for each n=9..2 in some distinct paragraph. Different n values
already force these paragraph domains to be disjoint. No pair implies full
model UNSAT. Independent code must reproduce the necessary exclusion.

If a pair survives, solve all numeral/word/paragraph assignments and the exact
decimal code jointly. For every atom and every chosen paragraph, exact complete
counts apply. Encode every allowed literal digit concatenation, prefix-free
digit pair, and global injectivity. Replay full sequences and add only necessary
binary-projection forbidden tuples for invalid provisional assignments. Counts
plus every two-atom projection uniquely determine each complete word sequence.
Continue until a complete witness, UNSAT or the unchanged1200-second shared
construction/fit/projection limit per panel;20seconds outer cleanup, no restart.
At most three panels concurrently, two CPU workers each.

A complete positive witness requires independent full projection, arithmetic,
decimal composition, prefix-free and global-background replay. Query every
realized value, every digit string and each paragraph assignment against the
original complete model with only that one witness value forbidden. Other
variables remain free; necessary cuts alone may be shared between queries.
Each query cap30seconds within the same1200-second budget. SAT alternatives,
UNSAT fixed values and UNKNOWN are distinct. Block-size/source rigidity alone
never proves unique target embedding. No confirmed numeral, held score or
translation follows without independently fixed meaning validation. No heldbody,
new Voynich image, numeral-tail manipulation or f69v rotation is allowed.

Outcomes change the research decision: an independently replayed complete key
with fixed numeric values licenses a separately preregistered held arithmetic
prediction; multiple keys leave the model unidentified; complete exclusion
closes this precise table/decimal/global-background/scope conjunction; timeout
leaves it unresolved. No outcome triggers automatic decoder repair.
