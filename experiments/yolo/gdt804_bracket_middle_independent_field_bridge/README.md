# GDT804 — bracket-middle independent-field bridge

GDT804 follows the eleven complete middle surfaces from GDT803's twelve exact
brackets into independently rebuilt fields, preferred-side amount
neighbourhoods and right-value contexts.

The corrected outcome is narrower than the first working draft: 18/45 target
cells occur in fields that survive a common 155-form semantic mask, but the
effect is control-sensitive; none of fifteen preferred-side amount neighbours
is an already licensed GDT760 content contact; and `cheol` obtains no
specificity lead for
`dain/daiin` against its matched controls. `cheol` remains a material rival,
not a translated word.

Primary interpretation: `REPORT.md`
Method: `METHOD.md`
Transparent design record: `PREREGISTRATION.md`

Reproduce with:

```bash
python3 experiments/yolo/gdt804_bracket_middle_independent_field_bridge/src/run.py
python3 experiments/yolo/gdt804_bracket_middle_independent_field_bridge/src/validate.py
```
