# GDT804 transparent exploratory design record

This is not represented as a blind preregistration. The GDT803 brackets, their
eleven middle surfaces, the inherited German role candidates and the first
`chol/cheol/sheol` inspection were already visible. The purpose of this file
is to make the exploratory choices, corrections and falsifiers explicit.

Fixed mechanical choices:

- target all eleven GDT803 middle wholes; use all other members of the 155-form
  paired-`Xl` universe as controls;
- mask all 155 forms together before rebuilding GDT744 fields;
- match controls without outcome fields using total events, page breadth,
  style profile and line-end distance;
- primary seed 804804, 200,000 injective draws, K12 pools, retain the 5,000 best
  aggregate matches;
- sensitivity seed 804 with 100,000 K10 draws; leave-`ol`-out seed 805;
- mark and subtract the twelve discovery occurrences where relevant;
- distinguish raw ZL3b adjacency, token stability and all-reader contiguous
  sequence stability;
- keep every semantic display occurrence-bound and export no component.

Audit corrections incorporated before publication:

- preferred FIRST/MIDDLE side is a candidate neighbour, not a GDT760 content
  licence;
- the target cohort has zero prelicensed content contacts, not one;
- the 41 pairs are raw ZL3b strings; only 33 recur as contiguous sequences in
  all three reader lines and 32 pass the stricter two-token gate;
- `cheol` has only two external all-reader sequence repeats, one external
  token-stable repeat, and obtains no specificity lead against K12 controls;
- `chol/cheol/sheol` is post-hoc, so all eleven middle forms must be shown.

The semantic success ceiling is a ranked whole-form role candidate. No result
may establish a plaintext word, ingredient, property, unit or translation.
