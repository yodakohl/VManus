# GDT644 artifacts

The compact decision and replay set is:

- `TARGET_DECISION_DECK.tsv`: the five scoped readings, rivals and gates;
- `FORM_FAMILY_ATLAS.tsv`: all 44 observed comparison cells;
- `COMPONENT_BINDING_AUDIT.tsv`: 15 exact-surface-bound composition claims;
- `ALL_OCCURRENCE_SEMANTIC_AUDIT.tsv`: all 195 target positions;
- `READER_VARIANT_AUDIT.tsv`: all 13 non-exact target occurrences;
- `SEQUENTIAL_DECISION_LEDGER.tsv` and `ROUND_COVERAGE_COUNTS.tsv`: the five
  ordered promotions and their exact marginal effects;
- `NEWLY_COMPLETED_LINES.tsv`: the five new complete practical readings;
- `NEWLY_EXPOSED_ONE_HOLE_LINES.tsv`: the fifteen downstream holes;
- the V21 glossary, coverage, complete-passage, one-hole and working-dictionary
  editions;
- `RESULT.json` and `VALIDATION.json`: hash-bound result and independent replay.

The retained line coverage is needed to reproduce downstream completion and
replacement. No manuscript image is stored here.
