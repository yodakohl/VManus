# GDT685 artifacts

- `TARGET_540_STATE_CELL_CENSUS.tsv`: every exact `chol/shol/tol` position with
  reader agreement, neighbours and local context mode;
- `RELATED_57_VALUE_REALIZATIONS.tsv`: the complete direct, fused and separate
  target-value expressions;
- `TARGET_OL_CONTACTS.tsv`: eight genuinely free target/`ol` contacts and two
  reader-bound apparent contacts;
- `SURFACE_STATE_DISPATCH_SUMMARY.tsv`: one-row totals and final default for
  each target surface;
- `CONTEXT_MODE_SUMMARY.tsv`: exhaustive partition of the 540 positions;
- `COMPOSITION_EVIDENCE.tsv`, `HYPOTHESIS_COMPARISON.tsv` and
  `COUNTEREXAMPLE_AUDIT.tsv`: evidence, competing analyses and failure modes;
- `V58_51_LINE_READER.tsv`: complete updated 51-line reader;
- `V58_PATCHED_LINES.tsv`: the seven changed lines with before/after prose;
- `V58_TARGET_POSITION_DEBT_DELTA.tsv` and `V58_DEBT_SUMMARY.tsv`: exact
  position and aggregate debt changes;
- `GDT685_V58_STATE_CELL_READER.md`: compact human-readable result;
- `RESULT.json` and `VALIDATION.json`: machine-readable outcome and
  reproducibility checks.
