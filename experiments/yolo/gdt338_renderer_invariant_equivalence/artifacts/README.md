# Artifacts

`gdt338_design.json`, `gdt338_capacity.tsv`, and `gdt338_freeze.json` are the
pre-score method and target-capacity freeze. The remaining tables, result JSON,
and validation JSON were generated only after the frozen runner was published.
