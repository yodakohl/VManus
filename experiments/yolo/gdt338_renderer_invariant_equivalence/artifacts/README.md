# Artifacts

`gdt338_design.json`, `gdt338_capacity.tsv`, and `gdt338_freeze.json` are the
pre-score method and target-capacity freeze. Result tables are added only after
the frozen runner is published.
