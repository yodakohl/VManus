# GDT510 — Four cross-frame local factor bridges

Status: `CELESTIAL_PCH_HAS_LOCAL_SUFFIX__THREE_SCHD_TARGETS_HAVE_LOCAL_HEAD_ARGUMENT_RECTANGLES`

This pass revisits only the four GDT509 cards whose complete support previously
came from another register. Celestial `P+CH+E+Y` gains one exact contiguous
target-register suffix carrier. Celestial, Pharma and Source `S+CHD+Y` each gain
a local rectangle in which both action heads occur on `Y`; the directed pair
order is still retained from the old Biological carrier.

The four target recipes remain unobserved `COMPOSED_WORKING` defaults. See
`REPORT.md` for the readable result, `METHOD.md` for the bounded search, and
`artifacts/` for the complete cards and validator record.
