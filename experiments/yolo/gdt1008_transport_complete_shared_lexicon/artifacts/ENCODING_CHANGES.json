{
  "unified_diff": "--- experiments/yolo/gdt1002_transport_joint_alias_alternatives/src/solver.py\n+++ experiments/yolo/gdt1008_transport_complete_shared_lexicon/src/solver.py\n@@ -2,11 +2,13 @@\n import z3\n REQ=('INITIAL','GOAL','SAFETY','CAPACITY','CONCLUSION')\n def values(g,slot):return g['types'][slot[1:]] if slot.startswith('@') else [slot]\n-def solve(paragraphs,lex,g,timeout=20000,witness_limit=2):\n+def solve(paragraphs,lex,g,timeout=20000,witness_limit=2,family=\"FUNCTIONAL\"):\n     terminals=sorted(set(lex.values())|{v for p in g['patterns'].values() for t in p for v in values(g,t)})\n     num={v:i for i,v in enumerate(terminals)};unknown=sorted(set(w for p in paragraphs for w in p['words'])-lex.keys())\n     xs={w:z3.Int('x'+str(i)) for i,w in enumerate(unknown)};s=z3.Solver();s.set(timeout=timeout)\n     for x in xs.values():s.add(x>=0,x<len(terminals))\n+    assert family in (\"FUNCTIONAL\",\"BIJECTIVE\")\n+    if family==\"BIJECTIVE\":s.add(z3.Distinct([xs[w] if w in xs else z3.IntVal(num[lex[w]]) for w in sorted(set(paragraphs[0][\"words\"]))]))\n     edges=[];flags=[]\n     for pi,p in enumerate(paragraphs):\n         raw=p['words'];es=[];bs=[]\n@@ -20,7 +22,7 @@\n                 for j,w in enumerate(raw[lo:hi]):\n                     if w in xs:s.add(z3.Implies(b,z3.Or([xs[w]==num[v] for v in vs[j]])))\n         for pos in range(len(raw)):s.add(z3.Sum([z3.If(b,1,0) for b,(lo,hi,_) in zip(bs,es) if lo<=pos<hi])==1)\n-        for k in REQ:s.add(z3.Sum([z3.If(b,1,0) for b,(_,_,kind) in zip(bs,es) if k==kind])==1)\n+        for k in (g[\"patterns\"] if pi==0 else REQ):s.add(z3.Sum([z3.If(b,1,0) for b,(_,_,kind) in zip(bs,es) if k==kind])==(1+int(pi==0 and k==\"THEN\")))\n         edges.append(es);flags.append(bs)\n     witnesses=[];queries=[];exhaustive=False\n     while len(witnesses)<witness_limit:\n--- experiments/yolo/gdt1002_transport_joint_alias_alternatives/src/independent.py\n+++ experiments/yolo/gdt1008_transport_complete_shared_lexicon/src/independent.py\n@@ -3,7 +3,7 @@\n from cvc5 import pythonic as c\n import cvc5\n \n-def check(ps,lex,g,timeout=20000,blocked=None):\n+def check(ps,lex,g,timeout=20000,blocked=None,family=\"FUNCTIONAL\"):\n     alphabet=set(lex.values())\n     for pat in g['patterns'].values():\n         for t in pat:alphabet.update(g['types'][t[1:]] if t.startswith('@') else [t])\n@@ -13,6 +13,8 @@\n     vs={w:c.Int('a'+str(i)) for i,w in enumerate(sorted(words-lex.keys()))}\n     s=c.Solver();s.set('tlimit-per',timeout);edge_variables={}\n     for x in vs.values():s.add(x>=0,x<len(alphabet))\n+    assert family in (\"FUNCTIONAL\",\"BIJECTIVE\")\n+    if family==\"BIJECTIVE\":s.add(c.Distinct(*[vs[w] if w in vs else c.IntVal(n[lex[w]]) for w in sorted(set(ps[0][\"words\"]))]))\n     def total(xs):return sum(xs,c.IntVal(0))\n     for pi,p in enumerate(ps):\n         raw=p['words'];incoming=[[] for _ in range(len(raw)+1)];outgoing=[[] for _ in incoming];kinds={k:[] for k in g['patterns']}\n@@ -29,7 +31,7 @@\n                         s.add(c.Implies(b,choices[0] if len(choices)==1 else c.Or(*choices)))\n         s.add(total(outgoing[0])==1,total(incoming[-1])==1)\n         for pos in range(1,len(raw)):s.add(total(incoming[pos])==total(outgoing[pos]),total(incoming[pos])<=1)\n-        for k in ('INITIAL','GOAL','SAFETY','CAPACITY','CONCLUSION'):s.add(total(kinds[k])==1)\n+        for k in (g['patterns'] if pi==0 else ('INITIAL','GOAL','SAFETY','CAPACITY','CONCLUSION')):s.add(total(kinds[k])==(1+int(pi==0 and k=='THEN')))\n     if blocked:\n         disj=[vs[w]!=n[v] for w,v in blocked['aliases'].items()]\n         disj += [c.Not(edge_variables[(pi,node['start'],node['end'],node['kind'])]) for pi,parsed in enumerate(blocked['parses']) for node in parsed]\n"
}
