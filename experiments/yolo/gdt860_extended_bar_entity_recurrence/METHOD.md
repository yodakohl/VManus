# GDT860 — literal extended-entity recurrence discovery

GDT859 already documents raw @167; and @168; at f56r.1. This is a discovery
inventory for additional text locators, not a visual-link or cipher test.
Freeze before new target reading: exactly those two case-sensitive complete
entity strings in all three GDT851 safe179 caches, all kinds. These are literal
ENTITY MENTIONS, not established physical glyph occurrences. Preserve and
flag raw annotation characters []{}?!*<>:; co-presence may encode competing
alternatives, not two actual glyphs. Zero mentions in a reading may reflect
notation convention, not physical absence. The annotation-character flag is
a mechanical inventory, not a full interpretation of uncertainty syntax. Only raw group
strings are searched, never metadata/notes. Preserve uncertainty, punctuation,
entities and both separator fields; no normalization or p/f/t substitutions.

Regex @167;|@168; counts every occurrence, including multiple occurrences in
one group. @1670; and incomplete @167 do not match. Save zero-based raw Python
character offset (Unicode code points), source group ID/index, original raw
group/separators, reader, page, locus and all line metadata. Retain every
complete source line containing a hit. Save each line's ordered entity-hit
list in stored-group order then raw offset; co-occurrence means both literal
entities occur somewhere within that exact stored physical source line,
without assuming adjacency or pair ownership. No cross-line pairing.

Report every reader/entity count including zeros, distinct loci and physical
leaf keys fN (not r/v faces), all same-line co-occurrences and complete order.
Report hits/loci/leaf keys outside known f56r.1 separately. Alternate readers
are readings of one manuscript, never independent copies. Any outside hit
=> ADDITIONAL_TEXT_LOCATORS_ONLY; none => SOURCE_LOCAL_ONLY_NO_TRANSFER_PANEL.
Additional locators are not visual admission or evidence of physical upper
links. No image access, visual state, glyph identity, meaning or score.

Validate cached selectors against frozen179 and reject f84 prefixes. Independent
validator uses literal str.find scanning (not imported regex runner), verifies
upstream hashes, occurrence offsets/source records/ordered hit sequences and
full-source-line equality, then reconstructs counts and decision. Cached
runner --check verifies byte replay. Pre-data fixtures: exact-prefix rejection,
multiple entities, unmatched string and source-order preservation. Upstream
hashing is allowed before GO; do not parse source bodies until public GO.

Budget10min08:00:31–08:10:31UTC, including preparation, validation and root
publication. Stop after this census, no automatic imaging or widened search.
