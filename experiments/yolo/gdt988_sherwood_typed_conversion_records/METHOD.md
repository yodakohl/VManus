# Complete typed categorical conversion records

Freeze src/SOURCE.json. Two complete source-inspired proof records each consist
of original major, original minor, intended conclusion, major-conversion opcode,
converted major, minor-conversion opcode, converted minor, SWAP, FERIO, and
repeated conclusion. Exact propositions and class names are in SOURCE.json.
The serializer is our hypothesis; the historical prose does not attest this
exact execution order or paragraph boundary. Every stated premise, conversion,
argument and conclusion is retained, but surrounding textbook material is not
claimed to be the candidate plaintext. The two chosen full examples are the
complete candidate scope; other moods are not fitted or silently repaired.

A proposition has three typed atoms Q:quantifier, T:subject, T:predicate. Each
opcode has one OP atom. FUSED concatenates the three atom strings inside one
written group; each opcode is one group, exactly10groups. SEPARATE writes each
atom as its own group, exactly22groups. All atom codes are nonempty lowercase
strings, globally stable throughout both records, distinct within each of the
Q, T, OP types. Cross-type identical/prefix-overlapping strings are allowed:
the fixed typed record slots supply their role. A global prefix-free code is
not assumed. For a complete shared FUSED code, every one of4*4*4quantifier/term/
term propositions, including reflexive arguments, must have a distinct fused
string. A local code must meet this condition for its own observed alphabet.
Thus variable-length proposition parts must decode without a lexical collision;
slot context alone does not erase ambiguous proposition segmentation.

## Semantic contract before surface fitting

A/E/I/O denote subset/disjoint/nonempty-intersection/nonempty-difference.
Each class is stipulated nonempty; there is no extra written existence guard.
P converts A(S,P) to I(P,S); S exchanges E/I arguments; SWAP exchanges premises;
FERIO applies E(M,P), I(S,M) => O(S,P). Check the source traces and all256Venn-cell
occupancy models on three classes, restricted to nonempty classes. Keep the
empty-class counterexample to unrestricted P, the false converse of A, and an
invalid figure/argument rival. These are formal checks of our assumptions, not
meaning evidence or an empirical full-search control.

## Target accounting and access

Use only GDT928's frozen PARAGRAPHS.json, reconstructed in that predecessor
from guarded915caches using original P-start/P-end flags and consecutive loci.
Include all659ZL3b and690IT2a paragraphs (RF1b supplies zero); every complete
paragraph is tested against each of2traces and2writers. No target passage or
internal window is newly selected. All data were exposed previously. Physical
leaf is the stored leaf number; ZL/IT/RF are readings of one manuscript.

A case with any non-anchor-eligible source line is UNKNOWN_SOURCE before count
checks; retain all raw groups and source IDs in the input reference. Every other
case must preserve all groups, characters and seams. Report count mismatches,
repeated-conclusion or opcode conflicts, exhausted finite equations, local
codes, and computational unknowns separately. No normalization, uncertainty
repair, free literal atom, reordering, header, filler, zero-length code, or
unwritten event. For SEPARATE bind22whole groups directly. For FUSED enumerate
all nonempty three-part splits of the six proposition groups, unify typed
codes across the record, and directly check the legal proposition dictionary.
Necessary exact-repeat and within-type inequalities may reject before splitting.
Finite limits are100000nodes/2seconds/10000complete local assignments per case;
a cutoff leaves completion UNKNOWN even if a witness was found. No timeout is
an exhaustion. Fixed maximum1000000joint local-code pairs; leftovers UNKNOWN.

Joint success requires a FAPESMO paragraph and a FRISESOMORUM paragraph on
different physical leaves within the same reading and writer, identical codes
for shared atoms, full within-type distinctness and64unique fused propositions.
Retain every local candidate; none is upgraded for restart agreement. If a joint
code exists, output its12typed assignments, both complete alignments, equivalent
codes and alternative pairings. No available case is designated independent
confirmation: all participating leaves are exposed and used for selection.
There are zero independent meaning-confirmation leaves in this test.

## Validation, interpretation and stopping

Before target access, run planted full12-atom positive codes for both writers,
context-overlap cases, changed-conclusion and changed-opcode negatives, and a
small finite brute-force comparison. A separate validator, importing neither
runner nor matcher, rechecks source semantics, accounting and every case with
a differently ordered finite splitter. It verifies all witnesses and shared
pairs independently of main implementation; same author, no outside reviewer.
No suitable control of the entire historical-content search exists, so no
p-value or significance. An exact surviving reading would still leave class
renaming, semantic-domain and source/serializer alternatives. No confirmed
plant name or word. Failure closes only these two exact record hypotheses.
The inclusive03:30UTC checkpoint forbids automatic serializer repair or
unbounded control work. Register source, contract and executable hashes before
opening target cases for this fit; retain prior exposure explicitly.
