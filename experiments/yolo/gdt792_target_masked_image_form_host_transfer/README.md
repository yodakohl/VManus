# GDT792 — Target-masked image/form host transfer

Status: `PARTIAL`; validation `PASS` (257 checks and two byte-identical
builder replays).

Across the same 30 released pages, GDT792 tests `otedy`, `okal`, `otchdy` and
`olaiin` without feeding inherited glosses into their host classification.
The useful result is structural: `okal` is the strongest complete whole in
this batch that repeatedly crosses singleton-label and prose scopes.

The preregistered observable trigger for `okal = Ziel-/Bezugsstelle` fires,
but that gloss is not selected. The contributing all-30 rank uses held labels,
the two control decks share seven of eight controls, all four reuse edges have
zero semantic credit, and the observations do not distinguish address, class
or addressed-member readings. The executable output is therefore the tag
`CROSS_SCOPE_LABEL_PROSE_WHOLE`, not a plaintext word.

See [REPORT.md](REPORT.md), [METHOD.md](METHOD.md), and
[artifacts/README.md](artifacts/README.md).
