# GDT845: fixed72-cell source-native discovery

Question: does the known bare ee+d sparsity recur under o and qo, after
conditioning expected counts on wrapper, k/t and ch/sh? GDT624 records48e0/1
cells,646 extends bare to24cells with three gaps and one reader warning.
Root additionally checked643/717: repairs of particular working word cards,
not a joint72-cell count/exposure analysis. Old semantic slots are not imported.

Before new source query freeze exactly {empty,o,qo}×{k,t}×{ch,sh}×e{0,1,2}×
d{0,1}×y. Use all raw complete groups in179admitted selectors, all source kinds,
ZL3b primary and IT2a/RF1b alternate readings. Exact whole-string matching only;
no removing braces/entities or altering separators. No new image, selector,
semantic edge, phonetic unit or role label. f84/f84r explicitly sealed.

Retain all72cells including zeros and every matching source row with native
metadata. Per-cell counts and folios are separate per reading; never pool
readers as independent witnesses. Shared locus multiplicity is min over the
three word counts at that locus, NOT exact cross-reader token alignment.
Within each wrapper/head/middle stratum, descriptive expected e2+d count is
N(e2)*N(d1)/N(all). Sum four such strata per wrapper; no fitting, p value,
threshold selection or claim that independence is an adequate language model.
Also report page exposure per cell. Pattern selection is informed by known
bare-arm gaps. This discovery has no untouched confirmation set.

If the extended arms show the same underrepresentation with appreciable
expected counts, nominate a conditional combination constraint for later
confirmation. Small expectations or mixed directions retain a sparse/unresolved
interpretation. Any existing exceptions must be shown; no universal prohibition
from absent cells. No new ending, larger e range or subgroup retuning.
Target5min total from scaffold to publication; checkpoint at first extraction.
Source fidelity and independent aggregation checks validate counting only.
