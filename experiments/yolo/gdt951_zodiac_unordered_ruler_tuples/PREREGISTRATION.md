# GDT951 preregistration

The authoritative complete protocol is METHOD.md. Before target intake,
METHOD.md, SPEC.json, the selected historical source rules/table and all
source predictions are bound in PREREG_LOCK.json. The lock's UTC timestamp
records the freeze. Source-only native inspection and correction are disclosed
in src/SOURCE_REVIEW.md; no target contents were used to select those values.

No new manuscript access is admitted. f84/f84r remain forbidden. All working
zodiac pages are project-exposed, and no reserve is being used for confirmation.
