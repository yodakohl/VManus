# GDT528 compact d-null working book

## Live rule

For a target `BASE-d-y`, allow the exact recipe of old `BASE-y` only when an
old exact equal-recipe `...y/...dy` pair is one visible substitution away and,
after removing `CARRIER_Q`, the two base recipes differ in exactly one known
action root. Exact old target conflicts win. Feature `log1p(9)`, weight `1.15`,
after GDT527. This is not global `d→NULL` or `q→NULL`.

## New complete current reading

```text
qocthedy = CARRIER_Q+O+CH+T+E+Y
```

Certificate:

```text
qocthey  ~ qockhey   surface: t>k
T        ~ K          recipe action: T>K
qockhey  = qockhedy   exact old terminal-d null pair
```

Working phrase: „Unter q-Träger/Ausführung: nehmen, auf Grad I einstellen und
posten.“ The visible `d` adds no atom in this bounded variant only.

## Scorecard

- old four-fold unchanged: `1098/1328/1386/1418`, rank sum `2109`;
- inherited current: top1 `148→149`, rank sum `179→177`;
- revised current: top1 `150→151`, rank sum `177→175`;
- current losses: zero;
- naive visible-only neighbour at the same weight is rejected: old top2
  `1328→1327`, rank sum `2109→2110`.

## Remaining queue

`aiicthy`, `chekchy`, `cthom`, `dairykodas`, `dalcheeeky`, `dsholdaiir`,
`qef`, `saiis`.

Next inspect `cthom`: compare the page-local `M_LOCAL` ending with the globally
favoured `AM_ADDR` parse through old `...om/...am` surface and recipe families.
Open no new page.
