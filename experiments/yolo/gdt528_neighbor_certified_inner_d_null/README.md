# GDT528 — Action-neighbour-certified terminal-`d` null variant

Status: `PASS_NEIGHBOR_CERTIFIED_TERMINAL_D_NULL`

GDT528 closes `qocthedy` as the already known `qocthey` recipe plus a bounded
visible `d` variant. The transfer is licensed by old `qockhey/qockhedy` and by
the matching `T↔K` action-root substitution. It does not define global
`d→NULL` or `q→NULL` rules.

See `REPORT.md`, `METHOD.md`, the compact working book under `artifacts/`, and
the executable scripts under `src/`. No new page is used.
