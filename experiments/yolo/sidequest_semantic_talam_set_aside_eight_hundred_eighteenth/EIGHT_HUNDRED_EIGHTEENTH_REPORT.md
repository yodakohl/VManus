# Sidequest Pass 818: talam means “beiseitestellen”

The single `talam` closes the short f55v sequence:

> `daiin chedy talam`
> SOLLMASS · UMSETZEN · BEISEITESTELLEN

It follows a prescribed measure and transfer of the active item. The next
statement immediately starts a new addition/extraction operation under the same
pictured plant. Long-term VERWAHREN therefore overstates the time horizon.

The selected short whole-card value is:

> `talam = BEISEITESTELLEN`

The full sentence now reads:

> Bei der breitblaettrigen rispigen Pflanze: Nach Sollmass den laufenden Posten
> umsetzen und anschliessend beiseitestellen.

AUFBEWAHREN has the same long-term problem. IN DAS GEFAESS invents an unseen
container. RUHEN duplicates the established SHED family. A pure BEENDEN value
duplicates the attached closure behavior.

The tempting visual split `T+AL+AM` is not used. T and AL have established
working values, but AM has no independent entry anywhere in the fixed-page
dictionary; the exact `talam` card also belongs to its own learned closed type.
It remains a memorized whole operation.

Next, integrate OS=DAZU, RESUME=DAVON, and TALAM=BEISEITESTELLEN into a sixth
complete 173-card / 381-event / 116-statement grammar and print the remaining
exception inventory explicitly.
