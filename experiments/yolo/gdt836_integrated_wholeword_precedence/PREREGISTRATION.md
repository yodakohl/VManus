# GDT836 source prerequisites and engineering scope

The Questio1–44/45–88 source split was recorded in GDT835 before support counts.
The exact inherited gates are in ENCODER_SPEC.json. Their first execution stops
for two held-active literal rules absent from discovery; its byte-preserved
snapshot is INITIAL_CAPACITY.json. No public preregistration before that
source-only count is claimed. No key or historical fit registration occurred.

After that STOP, the already requested decoder change was implemented and
validated on invented fixtures. ENGINE_SPEC.json and METHOD.md describe the
code's behavior and boundaries. No historical comparative outcome, threshold
adjustment, alternate partition, key selection or repaired predecessor score
is produced. Publication includes the stopped source and engineering artifacts
together so implementation success cannot be mistaken for control recovery.
