# GDT782 preregistration — recurrent-six target-external field adjudication

Status: `REGISTERED_UNSCORED`.

Registered: 2026-09-03.

## Question

When all occurrences of GDT781's six recurrent new complete wholes are masked
as targets, do their other eight reader-exact occurrences support the current
working default, one of its rivals, or a shorter field-compatible revision?

This is an exploratory meaning pass. Every form keeps a concrete replaceable
default. A weak or conflicting outside field lowers confidence or changes the
card; it does not erase the form into generic process prose.

## Fixed cohort and boundary

The cohort is exactly `cheedaiin`, `chedor`, `chockhar`, `keeor`, `shdair` and
`sheckhal`. GDT781 contains one selected `ol + whole` target span for each.
The admitted guarded cache must reconstruct fourteen reader-exact occurrences:
six selected target occurrences and eight target-external occurrences. The
external split must be 3/1/1/1/1/1 by those six surfaces.

No new page, image, OCR or transcription is opened. The inherited 179-page
cache boundary is a computational allow-list, not a claim that those pages were
visually interpreted. `f84` and `f84r` remain sealed.

## Masking and evidence layers

At each of the eight external occurrences the centre token is masked. Any
other cohort target within radius three would be reported as leakage and could
not become a donor. Selection uses complete surfaces only; no EVA character,
prefix, suffix or substring receives meaning.

Evidence is kept in four visibly different layers:

1. reader-exact complete wholes in GDT781's reconstructed 770-reading,
   769-surface clean W2/W3 pool;
2. exact registered multi-token constructions already published by GDT759;
3. lower-grade whole-card hypotheses, useful only as soft exploratory context;
4. nonexact, unknown, source-composed or retired literal
   powder/seed/root/wood rows, which remain visible counterevidence but cannot
   vote as clean donors.

The fourth layer is enforced against GDT754's later provenance sieve, not only
against the older GDT734 fields. A source-composed whole that still appears in
the reconstructed historical pool may be displayed only through its later
sanitized whole-form hypothesis. Later whole-form display corrections such as
GDT768's `cthy=Blattgut` replace stale wording without changing field axes.

The only scored radius-three cross-cohort analogy-source contact, `cheeor`
beside external `cheedaiin`, must be flagged. It is an older clean whole card
and not an analogy source for `cheedaiin` itself, so it remains visible; a
sensitivity column removes it. The full line also contains `cheor` at radius
four beside `sheckhal`; it must be flagged separately and receives no vote.

## Exploratory adjudication policy

`src/TARGET_6_CANDIDATES.tsv` retains all eighteen GDT781 proposals and adds
five field-compatible refinements where an adjacent quantity field would
otherwise be spoken twice, every old rival asserts a contradicted quality, or
the quality may belong to the surrounding field instead of the target. Three different readings are recorded in
`src/MANUAL_READER_ASSESSMENTS.tsv`: local field grammar, historical
apothecary record practice, and a deliberately surface-first external census.
Their agreements and disagreements are a working synthesis, not a statistical
proof.

The preferred card may be revised when the external field does one or more of
the following:

- repeats an independently registered amount construction immediately before
  a target, making an amount-bearing target gloss redundant;
- supplies a coherent complete-whole quality/material field opposed to the
  old card;
- places the target as a nominal patient or content head before process/state
  fields;
- favors an endpoint over a midpoint across the recurrent contexts.

Historical sources contribute only the architecture that descriptive
quality/material/degree fields and prescriptive amount/ingredient fields can
coexist. They do not identify a Voynich word.

## Expected practical output

The runner must publish the full 14-occurrence census, every external neighbor,
eight field summaries, candidate scorecards, all three readings, six final
cards, a six-row target-patch atlas, an eight-line external working reader and
the complete 376-row renderer. The external reader is an aggregate card audit,
not a licence at the eight outside positions. All six selected GDT781 spans
remain contextual and consumed exactly once; only their displayed whole-form
default may change.

## Claim ceiling

All German expressions are replaceable complete-whole renderer hypotheses.
GDT782 confirms no lexeme, plaintext, language, sound, glyph value, numeric
identity, unit, specific substance, plant, disease, cure, person, vessel or
historical spelling. It exports no component value.
