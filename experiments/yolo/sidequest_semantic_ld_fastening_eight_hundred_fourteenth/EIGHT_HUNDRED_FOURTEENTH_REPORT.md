# Sidequest Pass 814: LD is bound fastening

The only `LD` occurrence is the complete one-card statement
`qokylddy = OK+Y+LD+DY` at the visibly bow-connected B4 figure pair.

The existing reading is already the simplest:

> **Den laufenden Posten ansetzen und befestigen; den Schritt schließen.**

`BINDEN` would require cord or band; `VERSCHLIESSEN` and `DOPPELSCHLUSS`
duplicate the licensed `DY` closure; `ANLEGEN` overlaps `OK=ANSETZEN`.
Therefore:

> `LD = BEFESTIGEN`, bound immediately before `DY` in this learned frame.

It is not promoted to a free core. The most obvious shortened surface `ldy`
already belongs to the distinct recipe `L+DY = LEITEN · SCHLUSS`. Four other
extensions are unattested and are withheld rather than given invented forms.

The local-singleton class is now empty. The component inventory has 33
productive cores, 3 bound values (`AN`, `DA`, `LD`), and 3 whole commands.

Next, publish a fifth full grammar. Then attack the three memorized whole
commands separately: `os`, `dchol/schol`, and `talam`.
