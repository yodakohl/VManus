# GDT772 artifacts

- `EXPANDED_22_LINE_COHORT.tsv`: complete 186-cell score input; all 27 target
  roles and defaults are withheld.
- `TARGET_27_OCCURRENCE_INVENTORY.tsv`: direct target-independent roles and
  NULL orphans for every mask.
- `CANDIDATE_OCCURRENCE_SCOREBOARD.tsv`: all 115 candidate/occurrence scores.
- `TARGET_POLICY_SCOREBOARD.tsv`, `LEAVE_ONE_PAGE_OUT.tsv`,
  `BRANCH_COVERAGE.tsv`, `WINNER_GATE_AUDIT.tsv`, and
  `TARGET_DECISIONS.tsv`: unchanged GDT770 scoring and decision layers.
- `OL_POSITIONAL_VS_NOMINAL_CASES.tsv`: fifteen-case decomposition of the
  exact 56:56 tie.
- `GDT770_GDT772_SCORE_DELTA.tsv`: old/new score change for all 18 policies.
- `GDT772_7_NEW_LINE_READER.tsv`: four parallel display renderings for the
  seven added lines; it contributes zero score.
- `OL_MANUAL_RECIPE_READING.tsv`: practical recipe-style reading audit of the
  seven full branches; it contributes zero score.
- `GDT772_4_WORKING_DICTIONARY.tsv`: four replaceable whole-form defaults with
  evidence and counterevidence.
- `RESULT.json`: machine-readable result and source locks.
- `VALIDATION.json`: independent score reconstruction, mutation checks,
  guarded-source audit, and fourteen-output byte replay.
