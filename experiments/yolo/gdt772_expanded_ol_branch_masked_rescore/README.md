# GDT772 — expanded `ol` branch masked rescore

Status: `PASS` — no policy winner; `ol` positional and nominal models tie 56:56.

GDT772 adds the seven complete `ol` left-value/right-field bridges found by
GDT771 to the unchanged GDT770 tournament.  Every exact `ol`, `ckhy`, `ols`
and `otar` on an admitted line is still masked at the same time.  Three extra
`ol` occurrences carried by the seven new lines therefore enter as automatic
directional controls rather than being discarded.

Run:

```bash
python3 experiments/yolo/gdt772_expanded_ol_branch_masked_rescore/src/run.py
python3 experiments/yolo/gdt772_expanded_ol_branch_masked_rescore/src/validate.py
```

See `PREREGISTRATION.md`, `METHOD.md`, and `REPORT.md`.
