# GDT977: add shared LEAF and BROAD to every GDT976 candidate

Phase: fixed test on previously exposed data. Source, aliases, transcription,
target boundaries and the full GDT963 hypothesis remain unchanged. This is a
new necessary projection, not a new decoder or a repair of GDT963/GDT976.

## Decision and inclusive budget

Start 2026-09-19 17:30 UTC; preparation, implementation, execution, validation
and publication checkpoint 18:05 UTC (35 minutes). No automatic further model,
control corpus or source extension. At that checkpoint stop scientific expansion
and record any unresolved computation or closure overrun.

GDT963 left the full four-record code unresolved. GDT976 completely enumerated
8,990 name-code/page rows in 1,320 ordered name-code classes, binding only six
of 613 source occurrences. The genuinely unknown question is whether each of
those rows can also share one LEAF value and one BROAD value in the unchanged
four source streams. This adds seven occurrences and makes I.3 participate in
a shared content constraint. It does not require a new source or confirmed gloss.

Read primary GDT963 METHOD/REPORT and GDT976 METHOD/REPORT, the live structural
baseline, assessed GDT963/GDT976 cards, bounded duplicate and route screens.
IDEA357's unfrozen joint constituent grammar is not being tested. Image-based
plant identity, common drawing scale and pharma label-owner capacity do not
enter this prose projection; their stops remain unchanged. No old name gloss,
direct Latin/Greek-letter hypothesis, morphological parser or header is inherited.

If a base row has a valid four-value witness, retain it as a partial hypothesis.
If the exact finite search exhausts, exclude only that base row in this literal
model. A timeout is UNKNOWN, never exclusion. If many rows remain, this step
does not select a word and does not license a full-solver restart. If few remain,
they still need coherent additional content under the same source/channel.
Even all rows failing would not refute nonliteral source-unknown pages, other
writing hypotheses, plant content generally, or the manuscript's language.

## Frozen data and evaluation

Use only GDT963 SOURCE.json/ALIASES.json and GDT976 DOMAINS.json,
CANDIDATES.json.gz, RESULT.json and the corresponding reports/methods. The
GDT976 candidate list in its saved order defines all base IDs 0..8989. Every
one receives a result; no ranking, favorable page selection or discarded case.
No mixed raw TSV, image, new target admission, f84/f84r, f116v or reserve is read.
All targets were already exposed, including to this project and root. No
independent confirmation leaf exists; no new reviewer creates blindness.

The fixed codes IRIS/XIPHION and fixed I.1/IV.20 pages come from each base row.
Search nonempty LEAF/BROAD strings, pairwise prefix-incomparable with each
other and both fixed values, and any I.2/I.3 pages in the original domains on
four distinct physical leaves. All omitted atom occurrences remain arbitrary
nonempty strings as in the previous necessary relaxation. The new positions
are derived solely from the frozen source, not chosen from target matches.
Old earliest name positions are only witnesses and MUST NOT be frozen here.

The question is existential for each old row, not an enumeration of every new
LEAF/BROAD code pair. The first deterministic witness proves existence only;
it is not the preferred reading, a unique code or a probability estimate. Save
all Acorus/Meum page supports for that witness and their exact factored distinct-
leaf completion count. All other possible code witnesses remain unenumerated
and explicitly ambiguous. Do not rank meanings by witness frequency or restart
agreement. Additional matching substrings are allowed; no absence/count claim.

Use at most 32 workers. Each case gets 2 CPU seconds, all execution 300 wall
seconds. Unfinished cases remain UNKNOWN_CASE_CPU/UNKNOWN_GLOBAL_WALL.
No target-informed limit change or retry. Independent validation checks every
positive witness, all negative exhaustion certificates, input coverage and
source-derived gaps. Synthetic fixtures validate mathematics/code only, not
search significance. No whole-search null or independent meaning test is owned;
claim neither significance, a scored GDT388 packet, nor translated plant names.

## Assumptions and dependencies

The 963 copied-content order and aliases, nonempty prefix-free common code,
literal whole-page representation and physical-leaf identity are hypotheses.
Only thirteen source occurrences are constrained here; the other six hundred
are retained as obligations of the full model, not claimed read or discarded.
Repeated structures do not themselves identify these meanings. ZL3b/IT2a/RF1b
are alternate readings of one manuscript; their inherited capacity/source
unknowns stay explicit. GDT963's original UNKNOWN and GDT976's exact partial
candidate counts remain byte-frozen.

Before registration, 27 synthetic four-page problems compare the prefix search
against exhaustive common-substring code pairs using a separate regex matcher.
These fixtures include witnesses requiring both new values to share an initial
character and diverge later; they use no manuscript strings. Search controls
check correctness only, not a semantic null or a probability of decipherment.

CASE_PREDICTIONS.tsv writes every old candidate and its four complete projection
patterns before fitting. Its fixed names/pages come from the already public
GDT976 candidate file; no target text is opened to make these predictions.
The LEAF/BROAD placeholders must have the same values across all four patterns
and satisfy the common prefix-free condition. All original I.2/I.3 domains
remain eligible subject to the existing distinct-leaf rule.

Before public registration and any target fitting, the execution wall cap was
set to 300 seconds to leave time within the inclusive checkpoint for certificate
validation and publication. The per-case cap remains 2 CPU seconds.

Continuation decision: if at least 90% of all old rows have a checked witness,
classify this added content as insufficient discrimination for this candidate
pool and park serial one-atom projection additions. Fewer than 90% is not a
significance threshold: report all survivors and unknowns, and only assess a
new content consequence in a separately justified bounded work unit. No outcome
alone selects a name or authorizes reserve use. This decision threshold is set
before the new target fit and does not change any mathematical case status.

The certificate validator uses a separate regex/occurrence implementation and
independent leaf-count aggregation. Root reviewed and corrected its draft before
registration; this is a calculation cross-check, not blinded replication. All
27 synthetic search outcomes also have their certificates independently checked,
including a negative case requiring a prefix-extension branch.
