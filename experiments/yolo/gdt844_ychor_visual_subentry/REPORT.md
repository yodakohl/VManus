# GDT844 — Two native ychor line observations

**NO_COMMON_VISIBLE_SUBENTRY_CUE_IN_TWO_TARGETS.** Root directly viewed the
complete original Yale images of f6v and f9v, without OCR, masking or enhancement.
Both selected ychor loci appear within ordinary running text: neither has a
conspicuous left indent or enlarged gap above it. The clearly larger paragraph
gaps occur elsewhere: before line6 on f6v and before line5 on f9v.

| Page | Target | Approx. gap above/below | Approx. left shift versus neighbour mean |
|---|---:|---:|---:|
| f6v | 8 |22 /20 |−0.5 |
| f9v |11 |19 /22 |−1 |

Units are full-image coordinates normalized to1000×1000, estimated directly
from the displayed native images. They are descriptive annotations with errors
of several units, not calibrated pixel measurements; the tiny left differences
have no evidential meaning. All21 f6v and12 f9v principal text lines are retained
in OBSERVATIONS.json, including non-target paragraph starts. Baseline position
is estimated at the left text region because lines slope and wrap around plants.
Target matches use top-to-bottom principal line counts and the visible initial
groups; this is not an independently validated diplomatic transcription.

This is a new layout observation on two deliberately newly admitted pages.
It does not repeat the already known13/13 line-initial statistic. The specific
proposal that these two instances provide a conspicuous independent graphic
subentry cue gets no support. Stop this small visual lead; no automatic third
page, alternate feature search or claim that ychor cannot be an unmarked
continuation expression. Item, Recipe and all other proposed translations remain
unconfirmed. Plant pictures are not identified botanically.

Selection followed the first two GDT756 target loci in folio order. Root knew
the text positions, so this is exploratory and unblinded. Protocol and two page
admissions were committed as c6e2605d before acquisition; no unseen confirmation
is claimed. SOURCE URLs, exact Yale canvas labels and SHA-256 hashes are retained
in SOURCES.json. f6v is canvas1006087; f9v is1006093. No sealed folio was opened.
The discretionary balance is now14, visual scope36keys/41selectors.

Validator checks all33 annotation rows, monotone line order, target arithmetic
and locally cached image hashes. It cannot validate the visual judgments or
meaning. Binding and exact staged publication checks are separate. Global
repository audit retains the pre-existing GDT600/index debt.
