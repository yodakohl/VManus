# GDT922 — fixed adjacent same-remainder CH/SH order

Decision note registered2026-09-11 before this contrast is counted. Budget15min total including implementation, validation and publication. Source unchanged179selector GDT915 caches, all already exposed in this project; no fresh confirmation or image admission. f84/f84r sealed. No semantic meaning assigned.

Question: outside known chor/shor and chol/shol examples, is written chR/shR order asymmetric beyond fixed wordform/leaf/physical-position margins? GDT872 uses disjoint fouranchor/eighteenresponse wholes at distal3–8 (near1–2 descriptive), excluding same-pair endpoints; GDT873 stops its matched-position branch. GDT631 tests ch/sh+cth paradigms, not this same-R ordering. IL008's inherited record reports generic pair-identity-free shape nonconfirmation; primary file not located. GDT915/916 concern r/l suffix co-variation and novel stem combinations; they remain unchanged and closed. Success supports only ordered whole-form variants, failure stops this fixed directional bridge, capacityfailure is not absence of a relation.

Eligible endpoint: complete raw [a-z]+ group in kindP, beginning exactly ch or sh followed by a nonempty literal remainder R, left seam LINE_START/DEFINITE_SPACE and right LINE_END/DEFINITE_SPACE. Preserve all raw indices and original source_group_count. Position is SINGLE if count1, FIRST if index1, LAST if index=count, INTERNAL otherwise. Pair: two adjacent original group indices on the same stored line, both eligible and exact identical R. Count all four cells CC,CS,SC,SS; repeated complete words remain zero contributions to signed order. Primary excludes exactly R=or and R=ol, disclosed motivating examples. Display their complete census separately; no other exclusion or candidate selection.

Physical leaf is numeric f identifier, both faces/panels combined. T is the equal-leaf mean of (CS−SC)/all same-R pair opportunities on each leaf with >=1 primary opportunity. If x(token)=1 forCH,0forSH, each pair contribution equals x(left)−x(right); consecutive runs therefore telescope to endpoint differences. This identity is disclosed: the test is written ordering, not independent evidence from every overlapping pair.

Conditional null: permute CH/SH labels among ALL eligible tokens within (physicalleaf, exactR, positionclass), including tokens with no same-R neighbour. Sort strata lexically, positions by(page,numeric source_row_index,numeric groupindex). No raw word remainder, line position or pair opportunity changes. Exact expected pair difference is pCH(leftstratum)−pCH(rightstratum); this holds also if both endpoints share one stratum. mu is the same leaf-mean aggregation of those differences. In each1024worlds, Pythonrandom.Random(922000+j),j0..1023, shuffle the current label list independently within every sortedstratum. Recompute all outcomes on the fixed opportunity graph. Primary statistic is abs(T−mu), so neither orientation is privileged. Count ties within1e-12 against observed; rank=(1+worlds>=observed)/1025. The reference accounts for both directions only, not the projectwide search. No significance claim.

Capacity requires >=5 distinct primary R and >=5 leaves with at least one primary endpoint whose stratum contains bothprefixes and >=2tokens. Also report nonconstant world range; constant null is a capacity stop. Provisional written ordering requires capacity, nonconstant null, rank<=.05 and nonzeroresidual; descriptive IT/RF independently repeat census and expectations, not ZLselection or independentreplication. Print every R includingzero cells, exact witnesses, allleafcells/expectations, allworlds, counters and capacities. No later remainder subset, regex, lag, alternate reading or threshold repair.

Independent validator reconstructs every source token, opportunity, stratum, mean and all1024worlds without importing primaryrun. No relationpacket/GDT388 semantic score, normalization, productive morpheme or translation follows. If positive, phase/grammar/phrase-reuse/othermechanisms remain unresolved.
