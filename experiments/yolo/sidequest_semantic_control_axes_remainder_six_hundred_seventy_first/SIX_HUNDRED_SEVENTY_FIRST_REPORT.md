# Sidequest pass 671: five control axes and three whole commands

The apparent six-card remainder is smaller than it looked. Three cards compose
from common control axes:

- T = **EINTRAGEN**;
- CH = **ABNEHMEN**;
- O = **ARBEITSGANG**;
- DY = **SCHLUSS**, but only when the exact card construction licenses it;
- S = **TEILEN**.

Thus `tchody` is “enter—remove—work-cycle; close”, `ody` is “work-cycle;
close”, and `ches` is “remove briefly and divide”. These axes occur in 67
cards/122 events overall, so they are not patches invented for the remainder.
All 89 DY-component events are final or complete one-card statements.

Only three commands must remain memorized as whole cards:

| Card | Value | Events |
|---|---|---:|
| `os` | ARBEITSFACH | 1 |
| `dchol/schol` | WIEDERAUFNEHMEN | 2 |
| `talam` | VERWAHREN | 1 |

This is a plausible workshop balance: a small productive grammar plus three
short nomenclator commands. The active prose deck is now covered at 173/173
cards and 381/381 events without leaving a word meaningless.

The next useful step is editorial rather than another root hunt: publish one
integrated card dictionary that shows the exact recipe, atomic contributions,
short fluent default, occurrences, and whether a card is compositional or one
of the three memorized commands.
