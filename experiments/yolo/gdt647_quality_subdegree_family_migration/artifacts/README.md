# Artifacts

- `FAMILY_MIGRATION_DECK.tsv`: all 107 observed whole surfaces, parses,
  old/new meanings, reader tiers and occurrence counts.
- `NULL_CELL_HOLDS.tsv`: the 13 generated but absent cells; none enters V24.
- `AXIS_SCOPE_BOUNDARY.tsv`: three included whole-surface families and five
  explicit exclusion classes.
- `ALL_OCCURRENCE_SEMANTIC_AUDIT.tsv` and `READER_VARIANT_AUDIT.tsv`: all
  5,664 positions and the non-exact-reader subset.
- `MANUAL_PASSAGE_REALITY_CHECK.tsv`: fifteen strong, mixed and adverse
  line-level readings with local scope compression kept separate from the
  tokenwise edition.
- `AFFECTED_PASSAGES.tsv`, `NEWLY_COMPLETED_LINES.tsv`, and
  `NEWLY_EXPOSED_ONE_HOLE_LINES.tsv`: V23-to-V24 passage changes.
- `V24_EXACT_TOKEN_GLOSSARY.tsv`, `WORKING_DICTIONARY_V24.tsv`, and the three
  V24 line editions: the complete updated working reader.
- `ROUND_COVERAGE_COUNTS.tsv`, `RESULT.json`, and `VALIDATION.json`: aggregate,
  hash-bound result and independent replay.
