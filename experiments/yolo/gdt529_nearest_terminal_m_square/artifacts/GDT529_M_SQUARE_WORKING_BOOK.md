# GDT529 compact terminal-m square working book

## Live rule

For a target `BASEm`, start from exact old `BASE=STEM+O`. Add a terminal
`M_LOCAL` or `AM_ADDR` only when:

- `STEM` contains at least two known action roots;
- exact `STEM+O`, `STEM+OL`, and `STEM+OR` siblings all exist;
- the nearest exact old `X/Xm` pair is exactly one visible edit from `BASE`;
- every nearest pair adds the same terminal tag;
- the candidate is exactly `BASE_RECIPE+TAG`.

An exact contrary target wins. Binary feature `1`, weight `1.25`, after
GDT528. This is not global `m→M_LOCAL` or `om→O+M_LOCAL`.

## New complete reading

```text
cth = CH+T
ctho/cthol/cthor = CH+T+(O/OL/OR)
cho/chom = HO / HO+M_LOCAL
cthom = CH+T+O+M_LOCAL
```

Working phrase: “Nehmen, einstellen und hier ausführen.” `M_LOCAL` is the
local structural HIER sign; it is not a confirmed word.

## Scorecard

- old four-fold unchanged: `1098/1328/1386/1418`, rank sum `2109`;
- inherited current: top one `149→150`, rank sum `177→176`;
- accumulated revised current: top one `151→152`, rank sum `175→174`;
- current losses: zero;
- exactly one current decision changes: `cthom` rank `2→1`.

## Prospective family

- `ckhom = CH+K+O+M_LOCAL` licensed but absent;
- `cphom = CH+P+O+M_LOCAL` licensed but absent;
- `cthom = CH+T+O+M_LOCAL` visible and licensed;
- `tshom` unresolved.

## Remaining queue

`aiicthy`, `chekchy`, `dairykodas`, `dalcheeeky`, `dsholdaiir`, `qef`,
`saiis`.

Next inspect `chekchy` through internal `CH` insertion and repeated-action
families. Open no new page.
