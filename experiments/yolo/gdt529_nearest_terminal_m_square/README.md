# GDT529 — Action-slot-certified terminal-`m` square

Status: `PASS_ACTION_SLOT_TERMINAL_M_SQUARE`

GDT529 resolves `cthom` as `CH+T+O+M_LOCAL`. The route combines the exact
`cth` action-slot family `ctho/cthol/cthor` with the nearest old terminal-`m`
edit pair `cho/chom`. It does not define global `m→M_LOCAL` or `om→M_LOCAL`
rules.

See `REPORT.md`, `METHOD.md`, the compact working book under `artifacts/`, and
the executable scripts under `src/`. No new page is used.
