# Artifacts

- `gdt460_27_calibrated_edge_stems.tsv`: 24 unique surface stems as eight
  prefix and nineteen suffix channels, with running extension support,
  precision, pages, working values, and target labels.
- `gdt460_17_owner_class_family_stems.tsv`: replicated stellar-position and
  drug-family substrings with all occurrences.
- `gdt460_107_hybrid_label_dictionary.tsv`: complete hybrid decomposition,
  retained name core, edge values, family markers, image binding and concise
  working default for every GDT459 whole label.
- `gdt460_6_page_summary.tsv`: page-level reconciliation.
- `gdt460_result.json`: compact counts and claim ceiling.
- `gdt460_validation.json`: validation and deterministic rebuild result.
