# GDT531 — Atomic renderer-block superform peel

Status: `PASS_ATOMIC_RENDERER_BLOCK_SUPERFORM_PEEL`

GDT531 revises the working composition of `saiis` from `S+IIN+S` to
`S+A_ADDR+IIN+S`. The exact old carrier is
`saiisol=S+A_ADDR+IIN+S+OL`: peeling terminal visible `ol` together with
terminal recipe atom `OL` leaves the current form and GDT529's rank-one
candidate exactly.

The resulting short working reading is “Wählen; hier die Stufe wählen.” The
same construction is checked over all 159 current forms and selects no other
revision. It is therefore a local `saiis` certificate, not a global `ol`
suffix rule. See `REPORT.md`, `METHOD.md`, the compact working book under
`artifacts/`, and the executable scripts under `src/`. No new page is used.
