# GDT671 method

## Question

Can the 15 one-hole forms exposed by GDT670 receive concrete, order-preserving recipe defaults at every safe-panel position without adding a role or using contextual target dispatch?

## Frozen scope

The source frontier is GDT670's 15 first-occurrence-ordered rows: 71 target positions on 70 lines and 56 pages. The base is V47: dictionary 2,100, glossary 1,415, 24,088 known and 8,251 unknown positions, 1,223 complete multi-token lines, and 154 one-hole lines. Guarded queries materialize only required columns on the inherited 179-page allow-list. No new page or image is opened; `f1r` is excluded and `f84`/`f84r` are forbidden.

## Card synthesis

Three differentiated candidates—practical apothecary, stem compositor, and passage reader—were compared with the inherited V47 stem sheet, complete-line contexts, reader boundaries, and final audits. Thirteen forms use only the unchanged 56 roles. `daiiy` and `otoiir` remain learned exact wholes because mechanical decompositions would overstate their practical structure.

All 15 cards dispatch only on exact whitespace-delimited target equality. There are zero context renderers and zero suppressed tokens. Important bound decisions are:

- `qoeedy = QO_COMMAND+O_PREP+EE_END+DY_FINISHED`, an explicit command: “nimm den vollständig fertiggestellten Ansatz.”
- `teol`, `toldy`, and `shekain` are nominal material/state cards, not injected commands.
- `olcheol` begins with visible `O_PREP+L_WOOD`; the preparation and wood-drug values are both retained.
- At f18r.14 both alternate readers join ZL3b `ychekch y` as `ychekchy`. This is reader-boundary evidence only: no terminal `Y` is added to the card, and no join renderer or token suppression is licensed.

The inherited 19 OL and two SOL renderer specifications are carried forward byte-for-byte. GDT671 does not introduce or revise them.

## Passage and determinism audit

Twenty complete passages are kept separately from deterministic automatic renderings. The post-artifact audit retained ten and replaced ten manual readings. The replacements preserve the final target cards while restoring inherited directional stages or removing one unsupported relation. The builder also binds target counts, 70-line/56-page distribution, the two learned wholes, key atom orders, zero context renders, V48 projected dimensions, and the exact eight-row next frontier.

An official run rebuilds into a temporary directory and compares every output byte and `RESULT.json`. The independent validator passes 4,954/4,954 checks.

## Decision rule and claim ceiling

Pass requires 71/71 target positions concrete, 15/15 source rows closed, exact-whole-only dispatch, unchanged non-target token records, 20/20 complete manual passages, projected V48 counts, exact replay, and validator PASS. This licenses only replaceable German workshop defaults. It does not establish plaintext, language, phonetics, historical codebook identity, plants, diseases, patients, cures, or carrier liquids.
