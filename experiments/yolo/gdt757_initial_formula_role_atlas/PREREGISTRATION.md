# GDT757 working predictions

Before ranking the candidate deck, GDT757 fixes eleven target wholes from the
GDT756 rate/purity gate and three concrete role candidates per whole.

The main predictions are:

1. `pchor` should behave as a paragraph/rezept opener and keep `nimm` ahead of
   indication and preparation-heading rivals.
2. `ychor` should remain an internal `Item/ferner` marker rather than return to
   a universal opening `nimm`.
3. `polaiin` and `pol` should be heading-like; `tshol` should be the strongest
   Herbal purpose/indication candidate.
4. `ycheol`, `ychol`, and `dcheol` should prefer internal sequence readings,
   while `ycheor` should have the strongest closing reading.
5. High body-triad forms with less than 0.70 global line-initial purity should
   not be promoted to universal formula words.

Every selected reading remains replaceable and must retain two explicit
rivals. No character or substring receives a meaning from a whole-form result.
