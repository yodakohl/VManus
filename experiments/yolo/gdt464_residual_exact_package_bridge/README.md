# GDT464 — residual exact package bridge

Status: `FOUR_BOUNDED_BRIDGES_REDUCE_WHOLE_LABEL_TAIL_TO_ONE`

Four already supported channels reconstruct six of GDT463's seven remaining
whole labels. The complete 107-label dictionary now contains eighteen full
function formulas, 87 function-shell/name-core hybrids, one family-only label,
and only one wholly learned label (`oiil`).

See `REPORT.md`, `METHOD.md`, and `experiment.json`.
