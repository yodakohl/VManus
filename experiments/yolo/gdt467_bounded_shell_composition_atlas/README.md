# GDT467 — bounded shell composition atlas

Status: `ALL_BOUNDED_SHELL_COMPOSITIONS_PRESERVE_INTENDED_CHANNELS`

The complete GDT466 prefix/suffix and prefix/internal/suffix product is now a
2,760-entry phrasebook. Three opaque core markers produce 8,280 executable
probes; every probe selects exactly its intended channels.

See `REPORT.md` for the composition and renderer-factorization result.
