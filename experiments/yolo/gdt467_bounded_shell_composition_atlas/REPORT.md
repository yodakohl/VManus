# GDT467 — all bounded function shells compose cleanly

## Result

The GDT466 reader can combine its complete directional deck without losing a
card:

- 276 prefix–name-core–suffix shells;
- 2,484 prefix–name-core–internal–name-core–suffix shells;
- three opaque core lengths per shell;
- 8,280/8,280 exact intended channel selections;
- zero precedence collisions;
- complete exposure for all 44 channels.

This is now a practical 2,760-template phrasebook. Given a visibly supplied
surface with these boundaries, the ordered function reading and every learned
core span are determined by the same intake rule used in GDT466.

## Different visible shells can carry one recipe

The 2,760 exact channel signatures are all unique, but they flatten to 2,300
component recipes. There are 423 repeated-recipe groups containing 883 shells:

| group size | groups |
|---:|---:|
| 2 | 395 |
| 3 | 20 |
| 4 | 7 |
| 5 | 1 |

Eighty-six groups cross the two shell shapes. The largest has five visible
factorizations of `OT+OL+AIIN`:

- `otol{NAME_CORE}daiin`;
- `otol{NAME_CORE}aiin`;
- `ot{NAME_CORE}olaiin`;
- `ot{NAME_CORE_1}ol{NAME_CORE_2}daiin`;
- `ot{NAME_CORE_1}ol{NAME_CORE_2}aiin`.

All retain the same broad component sequence
`DANACH · FORTSETZEN · WERT`, but they do not have the same visible boundaries
or learned-name layout. They are therefore stored as one recipe family with
five exact channel traces, not collapsed into one spelling.

Other frequent causes are the independently calibrated `daiin`/`aiin`,
`oiin`/`oin`, and `dal`/`al` channels, plus multi-component edge cards such as
`otal`, `otol`, `olaiin`, `alor` and `aly`. This is precisely the mixed
abbreviation/whole-card behavior the working theory expects.

## What is and is not ambiguous

There is no ambiguity for an observed synthetic surface: longest directional
edges and strict internals choose one exact channel trace in every probe. There
is also no German working template shared by different component recipes. The
only collapse happens after deliberately erasing visible factorization and
keeping just the flat recipe.

The safe reader must therefore return both channels:

1. the broad ordered working meaning;
2. the exact surface/channel segmentation.

The second channel is not decorative—it distinguishes alternative renderer
cards and the placement of learned name material.

Validation passes 54/54 checks with a byte-identical rebuild. Synthetic cores
and templates predict no manuscript spelling or occurrence. No page, channel,
component meaning, name, plaintext, language or confirmed lexeme is added.
