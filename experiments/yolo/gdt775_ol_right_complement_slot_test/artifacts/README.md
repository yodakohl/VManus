# GDT775 artifacts

- `OL_327_RIGHT_COMPLEMENT_ATLAS.tsv`: exhaustive fallback and dispatch audit.
- `RIGHT_COMPLEMENT_SURFACE_CENSUS.tsv`: every exact right surface with target,
  complete-word, and balanced predecessor evidence.
- `RIGHT_COMPLEMENT_13_ROLE_REGISTRY.tsv`: the 57+9 selected family and concrete
  span defaults.
- `SLOT_ONLY_EXTENSION_AUDIT.tsv`: four two-folio throughput extensions.
- `PREDECESSOR_*` and `ANCHOR_PREDECESSOR_EDGE_ATLAS.tsv`: two
  surface-count-balanced nested control decks, surface equalization, 110 folio
  sensitivity rows, and target-type drop diagnostics.
- `CROSS_READER_BOUNDARY_AUDIT.tsv` and `EXACT_FRAME_REPETITION.tsv`: local
  fusion and anti-memorization checks.
- `GDT775_376_RENDERER.tsv` and `GDT775_WORKING_DICTIONARY.tsv`: current display
  layer with confidence, evidence, rivals, and zero semantic-credit fields.
- `GDT775_GDT388_RELATION_PACKET.tsv`, its edge crosswalk, and
  `RELATION_PACKET_INTAKE.json`: all descriptive text adjacencies, explicitly
  not score-ready as visual relations.
- `RESULT.json` and `VALIDATION.json`: compact result and independent replay.
