# GDT775 — `ol` right-complement slot test

Status: `PASS__RENDERER_THROUGHPUT_66_PLUS_8__PREDECESSOR_DIAGNOSTIC_NOT_SCORE_READY__NO_PLAINTEXT`.

GDT775 turns 66 previously generic, cached `ol X` positions into concrete
two-whole working phrases. Fifty-seven are the recurrent primary tier; nine
are an explicit `sheey`/`chy` family extension. A separate throughput layer
adds eight two-folio slot leads (`al`, `dain`, `or`, `chol`). Automatic
contextual output therefore rises from GDT774's 49/376 to 123/376, leaving
253 nominal fallbacks. The 66 family spans and 74 throughput spans carry
separate consumption flags and identifiers.

The complete right-follower distribution has a lead toward the authored
content/record-head mixture (internally `NOMINAL_HEAD`) in the two control
decks. The group includes a measured-field control and, in 8+8, two entry
headings; it is not an upstream gold class. That lead is descriptive, position-confounded, and
type-sensitive: dropping `daiin` flips the equalized 6+6 result, and dropping
all thirteen rendered family types flips both decks. The 8+8 deck contains the
6+6 deck and is not an independent replication. Only 33/66 rendered tokens
have an individual nominal Tier-A lead; 28 are uninformative and five (`kaiin`)
lean operator-like. The selected display therefore remains an explicit
throughput convention, not a decoded `ol` lexeme. For example, `ol chedy`
becomes “Ansatz; mittlere Trockenstufe erreicht,” and `ol aiin` becomes
“Ansatz, Menge III.”

`daiin=Wert III` and `dain=Wert II` are visibly C0 scope extrapolations;
their seven new spans do not inherit global lexical permission from GDT734.

The independent validator passes 27,356 checks and byte-replays all 20 runner
artifacts plus the report. No new page, image, OCR, or transcription is used;
no plaintext, lexeme, specific medium, or EVA component is confirmed.

Run:

```bash
python3 -B experiments/yolo/gdt775_ol_right_complement_slot_test/src/run.py
python3 -B experiments/yolo/gdt775_ol_right_complement_slot_test/src/validate.py
```

See `PREREGISTRATION.md`, `METHOD.md`, `REPORT.md`, and `experiment.json`.
