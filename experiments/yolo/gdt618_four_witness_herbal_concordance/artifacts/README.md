# GDT618 artifacts

- `REGISTERED_SOURCE_PLAN.json`: canonical source identities, hashes, witness
  roles, the corrected five-row developmental deck, exact Latin 6823
  canvas/service bindings, physical-folio correction record,
  manual-transcription contract, access counters, and claim ceiling.
- `REGISTERED_VALIDATION.json`: deterministic registration and manifest audit.

The first plan's DEV03 `Citruli` (`f42v`) collided with DEV04 `Cucurbita`
(`f42r`) on one physical Latin 6823 leaf. The corrected artifact supersedes
`Citruli`, registers `Liquiritia` at `f85v`, and validates side-stripped
physical-leaf uniqueness in all four witnesses.

Developmental source research for the initial deck and correction used network
access to official Mandragore search/notice HTML metadata, including
`Liquiritia` and alternatives, and consulted/downloaded the already hash-bound
Wagner PDF/text. No exact aggregate metadata-request count is asserted. No
manuscript image bytes or Voynich material were opened. The deterministic
builder and validator remain offline and make zero network requests.

No catalogue response, IIIF manifest, canvas, page image, Wagner PDF, Voynich
data, or transcription is stored here. Wagner's 62,861,131-byte PDF remains an
external hash-bound source. No image request profile exists until a later
public addendum.
