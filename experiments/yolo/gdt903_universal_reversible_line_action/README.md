# GDT903 — universal reversible common-endpoint actions

METHOD.md fixes the complete unrestricted class and45-minute decision.

Run `python3 experiments/yolo/gdt903_universal_reversible_line_action/src/run.py` for the proof-producing two-endpoint graph.
Run `python3 experiments/yolo/gdt903_universal_reversible_line_action/src/run_independent.py` for the separately coded difference subgroup under a600-second limit.
Run `python3 experiments/yolo/gdt903_universal_reversible_line_action/src/validate_certificate.py` to verify every local union and any finite permutation witness independently.
Finally run `python3 experiments/yolo/gdt903_universal_reversible_line_action/src/validate.py` to check source identity, exact independent canonical core parity and proof bindings.

SOURCE_LINES.json is unchanged GDT882 evidence. No new manuscript input is
required. Generic mathematical examples are tested by src/selftest.py; its
independent comparison imports the frozen subgroup implementation only on
invented examples. No source graph is computed by those checks.
