# Reproducible proof artifacts

SOURCE_LINES.json preserves the exact413old literal lines. FOLD_LOG.json.gz
will contain only justified unions identified by two original edges and direction.
FOLD_RESULT.json will store the complete folded graph, pointed subgroup core,
and any explicit finite permutation compatibility witness. The independent
subgroup result and independent certificate replay must agree before a claim.
