# Factored complete dictionaries and joint consequences

The original exact63group ZL f83r.18–24 paragraph and all source17productions
remain fixed. All47word values are free and aliases allowed. Extract the eight
previously witnessed complete clause layouts,intersect every occurrence's
allowed terminal domain by whole spelling,and keep the full Cartesian domain.
No old gloss,projection or sampled semantic result prunes these domains.

Two grammar encodings exclude those exact eight layouts with a disjunction
of unselected edges per layout. UNSAT in both certifies coverage. Only then
enumerate all1800complete maps,independently reconstruct their slot domains
and all32source content settings. Every map retains its full parse and variant
outcome,including reference errors and physical/assertion failures. Full traces
are retained for maps with any source-valid setting. Original3cargos/2distinct
hazards/7voyages is mandatory. BIJECTIVE is a marked subset;all47word domains
and layout outcomes are reported for both families without resampling.

For every valid original dictionary,query each of the four whole long
GDT1010grammar candidates under the existing1002five-required-pattern grammar,
with every original code value fixed. New spellings may be aliases;every shared
form retains its value. The fixed original parse supplies its known complete
source witness. No joint meaning can be lost by this factorization if original
layout coverage and complete domain enumeration both pass. Up to two added
grammar maps are retained;failed semantic samples leave other additions open.

Exact layout blockers are the only grammar-encoder additions;existing1002
extension encodings,993compiler/executor,1006bit replay and994binding checker
are unchanged. Independent implementation checks every map/variant and every
claimed extension negative. Same-author computation is not independent meaning.
Source-conditional flags remain;all leaves already exposed;no reserve, new
page,image,f84/f84r,f116v or outside contact. No significance or word confirmation.
