# GDT753 execution contract

The target is the exact `qokeol/okeol` whole pair named by GDT752. All 34 and
41 reader-exact occurrences are retained; there is no passage sampling. Pair
members cannot act as anchors for each other. Radius-five-censored fields
receive no active role credit.

Five q/base and five non-q pairs are matched only on base length and the two
reader-exact occurrence counts. Semantic values, positions and observed roles
are excluded from selection. The directional gate and its four conditions are
specified in `METHOD.md` and are applied identically to target and controls.

The provenance audit is a separate deterministic lookup of the exact GDT664
and GDT666 decision rows. A source card labelled `PRODUCTIVE_COMPOUND` cannot
serve as independent whole-word evidence for the very component roles used to
compose it.

This file records the fixed execution contract inherited from GDT752's named
next test. It is not a claim that the broader sidequest or the old source cards
were blinded before their historical construction.
