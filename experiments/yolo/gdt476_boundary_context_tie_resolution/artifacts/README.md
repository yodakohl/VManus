# Artifacts

- `GDT476_CONTEXTUAL_TIE_WORKING_EDITION.md`: readable eight-record edition
  followed by all 64 tied bundles and their active defaults;
- `gdt476_64_tie_context_decisions.tsv`: one complete row per GDT474 tie;
- `gdt476_8_contextual_record_readings.tsv`: integrated readings of every
  multi-locus record;
- `gdt476_6_page_tie_summary.tsv`: page totals and model-change counts;
- `gdt476_result.json`: compact result;
- `gdt476_validation.json`: deterministic validation certificate.

All artifacts are rebuilt by `../src/run.py` and checked by
`../src/validate.py`.
