# Two-hundredth pass: normalized B4, B5 and B6

## B4

B4 contains 47 events in 20 fields. It starts at the visible main pair with
contact, insertion and holding cells. The two consecutive singleton
`shckhedy` fields remain separate and are best read as a first and second pass,
not accidental duplication. The record then moves to the left underflow
station: measure, warm, continue, add a portion, transfer, collect briefly and
drain.

The working chain is:

`long contact → next item → insert → fix → load insert → pass → pass → hold →
settle → finish → warm/add/transfer → drain → reinsert → short run → clear
extract/target passage → source pour/settle`.

## B5 and B6

B5 is an eleven-event left-end-post record: after-transfer, introduction, then
a measured target-transfer and draw-off sequence. B6 is a nine-event right-end
record: collect, process briefly, continue, measure, place the insert/current
item at the final target.

They are separate visible-owner records. Nothing requires an invisible pipe or
flow connecting B4→B5→B6. The older phrase “without boiling” in B6 has been
removed because no card supplies it.

## Complete prose coverage

This pass adds 67 events, 27 fields and 20 statements. Together with passes
197–199, all 381 prose events, 135 fields and 116 statements on the seven fixed
prose pages now have the same four layers:

`visible surface → normalized card → literal card value → fluent working
translation`.

Next build that unified 381-event edition, preserve the one line-edge read-once
correction, and publish a compact current dictionary showing which meanings are
component-built and which remain learned whole cards.
