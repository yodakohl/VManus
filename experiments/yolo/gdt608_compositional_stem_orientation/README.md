# GDT608 — compositional stem orientation

Status: `PARTIAL_COMPOSITIONAL_BACKOFF__ATOMIC_MERGE_IDENTITY_RETAINS_RESIDUAL_ROLE`.

The 64 frozen GDT605 BPE merges carry real directed component information:
left components predict entry/left context and right components predict
closure/right context better than a global model, reversed composition, and
frequency/mobility-matched donor pairs. The rule transfers on all 23 held
folios. Exact merged-unit identity remains better on all 23 folios, so the
working architecture is a mixture of reusable directed abbreviations and
learned pair-specific cards.

Concrete stable sides include left `q` as near-obligatory entry and right
`y`, `dy`, and `aN` as strong closure carriers. Left `o` fails as a shared
role: `ol/or` are final-biased while `ok/ot` are entry-biased. No uniform
meaning such as `o=water` is licensed by this family.
