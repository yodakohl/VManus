# GDT362 remaining complete-array prospective test

Freeze status: **FROZEN BEFORE TARGET IMAGE REVIEW OR FORMAL QUERY**.

## Source-only capacity rule

Starting from the existing exact human locus annotations, retain every
non-f84 pharmaceutical label unit that:

1. has at least four mapped label loci;
2. lies on a physical folio not already used by the f89/f99/f100/f102
   CONTACT/GAP panels;
3. has an explicit source statement that the mapped locus count equals the
   visible plant/label count; and
4. has no known author-visible versus editorial unit-boundary defect.

The complete rule leaves one unit: f101v2/L2, loci `f101v2.10`–`.18`. The
source explicitly says there are nine plants and nine labels. Eight local
rows are unhedged; the first row is hedged only because it includes the unit
summary and a prior join note. The source descriptions place the labels
between, above, or east of the nine sequential plants but do not state
CONTACT/CLEAR_GAP.

Excluded capacity is disclosed rather than silently sampled: f101v2/L1 has
nine source-described labels but only eight current exact loci; f88 arrays
have the already published C/L editorial-boundary defect; f89/f99/f100/f102
are already exposed; every f84 surface is forbidden.

The official target is Yale canvas `1006250`, 2698 x 3779, SHA-256
`1122f1b13afdf1509402334816f95e5e9baa2b6c94aa9e347b04aa2e4e54f36b`.

## Frozen visual rule and formal prediction

Use the unchanged GDT361 visual states:

- `CONTACT`: a writing stroke visibly touches/crosses/is overlain by a
  non-writing contour;
- `CLEAR_GAP`: parchment separates every writing stroke from nearby drawing;
- `UNCERTAIN`: damage or ambiguity prevents a hard call.

The fixed formal predicate remains first-source-group prefix `AQ`, with `AQA`
an alias only if the masks are identical. The frozen direction is higher AQ
prevalence among CONTACT than CLEAR_GAP. Uncertain rows remain missing. Report
the exact within-array conditional permutation tail and every row.

This is the last complete new-folio endpoint available under the present human
source inventory, not an open-ended page choice. It remains a single-observer
YOLO prospective test, not confirmation.

## Claim ceiling

At most, GDT362 can strengthen, tie, or contradict the directional AQ/contact
lead on one further complete array. It cannot assign CONTACT, plant, object,
word, morpheme, POS, sound, language, plaintext, meaning, or translation. No
f84 access is permitted.
