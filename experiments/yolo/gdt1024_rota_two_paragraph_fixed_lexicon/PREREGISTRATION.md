# Fixed 95-group pair: predictions before execution

Preserve RAW487 and all parent meanings and clauses byte-for-byte. Parse every
group of .25–30 and .31–44 under their separate fixed line productions. All new
declarations refer to the single performance explicitly bound by qokylddy; they
do not execute a fresh start on each mention. dain explicitly restates the same
leader entry. Two shckhedy calls consume U then L in one cursor scope. Bare old
COMPANIONS in D06 denotes the existing rota group, not accompaniment performers.

Twelve baseline cases (C0–C3, N=2/3/4) predict complete compatibility with the
unchanged original performance. All four timing alternatives survive. Four
diagnostics are checked for every case: COMPANIONS_AS_PES violates the old role
extent/disjointness; EVERY_MENTION_EXECUTES tries a second leader entry;
DUPLICATE_PES_CURSOR gives U,U instead of consuming U,L; CO_ONSET_EVERY_MAIN_CYCLE
requires both pes to restart at each leader main-cycle start. The last is a
precise stronger reading: it predicts compatibility for C0/C1/C3 and failure for
C2 (first later main cycle at144 breves has pes phase6). It does not require that
every pes cycle be a main-cycle start. Do not choose a timing branch by this test.

Actual IT .29 has solchedy=EACH_ROTA_SINGER. The fixed D05 grammar rejects that tag
in the LOWER_PES_REFERENCE slot. Separately evaluate its *conditional* semantic
consequence if its quantifier were applied to the displayed predicates: each
rota singer's M has a terminal rest, contrary to WITHOUT_TERMINAL_REST. This
does not license a new production, complete IT reading or spelling correction.
The entire 92-group IT paragraph and all other unknown/forms/joins are retained.

Report all60 model/case rows, twelve conditional IT rows, the literal IT grammar
result and every projected position. Dynamic evidence uses full bound original
traces, not selected moments; early static contradictions are not claimed as
executed alternative full performances. No post-result meaning or grammar repair.

Success retains only this complete projected working reading; failure closes
its fixed binding. No significance, calibrated whole-search null, independent
meaning capacity, source identification or confirmed words. Inclusive05:40 UTC.
