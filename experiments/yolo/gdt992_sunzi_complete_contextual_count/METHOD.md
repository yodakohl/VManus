# Complete contextual count content and two fixed tree writers

The source is the entire unknown-object-count problem, answer, worked method
and concluding generic rules in Sunzi III.26. It is a proposed content, not an
identified quotation in the manuscript. SOURCE.json preserves18source units,
27primitive types and122occurrences in each of two full serializations. Read the
bound source audit and separate source-tree review for the original Chinese,
native inspection receipt, every source clause, numerical values and uncertainties.

## Meaning and context

The specific object collection is introduced once in S01; OBJECTS in S06 is the
same collection. UNKNOWN describes initial ignorance before the stated answer,
not an invariant after it. All six specific grouping conditions concern that
same collection. GROUP(REF,m) groups the objects by m; LEAVES(...,r) asserts its
remainder r. No number or phonetic value is assigned to a Voynich form in advance.

METHOD_BEGIN introduces the worked instructions; WHEN preserves each repeated
case condition, PLACE sets a computational term, COMBINE adds the three terms,
RESULT preserves the stated233. SUBTRACT(a,b) means b-a: first child is the
subtrahend. DONE retains the obtaining-result assertion. GENERAL_BEGIN switches
to generic rule templates; it does not continue assuming the specific count23.
The final generic rule applies once to an unspecified current x>=106 and yields
x-105. No unprinted sum, product, loop, smallest-answer clause or proof is added.

REF is one common primitive with typed contextual antecedents: objects under
COUNT/GROUP, the placed-value bundle under COMBINE, the current numeric value
under SUBTRACT. AT_LEAST is unary and takes the implicit current numeric subject.
This explicit dispatch is a modern writing assumption reflecting source context;
it is not a recovered Voynich anaphor or a nearest-word rule. Generic templates
are not asserted to form a complete modern CRT algorithm.

Numbers are composed canonically in modern positional decimal, using DIGIT_0
through DIGIT_7 as needed and DECIMAL_APPEND(a,d)=10*a+d. They are not base8;
source digits8/9 never occur and get no invented code. Chinese writes additive
number words, including no explicit zero glyph in140/106. The decimal zero and
internal partial prefixes such as14within140 are modern notation, not extra
historical instructions. This writer is semantic, not EVA=Chinese/Latin letters.

## Exact writing equation

PREFIX writes a node then all children in their frozen order; POSTFIX writes
all children in that same order then the node. Concatenate all18trees in source
order. No primitive is optional. Each of27primitives has one nonempty injective
prefix-free string shared across every occurrence within the complete case.
Different complete paragraph/writer cases have separate candidate keys. A code
may lie inside a written word, and a word may contain multiple whole codes;
no code crosses a word seam. Entire source and entire paragraph finish together.
All source occurrences, target characters and word seams are mandatory.

Prefix-freeness permits unambiguous code boundaries under a fixed complete key;
it does not establish semantic uniqueness or a unique key. The code is a narrow
modern model; it does not by itself explain all whole-form and entry-context
residuals in the structural baseline. No old prefix, numeral or word gloss is used.

The exact finite search and separately implemented reverse replay are the bound
unchanged GDT987 functions. Their completeness/pruning proof is in GDT987 METHOD.
In particular, assigned codes cannot be prefixes of other codes, repeated codes
need enough within-word occurrences, and word starts force any matching code.
No GDT987 source, result or function is edited. A fresh complete source is tested,
not a third algorithm on stopped GDT991 or a repair of GDT969/971–974.

## Full scope and limits fixed before target fitting

All1349paragraph readings in GDT928 PARAGRAPHS.json:659ZL3b,690IT2a;RF has no
complete boundary-marked paragraph in this input. Retain the original line flags
and paragraph boundaries. All lines must be anchor_eligible; no entity, uncertain
space or reading is normalized. Source-unknown rows remain, independently for
both writers. No favorable substring, header or small projection is selected.
Total candidate rows2698; all are reported, including paragraphs too short or
long for a complete equation. All target data were previously exposed.

Primary per eligible case:3seconds/200000nodes; external process ceiling8seconds.
Reverse replay of every primary exhaustion:unchanged987limit20seconds/1000000nodes,
with an external8second ceiling. One shared900second total target/reverse budget,
24workers, sequential journals flushed per result. Unstarted, interrupted, limited
and error outcomes remain explicit unknowns. No extension or retry. Source and
engineering controls are outside target fitting but inside the inclusive07:35UTC
implementation/validation/publication checkpoint.

Every positive code is checked directly for full spelling, nonemptiness,
injectivity, prefix freedom and every seam. The validator reconstructs streams
from trees without importing the primary serializer. Every primary exhaustion
receives a separately constructed right-to-left check; only a corroborated
exhaustion closes a case in the final report. Reverse timeouts leave primary
negatives uncorroborated. A reverse witness against a primary negative is a
validation failure, not an excuse to prefer one program. All workers' results
are retained immediately; --assemble reuses saved journals without matching.

Only a first full code per SAT case is sought. Other keys, primitive values,
source paraphrases and semantic renamings are not exhausted. There is no
uniqueness inference from a single witness or implementation agreement. Full
alignment and all source meanings are printed for every SAT; isolated matching
pieces receive no reading credit. Source-only controls include two packed and
two un-packed complete positives and two changed-reference negatives; all pass.
The inherited5100small controls validate the unchanged engines, not this meaning.

## Decision, exposure and claim ceiling

Complete corroborated exclusions close only this writing/source conjunction.
A positive retains a conditional complete reading for further substantive review;
unknown computation/source remains unknown. Stop after this batch regardless of
outcome; no automatic widening, phase repair or extra search algorithm. The entire
search lacks a suitable null control, so no significance claim. Arithmetic
consistency with stipulated operators is not independent meaning identification.

Both writers' complete source predictions and equality signatures are fixed
before fitting. Object/material identity, same arithmetic in different domains,
and verbal relabelings retaining all typed relations can remain indistinguishable.
The physical-folio field tracks exposure; no selection/confirmation split can
manufacture unexposed meaning evidence here. Capacity for independent meaning
confirmation is0for every candidate. Alternate transcriptions are one manuscript.
No reserve, new page, f84/f84r/f116v, native target image or outside person is used.
Confirmed translated words remain0unless a separate meaning test establishes more.
