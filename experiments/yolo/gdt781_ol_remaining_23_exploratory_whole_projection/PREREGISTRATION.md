# GDT781 preregistration — remaining 23 exploratory complete-whole projections after `ol`

Status: `REGISTERED_UNSCORED`.

Registered: 2026-09-03.

## Question

Can every one of GDT780's twenty-three still-unselected reader-exact,
cardless complete right surfaces receive an explicit, replaceable whole-form
working default, while preserving the exact parent renderer, consuming each
right token once, and exporting no EVA substring or component value?

This is deliberately an exploratory throughput pass. The user has selected the
policy that every observed complete form receives a default now and keeps it
until a better whole-form account or a clear contradiction replaces it. The
policy does not turn a default into a translation.

## Frozen parent and cohort

The parent is GDT780's 376-row renderer with:

- 247 contextual rows;
- 129 generic fallbacks;
- 207 uniquely consumed right tokens.

GDT780's 25-row reader-exact cardless intake contains two already selected
forms, `eees` and `sheeol`. GDT781's cohort is exactly the other twenty-three
complete right surfaces listed in `src/WHOLE_23_SPECS.tsv`. The cohort must
reconstruct as 23 spans, 23 distinct forms, 23 loci, 23 page labels and 20
physical folios. No occurrence is added or removed by its proposed meaning.

## Frozen selector

For every one of the 376 parent rows, select exactly when:

```text
parent contextual = 0
AND right reader exact = 1
AND complete right surface is an exact member of WHOLE_23_SPECS.tsv
```

All twenty-three spec rows are selected. Analogy tier, recurrence, meaning,
axis, occurrence ID, page, folio, locus, neighboring word, edit distance and
frequency are not selector inputs. They explain the chosen default and its
rivals only. There is no substring, prefix, suffix, character, phonetic or
component fallback.

Existing contextual parent rows retain precedence. A selected span consumes
the complete right token once and emits one `ol + complete whole` rendering.
No other token is consumed or rewritten.

## Frozen working cards

`src/WHOLE_23_SPECS.tsv` is the complete deck:

- twelve A3 distance-1 multiwhole consensuses;
- nine new A2 exploratory projections;
- the already published A2 `okalor` whole card;
- one A0/C0 target-frame-only default, `chorcholsal`.

The A0 row is intentionally not disguised as independent evidence. It is
licensed only for the enumerated exact `ol chorcholsal` span. All other rows
are also confined to exact `ol + complete surface` spans; none becomes an
unconditional global dictionary entry.

The principal recorded conflicts are binding metadata, not rejection rules:

- `cheedaiin`: whole-form analogy gives `DRY|AMOUNT|MIDDLE_STAGE`, while one
  target-independent but B0 serial frame gives `END_STAGE`;
- `eses`: the older clean pool gives `PREPARATION`, while the newer complete
  neighbor `eees=Mengenfeld` gives an amount/value lead;
- `okalor`: the multiwhole circle leans HOT, while its sole distance-1 neighbor
  `otalor` is COLD;
- `chsky`: three ZL3b cache surfaces reduce to one reader-exact occurrence;
- `chorcholsal`: no clean whole neighbor exists within edit distance two.

The A3/A2/A0 labels and neighbor counts are frozen from the original GDT745
769-whole pool.  Later GDT780 cards are a separate supplement layer and may
change the chosen exploratory wording or add a rival, but may not be inserted
back into that pool.  In particular, raw `eses` remains an A2 distance-2
`PREPARATION` consensus while the later `eees=Mengenfeld` card becomes an
explicit conflict and rival rather than a retroactive pool vote;
`sheeoy` keeps its six original A3 neighbors before the `sheeol` reinforcement;
and raw `chorcholsal` remains A0/NONE before its explicitly target-local frame.

## Exact predictions

If reconstruction and the selector behave as registered:

- selected spans/forms/loci/page labels/physical folios:
  **23/23/23/23/20**;
- contextual output: **247 → 270**;
- generic fallback output: **129 → 106**;
- collision-free right-token consumption: **207 → 230**;
- all other renderer rows unchanged: **353**;
- collision, takeover, protected-row rewrite and nonexact match counts: **0**.

The residual 106 fallbacks must partition exactly as:

- reader-exact cardless right wholes: **0**;
- nonexact right wholes with a V99R7 card: **49**;
- nonexact right wholes without a V99R7 card: **20**;
- line-final `ol` without a right token: **37**.

## Evidence interpretation

The clean whole-form projection reuses the GDT745 mechanism: complete observed
surfaces are compared to complete clean W2/W3 whole cards, and only role axes
shared by whole neighbors are summarized. Later complete-whole cards may
reinforce or expose a conflict. This never decomposes an EVA surface.

Recurrence is descriptive. Seven cohort surfaces have more than one
reader-exact cache occurrence; sixteen are reader-exact singletons; `chsky`
has three ZL3b cache occurrences but only one reader-exact occurrence. The exploratory
policy applies all twenty-three anyway and preserves recurrence limits in each
card's counterevidence.

## Claim ceiling and source boundary

Every German phrase is a replaceable renderer hypothesis. GDT781 identifies no
Voynich lexeme, plaintext clause, language, sound, historical abbreviation,
number, unit, ingredient, plant, disease, treatment, liquid or specific
substance. It exports no value for an EVA character or substring.

The run may use only the inherited admitted cache and already published
artifacts. It opens no new page, image, OCR or transcription. `f84` and `f84r`
remain sealed. ZL3b, IT2a and RF1b remain alternate readings of one manuscript,
not independent witnesses.

## Required outputs and validation

The runner must emit the full 376-row renderer, the 23-row selected atlas,
working dictionary/evidence, precedence audit, passage patches, residual
census, GDT388 relation packet with crosswalk, machine-readable result and
compact artifact README. The validator must independently reconstruct the
cohort, pure selection, precedence, counts, residual partition and all
zero-credit flags, then byte-replay every runner-owned output.

The GDT388 edge packet remains acquisition-only unless every executable gate
passes; its state cannot promote a working default into a lexical claim.
