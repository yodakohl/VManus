# GDT781 method — remaining 23 exploratory complete-whole projections

Status: executed; the registered method passed. See `REPORT.md` and
`artifacts/VALIDATION.json` for the result and independent 3,407-check replay.

## Question

Can the final twenty-three reader-exact cardless right wholes in GDT780's
`ol` cohort be replaced by explicit, auditable whole-span defaults under the
user-selected exploratory policy, without changing any existing contextual
decision or exporting an EVA component?

## Inputs

The fixed renderer parent is GDT780. Its 25-row cardless intake supplies the
cohort boundary; removing its two already selected surfaces, `eees` and
`sheeol`, must leave exactly the surfaces in `src/WHOLE_23_SPECS.tsv`.

Evidence for the card descriptions comes from already published complete-whole
sources:

1. GDT734's clean W2/W3 whole dictionary;
2. GDT745's complete-whole edit-analogy mechanism and calibrated A3/A2 tiers;
3. GDT762's global reader-exact occurrence/page census for these surfaces;
4. selected GDT748 target-independent serial evidence and later complete-whole
   cards through GDT780;
5. only for `chorcholsal`, its explicitly marked target-local complete frame.

The source-lock table and manifest bind the executed method separately. The
runner verifies every declared input hash before producing a result.

## Executed method

1. Load and hash-check the frozen GDT780 renderer, intake, residual census and
   result.
2. Reconstruct the twenty-three-form cohort by subtracting only `eees` and
   `sheeol` from the 25-row reader-exact cardless intake. Verify exact equality
   with the spec surfaces.
3. Reconstruct each complete-whole analogy record from the clean published
   whole pool. Compare whole strings only. Do not parse or assign any value to
   a character, prefix, suffix or substring.
4. Preserve two explicitly separate evidence layers: (a) the frozen GDT745
   pool result, including its neighbor count, consensus axes and A3/A2/A0
   tier, and (b) later whole-card supplements or target-frame metadata used to
   choose the exploratory display.  GDT780's `eees` and `sheeol` cards never
   enter the old pool or alter its counts or tier.  Preserve the fixed default,
   two rivals, confidence, positive evidence, counterevidence and source path.
   The deck membership is not chosen from target occurrence IDs or outcomes.
5. Apply the pure selector from `PREREGISTRATION.md` to all 376 parent rows.
   Existing contextual rows win. Each selected fallback consumes its complete
   right token once.
6. Emit a 23-row exact-whole atlas, 23-card dictionary/evidence table,
   precedence audit, complete 376-row renderer, 23 passage patches, residual
   census, relation packet/crosswalk and `RESULT.json`.
7. Independently reconstruct selection and every count, enforce all scope and
   zero-credit flags, run the executable edge-packet checker and byte-replay
   all runner-owned outputs.

## Working-card policy

The evidence tier controls how prominently uncertainty and rivals are shown;
it does not control membership in this pass. All twenty-three cards render by
explicit user policy:

- A3: distance-1 multiwhole consensus;
- A2: distance-1 plus radius-2 consensus, distance-2 multiwhole consensus, or
  the existing `okalor` card;
- A0/C0: `chorcholsal`, target-frame-only and enumerated-span-only.

For `eses`, the frozen pool result remains A2, three distance-2 neighbors and
`PREPARATION`; `eees=Mengenfeld` is recorded separately as the later ED1
conflict and visible rival, not as a pool vote.  For `sheeoy`, the six old
distance-1 neighbors and A3 tier remain unchanged while `sheeol` is only a
later END supplement.  For `chorcholsal`, raw analogy remains A0/NONE and its
target-local axes occupy a separate field.

The practical renderer may be concrete, but the machine-readable fields must
continue to say that the defaults are not translations and that literal,
lexeme, component, numeric and specific-substance credit are zero.

## Decision rule and claim ceiling

The only selection rule is exact parent fallback plus reader-exact complete
right-surface membership in the fixed 23-card deck. No occurrence, location,
frequency, neighbor, meaning or substring feature participates in selection.

Success means exact reconstruction of 23 selected spans and the registered
247→270 contextual, 129→106 fallback and 207→230 consumption changes with 353
unchanged rows. It does not mean that any German word is plaintext.

No new page, image, OCR or transcription may be opened. `f84` and `f84r` stay
sealed. No component or unseen form inherits a card value.
