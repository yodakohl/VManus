# GDT373 — ranked functional-operator roadmap

Status: **FUNCTIONAL_OPERATOR_HYPOTHESES_REGISTERED_BEFORE_SEARCH**.

Twenty-two requested hypothesis families are registered separately.  The
highest-value genuinely new endpoint is variable rewriting among
near-isomorphic **records of atomic tuples**, followed by hierarchical
attachment/scope, record/discourse shortening or resumption, and source-side
valency/coordination changes.  These tests are new only because they predict a
whole-record variant or downstream structural consequence across opaque base
records and held folios.

The audit prevents four common regressions:

- character or PAGE_HOST substring edits are GDT003/GDT094 descendants;
- isolated equivalence/synonym claims belong to the closed minimal-pair route;
- boundary association without a rewrite repeats the old scope/order work;
- next-state coordinate prediction repeats GDT345–347.

`ANALOGY_PARADIGM_RELATIONS` is therefore registered as
`DUPLICATE_UNLESS_RECORD_LEVEL`; `REGISTER_SPECIFIC_ALLOMORPHY` is
`CONSTRAINED_EXTENSION` because q/DY/EO and wrapper effects already exist;
`OPEN_CLOSED_CLASS_DIAGNOSTICS` is an auxiliary distributional diagnostic, not
an operator claim.  Every other family has an explicit distinct endpoint and
failure condition in the registry.

The next authorized action is the common GDT374 discovery instrument described
in `METHOD.md`.  No candidate form was inspected or scored in GDT373, no
semantic label was assigned, and no f84 row was accessed.
