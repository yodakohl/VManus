# GDT763 exploratory working registration

Status: exploratory continuation, not a blind or frozen confirmatory test.

## Registered alternatives

For `pcheey`, preserve all four live functions until the local evidence ranks
them: field-bearing dry-preparation head, parallel record field,
dry-source/content complement, and post-moist result. For `ol`, preserve a
quantity-bearing content head, measure/unit complement, oil/oleaginous base,
and function/frame role.

The two source TSVs publish the working alternatives and the five explicit
whole-role priors needed to interpret the target right fields. Working
semantic roles must remain separate from formal axes.

## Registered comparisons

- enumerate all 199 reader-exact H1 occurrences;
- compare `pcheey` with the 49 other paragraph-start medial H1 occurrences;
- count exact `H1 X daiin` and a frequency/geometry-matched non-H1 control;
- compare H1-H4 immediately after all eleven GDT762 state pairs;
- dispatch all sixteen `ol` amount positions in the fixed rule order in
  `METHOD.md`;
- compare `ol` placement with GDT760 material and preparation contacts;
- keep source, result, shell, unit, and oil as explicit rivals rather than
  silently deleting them.

## Scope

Only cached admitted positions are eligible. Nonexact or quarantined slots
cannot supply semantic evidence. Historical examples can support an
architecture or role analogy but cannot name a Voynich form. No component
export or specific substance identity is allowed.
