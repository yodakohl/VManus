# GDT763 — H1 content versus record discriminator

GDT763 turns the GDT762 `pcheey` and `ol` leads into typed local functions.

- `pcheey` is best rendered as a **bound dry-preparation/Form-II record-content
  field with a following-value affinity**. It occurs three times after
  `sho|sheo`; two cases have exact `pcheey X daiin`, versus 3/196 among all
  other H1 occurrences. A source/complement reading remains a local C1 rival;
  a result reading is downgraded because all three targets lack a nearby
  process or close signal.
- Of sixteen `ol` amount positions, nine are head-like, one is a patient, five
  are context/second fields, and one is bilateral. The best whole role becomes
  **quantity-bearing preparation/content head with contextual side uses**.
  Oil remains an unselected identity rival.

The run uses no new page. Validation performs 2,364 invariant checks and a
byte-identical replay.

```bash
python3 experiments/yolo/gdt763_h1_content_vs_record_discriminator/src/run.py
python3 experiments/yolo/gdt763_h1_content_vs_record_discriminator/src/validate.py
```

See `REPORT.md` for the interpretation and `artifacts/README.md` for the
machine-readable output map.
