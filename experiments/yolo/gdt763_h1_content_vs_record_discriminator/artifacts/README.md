# GDT763 artifacts

- `H1_199_OCCURRENCE_SEQUENCE_ATLAS.tsv`: every reader-exact H1 occurrence,
  geometry, H1 co-occurrence, and typed L3–R5 slots.
- `H1_PARAGRAPH_MEDIAL_R1_R3_PROFILE.tsv`: target, 49 position-matched H1
  controls, combined matched cohort, and all-other-H1 summaries.
- `H1_CLASS_STATE_PREDECESSOR_PROFILE.tsv`: H1-H4 dry/moist predecessor counts,
  opportunity-normalized rates, and the H1 post-moist lead.
- `PCHEEY_3_FIELD_FRAME.tsv`: the three target sequences with neutral and
  aggressive renderers.
- `GAPPED_DAIIN_FRAME_COHORT.tsv`, `GAPPED_DAIIN_FRAME_HITS.tsv`, and
  `STRICT_N3_PARAGRAPH_MEDIAL_CONTROL.tsv`: the exact `TARGET X daiin`
  recurrence and its eleven-surface/33-occurrence control.
- `PCHEEY_HYPOTHESIS_SCORECARD.tsv`: ranked record-head, source, and result
  interpretations.
- `OL_16_SLOT_FUNCTION_ATLAS.tsv`: deterministic HEAD/OBJECT/CONTEXT/BILATERAL
  dispatch for all amount contacts.
- `OL_18_MATCHED_CONTENT_COMPARATORS.tsv` and
  `OL_CONTENT_CLASS_PLACEMENT_COMPARISON.tsv`: exact placement controls against
  known working material and preparation heads.
- `OL_AMOUNT_FORMULA_RECURRENCE.tsv`: repeated `ol` amount orders and the
  related `ols` whole-form control.
- `HISTORICAL_FIELD_FUNCTION_COMPARISON.tsv`: eight period role analogies with
  zero target-assignment credit.
- `TWO_WHOLE_RENDERER_REVISION.tsv`: the revised `pcheey` and `ol` defaults,
  evidence, confidence, and counterevidence.
- `RESULT.json` and `VALIDATION.json`: compact result and 2,364-check
  byte-identical replay validation.
