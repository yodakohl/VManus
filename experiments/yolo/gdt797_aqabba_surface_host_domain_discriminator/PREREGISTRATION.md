# GDT797 fixed scope and decision protocol

- Manuscript scope: the existing thirty-page GDT791 spine plus the two
  `AQABBA` source labels already admitted by GDT795. No new page, image or
  transcription may be opened.
- Primary target events: `otaldy` at f70v2.17, `okaldy` at f72r2.27,
  `okaldy` at f75r.27 and f82r.37, and `otaldy` at f88r.12 and f95v1.4.
- Boundary sensitivity only: f18r.8, where ZL3b and RF1b have `okal dy` but
  IT2a has `okaldy`. It cannot count as a primary exact-whole occurrence.
- Running occurrences are collapsed to their complete paragraph; local labels
  remain one visible-owner locus. Token multiplicity inside a paragraph never
  creates extra evidence.
- Host domains are fixed before scoring: radial array = celestial array;
  pool/apparatus network = figure/station system; whole-plant article or
  material register = plant/drug material; text block = other text.
- All eleven recurrent GDT795 boundary families form the control census. The
  101 source-register loci are removed before external host contacts are
  counted.
- As a second target-independent calibration, every distinct single-token
  surface in the 101-locus atlas receives an external local-versus-running
  host profile. This asks whether a non-celestial local-label plus paragraph
  bridge is common before inspecting the two target surfaces.
- Three rivals are scored: one family-pooled status, two surface-specific
  learned entries, and opaque recurrence. Inherited semantic glosses are
  masked and may not define an outcome.
- The surface-specific model may become the primary C0 working theory only if
  both surfaces have at least two external host units, each is domain-pure,
  their modes differ, leave-one-host-out surface prediction beats the pooled
  family baseline, and no capacity-qualified control has an equal positive
  gain.
- The concrete cards `okaldy=Figurenname` and `otaldy=Wurzeldroge` require one
  exact visible-owner anchor and one separate whole-page/paragraph contact in
  the same fixed domain. They remain context-bound C0 readings.
- No `ok`, `ot`, `al`, `dy`, Source-Family component or alternate-reader
  spelling receives meaning. No result confirms plaintext or exports to an
  unseen occurrence.
- f84 and f84r remain forbidden. The seven alternate-reader rows are acquired
  only by an exact-locus guarded query; the three editions are readings of one
  manuscript, not independent witnesses.
