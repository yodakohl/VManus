# GDT743 — R2 carrier-coverage and conditioned-intersection adjudication

Status: `PARTIAL__REQUESTED_CARRIER_COVERAGE_GE3_ANALOGY_ADDS_D0075__RUN_GATE_ABLATION_ONE_TO_ONE_EXCLUDES_ZERO__CONDITIONED_HOT_INTERSECTION_REMAINS_OPEN_LEAD__36_AXIS__46_CARRIER__59_SPECIFIC__143_OPEN__PROVISIONAL_COMPOSITE_RULE__ZERO_LEXEME_OR_COMPONENT_EXPORT__NO_NEW_PAGE`

GDT743 closes the local attachment-polishing route with one deliberately
exploratory renderer assumption and a stronger negative result. The relaxed
reverse/full-carrier R2 trigger selects one contact; the requested-carrier
coverage gate still selects that same one contact and excludes zero. Therefore
the long run does not independently adjudicate the collision.

The occurrence at `G739-D0075` receives a provisional `Carrier=PREPARATION`
tag while its axis remains open. Its renderer is explicitly bracketed as a
working model, not plaintext. The complete renderer moves from 45 to 46
carrier-bound occurrences and from 58 to 59 specific occurrences; 143 remain
fully open.

A carrier-unconditioned scan exposes six qualifying target/carrier pairs,
including two hidden by the inherited requested-carrier choice (`D0027` and
`D0187`). D0040 remains only a conditioned HOT lead and receives no axis.
No component, lexeme, plaintext, historical expansion or literal ingredient is
exported.

Reproduce with:

```bash
python3 experiments/yolo/gdt743_r2_run_intersection_adjudication/src/run.py
python3 experiments/yolo/gdt743_r2_run_intersection_adjudication/src/validate.py
```

See `REPORT.md` for the result, `METHOD.md` for the executable rule and
`artifacts/GDT743_RUN_INTERSECTION_READER.md` for the compact human-readable
audit.
