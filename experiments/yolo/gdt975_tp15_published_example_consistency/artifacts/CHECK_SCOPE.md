# Check scope

GDT975 separate validator PASS (8 checks). Context format/retrieval and registry checks PASS. Full `vmanus-exp check --all` retains8 preexisting failures:7 unbound GDT600 reproducibility files and1 GDT953 large-artifact justification. No new GDT975 failure; no global PASS claimed. Exact staged privacy/scope gate must pass before publication. These checks do not validate manuscript meaning.
