# GDT975 declaration before machine execution

Question: are ALL eight explicit Appendix F input/output pairs compatible with the published TP15/CVC fragment and its exact append-only decode functions?

This is an exploratory audit. The paper pairs, Section7.5 traces and likely contradictions were seen before this declaration. No blind prediction or unseen target evaluation is claimed. The full target-paragraph test proposed in the parent decision note is not executable from the owned incomplete tables and has not run.

Freeze: pinned upstream commit and exact excerpt receipts in src/SOURCE.json; all eight aligned pairs printed before the two ellipses in AppendixF; all three Section7.5 reproduction examples. Do not silently turn these prefixes into complete passages. Their manuscript/page attribution is not independently checked. No manuscript cache, raw corpus, image or reserve is opened. f84 and f84r remain sealed; f116v not admitted.

The five documented or explicitly source-derived syllable values and the15 listed modifiers define only the relevant fragment. k remains an unknown string, displayed as ?. Do not fill it from a dictionary. The first syllable ra cannot be erased or replaced by any completion. e is absent from the complete15-modifier list; treating it as either SYLLABIC or AMBIGUOUS has the same effect because loop is not a coda stroke. Source sh→se is derived from the published CV shedy→sera example and fixed e→ra; mark this derivation explicitly.

Execute upstream tokenize_eva_chars, classify_token_chars, classify_token_chars_v2, get_coda, decode_token_cvc_v2 without edits using only their exact source nodes and static reference table. Do not run corpus loaders, optimization, codebook fitting, spell correction, wildcard expansion or postprocessing. AST extraction is a scope harness, not a new decoder. Validate via independent constituent concatenation and fixed-prefix compatibility, not by importing the primary runner.

Report each pairing's exact tokenization, known contributions, unknowns, output/pattern and agreement or contradiction. Verify all three reproduction cases. Separately retain missing complete runtime artifacts, source-version inconsistency and absent meaning confirmation. A contradiction closes use of these published passage examples as faithful outputs of the cited current rules; it does not disprove Italian, Latin, tachygraphy or every TP15 variant. An agreement confirms only code/source consistency, not a gloss. No significance claim or independent confirmation capacity.

Decision: withhold semantic target follow-up if the claimed examples conflict. Reopen only with a complete fixed, versioned table and source-consistent examples; keep this audit intact. Inclusive parent budget ends15:25UTC, including preparation, validation and publication.
