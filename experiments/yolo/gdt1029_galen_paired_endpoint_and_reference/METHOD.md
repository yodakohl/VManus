# GDT1029 method

Contract: PREREGISTRATION.md. Selection/budget: DECISION.md. Byte-exact proposal:
src/SOURCE.json. Candidates/worlds: src/SPEC.json and src/CASES.json. No raw string
is normalized. The compiler consumes all95 groups and evaluates references,
capabilities, histories, endpoints and norms under the fitted productions.
The independent module imports no new model/executor; it checks reversed
productions, named-case predictions and trace invariants. Invented-form preflight
tests implementation only. The parent's locked model is imported without its
file-writing entry points; all228 parent rows are replayed into child artifacts.

After public registration, reproduce from repository root:

```sh
python3 experiments/yolo/gdt1029_galen_paired_endpoint_and_reference/src/run.py
python3 experiments/yolo/gdt1029_galen_paired_endpoint_and_reference/src/validate.py
```

PREREG_LOCK binds all scientific files before target use. artifacts/PUBLIC_REGISTRATION
records the public commit. RESULT is executor output; VALIDATION is the separate
software check. Neither turns assigned meanings into observations. No statistical
significance test or new semantic control search runs.
