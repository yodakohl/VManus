# GDT664 — one hundred forty residual family completion

Status: `IN_PROGRESS`

GDT664 turns the next V40 frontier into a V41 workshop reading. It assigns a
concrete replaceable default to 140 residual surfaces at 1,141 positions,
closes the 146 inherited one-hole lines, and checks thirty complete passages by
hand.

The central revision is compositional: material-bearing `ol*` forms are read as
`O_PREP + L_WOOD` (Holzdrogenansatz) while naked `ol` remains the learned whole
“Grundansatz” and `oly/olyly` remain straining actions. A manual passage audit
also repairs the inherited `solaiin/sols` salt inconsistency to the already
dominant `sol=Saatgutansatz` family.

See `METHOD.md`, `HISTORICAL_ANALOGUES.md`, and `experiment.json`.
