# GDT882: no nonzero fixed additive whole-line invariant

The guarded odd-leaf discovery projection yields413 eligible complete prose
lines on43 physical leaves. All three alternate readings agree on each retained
concatenated literal string. The observed alphabet has20 transcription
characters: a c d e f g h i k l m n o p q r s t x y.

The integer lattice of line-count differences is the full Z^20, with rank20
and index1. The incremental computation reaches this after192 differences.
Its retained certificate uses100 difference rows and the reference f19r.8
(101 original lines). The exact integer certificate C D = I is independently
replayed from a second guarded source reconstruction. This is not a numerical
rank estimate or a modulus search.

Consequently every fixed assignment of these20 literal characters into any
abelian group that makes all included full-line sums equal assigns zero to
every character. This excludes the preregistered nontrivial fixed additive
whole-line mechanism, including all modular weight choices simultaneously.
Do not rerun it with a different modulus or weights.

Mathematical corollary, independently reviewed, using the same certificate:
if the characters are assigned fixed elements of a solvable group and all
ordered full-line products are equal, the generated image group H has trivial
abelianization by the certificate. Thus H is perfect and solvable, hence trivial.
This also excludes that exact constant-product mechanism in any solvable group,
including permutation groups on at most four states. Equality of the resulting
state from one starting state is weaker than equality of group products and is
NOT excluded. No new source computation or manuscript finding is claimed by
this corollary; nonsolvable groups and general automata remain outside scope.

Scope: exact universal rule on the declared literal representation and
eligible lines. Incorrect transcription or line scope could invalidate its
application to authorial writing. This does not reject stateful, positional,
nonlinear, error-tolerant, differently segmented codes, language or meaning.
Letters absent from this observed alphabet receive no inferred value.

Selection:88 explicitly allowed odd-leaf selectors;413 retained loci.
Whole-locus exclusions:1157 nonliteral/uncertain character;171 alternate-string
disagreement;112 drawing/other gap;169 nonprose;2 missing alternate locus.
Uncertain small spaces are preserved but do not affect the space-free sum.
Even leaves were not queried; no image, new visual admission, plaintext,
ordered relation packet or meaning was scored.

Four distinct synthetic algebra cases pass. Independent validation rebuilds
the source selection and count vectors and verifies every coefficient product
against the identity matrix. This verifies the conditional mathematical result,
not that literal EVA characters are historically correct atomic units.

Sources and protocol were public in11e55687 before extraction. The first result
and independent source/certificate check completed at02:04:22UTC, under six
minutes after the user-requested10h session began at01:58:34UTC. Overall work
continues; this experiment does not consume the entire research session.

Reproduce: src/run.py --check; src/validate.py --check. Protocol: METHOD.md.
Compact artifacts: SELECTED_LINES.json, EXCLUSIONS.json, RESULT.json and
VALIDATION.json. No synthetic decoder or new dependencies were needed.
