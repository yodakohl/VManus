# GDT699 — one concrete YKA-II participant edge

Status: `PASS_V72_YKA_II_III_SISTER_FRAME__5_CASES_2_REPLAY_1_NEW_B_EDGE_2_HELD__AIIN_UNBOUND__ZERO_WORD_DELTA`

## Result

There are exactly three action-licensed members of the inherited YKA-II/III
family in the complete current 479-token scope:

| locus | action | grade | participant decision |
|---|---|---:|---|
| f105v.1#4 | `ykaiin` | III | replay existing C001 from written wood powder |
| f86v6.25#5 | `ykaiin` | III | replay existing C006 from measured action output |
| f86v5.24#3 | `ykain` | II | add C010 from `oar#1` only |

The new practical reading is:

> Anteil I des Ansatzes. **[Ungebundene Mengenangabe #2: „Menge III“.]**
> Den Ansatzanteil auf Stufe II erhitzen.

C010 is the single edge `oar#1 → ykain#3`.  It is B-tier and bound to this
occurrence.

## Why `aiin` is not part of the object

The immediately preceding V68 block is exactly `oar|aiin`, but it contains two
semantic units and no binding.  GDT693 types `oar` through its bounded share
slot as *Anteil I des Ansatzes*.  The second token is separately *Menge III*
and has no inherited relation role or edge.

GDT696's held H007 rival had used the whole #1–2 block and rendered “in Menge
III”.  V72 does not promote that rival.  It replaces the question with a
different, narrower one and admits only #1.  The bracketed output keeps this
distinction visible instead of relying on punctuation that could suggest a
quantity attachment.

## The two controls still fail

- R010 `yky` is outside the exact YKA-II/III family and has competing donors at
  #1 and #3–4.  The separate #2 `Hierzu:` is structural and selects none.
- R012 `ykecthey` is also outside the family and already contains the written
  object *Krautdroge*.  Linking #3 would be a different source relation, so it
  remains held.

This is not an action-surface default.  The new leverage is the conjunction of
an already registered II/III sister pair, two admitted YKA-III object frames,
one unique typed material share in R021, and explicit exclusion of the other
unit.

## Scope and next move

- 2 prototype replays, 1 new local edge, 2 controls held.
- 0 AIIN bindings and 0 promotions of the old H007 block rival.
- 479 token glosses, 51 inherited clause readings and 3 bound spans unchanged.
- 0 new word meanings, pages or f84/f84r access.
- The GDT388 packet is explicitly not score-ready; C010 remains workshop-only.

There is no second `oar` occurrence in the current 479-token scope.  The next
pass should instead test only the narrower H003 split at `f26r.2`: carry the
written *Krautdroge* output of `ykecthey` #4 to the objectless `ytedy` #6 across
exactly one state-only `chedy` #5 register.  Neither #3 nor #5 may become a
donor, and the output may not pass beyond #6 or become a general carry rule.
