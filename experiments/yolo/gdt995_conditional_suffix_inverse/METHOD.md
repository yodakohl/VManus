# GDT995 — conditional suffix inverse on the complete frozen GDT837 panel

This is a retrospective control diagnosis, not a fresh recovery experiment.
GDT837's STRICT_RECOVERY_FAIL and all six original selections remain unchanged.
The analyst knows its ae-to-a error and the absence of any entirely correct
restart. No Voynich transcription or image is an input; f84/f84r stay sealed.

The new condition is the full forward-writing contract already declared by the
control: a candidate's wholeword dictionary has priority, then an applicable
suffix with at least three literal stem characters, otherwise literal letters.
Both dictionary and suffix values belong to the candidate, not planted truth.
All 38 atoms, role capacities and output pools remain fixed. Roles and literal/
wholeword values stay exactly as in each of ALL 48 saved restart maps. Only the
four S outputs are exhaustively assigned injectively from the unchanged twelve
reference-derived candidates: 12P4 = 11,880 possibilities per restart. This is
conditional enumeration, not a new role/letter/wholeword decoder or restart.

A hidden suffix order is not supplied by the saved candidate maps. Existentially
quantify all 24 global orders of the four S carriers. A single order must encode
every discovery word exactly; overlapping suffixes cannot select an order per
word. Report all surviving orders and preserve them for held checking. Inactive
W/S entries participate, exactly as the mandatory encoder requires. Reject
role-position, W priority, mandatory suffix and global-order contradictions.
The implementation may first exclude any suffix value contradicted by an
all-literal word and candidate W priority, but must account for every one of
11,880 assignments. Output every feasible assignment, not only the best.

Score feasible completions using the unchanged GDT832 OFF reference objective
and GDT837 native Monarchia reference. No new training, normalization, factor,
weight or score. For each restart choose greatest discovery score; exact ties
use lexicographic output tuples in sorted S-carrier order. Preserve ties and
score gaps. Candidate multiplicity is not evidence of historical probability.
All discovery rows, feasible maps and selections are written and hash-locked
before the evaluation command can read truth or held ciphertext. The old truth
is public and previously exposed, so this procedural separation is not blindness.

After locking, join all feasible candidates to active-map truth. For each
conditional selection evaluate all held words/source sentences and full forward
compatibility. A discovery-admissible priority order must work on held data too.
Also audit each old original map on discovery; never replace GDT837's result.
Use all 48 restarts, report six original selected identities explicitly, and
separate suffix-only completions from unresolved fixed non-suffix errors. The
three worlds encrypt one content split; alternate keys are not independent texts.

A separately written validator independently filters the finite assignments,
reencodes each word directly under every inequivalent global order, checks the
complete enumeration and independently scores via the old sentence scorer.
The runner instead derives suffix-order precedence constraints and aggregates
word transitions for scoring. Invented fixtures cover W precedence, obligatory
suffixes, minimum stems, inactive values, overlap and incompatible global orders.
Lock all scientific code and input bytes before the real audit. No repair after
outcomes. Budget includes preparation, implementation, validation and publication
through 2026-09-20 14:05 UTC; stop expansion at that checkpoint.

Interpretation: exact conditional winners would show a specific inverse rule
can remove the known error inside fixed old non-suffix maps. Wrong feasible
winners retain an objective error. Empty spaces expose a fixed-map limitation.
None establishes a Voynich writing rule, language, word, fresh full-key recovery,
significance or permission to reopen GDT616/CDA001. No automatic repair chain.
