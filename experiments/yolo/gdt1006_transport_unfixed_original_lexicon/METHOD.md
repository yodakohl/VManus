# GDT1006: release every old word assignment, retain the complete source inventory

This prospective counterfactual uses the complete already-exposed GDT993 source
packet: ZL3b f83r.18–24 has 63 groups and an uncertain sol/chedy seam; IT2a has
64 groups; RF1b has 63 raw groups but no complete paragraph contract. The latter
stays UNKNOWN_PARAGRAPH_CONTRACT. IT2a contradicts the fixed 63-group inventory;
this is a source-length consequence already available from predecessor data,
not a new discovery about spelling. ZL is searched conditionally, with its source
qualification unchanged. No normalization, omitted group, new admission, image,
reserve, f84/f84r, f116v, outside contact or independent manuscript is involved.

The search receives the 17 unchanged GDT993 productions, their 47 terminals,
and type slots. It receives NO old word-to-terminal dictionary. INITIAL and
CONCLUSION occupy the beginning and end. Every production occurs once, except
THEN twice; all other clause orders and boundaries are unknown. Each spelling
gets one global terminal value. FUNCTIONAL allows aliases; BIJECTIVE requires
the 47 spellings to receive 47 distinct values. Neither family is selected from
its performance. Every word and every position must be covered exactly once.
All three named cargos, two distinct hazard pairs and seven complete voyages
must be preserved. These source/inventory requirements are explicit assumptions
from the constructed full account, not independently recovered manuscript facts.
They are substantially stronger than the grammar of arbitrary short extensions.

The original 32 variants are all retained: exclusion versus WITH, first versus
recent prior cargo, OTHER versus FIRST, goal versus current bank, copy first
versus second pair argument. Old failures under the former dictionary do not
license discarding them when the dictionary is free. The original semantic
compiler/executor remains unchanged. A source cardinality mismatch is reported
separately from binding and execution failures. Every candidate is checked under
all 32 settings, including the full physical, safety, reference and goal effects.

The finite question projects all nine repeated forms plus the two other forms
among the six inherited hypotheses: 11 word roles in all, listed before search
in PREDICTIONS.json. It also projects the entire five-setting meaning variant.
A positive tuple therefore means that a complete shared dictionary and clause
tiling exists with those role/settings values; it is not just restart agreement.
Every candidate supplies a complete dictionary and parse, not isolated examples.
All 47 word rows are reported, but the other 36 nonprojected word domains are
witnessed values only, even if the 11-role projection is exhausted. Singleton
maps/parses within a positive tuple are not fully enumerated.

Primary Z3 enumerates whole-position clause covers. For each complete map/parse,
the full 32-setting semantic evaluation is performed. Every coherent projected
role/settings tuple is saved and blocked. The exact code/parse is also blocked
for all settings, since all 32 were evaluated. Failed exact maps never block
other maps with the same projected roles. A final UNSAT proves all remaining
syntactic candidates are covered by checked failures or saved coherent tuples;
it does not prove the whole dictionary unique. An auxiliary 0–31 variant index
in the grammar represents coverage of this finite product, not a new decoder.

Each family receives 600 seconds including construction/evaluation, 10 seconds
per SMT query, at most 2048 evaluated exact maps, and a target cap of 64 coherent
tuples. The tuple cap is checked between complete 32-setting evaluations, so it
can be exceeded by at most 31. A count cap permits one final residual syntax
query within the remaining wall budget, but no additional map evaluation. All
wall, solver and count stops retain the residual alternatives as UNKNOWN. There
are two workers. No restart, retry, larger time budget or new world encoder is
licensed by a failed or unknown result.

Independent cvc5 uses boundary-flow equations with the same complete inventory
and variant coverage. It receives every evaluated exact-map block and every
positive role/settings block. Primary exhaustive projection claims require its
residual UNSAT. All saved maps and all 32 outcomes are checked using an independent
bit-state replayer and the GDT994 binding checker. These implementations share
an author and assumed source semantics; they provide no independent word meaning.

The bit replayer is copied from the old GDT993 validator into src/bit_replay.py,
with one pre-registration scope generalization: explicit cargo names anywhere
inside the current clause enter the available list, as GDT993 compile_reading
and GDT994 binding_error already specify. The old report used no valid PAIR
reference placement exposing that metadata difference. Source-only stress
fixtures did expose it before free-code search. Legacy files are unchanged;
all original 32 outcomes reproduce. The preflight additionally rotates whole
source clauses and varies all 25 PAIR argument combinations. These fixtures
check source semantics, not a newly inferred free dictionary. Projection blocks
retain alternative meaning settings explicitly. No source text is repaired.

If the residual space is exhausted, report complete role/settings tuples,
invariant projected roles, remaining alternatives and cargo-label symmetries.
If it is not, report only witnessed possibilities and the precise remainder.
W/G/C are anonymous cargo identities here. Their global permutation cannot
identify wolf, goat or cabbage; source assumptions and identical consequences
must remain visible. Initial/end templates can themselves force roles, so an
invariant is not automatically a contribution of physical meaning. Productive
word formation remains unexplained. No significance, likelihood, recovered
historical language or confirmed word is claimed; all meaning capacity is zero.
