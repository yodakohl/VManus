# GDT865 preregistration

Decision made 2026-09-06 after a static feasibility audit of GDT808 and GDT858,
before new target queries or refits. The earlier scores and holdout counts are
already public and known. This is retrospective robustness, not blind discovery.

The complete contract is METHOD.md and src/SPEC.json. Source and protocol hashes
are sealed in src/PREREG_LOCK.json before execution. A public commit precedes
all new target reads and fits. Implementation and validation are independently
authored; they share this public contract, not hidden outcome expectations.

The current-route prohibition of an automatic rerun is resolved by this explicit
one-factor comparison and predeclared consequence: strengthen only the specific
primary structural transfer threshold if it survives, otherwise withdraw that
whole-leaf transfer claim for the affected axis. No semantic route is reopened.
