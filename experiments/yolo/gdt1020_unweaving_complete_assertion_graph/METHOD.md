# Complete assertion and reference interpretation

Input is exactly the33groups in the unchanged P12projection of f83r.25–30.
The24guesses and8production shapes come from RAW404/483; no defaults or learned
word values. Primary parsing enumerates whole-stream covers forward; independent
parsing uses a reverse endpoint chart. Neither receives stored expected clauses,
line-based switches or English story output. All surviving parses are retained.

Typed graph construction keeps action types distinct from asserted actions,
prospective prerequisites/endpoints distinct from achieved states, habitual
assertions distinct from individual physical occurrences. Current cloth and
worker are resolved only from written compatible constructions. The public
recipient is an explicit unfilled role until the later audience noun binds it;
the prerequisite worker is bound by the later pledge. No hidden event is added.

The original direct pairing assigns the first written phase to the first
ordered action type, the second phase to the second. The separately frozen
reverse fork exchanges these assignments only. Both retain the same24values,
all clause shapes, participants,concealment,discovery time and final action.
Discovery at night does not require that its referenced action happen at that
instant. That stronger caught-in-the-act meaning is absent from the old draft.

The final completion resolves the latest prior UNTILendpoint with matching
action kind,worker and cloth. RAW483 explicitly adds this operational binding
clarification. Missing endpoint or actor is reported,not invented from a story
label. Shared FINISHED can be a prerequisite and prospective goal without being
asserted true; only the bounded final action asserts its reached endpoint.

The graph has strict prior-period-before-discovery and discovery-before-final
edges; report is not later than discovery. Phase labels do not impose numerical
clock values. A returned integer rank is a witness to partial precedence,not a
date or a physical weaving schedule. No row amounts,cycle count,exact cancellation
or marriage event is supplied. Secret status before discovery and knowledge at
discovery have different scopes; report alone is not treated as proven knowledge.

Independent verification resolves references by relations on earlier production
records,checks all generated graph facts,assertedness,role closure and temporal
transitive closure,then compares whole position ownership and inverse rendering.
It imports neither the primary parser/constructor nor its time solver. Both
implementations have the same root author and inspect the same exposed input;
this is implementation validation,not independent meaning evidence.

Source scope is separate: diplomatic salche'dy and saii@208; are outside the
fixed lexicon. Their full six-line33group record remains intact. They receive
SOURCE_FORMS_UNBOUND,not a semantic failure or an assumed alias. The two projected
candidates are the only fixed semantic trials. Source-consistency of the direct
schedule with the known Homer narrative cannot empirically select that schedule.

All24values and9grammar conditions remain assumptions;17guesses/16singletons
were added in the old manual draft. No productive word morphology is explained.
No whole-search null,confirmed word,independent confirmation or reserve use.
