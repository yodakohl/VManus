# Fixed whole amulet instruction account

Consume all nine declared line productions and every projected group of
f83r.9–17. One meaning per whole form, including all repeated occurrences.
The words, line boundaries, nine productions, goal scope, actor/reference
conventions and explicit two-branch choice are freely guessed and fully disclosed.
No component code, source-language identification, alternate spelling or deletion.

Compile the whole declaration block before execution. S is one physical stone,
G one seed, P one physical bird-part role, I one depiction hosted by S, A their
planned assembly and W one addressee/wearer. The kind of P's source body B is E
or H. Image content and material provenance are separate typed uses of OF;
the body record is provenance, not an additional ingredient. Both WITH forms
have the same binary accompaniment meaning, with fixed material-group and carrier
arguments. Repeated referents never create copies. Exact wing/feather-tip anatomy
is retained as unresolved.

Declarations create planned components and required UNDER relations, not actual
placement. C01 TAKE obtains S; its GOAL quotes the same ENCLOSE operation later
used by C08 and has no physical effect. C03 engraves and checks seed availability;
C04/C05 preserve carrier/image and declare the material pose. Exactly one of
C06/C07 executes, with the written availability check on C06 and intrinsic
availability/pose/type checks on both placements. C08 encloses only the completed
assembly and then wears it. C08/C09 append three attributed effect claims and
close the first alpha section. No health/social-success state is created.

Physical operations are atomic: collect all failed preconditions, leave the state
unchanged, retain the failed step and unexecuted tail. Store every before/after
state. Goal quotation, preparation checks, declarations, physical steps, attributed
claims and textual closure remain different kinds of effect. Baseline simulation
is conditional on following the proposed instruction, not evidence of an actual
historical act or an amulet's efficacy.

Independent validation uses a reverse recognizer and set-valued fact transitions,
not the forward dictionary-state transition functions. Compare compiled roles,
plans, every state, errors, claims, preserved identities and unexecuted steps.
Both implementations have one author and exposed sources; agreement is code
verification, not an independent meaning test. Missing/uncertain source forms
are reported separately from semantic contradictions.

Countermodels: CHOICE_IN_IMAGE leaves the physical part eagle-derived and lets
E/H select the engraving instead. Ordinary physical preconditions remain; compare
the original fixed content obligations separately, so a different coherent story
is not called physically impossible. EARLY_ENCLOSE performs the quoted operation
before assembly. IMAGE_AS_PART substitutes I into the material argument.
BOTH_BRANCHES performs E and H placements in order with one existing P/G and
no replacement or new ingredient. Preserve provenance and loose-material checks;
no countermodel is rescued with added objects or delayed enclosure. Report all
ten branch/model cases. These are fixed semantic diagnostics, not a calibrated
null, independent manuscript translations, or evidence selecting the source.

Three raw ZL groups remain unmapped; the 83-group IT paragraph also has unresolved
joins/forms. The entire scope was already exposed. No new source, page/image,
reserve, contact or GDT388-scored relation evidence. All historical claims stay
attributed; I.1.1–38 remains a source reference, not a newly enumerated disease set.
