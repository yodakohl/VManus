# Artifacts

`SOURCES.json` is the frozen source manifest. Root-owned
`ROOT_OBSERVATION.json`, `B_OBSERVATION.json`, and (after adjudication)
`RESULT.json` are preserved as exact JSON packets. Runtime images stay local.
The scripts print compact validation results and do not create a result or
change native observation packets.
