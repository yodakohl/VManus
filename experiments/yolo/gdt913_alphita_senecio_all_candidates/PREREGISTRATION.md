# Registration

METHOD.md is the prospective contract for this explicitly requested follow-up.
PREDICTIONS.json and its hash-bound source inputs must be committed and published
before the first selection-body access. SELECTION.json must be frozen separately
before confirmation-body access. GDT888 remains byte-for-byte unchanged.
