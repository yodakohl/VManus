# GDT913 — all 18 frozen GDT888 Senecio predictions

User explicitly authorizes this new follow-up. GDT888's original nonuniqueness
and refusal to use held bodies remain unchanged. No fitter, decoder, key,
head/body prefix, source name, eligibility, paragraph boundary or transcription
rule is changed or rerun. This is a finite consequence test of its 18 saved
IT2a surface lexicons in stored list order K01–K18.

Unknown: whether their complete withheld S-row consequences actually occur in
all fixed-head paragraphs, and which ambiguities those consequences discriminate.
Success retains conditional candidates; contradiction eliminates that candidate
under this unchanged contract; insufficient independent leaf coverage stays an
explicit limitation. Smallest adequate test: all18 predictions, all matching
heads, one selection phase and one separately opened confirmation phase.
Budget45 minutes including preparation, independent audit and publication;
checkpoint after prediction registration. No automatic repair or scope extension.

## Inputs and exposure, before held-content access

Read GDT888 REPORT.md, METHOD.md, artifacts/RESULT.json, src/SOURCE.json and
artifacts/FIT_INPUT.json. The latter has odd-leaf training bodies and only
eligible even-leaf head codes/identifiers. It supplies the complete allowed
head census (239 IT2a heads on37 even physical leaves). No GDT887 SELECTED.json
payload has been opened in this follow-up before these predictions are fixed.
The source S row is exposed and intentionally supplies the prediction.
All these manuscript data were available to earlier project work; GDT887's
cache and GDT888's preparation handled complete paragraphs. Thus this is a new
computational holdout from GDT888 fitting, not historically unseen material.
Header presence and whole-paragraph eligibility were already used in that fit.

No new selector or visual page is admitted. f84 and f84r remain closed. The
previously guarded GDT887 SELECTED.json is the unchanged source cache, with
its expected hash taken from GDT888 FIT_INPUT.json. During evaluation stream
only fixed selected paragraph objects: skip every nonrequested JSON object
before materializing payloads, and verify page/physical-leaf/head/eligibility
against the frozen head metadata. Do not call GDT888.prepare or its evaluator,
which would load bodies outside the current phase. No mixed raw TSV is read.
Hash verification may read file bytes but must not interpret nonselected bodies.

## Freeze all candidate consequences

K01–K18 correspond exactly to GDT888 panels.IT2a.lexicons order. For each, retain
all six original head_forms and mention_forms plus every associated saved
training assignment and all matching eligible even S-head paragraphs. Bind the
head census hash, source hash, candidate hash, paragraph IDs, page and physical
leaf. Recompute the exact source incidence with GDT888's unchanged longest-name
rule: B=1, D=1, C=0, L=0, M=0, S=0. Every complete paragraph body, excluding its
first group only, must contain exactly that multiset of exact STA mention forms.
Other groups are unmodelled. No substring match, normalization, alias, homograph
excuse, dropping a zero-count role, subset selection or exception is permitted.

Before body access publish PREDICTIONS.json and PREDICTIONS.tsv, the full-source
bindings and this protocol. Each paragraph must satisfy all six counts. A single
mismatch contradicts the candidate for that phase; all its matching paragraphs
must pass. Counts are never pooled across paragraphs. Retain every mismatch,
including missing positives, extra positives and forbidden zero-role mentions.

Group exact observable predicates by target paragraphs and the mapping from
literal mention forms to required counts (roles sharing zero are unordered for
this purpose). Separately group the positive B/D consequences only; explicitly
show zero-form differences that prevent those groups being identical complete
predictions. Source-name labels within a fixed zero-count set cannot be
identified by this S-row; equally absent different forms can also remain
observationally indistinguishable on the tested sample. Do not infer equal full
predictions merely from the common abstract vector or eventual equal verdicts.

## Whole-leaf selection and confirmation

Using head metadata only, select the smallest-numbered matching physical leaf
for each candidate, then take their union as the GLOBAL selection leaf set.
Every paragraph on any such leaf belongs to selection for every candidate.
All remaining matched leaves form the GLOBAL confirmation set. This yields
selection f80/f104/f112; confirmation f108/f116. Shared recto/verso and panels
never enter different phases. Every candidate has selection coverage; some
have no separate confirmation. Odd training leaves are disjoint from both.

Open selection paragraphs only, evaluate ALL18 and freeze SELECTION.json and
its complete survivor list before opening any confirmation paragraph. Never
rank tied survivors. Then open all registered confirmation paragraphs, retaining
results even for selection-failed candidates as diagnostic consequences, never
as a rescue or new selection pool. A candidate is selection-compatible iff
ALL its selection paragraphs pass. Additional computational confirmation needs
at least one disjoint confirmation leaf and ALL its confirmation paragraphs
passing. No-confirmation candidates remain selection-only; a failed selection
cannot be reversed by a confirmation match. Full-contract compatibility requires
all bound paragraphs passing, with independent capacity reported separately.

Evaluate all18 regardless of early failures; no stopping after an appealing
case. Report the selection survivor set, confirmation outcomes, full survivors,
per-candidate leaf/paragraph capacity and equivalence classes of observed
predicates. Do not use confirmation outcomes to invent new candidates or rules.

## Validation and claim ceiling

An independent implementation checks all18 keys, complete head matching,
source counts, full-leaf partition and every observed occurrence/count using
only the released phase. Freeze its code before confirmation access where
possible. Validate source hashes and that all GDT888 tracked bytes remain
unchanged. Report disagreements before claiming a result.

No whole-search matched null is supplied: no significance, p-value or search
success probability. One remaining candidate, if any, is conditional on the
compiler, source selection and eligibility; restart agreement is irrelevant.
No independent botanical/semantic test is supplied: none of B/C/D/L/M/S is a
confirmed plant name. No new relation edge is score-ready or promoted without
GDT388 capacity, held-folio, provenance and mobile-null gates. This consequence
table is not a scored relation packet or a whole-paragraph translation.
