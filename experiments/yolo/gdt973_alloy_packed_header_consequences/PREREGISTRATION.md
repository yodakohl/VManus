# GDT973 preregistration — two frozen packed-account writers

Registered 15 September 2026 before any R1/R2 target-condition census. The complete R1 proposal (IDEA355) was public at 0eb1a2524 before R0's outcome; R2 (IDEA356) was drafted source-only and frozen at 14:31 UTC without reading R0 or R1 target results, then public at 76e92ed62. Neither model is changed here. GDT972's R0 rejection remains final for its stated panel.

## Decision note and inclusive budget

Unknown after primary review: whether either packed writer has any compatible complete literal paragraph under even these necessary surface consequences. Known positives: S0 evaluates whole alternative accounts; GDT971 identifies source numeral values only under known roles/arithmetic, with zero added elimination from its second path. Known limitations: word composition, whole-form residuals and entry context are established, but neither ENTRY pieces nor operator allomorphs are known meanings. IDEA124 whole-group instructions and missing physical endpoints remain; GDT882 fixed line invariants and GDT903 free automaton ambiguity remain. GDT571's inherited rendering is no operator translation license. R1/R2 add distinct modern spelling assumptions; their source examples are not Voynich data.

Root read the live route/composition topic, bounded ideas searches/show/duplicates, the alloy operator route-check and GDT972/971, S0/R0, full R1/R2 cards/code and GDT571 primary. No imported lexical hit is treated as an executed test. R1/R2 have not been tested by R0's word-boundary model.

Smallest adequate test: all complete paragraphs against the seven fixed necessary conditions below; no parser, code fitter, arithmetic solver or new controls. Inclusive wall budget 14:33–14:46 UTC covers preparation, proof review, implementation, validation and publication. Zero survivors closes that exact writer in the eligible panel. Survivors retain only header/form compatibility and identify the complete candidate IDs for a later separately bounded code/semantic test. If both fail, do not invent R3 or relax any rule. No automatic implementation chain.

## Scope and prior exposure

Exactly GDT970 artifacts/PARAGRAPHS.json, already guarded under GDT915's 179 admitted selectors, with all 1,349 complete rows: ZL659 (32 literal), IT690 (529 literal), RF0 paragraph fences. Every row is retained; original eligibility/defects and cached word-list boundaries are unchanged. No full_text resplitting or deletion; joined cached groups must equal full_text. Nonliteral rows remain UNKNOWN, not contradictions. Prior use in GDT915/970/972 makes every row project-exposed. No independent physical-leaf confirmation capacity; edition variants are the same manuscript. f84/f84r and other reserves stay closed; f116v is not admitted. No new image or external contact.

## Fixed model families

S0 semantics, bounds and complete-account ownership remain those of ALLOY_FINITE_GRAMMAR.json. R1 uses its frozen 47-component prefix-free code, R2 its frozen 57-component prefix-free code, each nonempty a–z codeword of length1–8. All operator arguments, HOLE children and program groups are consumed. The first four groups are GRADES, TARGET, TOTAL, FIRST under each writer's exact packing. No example's numerical values, number of steps or paragraph length is imposed.

Conditions, computed independently in this fixed first-failure order:

1. MIN_GROUPS: at least5 groups. This is deliberately weak and necessary; not a claim that a valid5-group account exists.
2. HEADER_WIDTH: first4 lengths in R1 [12,144],[5,56],[5,56],[3,24]; R2 [10,128],[3,40],[3,40],[1,8]. Bounds follow minimal/maximal number of components and1–8lengths: positive header numerals have1–3digits. Record all4observed lengths.
3. HEADER_ISOLATION: each of the first4complete words is prefix-incomparable with every other complete word, including equality as a conflict. These four heads occur once per complete account. All groups begin with an injective prefix-free head code (R2), or an entry code followed by such a head code (R1). Distinct head/context words therefore cannot be prefixes of each other. Record every conflicting (header index, other group index, other word), zero-based.
4. ALL_GROUP_MIN (R1 only): every group has length at least3, because entry, head and agreement are nonempty.
5. GLOBAL_INITIAL (R1 only): at most2distinct first characters across the complete paragraph, because there are only2nonempty ENTRY codes.
6. HEADER_ENTRY (R1 only): groups2,3,4 have the same first character (ENTRY1).
7. HEADER_END (R1 only): first4groups have the same last character (AGREE_S0).

For fewer than4groups, only MIN_GROUPS is evaluated; other conditions are unknown. For4groups, all applicable header/whole-group conditions are evaluated despite MIN_GROUPS failure. R2conditions4–7 are not applicable (null), not affirmative evidence. Nonliteral rows have all conditions null and no selected header. All false conditions and all prefix conflicts are retained. No criterion is suppressed because an earlier one failed. Survival is NECESSARY_FORM_ONLY. All other eligible decisions are the first failed rule. Counts include separate eligible-first-failure and overlapping all-failure totals per writer and edition, all candidate IDs and leaves. No ranking or cherry-picked matching clause.

## Validation, limits and outcome policy

Before target enumeration, encode all4complete source/generated accounts under R1 and R2 fixed source-only witness keys; all must pass. Fixed corruptions exercise each criterion without target data. An independent direct implementation reconstructs every candidate row and panel aggregate and checks the complete TSV. The source-only proof reviewer sees no target results. Source checks are not independent meaning confirmation.

A survivor is not a complete parse, shared code, arithmetic fit, source identity, operation/material name, translated word or significant result. Model selection versus confirmation cannot be separated into unexposed whole physical leaves here; independent capacity is zero. A failed model does not reject all mixtures or arithmetic. Do not erase contradictory candidates, make post-result exceptions, use restarts as uniqueness, or open reserves. No significance claim without an appropriate control of the whole search. A future semantic test needs a complete candidate reading and explicit rival consequences; no confirmed word is presumed.
