# Method

The frozen preregistration defines all seven conditions, source-only fixtures, exhaustive old-cache scope and decision ceilings. R1 and R2 are distinct writing models; neither modifies R0. This is a necessary-form census, not a parser or semantic fit.
