# GDT656 artifacts

- `RESULT.json` — compact machine-readable result, metrics and artifact hashes.
- `TARGET_DECISION_DECK.tsv` — all 21 meanings, rivals and decision bases.
- `AL_QUALITY_LATTICE_ATLAS.tsv` — 44-cell observed/absent/held AL atlas.
- `LOCAL_EE_SISTER_EVIDENCE.tsv` — the bounded OEEAL/OEEAR analogy and its
  missing or unstable rivals.
- `BOUNDARY_EVIDENCE_ATLAS.tsv` and `PAIR_CONTRAST_COUNTS.tsv` — direct reader
  splits and within-line contrasts.
- `ALL_OCCURRENCE_SEMANTIC_AUDIT.tsv` and `READER_VARIANT_AUDIT.tsv` — every
  target occurrence and all reader warnings.
- `CURATED_COMPLETE_PASSAGE_READINGS.tsv` — the three newly complete concrete
  readings with reader notes.
- `SOURCE_PASSAGE_REALITY_CHECK.tsv` and `AFFECTED_LINE_TRANSLATIONS.tsv` —
  manual examples and all 546 affected lines.
- `ROUND_COVERAGE_COUNTS.tsv`, `NEWLY_COMPLETED_LINES.tsv` and
  `NEWLY_EXPOSED_ONE_HOLE_LINES.tsv` — sequential coverage effects.
- `V33_WORKING_TOKEN_GLOSSARY.tsv`, `WORKING_DICTIONARY_V33.tsv`,
  `ALL_LINE_CONCRETE_COVERAGE_V33.tsv`, `COMPLETE_PASSAGES_V33.tsv` and
  `ONE_UNKNOWN_PASSAGES_V33.tsv` — the complete reproducible V33 edition.
- `PAGE_ALLOWLIST.tsv` — inherited 179-page safe selector list.
- `HISTORICAL_SUBDEGREE_COMPARATORS.tsv` — compact links to the 1415
  architecture comparators.
- `VALIDATION.json` — independent release validation.
