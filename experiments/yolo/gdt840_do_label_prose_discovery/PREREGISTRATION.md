# GDT840: do label/prose discovery

Known motivating example: GDT811 ofaldo label f88r.25, ofal prose f88r.30 and f108r.14. Shared beginning and rarity already known; immediate prose contexts differ. Root revisited admitted f88r image for orientation, not a new owner or glyph annotation. GDT460/461 concern other calibrated stems; targeted route audit finds no specific do label/prose test. Discovery, not unseen confirmation.

New question: does the exact complete Xdo-label / X-prose surface contrast recur for other bases? Use all179 admitted GDT631 text selectors, before payload rejection of f84/f84r, unchanged GDT829 query/records/opaque atoms. All three editions separately; no independent-witness pooling. A label is raw kind L with exactly one source group; prose raw kind P. X has at least two opaque atoms; terminal d,o must be separate ordinary atoms. No normalization or relaxed gaps. Whole groups only; no split of Xdo into two manuscript groups.

Save every Xdo label and every bare-label/reverse-prose candidate, including zero matches, source IDs and same-page matches. Derive candidates from the union of all complete raw Xdo forms, then retain rows with either Xdo or X as a singleton label. Count bare and extended prose for both directions. Report all bases; known ofal has no discovery credit. At least one additional forward base supplies replication capacity only; absence stops this contrast. No p-value, causal suffix, same referent, naming or translation claim. Opposite-direction and unpaired cases remain visible. No new page image, no held confirmation. Existing f88r visual orientation does not assign any label to a plant species.

Smallest test is one guarded census and independent reconstruction from compact saved source occurrences; source extraction shared and byte-replayed. Total target5min from02:28:30 UTC including publication; finish this census without tuning, even if overhead exceeds target. Positive capacity would justify a separately specified contextual/visual contrast, not word promotion. Inconclusive means retain only source observations, no larger pipeline.
