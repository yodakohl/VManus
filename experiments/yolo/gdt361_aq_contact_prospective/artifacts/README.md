# GDT361 artifacts

- `gdt361_selection.tsv`: source-only seven-locus target and `.10` exclusion.
- `gdt361_freeze.json`: corrected target/prediction freeze, binding the
  superseded wrong-canvas freeze hash.
- `gdt361_localizations.tsv`: official-canvas crop coordinates and hashes.
- `gdt361_visual_observations.tsv`: direct CONTACT/GAP/UNCERTAIN calls.
- `gdt361_visual_freeze.json`: hash binding before formal reveal.
- `gdt361_formal_reveal.tsv`: guarded family join after visual freeze.
- `gdt361_counterexamples.tsv`: explicit failures and provenance limits.
- `gdt361_result.json`: compact score and access/accounting record.
- `gdt361_validation.json`: independent retained-row arithmetic and hash check;
  it is not a second visual review.
