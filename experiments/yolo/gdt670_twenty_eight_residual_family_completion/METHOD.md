# GDT670 method

## Question

Can every occurrence of the 28 first-occurrence-ordered V46 residual surfaces be assigned a concrete, executable workshop default while preserving the inherited spelling roles, reader evidence, and complete-line order?

## Frozen scope and inputs

The source frontier is GDT669's `NEWLY_EXPOSED_ONE_HOLE_LINES.tsv`: 28 unique surfaces, 91 positions, 90 lines, and 64 pages. The build uses the unchanged 179-page safe panel and guarded materialization of only required columns from the ZL3b token and three-reader line sources. `f1r` is outside the exact allow-list; `f84` and `f84r` are forbidden.

The semantic base is V46: dictionary 2,071, glossary 1,387, 4,128 physical lines, 23,997 known and 8,342 unknown token positions, 1,191 complete multi-token lines, and 170 one-unknown lines. GDT659–GDT669 occurrence audits and the Y census preserve inherited occurrence-scoped behavior. The final synthesis compares three independent candidate lenses—practical apothecary, stem compositor, and passage reader—then applies the final card and passage audits.

## Card construction

Every target receives one exact-surface German default, composition, rival, and family. Twenty-five cards use only the unchanged 56 V46 roles. Three forms remain learned exact wholes because forced composition would add or reorder operations: `oschotshl`, `qetal`, and `secheeol`.

No substring dispatch is allowed. The only short form is `ka = K_HOT+A_PART_OR_LINK`, default “ein erhitzter Teil.” It remains free at f32v.3. At f40r.10 only, both alternate readers join visible ZL3b `ka ar` as `karar`; that locally attested alias renders “erhitzte Wurzelfraktion I” without inserting the reader's extra `r` into the productive card.

The 19 OL and two SOL rendering specifications are copied from GDT669. GDT670 does not revise them: OL descendants retain the inherited `O_PREP+L_WOOD` renderer at 256 positions, and the two SOL forms retain `SOL_SEED_PREP` at four positions with the salt rival recorded.

## Complete-line audit

The builder reconstructs every line from the V46 token arrays, changes target positions only, preserves non-target rows by identical SHA-256 before/after hashes, and emits deterministic automatic translations. Twenty manually selected complete lines are kept as a separate prose layer. The final passage audit accepted 16 unchanged and supplied four exact corrections:

- f81v.1 restores directed `shey` and `keedy` stages.
- f111v.50 restores directional `chey` rather than a static stage noun.
- f83r.55 keeps inherited free `sol` as “Samenmaterial/Saatgut,” not “Saatgutansatz.”
- f10v.3 removes an unlicensed extra finalization after free `dy`.

## Hard checks and determinism

The builder binds source order and counts, the 90-line/64-page census, the double `otair` locus f105v.30, the three learned wholes, OT and SHK/KCH families, `qoiiin`, initial `olaiiny`, the `ofal` split, repeated `O_PREP` in `otodar`, and the exact one-free/one-merge `ka` distribution. An official run rebuilds in a temporary directory and compares every output byte plus `RESULT.json`.

## Decision rule and claim ceiling

Pass requires all 91 positions concrete, zero unfilled target slots, unchanged non-target token records, projected V47 coverage counts, all 20 manual lines complete, and exact replay. Passing licenses a replaceable recipe-register working theory only. Structural tags remain distinct from German meanings; no mapping is a confirmed Voynich lexeme or translation.
