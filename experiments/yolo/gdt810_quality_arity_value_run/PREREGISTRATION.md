# GDT810 — two qualities, two following values?

The known f32v.8 span `otchol daiin daiin` motivates this outcome-aware
exploration. If the inherited whole otchol candidate is cold-and-dry, could
the following two values assign a grade to each quality? Neither meaning is
assumed true. Disclose the discovery line; exclude it in one summary and its
entire page in another.

Before further source extraction, fix all54 whole surfaces of the GDT628
OL/OR matrix. Old quality_core NONE gives arity0; k/t/ch/sh give arity1;
kch/ksh/tch/tsh give arity2. These are hypotheses about complete words, not
decoded components. OL is the proposed quality class; OR/arity0 are controls.
Values are exactly dan,dain,daiin,daiiin. Count their maximal immediately
following run without crossing a physical line. Compare single/multiple
values only where all three readings agree on head/run/stop, the run has
at least one value and stops before line end. Export zeros and censored rows.

A provisional arity lead needs multiple-value examples on at least two pages
outside f32v and a higher conditional rate for OL arity2 than OL arity1.
OR controls must always be shown; similar OR behaviour weakens specificity.
This is exploratory, without significance or semantic-identity promotion.
Absent examples constrain this compact format, not the whole quality theory.
Shared grades and separately stated grades remain historical rivals.

Novelty check: GDT627 compares thermal/moisture values, GDT628 builds the
carrier/value lattice, GDT686 dispatches degree versus amount. None of the
inspected methods tests this specific post-compound value-run arity rule.
No new page or image; inherited179 selectors; f84/f84r forbidden.
