# Artifacts

HEAD_VALUE_RUNS.tsv retains every exact head, maximal following value run,
next stop and alternate-reader support. SUMMARY.tsv contains all18 declared
strata, including zero cells and both discovery exclusions. RESULT.json binds
the five builder outputs; VALIDATION.json records independent reconstruction.
The GDT388 packet is empty and not score-ready. No semantic identity is selected.
