# Reproduction artifacts

PREDICTIONS.tsv was public before evaluation. CANDIDATE_TABLE.tsv and ALL_CASES.json
retain all 1,071 cases. NECESSARY_CASES.json contains first-stage certificates;
EQUIVALENT_OBSERVATIONS.json groups only equal observations. RESULT.json preserves
whole-scope uncertainty. VALIDATION.json is the independent 14-check replay.
Run the commands in experiment.json; compute UNKNOWN may depend on machine timing.
