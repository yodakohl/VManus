# GDT741 — local attachment boundary/relay grammar

Status: `PASS__ID_FREE_GRAMMAR_REPLAYS_13_OF_13_OVERRIDES__ZERO_103_ROLE_FLAG_ERRORS__EIGHT_OF_EIGHT_RESULT_MODES__TWO_SINGLETON_RELAYS_EXPLICIT__SIX_OPEN_COLLISION_ROLE_CANDIDATES__NO_NEW_RENDER_CHANGE__ZERO_LEXEME_OR_COMPONENT_EXPORT__NO_NEW_PAGE`

GDT741 replaces GDT740's thirteen occurrence-ID overrides with an executable
feature grammar. The dispatcher receives no target ID and no locus. It uses
only the current whole-field tags plus local direction, flank, boundary,
single-host coverage and middle-cell continuity.

The grammar reproduces all thirteen former overrides, all 103 binding-contact
role flags, all eight former `PROCESS_RESULT` candidates, all 202 renderer
patches and all twenty cached passage renders. The active renderer is therefore unchanged:
36 axis-specific positions, 43 carrier-bound positions, 56 specific positions
and 146 open positions.

This is an ID-/locus-free in-sample compression gain, not yet transfer
validation. Six of the role rules trigger on only one target. A deliberately looser relay detector
finds six additional roles on five open targets; they remain visible as
`OPEN_COLLISION` and receive no renderer licence.

See `REPORT.md`, `METHOD.md`, `PREREGISTRATION.md`, and
`artifacts/GDT741_BOUNDARY_RELAY_GRAMMAR_READER.md`.
