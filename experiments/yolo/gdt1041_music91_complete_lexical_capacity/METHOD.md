# Fixed complete-unit lexical capacity

Bind SPEC, all91 unchanged full entries, the native928 paragraph cache, current guarded179-selector projection and all code before execution. A public registration receipt gates both implementations. Do not alter a locked file after execution.

Enumerate every native complete paragraph in the safe hash-bound source. Before processing a paragraph body assert its selector is admitted, its raw page is not f84/f84r/f116v, and its physical leaf agrees with the page. Check group/ID lengths and native first_start/last_end metadata as available in the retained source. No source normalization, aliasing, new values, word splitting, missing-group discard or minimum-length threshold. All known and unknown positions count. Partition every unit on physical76/83 as DEVELOPMENT_PHYSICAL_LEAF; all other admitted units as OTHER_ADMITTED_EXPOSED_LEAF. No per-reader or per-page winner selection.

Primary compares each literal whole to the fixed dictionary. Separate validator reconstructs a type-to-occurrence index and sets before expanding all missing occurrences. Save all rows, full candidate counts/flags and every capacity survivor. No survivor is interpreted by the old locus-specific recognizer. Classification is FULL_LEXICAL_CAPACITY or MISSING_FIXED_VALUES, never semantic contradiction. All new values/rules/semantic executions are0.

Report native-reader, physical-partition, strict-anchor and complete-unit totals without treating these overlapping views as independent observations. If no foreign-leaf survivor, close the unchanged inventory capacity route. If any, retain all for a separate grammar/consequence decision; no immediate decoding. Controls are implementation checks only; no search-wide significance or confirmed word.
