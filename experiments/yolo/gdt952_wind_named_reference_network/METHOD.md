# GDT952 fixed wind-name reference network

The hypothesis is a twelve-entry wind register with a stable complete name-code, and the8explicit off-diagonal named references in WaltersW.73f2r. One bijection maps each source wind to a physical Voynich sector, jointly owning its outerT1title and wholeQ1body plus same-sectorM1tail. All12!assignments are considered. No circular rotation, transcription choice, reference direction, source alias or match rule is fitted. This is a hypothetical concept-code register, not a claim of directly written Latin. Inflected and synonymous source names need not share a literal word; stable rendering is an explicit additional assumption.

The Stolfi comments fix12sector owners before new target text intake. The08:30wrap is title.12/body.48–.51; title.1 owns09:30body.13–.15. All51loci belong to the twelve records; laterM2/M3 and the three horizontal lines.72–.74 are excluded prospectively. AttachedM1tails are included generously; no reference missing from Q1can be rescued later by adding another ring.

Two fixed models are tested separately in all3alternate readings. LITERAL compares complete raw title group sequences to every contiguous body window. LEGACY_ROOT_EXACT uses the existing cached root_sequence, preserving each +compound as one group. It requires exact alignment of cached surface to raw groups, and never repairs entities, drops groups or fits morphology. The original parser module is absent locally: this second model tests the published cached representation, not independent regeneration or a demonstrated linguistic lemmatizer. The earlier pre-grounding builder was read to establish that spaces separate groups and +joins root components. Every title group must match; no partial-name selection.

The raw source group census defines uncertainty. Nonliteral groups and ambiguous seams do not become negative evidence; a title with any uncertainty allows all upper edges. Body windows with unknown tokens can supply possible upper matches; ambiguous seams/count or unaligned cached roots prevent a definite absence. The upper graph treats unknown edges independently, so its compatible assignments are an upper bound and can overstate any coherent completion. Lower graph uses only fully observed matches. All144title/body cells, including unscored diagonal cells, are reported permodel/reading (864total); source obligations are off-diagonal only.

Exact enumeration factors the source graph into components of6,3,2,1vertices. Enumerate injective component embeddings into12sectors, then combine disjoint used masks. Report counts and every name/sector marginal. This covers479001600possible whole assignments percase without restarts. A zero upper count proves this conjunction incompatible; a positive lower count establishes conditional compatibility only; upper-only surviving counts are unresolved. Different names/forms/assignments with equal observations remain ambiguous. No graph score, nullp-value, GDT388eligible relation or word meaning is claimed.

One previously exposed physicalleaf supplies the entire target. It cannot supply independent candidate selection and confirmation. No withheld leaf, f84/f84r or f116v is opened. GDT888/GDT913 and the old failed W73/rootconcordance contracts remain unchanged. Exact old fullreports are missing locally; imported summaries were audited as such. This source-name-reference test differs from their semantic-effect similarity statistic; it does not reopen that statistic.
