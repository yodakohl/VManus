# Post-exposure separator-enum correction

The original frozen code/specification compared separator fields with the raw transcript dot rather than the table enumDEFINITE_SPACE. This marked every multi-group line uncertain and returned6entirely unresolved cases. Those numbers reproduce the code but are an INVALID_TEST of the intended definite-space contract, not a manuscript failure or evidence of poor readings. Original lock, code, specification and result artifacts remain unchanged.

The intended test uses definite transcription spaces. The longstanding source table encodes them asDEFINITE_SPACE (also explicitly selected in precedingGDT951src/SPEC.json). This is a source-schema correction, not a changed decoder, name rule, match width, source graph, sector assignment, case collapse or exclusion. run_corrected.py differs only in this enum, its docstring and output destinationartifacts/corrected. Input stays byte-identical. Every originally declared candidate and prediction is retained. The corrected diagnostic is post-exposure and does not acquire fresh preregistered or independent status. Its status supersedes the misleading manuscript-level interpretation of the uncorrected outputs, not their frozen bytes.

Decision note: spend at most5additionalminutes on this exact enum correction and comparison, no further broad repair; a corrected incompatible result rejects the intended exact invariant-rendering conjunction, a compatible/unresolved result leaves that finite candidate set for review. Validation must verify the precise diff and both output sets.
