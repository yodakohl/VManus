# GDT808 — exact relation slot residual bridge

Status: `REGISTERED_UNSCORED__OUTCOME_AWARE_RECTANGLE_DESIGN`

GDT808 tests one concrete compositional question left by GDT807: whether the
two exact changes in

```text
X-eol : X-ol  ::  X-eody : X-edy
```

are reusable across unseen carrier families, whether they are the same
relation or two different relations, and whether either lives in the local
slot rather than only in paragraph topic, formula, register, or learned-whole
ecology.  The primary CORE13 population contains 1,777 reader-stable focal
events.  No new page is opened and `f84`/`f84r` remain forbidden.

The experiment always reports the leading structural and historical role
rival.  A positive result can license only a formal reusable slot-side
relation; it cannot by itself turn EVA `e`, `o`, `ol`, `edy`, or a carrier into
a word, morpheme, sound, or translation.

See `PREREGISTRATION.md`, `METHOD.md`, and `experiment.json`.
