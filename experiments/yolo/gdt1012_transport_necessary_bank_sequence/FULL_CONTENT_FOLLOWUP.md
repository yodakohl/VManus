# Exact scope for a possible full-content follow-up — not registered or run

The current failed bank projection does not justify another added-bit sampling
loop. A complete-content formulation would change the scientific decision:
SAT would give an actual original-plus-f50rreading with shared values; exhaustive
UNSAT would close this original extension within the existing complete context
census. UNKNOWN must remain open. Productive word structure and independent
meaning would still remain unsolved even after SAT.

The candidate table already reduces the complete question exactly. Only chedy,
otaiin,qokedy,qoky are shared with either f50rreader. Cargo renaming is a symmetry
of the entire fixed grammar,reference resolver and physics. Set qokedy=C.
Then chedy is either another cargo G or FIRST_CARGO. Keep all variants valid in
at least one original candidate of that class:8for named chedy,2for FIRST.
With two readers this gives20canonical full-content queries instead of312
original-map/valid-setting/context cases. No original candidate is discarded.

[NEXT_FULL_CONTENT_CASES.tsv](artifacts/NEXT_FULL_CONTENT_CASES.tsv) lists the20
prepared inputs. JSON preserves all312members and all invertible cargo maps.
FIRSTclass has two inverse maps per member, both retained. A positive must be
lifted only to members whose original variant is valid, merging their entire
unchanged47word code,then replayed in both full executors. The grouping script
independently reconstructs membership and inverse mappings. It runs zero new
target queries; it supplies no new exclusion or positive reading.

GDT1003's world solvers CANNOT be reused by removing a length assertion. Their
PAIR/COPY omission is proved only for paragraphs shorter than38groups;f50rhas
89/93. They also fix FIRST rather than covering the RECENT settings now retained.
A proper full formulation must cover exactly the unchanged GDT993 behaviors:

- Prefix introduction of unique named cargoes; FIRST/RECENT means first/last
  *introduced distinct* cargo,not latest repeated mention. Future names belong
  to initial physical inventory but cannot repair a backward reference.
- PAIR's two distinct resolved arguments and persistent last explicit pair.
  COPY uses the old pair plus OTHER/FIRST and does not overwrite last_pair.
- Every declared hazard pair applies to every actual state,including states
  before its written clause; hazard assertions are not transport actions.
- Agent,boat,cargo availability; OUT/RETURN/FERRY directions; EXCLUDING load
  alternatives; no trip after FINAL; unloaded STAY,RESULT and whole-goal checks.
- Both OTHER variants, both FIRST variants where retained,and both THERE
  variants. No new EXCLUDE=WITH or COPY=SECOND variant is needed: none survived
  original full content. These are inherited exhaustively established limits.

Smallest adequate new test would be all20systems with independently checked
full world encodings and unchanged executors,not another component/word sample.
Before implementation it needs a separate registered decision,fixtures covering
PAIR/COPY/safety/RECENT,and an inclusive budget. This file is a scoped proposal,
not permission to repair the frozen experiments or consume reserves.
