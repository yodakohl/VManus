# GDT613 artifacts

- `RESULTS.json`: final pre-world stop and query fingerprints;
- `VALIDATION.json`: executable validation ledger;
- `structural_queries.tsv`: strict, qok-family sensitivity and one-gate-relaxed solver results;
- `raw_leaf_merge_bound.json`: supplemental exact 60/64 raw-leaf upper bound;
- `relaxed_control_witness.json`: fixed SAT control showing the removed card-child gate;
- `grammar_scope_bridge.tsv/json`: exact versus diagnostic core-run legality on the old planted world;
- `grammar_scope_failures.tsv`: complete labelled failure rows for that bridge;
- `length_deck_feasibility.json`, `length_deck_ngram_capacity.tsv` and
  `wholeform_envelope_capacity.tsv`: collision/exposure capacity bounds;
- `compiled_model.json`, `length_card_profile.json`, `PREPARED_MANIFEST.json`
  and `reference_split_manifest.tsv`: compiled contract and source bindings;
- `reference_splits/`: the four preregistered disjoint Latin blocks.

No artifact contains a Voynich target sequence, target fit or f84/f84r material.
