# GDT503 — four-open-edge long-chain recovery

Status: `BOTH_OPEN_RECIPES_HAVE_ONE_OLD_DIRECTIONAL_CHAIN__DIRECT_AND_SEPARATOR_SUPPORT_DISTINGUISHED`

GDT503 corrects the overly broad “atomic-only” shorthand left by GDT501 for
the four remaining `CH+CHD` and `CH+OK` target cells. Neither pair occurs as an
exact two-head recipe, but each directed pair occurs once inside an older,
longer action chain: `CH>OK` as direct component adjacency and `CH>CHD` with a
visible `EE+D_ADDR` separator.

Start with `REPORT.md`; the four corrected cards and both exact old clauses are
in `artifacts/`.
