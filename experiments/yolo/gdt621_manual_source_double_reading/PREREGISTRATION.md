# GDT621 manual source double-reading preregistration

Registered: 2026-08-29

Status: `DOUBLE_READING_PROFILE_REGISTERED__NO_SOURCE_IMAGE_OPENED`

GDT620 acquisition code is bound to registration commit
`61a253ce2756ad06a6c69c620e702500f5e640ef`; its result was published
separately in commit `798e05f46e79c4abd2047577669d3a67d561ec51` as artifact
SHA-256 `f14976f54fd4ea0424ada9f23d19e7f02424beff739f5b4943dd3b0329ae378e`.
The ten GDT620 JPEGs are frozen by filename, SHA-256, and dimensions in
`artifacts/REGISTERED_READING_PROFILE.json`; no private directory is recorded.
Registration performs zero network requests and opens zero images.

Reader A and B work in separate agent/rendering sessions with distinct session
IDs and see only opaque DEV IDs plus private JPEGs. Known headwords are locator
hints, not a blind discovery endpoint. Profile, repository, catalog, edition,
network, other sources, and other-reader material are forbidden and attested.
Each submits the diplomatic rubric and exactly twelve following tokens. Outputs
stay only in root-mailbox storage, never the shared workspace, until both
canonical, hashable five-page submissions are immutable.
The opaque packet is canonical JSON with exactly `opaque_candidate_id`,
`source_sha256`, `session_id`, and a path-free `opaque_rendering_handle`; no
extra key is allowed. Its nonrecursive SHA-256 and exact keys are attested. It
excludes headword, witness/control identity, URL, crosswalk, expected rubric,
repo/profile locator, filename/path, and other submission.
The handle matches `^R[AB][0-9]{2}-[0-9A-F]{16}$` and carries no semantic text,
path, URL, or headword.

The main text block is read top-to-bottom then left-to-right; marginalia and
image labels are excluded. Rubric end is its visible transition to main text.
Tokens are maximal non-whitespace runs; line breaks are whitespace and
punctuation/abbreviation signs remain attached. Controlled literal uncertainty
notation permits no silent expansion or normalization.
`GDT621_DIPLOMATIC_V1` preserves exact UTF-8 without Unicode normalization and
uses whitespace-free `<SIGN:...>`, `<UNCERTAIN:x>`, and `<UNREADABLE>` tags.

Reconciliation audits every codepoint, spacing decision, abbreviation mark,
and token boundary through an explicit difference ledger. Only afterward may
the canonical `LATIN_RECONCILIATION_FROZEN__CLM_UNOPENED` checkpoint be publicly
committed. Only then may Clm pages open as controls; Clm cannot alter Latin.
The irreversible order is A five rows/verified commitment, B five/verified
commitment, both commitments frozen, reconciliation, frozen artifact validation,
then public commit-and-hash verification, then Clm.
Each reader contributes exactly one ordered DEV01–DEV05 bundle, every page once;
bundle SHA excludes its own field, source hashes match the Latin bindings, and
no recursive per-page submission hash exists. The nonempty checkpoint exactly
contains experiment/status, GDT620 binding, both bundle hashes, five reconciled
readings, complete ledger, adjudicator audit, canonicalization, claim ceiling,
and nonrecursive hash. Final result commit/SHA binds it and reuses its bundle
hashes, Latin readings, and ledger byte-identically; no later Latin change.
Before hashing, adjudication attests that only frozen bundles and the five Latin
JPEGs were used; Clm/network/repo/profile/catalog/edition/other-source/Voynich/
f84/f84r access counts are zero.
Full-page display precedes temporary scaling or zoom. Source pixels and stored
source JPEG bytes remain immutable; only temporary in-memory renderer output
may resample for display. No derivative persists and no rotation, enhancement,
annotation, or source rewrite is allowed.

Canonical artifacts use UTF-8 sorted-key compact JSON, final LF, and SHA-256.
The sole canonical access-audit schema binds reader/session/page/timestamps and attests input/source
restrictions, full-page-first, mutual blinding, and automation use.
It has exactly ten ordered Latin and five later Clm events with source hashes,
required true nonaccess/checkpoint attestations, false automation/Latin-change,
and zero target/Voynich/f84/f84r access. Hash preimages omit their own hash field.
Every Clm event records checkpoint commit time and must open strictly afterward.
Public artifacts exclude absolute/private paths and image bytes; reader results
use IDs/hashes rather than filenames.

OCR, automatic image/text processing, Voynich target access, f84, and f84r are
forbidden. The result schema and intermediate/terminal statuses are frozen in
the registered profile.
