# GDT484 complete microrecord support-provenance edition

## Outcome

All 135 fixed records now have one current reading and one strongest support
route in the same source order. The tier distribution is:

| strongest support | records |
|---|---:|
| recurrent strict whole record | 28 |
| recurrent whole-record role shape | 23 |
| all events strict-recurrent | 4 |
| at least one event strict-recurrent | 19 |
| all events role-recurrent | 6 |
| at least one event role-recurrent | 7 |
| same-model component tile | 42 |
| model-free component backoff | 3 |
| exact running carrier | 1 |
| learned lexical slot | 2 |

Thus 51 records have whole-record support, 36 more have event-fragment support,
45 more are completely built from recurrent components, one is closed by an
exact running carrier, and two retain only expected learned lexical content.
Every record has a concrete default and zero record retains unexplained
functional matter.

## The missing three multi-event tails

GDT482 intentionally tiled only 45 one-event records. GDT484 closes the other
three GDT481 fragment tails:

- `G475-R074`, `ochey|fydy`: 2 events, 7/7 same-model components;
- `G475-R084`, `aral|oletal`: 2 events, 5/5;
- `G475-R107`, `okchshy|qkol|oldam`: 3 events, 8/8.

Their seven events contain twenty components. Ten components fall inside
recurrent multi-component fragments and the other ten recur as atoms. No local
component remains.

## What the edition now says about uncertainty

The support tier tells the reader whether a phrase repeats as a complete card,
as a role frame, as smaller events, or only as components. It does not erase
the exact text. In particular, the two tier-10 records still say something
concrete: `cheosdy` has a learned family/name relation, and `saloiinsheol` has
three learned drug entries inside a target/hold/continue frame.

Only `G475-R125` changes its displayed German, inheriting GDT483's smoother
sentence: “Wähle den Eintrag und markiere ihn – als Ausführung auf der zweiten
Stufe.” Its literal four-component back-projection is unchanged.

No meaning, model, boundary, surface, recipe, event, or page changes.
Validation passes 103/103 checks.

The useful next step is a fluent, reversible German layer for all 135 records:
smooth the remaining mechanical repetitions while retaining each literal
component trace and this support tier beside it.
