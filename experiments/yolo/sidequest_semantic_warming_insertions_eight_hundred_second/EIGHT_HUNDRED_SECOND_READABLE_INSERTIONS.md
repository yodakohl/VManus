# Sidequest Pass 802: missing warming cells in real statements

The three missing `CHK` cells are inserted into the nearest existing statement
frames. These are workshop predictions, not claims that the unseen cards occur
on the selected pages.

## CHK-P01 — short and closed

Source `f83r / B3-S002`:

> Danach zur Zielstelle; dann länger wärmen; den Schritt schließen.

With predicted `chkedy = CHK+E+DY`:

> Danach zur Zielstelle; dann kurz wärmen; den Schritt schließen.

## CHK-P02 — full grade, item remains active

Source `f55v / H4-S003`:

> Den laufenden Posten nach Sollmaß zugeben; dann kurz im Arbeitsgang aus der
> Quelle entnehmen; dann ihn länger erwärmen; dann weiterarbeiten; den Schritt
> schließen.

With predicted `chkeeey` (alternate renderer `cheeeky`) = `CHK+EEE+Y`:

> Den laufenden Posten nach Sollmaß zugeben; dann kurz im Arbeitsgang aus der
> Quelle entnehmen; dann ihn vollständig erwärmen; dann weiterarbeiten; den
> Schritt schließen.

## CHK-P03 — full grade and closed

Again in `f83r / B3-S002`, with predicted `chkeeedy = CHK+EEE+DY`:

> Danach zur Zielstelle; dann vollständig wärmen; den Schritt schließen.

In all three readings the picture owner, target, surrounding cards and endpoint
remain fixed. Only the E-grade changes. This is the first new semantic mini-grid
that yields fluent instructions both open and closed.

Next, re-read all fifteen `SHED` events as action **ABSETZEN** versus result
state **ABGESETZT / STEHENGELASSEN**. Let the local word order choose which
version makes the workshop instruction simpler.
