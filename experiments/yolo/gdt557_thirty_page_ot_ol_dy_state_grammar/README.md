# GDT557 — thirty-page OT/OL/DY state grammar

Status: `PASS_OT_RIGHT_402_OF_404__OL_FLEXIBLE__DY_CLOSE_702_OF_705__TWO_REVERSE_COMPOSITIONS`

This experiment expands the six-page GDT478 OT/OL seed into a complete
positional reader over all 5,122 running events on the 30 already admitted
pages, adding GDT556's DY closure role. The resulting working machine is:

- `OT=DANACH`: open or advance to the next carrier;
- `OL=FORTSETZEN`: keep the current carrier active;
- `DY=ABSCHLIESSEN`: close the current step and, normally, the statement.

All 1,870 occurrences, nine observed marker-only sequences, pair orders,
thirty page profiles and twelve compositional edge rows are retained under
`artifacts/`. The independent validator passes 37/37 checks.

Start with `REPORT.md`; use `artifacts/GDT557_THREE_STATE_GRAMMAR.md` as the
compact working reader.
