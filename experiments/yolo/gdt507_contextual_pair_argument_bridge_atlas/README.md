# GDT507 — contextual pair argument bridge atlas

Status: `FOUR_CONTEXTUAL_TARGETS_HAVE_CONCRETE_BRIDGES__THREE_LOCAL_ONE_CROSS`

GDT507 reopens only the four contextual pair targets left by GDT506. Broader
`CH>CH` action chains and consecutive same-statement `CH→CH/SH` cards give all
four a concrete old argument bridge. Three bridges are in the target register;
source `CH+CH` remains cross-register. The target recipes remain unobserved
`COMPOSED_WORKING` defaults.

See `REPORT.md`, `METHOD.md`, and
`artifacts/GDT507_CONTEXTUAL_PAIR_ARGUMENT_BRIDGE_ATLAS.md`.
