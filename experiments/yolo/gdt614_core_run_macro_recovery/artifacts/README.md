# GDT614 artifacts

At registration this directory contains `REGISTERED_MODEL.json` and
`REGISTERED_TRANSITIONS.tsv`: the prospectively frozen repaired grammar, deck,
primitive assignment, macro licenses, transition catalog, objective,
thresholds, and seeds. The scored fail-fast bundle adds `RESULTS.json`, the
64-row `necessary_merge_bound.tsv`, the exact 18-node
`minimum_hitting_witness.tsv`, the consumed-input manifest, and the independent
398-check `VALIDATION.json`.
