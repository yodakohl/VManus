# GDT984 — full remainder prefix compatibility

## Decision before implementation and target evaluation

Selection review began 2026-09-19 23:23 UTC after GDT983. This separate bounded
unit has a 60-minute preparation-through-publication checkpoint at 00:23 UTC
on September20; it is part of the ten-hour block beginning23:05:26UTC September19
and ending no earlier than09:05:26UTC September20. No automatic runtime extension.

Known positive baseline: spaces and reusable directed components both matter,
with whole-form and entry residuals. No phonetic alphabet or Latin default is
identified. GDT963 is a narrow CONTENT-code hypothesis, not a default language.
Its four complete source arrays and original page frames remain unchanged.

Primary review: GDT963 full common prefix-free code was computationally unresolved;
GDT976 has8,990 necessary two-name bindings in1,320 ordered name-code classes;
GDT977 retains8,986 after LEAF/BROAD and stops serial small atom additions;
GDT978 retains all78 recurrent types but permits singleton spans to collide with
recurrent codes, and all33 bounded partitions remain UNKNOWN. No full witness
or independent plant-name evidence exists. These original results remain intact.
GDT900/965 exact-source exclusions concern other frozen sources and are not reopened.

The distinct new consequence concerns EVERY omitted occurrence, not an added
noun: a non-name code in the full GDT963 prefix-free dictionary can neither
start with a complete fixed name code NOR be a prefix of either fixed name.
The old free gaps allowed both. Enforce this codeword boundary restriction on
all607 non-name occurrences (including all255 singletons), while relaxing their
mutual equality/injectivity/prefix relations. All six name occurrences stay exact.
This projection is different from GDT978: it restores the omitted singleton/name
prefix obligations and drops recurrent non-name equality. Neither subsumes the
other. It does not change the original full writer or nominate a new decoder.

Unknown: do any active two-name/page bindings survive this whole-remainder
necessary condition on four distinct leaves? Outcomes change the decision:
zero survivors excludes the literal full-code conjunction (source-unknown pages
remain unknown); a sharply reduced set supplies explicit partial candidates for
a new full-reading decision; >=90% survival stops this projection route as weak.
Every survivor remains partial, not a translated word. No automatic code expansion
or solver continuation follows any outcome. Keep the four prior GDT977 failures
as inherited exclusions, never rescue them by a weaker success here.

## Fixed population and predictions

Read unchanged GDT963 src/SOURCE.json, GDT976 DOMAINS.json and all8,990 rows in
CANDIDATES.json.gz, plus GDT977 results for the four inherited exclusions. The
same literal IT2a role domains (33/101/101/100 pages) and physical leaves apply.
ZL/RF lack of joint capacity and original source-unknown frames stay unchanged.
No raw TSV, image, admission, reserve or outside contact. f84/f84r/f116v closed.

For each active name-code class use its exact IRIS and XIPHION strings; do not
freeze earlier alignment witnesses or LEAF/BROAD guesses. For each of all four
source roles and every original role page, retain every source-atom position in
order, and partition the ENTIRE literal target string into one nonempty substring
per source occurrence. Name positions must equal their fixed whole code strings.
Every other position must be prefix-incomparable with both names. Its substring
may differ from another occurrence of the same non-name atom and may equal another
non-name value: those are explicit relaxed obligations, never learned meanings.

Two selected name codes are already prefix-incomparable. All pages must have
four distinct physical leaves. A base row survives only if its fixed I.1/IV.20
pages pass and some allowed I.2/I.3 pair passes with distinct leaves. Publish all
8,990 rows, grouping surviving exact name pairs, every local class/role/page
outcome and support sets, and complete example boundary witnesses per surviving
class. Counts of reading representations are not independent manuscripts.

## Exact finite calculation (to implement before execution)

For fixed name strings and target T, let g(b) be the earliest end >b such that
T[b:end] is prefix-incomparable with BOTH names, or infinity if none exists.
Once a prefix diverges from both names, every longer extension remains legal.
If a complete name prefix has already matched at b, no extension is legal.
Therefore legal non-name ends from b are exactly g(b)..len(T), or the empty set.

Reachable ends after an atom are either an explicit finite set (after a name)
or an upward interval (after a non-name). Start with the finite set{0}. A name
step filters starts that spell the exact fixed name and shifts by its length.
A non-name step has reachable ends m..len(T), where m is min g(b) over previously
reachable b. Precomputed suffix minima compute this exactly for an interval;
an explicit finite set is inspected completely. Empty sets remain contradiction.
The complete record passes exactly when len(T) is reachable after its last atom.
Save a deterministic predecessor for each step; backward recovery from len(T)
gives a full nonempty atom-boundary witness. No beam, heuristic length limit,
random restart, wall-limited SAT status or guessed unknown word is involved.

Proof: prefix-incomparability is preserved under extension once attained;
every legal code end from a fixed start is in the interval above. Taking the
union over all previously reachable starts gives the stated interval. Exact
name transitions preserve precisely their witnessed starts. Induction on source
positions proves completeness. A full GDT963 code supplies a partition accepted
here, so a negative local result is a necessary contradiction under that writer.
A positive result does NOT construct a common code for the non-name atoms.

Validate independently without importing the runner: direct finite-end-set
enumeration on short exhaustive fixtures, all ground positive partitions,
complete local decision coverage and separate replay of negative frontiers.
Implementation tests do not establish manuscript meaning. Stop implementation
expansion at the inclusive checkpoint; preserve errors/unknowns if any arise.

## Assumptions and ceiling

The copied-content source relation, limited aliases, printed lexical order,
concatenation across known group boundaries and prefix-free code are all inherited
hypotheses. There is no identified Voynich morpheme, phonetic value or plant.
All sources and target pages are exposed; no independent confirmation leaves.
This is fixed exploratory candidate evaluation, not reserved confirmation.
No search-wide control, significance, GDT388 score-ready relation or confirmed
word. Counts or complete boundary coverage alone are not decipherment progress.

IDEA365/366 remain RAW, not tested by this unit. Their qochey output ownership and
rank-reference initial inventory are unbound in the already read predecessors;
no extra stock or operand is introduced to call their example a full reading.
