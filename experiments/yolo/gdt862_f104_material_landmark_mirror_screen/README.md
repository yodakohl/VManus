# GDT862 material-only mirror screen

See REPORT.md and frozen METHOD.md. Run src/run.py, src/validate.py and src/run.py --check
with the two previously cached original images at the paths in src/SPEC.json.
The scripts validate recorded observations; no native material judgment is automated.
