# GDT909 — no complete fixed-template division chain

**NO_FIXED_TEMPLATE_CHAIN.** All eligible equal-step partitions of the frozen
whole-paragraph intake were checked. None admits the required dividend/divisor/
quotient/remainder positions and literal transfer to the next step.

| Alternate reading | Whole paragraphs | Physical leaves | Eligible partitions | At most four varying columns | Structural candidates |
|---|---:|---:|---:|---:|---:|
| ZL3b | 14 | 7 | 8 | 4 | 0 |
| IT2a | 259 | 36 | 367 | 65 | 0 |
| RF1b | 11 | 9 | 9 | 2 | 0 |

These are three alternate readings, not independent manuscripts or 284 distinct
paragraphs. The intake was already exposed by earlier experiments, and its
paragraph segmentation differs sharply by reader. All target paragraphs are on
previously admitted odd leaves; no held even body or new image was accessed.

The structural test is necessary for the registered model: each paragraph is
at least three equal-sized consecutive steps, all four values are single whole
groups, and all other columns repeat exactly. It checks every role order and
uses the entire paragraph. Every next dividend must repeat the previous divisor,
and every next divisor the previous remainder. Dividend, divisor and remainder
must differ within each step. No proposed word values or numeric tail counts are
used to obtain the zero.

Consequently no arithmetic fit or omitted-step prediction is reached. The zero
holds independently of the planned numeric bound of4095. The unexercised numeric
branch supplies no completeness, recovered-value or prediction claim. The source
code deliberately reports no prediction from full-chain conditioned solutions.

Independent validation reconstructs source hashes, all partitions and candidates
with a separate role-placement enumeration. Its positive and negative fixtures
and exact checks are recorded in artifacts/VALIDATION.json. Primary synthetic
fixtures recover planted three-step chains under all24 role orders both with
and without repeated scaffold. These checks validate this narrow search, not
manuscript meanings.

No numeral, operation word, readable statement or translation was recovered.
The result excludes only this fixed complete-paragraph writing model. Unequal
step wording, implicit quantities, inflection, multi-group numerals, different
paragraph boundaries and arithmetic elsewhere were not tested. No automatic
window, unitization, template or source repair follows. GDT880's separate
immediate-triple capacity stop is unchanged. This is not a GDT388 semantic score.

Reproduce with the run and validate commands in experiment.json. Protocol was
published in16763d65 before target execution. No external historical source or
language identification was assumed.
