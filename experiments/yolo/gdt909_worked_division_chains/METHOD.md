# GDT909 — complete worked division chains

User accepted this exploratory meaning-construction hypothesis on 2026-09-10.
GDT880 found only three immediate same-head triples and ran no arithmetic test.
Its source, threshold, tail values and closed wider-window successor are not
reused: this new falsifier requires consecutive quotient/remainder role transfer
through a complete paragraph. No historical source identity is asserted.

Decision: unknown whether any complete admitted paragraph has the necessary
literal structure of at least three division steps. A surviving structure opens
finite exact arithmetic construction and explicit ambiguity analysis; no structure
closes this fixed writing model. Numeric solutions would be hypotheses, not
translations or grounds to unseal held pages. Smallest adequate test: exhaust all
whole-paragraph equal-step partitions and role placements on the frozen899
odd-leaf panel. User removed elapsed-time budget; do not expand this finite scope
or begin a decoder/source repair after its result. Preparation, implementation,
independent validation and publication are included in this task.

## Fixed contract before target execution

Use all ZL3b, IT2a and RF1b panels in the hash-bound SPEC source, separately.
No consensus panel, new page, even leaf or f84/f84r access. These paragraphs were
previously exposed; they are discovery data. The inherited intake determines
paragraph boundaries; alternative transcriptions are not independent manuscripts.

A complete paragraph of N raw whole groups must partition from its first group
to its last into K>=3 consecutive equal blocks of L>=4 groups, N=K*L. No ignored
intro, ending, window, punctuation deletion or word splitting. Four DISTINCT
positions in every block denote A (dividend), B (divisor), Q (quotient), R
(remainder), one whole group per value. Try every ordered placement of these
four roles. Every other position must be exactly constant across all blocks.
No constant position is assigned an operation-word meaning. A numeral may occur
at a constant scaffold position; allowing that is a relaxation, not a translation.

For each adjacent pair of steps, next A equals current B as a literal whole,
and next B equals current R. Within each step A,B,R are pairwise distinct,
necessary for A>B>R>=0. The numeric key is globally consistent and injective
within the complete candidate paragraph. This excludes homophony, case variants,
implicit arguments, differently worded steps and multi-group numerals; failure
does not exclude those or arithmetic in general. Different candidate paragraphs
are alternative constructions, not permission for one document to change keys.

For every structural candidate, enumerate integers 1<=B<A<=4095 as initial
values. Compute Q,R by divmod at each step, propagate next A=B and B=R, require
positive Q, nonzero B at every written step, and exact consistent injective
whole-to-number assignments across ALL steps. Preserve total solution count,
per-token value ranges, first two keys, and examples of nonunique assignments.
Zero solutions excludes only this bound. The structural zero, if any, holds
independently of the numeric bound. Do not infer absolute scale from ratios.

A proposed omitted-step prediction requires its four number wholes already
assigned by prior steps and all compatible preceding keys to agree. Record such
internal predictions separately; structural discovery sees the whole paragraph,
so this is NOT an untouched held test or semantic confirmation. Independent
meaning evidence and applicable GDT388 gates are still required before promotion.
No target semantic score or GDT388-ready relation packet is asserted here.

## Validation and stopping

Independent validator reconstructs every paragraph partition, all role placements
and candidate set using a different loop order; no import of primary code.
Known-positive invented three-step divisions exercise all24 role orders with
and without constant scaffold; altered transfer and inconsistent numeric tokens
supply negative checks. These are implementation checks, not manuscript findings.
If candidates exist, independently check arithmetic solution sets/aggregates.
Publish all outcomes, source, protocol, validator and compact reproducible results.
No post-result relaxation or automatic additional model is authorized by this test.
