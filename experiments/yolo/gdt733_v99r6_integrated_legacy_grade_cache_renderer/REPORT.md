# GDT733 report — one current cache, with its remaining debt visible

Status: `PASS_32339_CELL_CURRENT_CACHE__479_EXACT_CONTEXTS__2431_GDT732_SPOKEN__6866_ADDITIONAL_UNCONDITIONAL_GLOBALS__4692_LEGACY_CONTEXT_GRADE_RENDERS__52_SUPERSEDED_EXACT_V48_CELLS__8_CURRENT_V99_BOUND_SPANS__8_LEGACY_ALIAS_MERGES__4_STRUCTURAL_PUNCTUATION_ATTACHMENTS__7132_GRADE_CELLS_SPOKEN__ZERO_AUDIBLE_GRADE_FRAMES__32319_PRACTICAL_UNITS__NO_DEBUG_TEXT_IN_PRACTICAL_OUTPUT__NO_DOUBLED_SEPARATORS__NO_NEW_MEANING_NO_NEW_PAGE`

## Result

GDT733 produces the first single current renderer for every one of the 32,339
cached token positions. The integration partition is:

| Priority | Cell source | Cells |
|---:|---|---:|
| 1 | exact V99 + GDT732 spoken | 30 |
| 2 | exact V99 superseding old V48 grade text | 52 |
| 3 | other exact V99 | 397 |
| 4 | global GDT732 spoken overlay | 2,401 |
| 5 | occurrence-local legacy grade context | 4,692 |
| 6 | legacy contextual alias/merge anchor | 8 |
| 7 | further unconditional V99R4 global default | 6,866 |
| 8 | inherited other | 17,893 |
|  | **Total** | **32,339** |

The integration changes 1,530 current semantic cells relative to V48 and
applies spoken formatting to 7,132 cells. It neither changes nor invents any
dictionary reading. All confidence, evidence, score, scope and export fields
remain inherited.

## What the voice now says

The former split view could still say `trocken in der Mitte des Grades` in an
old V48 cell even though a current dictionary layer already used shorter state
prose elsewhere. GDT733 resolves that display inconsistency positionally:

- `trocken in der Mitte des Grades, abgeschlossen` becomes
  `bis zur Mittelstufe getrocknet; abgeschlossen`;
- `kalter Ansatz am Ende des Grades, abgeschlossen` becomes
  `vollständig abgekühlter Ansatz; abgeschlossen`;
- `heiß und trocken am Anfang des Grades` becomes
  `heiß und trocken, Anfangsstufe`;
- f104v.2 `chockhy` becomes `leicht getrocknete Arzneizubereitung`.

The 7,183 grade-frame cells in the inherited V48 projection fall to zero in
the integrated cell and line views. This does **not** mean all numerical or
stage jargon is gone. The separate blocker census still finds 755 audible
index placeholders, including `Grad II`, `Menge III`, `Form I` and similar
working values.

## Manual audit corrections

The first internally passing draft counted only the eight new legacy merges
and reported 32,331 practical units. Manual reading exposed two visible
failures: f7r.2 printed `keo|r` both as component prose and as an editorial
`Gesamtspan`, while f86v6.4 did the same for `l|karchees`. Four punctuation
tokens also appeared as standalone `; ;` or `; .` units.

The correction replays **all eight** existing GDT727 bound spans, not merely
the two noticed examples, and separately attaches all four exact punctuation
cells. Together with the eight legacy merges, sixteen spans consume 32 cells
and emit sixteen units. Four punctuation cells emit none. The corrected total
is therefore 32,319 practical units, and no `keine Einzelausgabe` or
`Gesamtspan` text survives in practical prose.

A second manual pass found 48 doubled `; ; hierzu:` joins. The final renderer
recognises that `; hierzu:` already owns its separator. All 48 now read once,
without dropping the connective.

For example, f7r.2 now begins:

> eine Portion vollständig trocknen und abschließen; heiße Portion; Blüte;
> diese Blüte abmessen und abschließen

The cell audit still preserves both source tokens and their exact provenance.

## What remains unresolved

The integrated denominator makes the next work measurable:

| Remaining blocker | Cells |
|---|---:|
| unknown cell | 7,989 |
| indexed placeholder union | 755 |
| strict generic-material surface class | 625 |
| audible structural wording | 254 |
| analytical grade frame | 0 |

The unknown tail spans 5,107 surface forms; 3,998 occur only once. It cannot be
honestly erased with one generic default. The useful next throughput route is
to group the recurrent unknown forms by already learned whole-form and
component families, assign concrete working candidates with confidence and
evidence where composition is informative, and leave genuinely name-like
singletons distinct. Coordinate-only or neighbouring-word guesses are not a
substitute for a word value.

## Scope

All 179 pages were already present in the f84/f84r-free GDT671 cache. GDT733
opens no page and decodes no image. Its fluent output is a renderer for the
current working theory, not historical confirmation or plaintext.
