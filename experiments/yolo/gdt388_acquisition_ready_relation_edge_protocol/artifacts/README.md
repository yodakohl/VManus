# GDT388 artifacts

- `gdt388_page_frame.tsv`: exact 61-page/30-folio geometry-review universe.
- `gdt388_relation_type_capacity.tsv`: independently reconstructed local
  relation counts and why they are not ordered target edges.
- `gdt388_acquisition_batches.tsv`: ranked, geometry-only acquisition frames.
- `gdt388_acquisition_gates.tsv`: exact prerequisites for later scoring.
- `gdt388_edge_packet_template.tsv`: empty schema for future blinded edge rows.
- `gdt388_result.json`: compact hash-bound result.
- `gdt388_validation.json`: nonimporting source-count and protocol validation.
