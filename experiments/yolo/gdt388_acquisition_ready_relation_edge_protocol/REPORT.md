# GDT388 — acquisition-ready relation-edge protocol report

## Outcome

`ACQUISITION_PROTOCOL_FROZEN_ZERO_ELIGIBLE_CURRENT_EDGES`

The next experiment is now specified operationally, but the current archive
still supplies zero eligible ordered inscription-to-inscription edges. No
Voynich formal identity was opened or scored.

## What the existing 1,059 relation rows actually contain

The four human relation channels cover 335 unique loci on 20 physical folios.
They contain 95 explicit positive relation rows on 94 loci and 12 folios:

| local relation | positive rows | unhedged positive rows | unhedged folios |
|---|---:|---:|---:|
| attachment | 73 | 42 | 2 |
| contact | 9 | 5 | 4 |
| enclosure | 7 | 5 | 2 |
| array/group | 6 | 0 | 0 |

The remaining 964 channel rows are `PROXIMITY_ONLY`. Of the 95 positives, 52
are unhedged and 43 hedged. The apparent attachment capacity is especially
misleading: 42 unhedged attachment rows occur on only two physical folios, and
most are star-label ownership on f68.

These are genuine useful geometry observations. They are not independent
record targets: the inventory has no normalized second target locus and no
ordered inscription edge in any of the four channels.

## Ranked acquisition plan

1. **Authorial connectors between labeled endpoints.** Census all 61 pages on
   30 folios in the frozen permitted-annotation frame for visible arrows, lines,
   ducts, paths, or pointers whose two endpoint inscriptions can both be
   localized. This is the only primary route that directly matches the GDT387
   relation endpoint.
2. **Normalize local text-object relations.** The 95 current positives can
   support a separate object-relation endpoint after object IDs and ownership
   are normalized, but they may not be smuggled into the parent-link test.
3. **Upgrade cross-page referents only if direction is external.** Forty frozen
   same/similar-drawing pairs have no ordered edge.
4. **Special-circle pointer census.** The 45 frozen arrays on seven folios have
   no authorial start/direction; radial or catalogue order is excluded.
5. **Q20 inter-record pointer census.** The 170 star-owned records on eight
   folios have no visible record-to-record target. OPEN/BODY is source hierarchy,
   not a relation edge.

## Packet and stop rule

The published packet schema binds page, physical folio, diagram unit, visual
and manuscript IDs for both endpoints, relation and direction bases, crop
hashes, localizer/reviewer identities, ambiguity, formal-access state, and
preassigned fold. The packet is intentionally empty.

No score is authorized until every frozen gate passes. In particular, capacity
must reach at least 50 singular directed edges on five folios, and the target
must remain mobile after matching. Confirmation planning should still prefer
the stronger GDT372 design of one to three external comparisons with many
complete arrays and multiple held folios.

## Why this is useful

This result prevents the most likely next mistake: interpreting a large count
of local proximity/attachment annotations as hundreds of independent semantic
links. It also gives a concrete data contract for acquisition, so a future
review can add evidence without changing the endpoint after seeing Voynich
forms.

## Claim ceiling

This is a text-blind acquisition and capacity result only. It establishes no
Voynich parent, reference, operator, syntax, POS, meaning, language, plaintext,
or translation. No image, formal row, or f84 material was opened.
