# GDT927 — fixed chor / ykchor chordy comparison in full context

2026-09-12. User approves three concrete comparisons after926: whole paragraphs, all admitted occurrences, and closure versus content versus longer-expression hypotheses. No word meanings assigned. The seed and its unequal paragraph-end positions are already exposed. This is not blind confirmation.

Primary predecessors:926 literal-context inventory;910 generic short/long exact-flank search stopped on its old40-selector discovery;756 concerns ychor, NOT ykchor, and its historical Item gloss is not transferred. No rerun of910's search, no unseen transfer selectors opened. Fixed targets only chor, ykchor, chordy and exact adjacent ykchor chordy, plus the exact preceding sequence daiin cthor chol.

Use only six already guarded/exposed GDT915 source JSONs and SPEC.json (same179selectors as926). No new page/image/raw mixed table, no contacts; f84/f84r sealed. Keep all original P-line groups and uncertainty markers for contextual display. For occurrence counting, report every exact target group separately, flag local boundaries: both existing neighbouring seams must be DEFINITE_SPACE with consecutive group indices; pair must additionally have safe internal seam. Entire-line literal eligibility from926 is a separate stricter column, not an occurrence requirement. This declared change permits complete occurrence census without claiming uncertain surrounding text is resolved;926's whole-line findings remain unchanged.

Retrieve both seed paragraphs separately in each reading: nearest preceding paragraph_start=1 and following paragraph_end=1 within selector; do not cross another start/end or numeric-line gap. If boundaries absent/unusable, label partial cached-page context, never silently call it a full paragraph. RF may have no paragraph flags; report this explicitly. Include all original groups in the context regardless of scoring eligibility. Retain all target-containing host lines, all local left/right groups and original metadata. Show prefix and later context in the two seed paragraphs with literal repeated words, no entity identity inferred.

Hypotheses fixed before census:
CLOSURE_STRICT: chor occupies final group of a marked paragraph and extended phrase is not such a close. Any locally safe nonfinal chor contradicts the universal closure rule; separately show descriptive rates, including line-final-only comparison against all locally safe literal groups/line ends. No p-value or fitted cutoff. If paragraph flags are absent, denominator unavailable, not zero.
EXPANSION: chor and ykchor chordy can substitute in an unchanged context. Inventory shared exact flank signatures (two preceding whole groups, one following whole group); use distinct explicit LINE_END/PARAGRAPH_END/UNKNOWN_PARAGRAPH boundary tokens instead of inventing absent words. All three flanking groups must be literal and safe within line; target may be at end. Require>=2distinct shared signatures with both alternatives represented and>=3physical leaves collectively before considering a recurring substitution candidate. The original seed differs in ending status and cannot count as a shared right flank. This is a targeted test of926's fixed pair, not a new generic search or a claim synonyms follow from shared flanks.
CONTENT: would need recurrent contrasting following relationships beyond the chosen target labels. Publish all whole contexts and suffixes; if the extended phrase is a singleton, or no repeated contrast exists, label missing discrimination capacity, not content refuted. No words for plant/action/quality assigned.

Also inventory every individual ykchor and chordy, whether paired or not, and every chor immediately preceded by the fixed seed anchor. Report occurrence/leaf counts, ordinal/paragraph placement, reader disagreements, all exceptions, overlaps and missing boundaries. No prefix stripping, ychor merging, seam repair, or extension of targets. Match alternate seed lines by page/locus; readings are one manuscript.

Smallest test: exact occurrence scan, boundary-aware paragraph extraction, targeted flank join and independent replay. Budget35min including preparation, implementation, validation and publication. Outcomes determine whether to retain a positional or substitution relationship as candidate; if all insufficient, stop this pair without inventing glosses or moving thresholds. Deliver actual source contexts, counts and per-hypothesis decision, not general methodology. No significance, semantic confirmation or score-ready relation packet.
