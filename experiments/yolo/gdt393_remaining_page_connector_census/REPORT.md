# GDT393 — residual page connector census report

## Outcome

`COMPLETE_RESIDUAL_CENSUS_ZERO_ELIGIBLE_DIRECTED_EDGES`

The mechanically frozen remainder contained 12 pages on 11 physical folios
and 11 official canvases. It adds no eligible directed relation edge.

| page outcome | pages |
|---|---:|
| no connector candidate | 8 |
| ambiguous radial/path-like geometry | 4 |
| connector with two exact inscription endpoints | 0 |
| author-visible directed exact-locus edge | 0 |

The frame contains 102 catalogue label loci, 13 circular loci, and 16 radial
loci. Those counts are layout capacity, not relation edges.

## What was reviewed

The eight herbal, list, pharmaceutical, or back-page cases contain labels,
marginal marks, or prose but no device connecting two inscriptions. Four
cosmological panels (`f68v1`, `f68v3`, `f70r2`, `f86v4`) contain spokes,
curved paths, or center-to-corner arms. They remain ambiguous because the
geometry fixes neither an orientation nor two singular exact inscription
endpoints. Radial adjacency and conventional reading direction were not used.

The already exhausted Rosettes road/connector route was excluded before image
review.

## Capacity and access

Observed capacity is zero directed edges on zero eligible folios, against the
inherited floor of 50 edges on five folios plus a mobile matched target. No
formal identity was joined and no grounding score was run.

The page universe and outcomes were frozen publicly in commit `2a033ea` before
image review. The same AI performed the direct visual census. A later
filename-location command printed already-public candidate transcription rows
for several allow-listed pages before the images were judged; therefore this
is provenance-qualified exploratory review, not analyst-blind confirmation.
It did not change the frozen universe or rules.

An earlier capacity command also split two forbidden page-description metadata
rows before output filtering. Neither row was displayed, selected, retained,
scored, or used; no forbidden image or formal payload was opened. The actual
experiment loader rejects the raw page selector before parsing the row.

## Decision

The residual page-role frame does not rescue the independent relation-edge
route. Together with GDT389–392, the currently available internal visual data
remain exhausted. The next score still requires genuinely new externally
owned or authorially directed repeated evidence.

## Claim ceiling

This is a complete geometry-only capacity census of the residual page-role
frame. It establishes no parent, reference, operator, syntax, POS, meaning,
language, plaintext, or translation.
