# GDT794 — Complete-label circle transfer and renderer correction

Status: **PASS — raw slot order corrected; no multiform position codebook;
circle-label token composition quarantined**.

## Outcome

The admitted material contains 216 transcribed circle-label loci in nineteen
arrays on six physical folios. Those loci contain 274 tokens but 199 distinct
**complete label strings**. Only fifteen strings recur at all, and only eight
recur across physical folios. A large exact-whole position dictionary therefore
does not exist in this panel.

The eight cross-folio strings fail as a joint key under all four deliberately
separate coordinates:

| model | held targets / forms | mean within-array rank | closest quartile | result |
|---|---:|---:|---:|---|
| linear source-list position | 17 / 8 | .333 | 9/17 | fail; weak editorial-rank trace |
| visible page angle | 17 / 8 | .469 | 5/17 | fail |
| visible diameter/opposition class | 17 / 8 | .519 | 3/17 | fail |
| Kluge-A catalogue member | 6 / 3 | .548 | 2/6 | fail and under capacity |

Thus no exact multiform calendar/day/degree codebook is selected. The source
order trace may stay as an exploratory copy/list-position effect, but it does
not reach the working gate and is not a sky coordinate.

## Correction to GDT793

GDT793 called five outer `slot_index=4` rows a homologous slot sensitivity.
That wording was too strong. `slot_index` is source/editorial order inside each
array, and the five rows actually have:

| label | raw slot | visible angle | Kluge-A |
|---|---:|---:|---:|
| `okalal` | 4/10 | 03:00 | 09A |
| `okala` | 4/19 | 00:45 | 17A |
| `okalam` | 4/10 | 01:30 | 09A |
| `okaly` | 4/16 | 00:45 | 13A |
| `oraiinam` | 4/12 | 00:00 | unavailable |

Only `okalal` and `okalam` share the genuine supplied Kluge homolog. The old
4/5 observation is a raw-list-position pattern, not one calendar or degree
position.

The complete true 09A comparison contains five records:

```text
f70  okaldal
f70  okalal
f71  otchody
f72  okalam
f72  okarcham
```

Three of five start literally with `okal`, across two positive physical
folios; f71 is a direct non-`okal` case. This leaves a replaceable
template-slot/class rival, not `okal = nine`, not a fixed degree word and not a
component meaning. Across all 101 Kluge-A records on eleven arrays and three
physical folios, no repeated complete cross-folio label holds the same A-code.
`otalaiin` shifts 02A→03A, `okaram` 06A→28A and `otar` 13A→20A.

## What the repeated wholes look like

Thirteen recurrent strings are doubletons and two are tripletons. Only three
repeat inside one array:

- `okeod`: slots 8, 12 and 21 of the f69 28-member radial array;
- `okal`: slots 3 and 5 of the f72 16-member ring, plus one f67 occurrence;
- `okaly`: slots 4 and 12 of that same f72 ring.

These collisions reject unique-member or unique-day readings for those exact
strings. They remain compatible with a repeated status, ruler, phase or entry
class.

Only one pair of exact labels co-occurs in two arrays on two folios:
`okody` and `otody`. Their separation changes from 1/28 to 3/19 of the source
list and from 0.5 to 1.75 visible hours. There is therefore no stable relational
spacing code hiding behind an unknown rotation.

## Concrete whole-form candidates retained

`otody` is the sharpest individual exception to the failed joint model. Its two
standalone circle labels lie at 01:00 on f69 and 01:15 on f70. It also has two
released running uses, on f75 and f95. GDT794 retains the replaceable C0 card:

```text
otody = oberes rechtes Zeit-/Gradfeld, sichtbar ungefähr 1 Uhr
```

This is a **position rival**, not an established clock word. The two prose uses
do not yet show that the coordinate reading belongs there, so the card is not
installed in the integrated prose cache.

For exact `okal`, the GDT793 system-entry code remains primary. Its three timed
circle labels occupy 11:30, 00:00 and 01:15, so “upper-sector entry class” stays
as a more concrete rival. Two `okal` entries in the same ring prevent a unique
sun, day or degree identity.

`otalaiin` receives a weaker complete-whole card, “early inner five-member
slot,” because it occurs at members 2 and 3 in two homologous inner five-label
bands. It is a window/class candidate, not an exact number.

Every one of the fifteen repeated complete labels now has a nonempty working
default, evidence and counterevidence in the candidate atlas. Divergent forms
receive learned-entry/class defaults rather than generic process prose.

## Renderer repair

The circle panel exposes a concrete reason earlier output could sound fluent
while saying nothing. Of 274 circle-label tokens, 97 occurrences representing
64 token forms had an unconditional V99R7 semantic default. Thirty-six of the
47 multi-token inscriptions contain at least one such default, and fourteen
would have every token translated. For example, the recurring two-token label
`okeey ary` could be assembled into a pharmaceutical mini-sentence even though
the evidence shows only one complete celestial inscription.

GDT794 therefore installs a context precedence for all 216 admitted loci:

```text
RADIAL_LABEL_SEQUENCE > unconditional global token default
```

Each local inscription renders once as a complete label sequence. A singleton
is displayed as an individual circle designation with its visible Kluge or
clock position and meaning open; it is not decomposed into a recipe. This does
not delete the older dictionary in prose contexts.

The fifteen repeated labels were audited against V99R7. Six universal
pharmaceutical wholes move to cross-scope HOLD:

| form | inherited universal display now held |
|---|---|
| `okaly` | raw-material preparation, heat and finish |
| `okaram` | measure of a first heated preparation fraction |
| `okeal` | hot middle-grade raw-material class |
| `otaly` | cool and finish a raw-material preparation |
| `otar` | cold preparation portion I |
| `otody` | cold finished preparation |

`otar` already had a later GDT769/GDT770 context-bound
sequence-versus-nominal correction; GDT794 does not undo it. `okal` was already
superseded by GDT793. All older readings remain visible as historical
candidates, but none may leak unqualified into a circle label.

## Best architecture after this pass

The high uniqueness is more compatible with **learned member names plus a
small number of repeated class/status fields** than with a compact universal
slot-word dictionary. An exploratory lunar/astrological register remains a
good architecture prior because the admitted arrays visibly organize 7, 12,
15, 28, 29 and 30-like inventories. Those cardinalities can accommodate
planets, signs, lunar stations, days or half-cycles, but cardinality alone does
not identify which one is present and supplies no word meaning.

The next useful test is not another exact-word positional search. It is a
guarded reconstruction of the full source-native **family sequence** for the
101 Kluge-A records, preserving multi-token label boundaries, followed by
leave-one-physical-folio-out A-code retrieval. A real result must recover
several different A-positions under one shared array transform; selecting a
new rotation separately for each word would manufacture matches.

## Scope and ceiling

No new page, image or transcription was opened. No mixed source was queried;
f84/f84r remained sealed. The validator passes 167 checks and two
byte-identical builder replays.

GDT794 confirms no Voynich lexeme, component meaning, calendar, day, degree,
planet, substance, action, disease, treatment, language, sound, cipher or
plaintext translation.
