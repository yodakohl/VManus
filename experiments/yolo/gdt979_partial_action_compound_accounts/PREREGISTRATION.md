# GDT979 preregistration: complete partial-action compound accounts

Phase: fixed finite-model test; previously exposed targets, not a blind test.
Decision note: research_registry/work_batches/ten_hours_20260915/PARTIAL_ACTION_DECISION_20260919.md.

Use every complete paragraph in GDT928 artifacts/PARAGRAPHS.json. Preserve its
page, physical leaf, source IDs, groups and paragraph limits. ZL3b and IT2a are
separate alternate-reading panels. RF1b has no registered complete paragraphs:
NO_PARAGRAPH_CAPACITY, never an empirical zero. Each line must have the original
anchor_eligible flag, or consist of one literal lowercase group (no internal
boundary to question). Other paragraphs are SOURCE_UNKNOWN, not model failures.
No text is edited, no boundary is invented, no group is omitted.

IDEA361 supplies six atoms in this fixed order: L,P,S,OA,OB,OC. Main M maps:
L:A->B; P:B->C; S:B->A and C->A; OA:A->A; OB:B->B; OC:C->C.
Rival D changes only P:B->A. All other transitions are undefined. Fit each
model's own code; never compare only under M's fixed winning key. A generic
procedural interpretation with identical transitions remains an inseparable
meaning rival even if D fails. Institutional meanings are stipulated.

Each paragraph starts A, contains L, has 1–24 atoms, and ends with OA in A.
Each written group has 1–3 atoms. One injective prefix-free dictionary maps
the six atoms to lowercase a–z strings of length 1–3. No atom crosses a space;
line wraps do not reset state. All characters must be consumed. The code is
shared across the two paragraphs in a joint candidate. At least one paragraph
contains P and another, on a distinct physical leaf, does not. P need not have
an observation marker. Unused atoms are not forced to occur.

Predeclared necessary capacity checks: <=24 groups, <=72 total characters,
every group length <=9, <=6 distinct initial and <=6 distinct final characters.
Failing a necessary bound is a model contradiction on that literal paragraph.
All causes are retained; this is not a selected successful substring search.

Exact backtracking at actual atom boundaries tries every applicable atom and
every legal 1–3-character code prefix. Existing codewords, prefix-freeness,
the state, atom/group limits and complete account requirements are enforced.
Save every distinct used-atom dictionary and its deterministic whole trace.
Unseen values stay null in equivalence classes. Canonical completions choose
unused initial letters and do not claim identification of unused codewords.
Every merging of compatible dictionaries across the two P-presence classes
and different leaves is a candidate; report all, with unresolved limits.

Eight workers; at most 200,000 explored nodes or 5 wall seconds per paragraph
and model; at most 20,000 unique witnesses per case. A cap yields UNKNOWN with
partial witnesses retained, never UNSAT. At most 2,000,000 cross-class pair
comparisons per reading/model; a cap makes untested pairs UNKNOWN. Whole primary
execution ceiling 300 seconds; unfinished cases are UNKNOWN. No extension or
post-result rerun. The inclusive work checkpoint is 19:15 UTC.

Before targets: synthetic complete accounts in both models, non-prefix-free
collision, undefined transition, cross-space atom and group/atom-limit controls.
Independent validation replays saved witnesses, reconstructs all source and
capacity rows and checks pair coverage. Any proof of exhaustive failure must
also be independently enumerated, or labeled producer-only rather than proved.
Freeze runner, specification, validator, source/proposal hashes and controls
before target execution. Publish the registration first.

No significance, probability of translation or independent meaning check.
No new relation evidence packet; these are explicitly hypothetical traces.
All reserve pages, f84 and f84r remain closed; f116v is excluded. Original
experiments and raw proposals remain byte-identical. Results must separate
surface capacity, syntactic/state fit, joint-code fit and meaning.
