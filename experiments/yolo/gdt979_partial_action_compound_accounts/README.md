# GDT979: partial-action compound accounts

A separately registered finite test of IDEA361 against all complete exposed
GDT928 paragraphs. No GDT978 solver continuation or inherited word meanings.
See PREREGISTRATION.md, METHOD.md, src/SPEC.json and, after closure, REPORT.md.

Run with Python 3 standard library:

```
python3 experiments/yolo/gdt979_partial_action_compound_accounts/src/run.py --controls
python3 experiments/yolo/gdt979_partial_action_compound_accounts/src/validate.py --source-only
python3 experiments/yolo/gdt979_partial_action_compound_accounts/src/run.py
python3 experiments/yolo/gdt979_partial_action_compound_accounts/src/validate.py --full
```

The registration lock binds sources and executable rules before target parsing.
CASE_TABLE.tsv retains every paragraph/model case. CASES.json.gz retains complete
literal groups, source IDs, reasons and every found used-atom dictionary/trace.
PAIRS.json.gz retains every computed compatible cross-leaf contrast pair, subject
to declared caps; RESULT.json distinguishes complete from capped enumerations.
SOURCE_UNKNOWN and RF's missing paragraph capacity are not contradictions.

No reserve, new admission, image, mixed raw TSV or outside contact. Zero
independent confirmation leaves and no confirmed word or significance claim.
