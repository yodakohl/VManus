# Method

The complete frozen contract is PREREGISTRATION.md. The primary enumerates all
10! digit permutations, retaining the native mixed numeral components.
The independent checker must reconstruct all five final balances without
importing the primary runner. Outputs retain all surviving mappings per layer.
No prose renderer, target decoder or Voynich meaning score is supplied.
