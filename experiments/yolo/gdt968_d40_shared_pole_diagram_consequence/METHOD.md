# GDT968 — shared-pole arc framework

Before viewing, fix all six rows ofsrc/SOURCES.json. Each source is a complete
already admitted rendering. Inspect every major circular composition on each
canvas, including compositions with an animal/face at their centre. Open star
fields without an enclosing circular diagram are recorded as outside this
whole-unit class. Do not extract convenient local lenses or arc fragments.
The union coversf67r1/r2,f68r1/r2/r3,f69v,f70r1/r2,f70v1/v2,f71v,f72r1/r2/r3.
Exact sibling-to-region naming is unnecessary: usecanvas+left/middle/right IDs.
Enumerate major units from left to right by their centres; for units in the
same vertical column, enumerate top to bottom. Unit indices are1-based and
this fixed spatial order binds the cross-observer comparison.
The common-pole carrier may be an inner enclosing frame circle, not necessarily
the outermost decorative circumference.
f84/f84r,f116v andallreserves are closed. No new admission.

## Prediction fixed for EVERY candidate whole circular unit

A representative Digby40chapter2 framework must have an enclosing circular
boundary with two distinct pole points on it. At least two nonstraight
INTERIOR partition arcs, one on each side of the pole-to-pole diameter, must
run between those SAME two poles. Every interior member assigned to this
partition family must share these endpoints. A single enclosing circle's two
semicircles do not count as interior members. Concentric circles, disjoint
roundels, spokes meeting at one centre, or curves ending at different rim
points cannot by themselves instantiate the predicted frame.

This is a necessary topological predicate, not a test of precise circularity,
angle scale, source spacing, correct projection, degree values or full text.
All residual geometric boundaries/uncertain shapes are described. Additional
geometric structures may leave the COMPLETE-stage reading unresolved even if
a local necessary pair exists. A positive means a candidate framework only.

## Fixed visual layer and decision categories

Ignore writing, pigment fill, and COMPLETE pictorial icons (whole people,
animal silhouettes, faces with their outline, individual star symbols).
Preserve the enclosing wheel/frame, rings delimiting text compartments, and
all non-icon lines/curves partitioning its interior. Do not turn only a useful
part of an icon into a construction arc. Ambiguous icon/boundary classification
is recorded as uncertain. This projects a geometric framework; it does not
claim to read the omitted pictorial content or complete illustration.

For every whole composition record itscanvas/region, retained boundary summary,
common-pole predicate MATCH/CONTRADICTED/UNRESOLVED, concrete reason, extra
geometry and visibility limits. Definite concentric/radial architecture without
the required second pole is a contradiction of this source-stage hypothesis.
Clipping, unresolved line continuation or uncertain eligibility cannot count
as absence. Preserve all compositions, including small/poorly rendered ones.

Root and a second native observer inspect independently and seal their records
before comparison. Both already have project exposure. Agreement is not an
independent manuscript witness or semantic confirmation. A joint MATCH requires
both to identify the same entire unit and pole/arc arrangement. Contradiction
requires both to record the definite missing requirement. Disagreement stays
UNRESOLVED. Validator checks record/source integrity, not native visual truth.

No target transcription or legend is read for decoding, no new key, no automatic
post-hoc metric model and no glyph/underlayer inspection. GDT388 relation packet
scores are not used. No suitable control covers this entire project search,
so no significance assertion; independent meaning confirmation capacity0.
