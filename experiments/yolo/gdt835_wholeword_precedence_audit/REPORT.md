# GDT835 — mandatory wholeword precedence on frozen keys

Status: REGISTERED_UNSCORED.

GDT834's fitted key space omits a necessary condition of its deterministic
control encoder: whenever a candidate assigns a complete word to a W carrier,
that word must appear as exactly that singleton carrier. A competing composed
spelling cannot also be emitted under mandatory wholeword priority.

The selected diagnostic applies this condition to every one of the 48 frozen
keys, using only each key's own word assignments and discovery ciphertext.
The complete gate is locked before joining known truth and held observations.
No new key selection, language score or fitted reading is produced.

The control outcomes are already public, so this is retrospective diagnosis.
Its code/protocol are registered before new compatibility classifications.
The independent source-only grammar census is separately identified as
exploratory; it supplied no condition or threshold for the mechanical test.

See METHOD.md and PREREGISTRATION.md. GDT834 remains BASELINE_RECOVERY_FAIL.
No full encoder inversion, optional-abbreviation rule or Voynich meaning is
claimed by this necessary wholeword-priority condition.

Ten invented-fixture tests pass, including agreement between independent gate
implementations and a 48-candidate stage-isolation fixture. The separate source
census finds annotated heads beyond immediate neighbors in35/42 affected word
occurrences, but a substantial reference/control construction shift and only
9/38 finite-anchor forms literally covered by the reference. No grammar rule,
likelihood decomposition or fresh-control model is selected from that census.
