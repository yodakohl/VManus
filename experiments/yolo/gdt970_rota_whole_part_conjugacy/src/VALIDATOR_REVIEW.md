# Independent GDT970 validator review

Full independent validation passes after public registration commit `3cc6748f5` (confirmation 13:13:52 UTC). Registration-only validation had passed before publication, with all six target cache bodies and hashes explicitly deferred. The validator imports neither the primary runner nor GDT928. It binds the frozen source and protocol, checks all 97 source events against the unchanged precomparison array, independently compares all 18 pes events to the earlier native-observation receipt, and reconstructs every source-consequence event/duration/block in all four branches. The parent GDT928 bindings are inherited unchanged.

All four declared duration branches retain P1 = AB and P2 = BA with 5 and 4 events respectively. Three branches give A = B = 12 conditional breves; the Hurry prose countercase gives A = 11, B = 12. Cyclic conjugacy survives that unequal duration branch. This is a source consequence, not native certainty about every duration or a complete account of musical initialization.

The 21 initial independent synthetic checks cover direct doubled-string offsets, equal strings and offset zero, unequal length, unequal inventory, equal inventory without cyclic order, minimum event length, variable event widths within a single group, one-group line eligibility, uncertain interior seams and paragraph-fence gaps. The independent cyclic canonical form is defined by enumerating rotations, rather than importing the runner's least-rotation algorithm.

The essential projection counterexample uses seven distinct equal-width, hence prefix-free, codes: F=`aaaa`, G=` aaa`, g=` a a`, A=`aa a`, C=`a aa`, B=`aaa `, R=`a a `. Here g denotes the second G event with its distinct duration. The complete pes event strings are `FGFgACBCR` and `CBCRFGFgA`. Their full encodings have length 36; trimming exterior spaces gives lengths 35 and 36, while both letter projections are 27 copies of `a`. Every code contains a letter; neither trimmed string contains consecutive spaces. Therefore full-code injectivity does not justify rejecting equal projected strings, and externally padded terminal spaces cannot be the decisive gate. All 27 projected offsets must survive this fixture.

A second counterexample retains a conjugate pair of nine different letters. It passes the necessary screen but cannot encode the repeated first/third F event with a non-erasing nine-event homomorphism. Thus even actual positive pairs would establish capacity only, with no event code, complete main melody, musical meaning, or independent confirmation claimed.

The full branch reconstructs the six original guarded GDT915 caches using explicit start-to-next-start intervals and the first intervening end fence. It checks the raw selector metadata before accessing groups, rechecks consecutive line and group indices, both sides of every interior seam, and all raw literal groups. One-group lines are eligible; the old two-group anchor flag remains separate metadata. It verifies all paragraph offsets, ownership, defects, full text and glyph projections, rather than trusting the primary paragraph output.

All 1,349 complete paragraph records reconcile: ZL3b has 659 complete / 32 literal / 627 nonliteral records; IT2a has 690 / 529 / 161; RF1b has no complete paragraph fences. There are 4 one-group lines inside eligible ZL3b paragraphs and 9 inside eligible IT2a paragraphs. The new one-group rule admits 1 and 6 additional complete literal paragraphs respectively. Six ZL and seven IT intervals fail the unchanged consecutive-line completeness rule. Nonliteral contents and missing RF paragraph boundaries remain unknown for the broader model.

Every unordered literal pair is independently checked with direct doubled-string searches. ZL3b has 496 pairs: 493 unequal lengths and 3 unequal inventories. IT2a has 139,656 pairs: 139,221 unequal lengths and 435 unequal inventories. All 140,152 pairs therefore have a necessary contradiction. The audit also checks all 438 equal-length certificate records, all 1,349 candidate-prediction rows, all 314 length-partition rows, same/different-leaf denominators, every partner list, and every result-scope field. No literal pair is below nine letters.

The actual primary data exercise only the length and inventory contradiction branches. No actual pair survives inventory to test cyclic order, identity offsets, nonzero offsets, a full event code or the main melody. Independently, the validator calls its direct-offset checker before the decision filter for all 140,152 pairs: unequal lengths return immediately, while all 438 equal-length pairs undergo doubled-string searches and yield no offsets. Thus no claim is made that the primary survived to its order or positive branch. Synthetic positives are software and logical checks; they are not manuscript positive evidence. No runtime cap, incomplete search or solver branch exists in this finite census.

The added EQUAL_LENGTH_COUNTEREXAMPLES.tsv is also independently checked in full: all 438 rows, their paragraph ownership and length, every sorted differing letter with its two exact counts, the decision, and the complete offset list. The validator computes this table from its independently reconstructed paragraph strings. The report's numerical and scope claims were reviewed and agree with these results, including the six/seven gapped intervals and one/six newly eligible paragraphs. No reporting or implementation correction was required.

The result excludes the declared full three-part copy model on the fixed literal panel through a necessary pes relation. It does not establish a musical translation, rule out music generally, resolve nonliteral readings, or demonstrate independent replication across editions. The generated VALIDATION.json records PASS / target_validation COMPLETE, exact frozen-input and output hashes, per-edition path coverage and retained limitations. Registration-only mode remains available and continues to label target validation NOT_RUN.
