# Fixed whole-part model and necessary test

The exact contract is [PREREGISTRATION.md](PREREGISTRATION.md). The source is
the complete paired pes notation of Harley978f11v, acquired and independently
compared before this target model. Under a shared concatenative event code,
the two whole parts AB/BA must yield cyclically equivalent glyph projections.
The full hypothetical code retains internal spaces; projection is only a
necessary screen and cannot itself decode words.

The primary implementation groups all complete literal paragraphs by the
lexicographically least character rotation. A distinct paragraph pair can
survive only with the same minimum-length-compatible canonical string.
Every possible pair remains in the denominator. The validator reconstructs
the intake separately and uses direct membership in a doubled string.

No source fragment, target word count, equal-width key or semantic label is
fitted. Literal exclusions, boundary absence, contradictions and surviving
capacity remain separate. Full musical timing, the main part, source-control
semantics and a prefix-free event code are not solved by this screen.
