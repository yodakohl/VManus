# GDT970 — whole-part cyclic consequence

Fixed test, registered before querying target paragraph contents or cyclic
classes for this model. Preparation begins13:03UTC on2026-09-15. The inclusive
checkpoint is13:35UTC, covering review, implementation, source checks, full
target census, independent validation and publication. No general decoder is
part of this work. The public lock must precede the first target execution.

## Decision and retained predecessors

The source-only Rota acquisition is public in e1e48a273, push confirmed
13:02:48UTC. It supplies97written events, with two complete nine-event pes
parts AB/BA. Their corresponding pitch/stem/ligature/pause events agree in
separately sealed observations, with the disclosed upper-ruling correction.
All four documentary timing branches retain conjugacy; numeric main-melody
duration and initialization checks remain qualified. See the bound full
source report and ROTA_VARIABLE_CODE_REVIEW.md.

Root read the live composition/variants topics, IDEA142, the relevant bounded
idea and duplicate searches, route-check, the closed-family rows, GDT928,
GDT893, GDT857 and the current source primaries. The old circular-inventory
history card points to a report absent from this checkout; it is not treated
as verified evidence or as proof that this screen is novel. GDT928 tested
multiword anchors inside lines across different leaves. This test uses whole
paragraph glyph strings, including same-leaf pairs and cuts within groups.
GDT893's fitted source windows and GDT894's adjacent-copy failure are not
reopened. GDT901's fixed solmization model remains closed. GDT608's directed
composition, whole-form residuals and entry effects do not establish this
context-free channel; they remain limitations, not rules to delete wrappers.

**Unknown:** does any pair of complete literal paragraphs have even the
necessary cyclic relation of the two whole source parts under a shared code?
If no pair survives, the stipulated complete-part copy family is contradicted
in this literal scope. If pairs survive, retain every one as capacity only;
a later full three-part code needs a separate decision, all source and space
constraints, and all equivalent witnesses. Either outcome changes whether a
full parser is worth considering. Do not automatically build it or repair
this channel after a negative. Source-copy identity remains a hypothesis.

## Hypothetical full channel, not an asserted musical reading

Three distinct complete paragraphs in one edition would encode the normalized
note/rest contents of M,P1,P2 through one global prefix-free code h. Events
are pitch/duration types, not occurrence IDs. Each codeword contains at least
one literal lowercase letter and may also contain ordinary spaces. Codewords
have arbitrary positive widths. Every target group and internal separator
must be explained by the full code; event boundaries may split a group or
span spaces. The observed whole paragraph is trim_spaces(h(part)). Only
exterior spaces are trimmed. All normal internal separators and line wraps
are serialized once as single spaces. No voice-specific header, null event,
prefix deletion, per-paragraph key or ignored text is allowed.

This encodes the complete normalized musical parts, not the whole source
page. Clefs/flat signs are interpreted into pitch. The entry/repeat rules
would be externally stipulated performance semantics, not decoded words;
the lyrics, Latin prose and cross are not extra target tokens silently
consumed by this model. Exact source controls or different headers would
require a different full hypothesis. A confirmed target word is not required
to propose this conditional reading.

Only the necessary consequence is tested here, so the source main-duration
uncertainty cannot produce a preferred branch. The source event-word relation
holds for any shared rotation-equivariant interpretation of the corresponding
pes notation, not just the four conditional numerical examples.

## Fixed consequence and all candidates

Let pi erase only the space symbol. Since P1=AB and P2=BA,

    pi(trim_spaces(h(P1))) = UV
    pi(trim_spaces(h(P2))) = VU

where U=pi(h(A)) and V=pi(h(B)) are nonempty. Therefore the projected whole
strings must have equal lengths, equal letter inventories and equal cyclic
canonical strings. Each pes has9events, so it needs at least9letters under
this channel. These are necessary conditions only. All literal paragraphs
shorter than9letters receive an explicit length contradiction.

Compare ALL unordered pairs of distinct complete literal paragraph IDs within
each edition, across any physical leaves or selectors. Retain equal projected
strings and rotation offset0. Projection loses injectivity; neither identical
projections nor different full lengths are exclusions. A pair occurring in
two alternate transcriptions is still one manuscript observation. No same-
leaf restriction, minimum number of target words, fitted paragraph length,
visual-owner selection or chosen matching passage is introduced.

Record first necessary contradiction in this fixed order: source minimum
length; unequal projected length; unequal letter inventory; unequal cyclic
order. A surviving pair is NECESSARY_CONSEQUENCE_ONLY, never a full code.
For complete audit, output every complete paragraph and its literal defects,
every paragraph's projected prediction class and possible partners, every
equal-length pair and decision, and counts for all pair decisions. Unequal-
length pairs are represented exhaustively by the complete length partition
and its combinatorial counts; they need not be duplicated in a large table.
Source/target tables preserve exact strings and provenance, not just hashes.

## Intake, exposure and data boundaries

Use the six guarded GDT915 SOURCE_DISCOVERY/EVALUATION JSON caches and its
179-selector SPEC. Both historical phase names are already exposed. Reuse
the frozen GDT928 paragraph assembly/fences: P lines, explicit starts/ends,
consecutive locus suffixes; gapped and unclosed material remains outside
complete coverage. Re-evaluate literal eligibility from the original cached
groups: each line nonempty, every raw group matches [a-z]+, source group
indices consecutive, and both sides of every interior separator read
DEFINITE_SPACE. A one-group line is allowed: GDT928's >=2 rule was for anchors
and is not an implication of this model. No uncertain group is normalized,
deleted or given a convenient reading.

All complete paragraphs, including ineligible ones, are reported. Literal
defects are missing test capacity, not contradictions of their contents.
RF1b's absent paragraph boundaries cannot become a negative manuscript result.
No new TSV is read; these JSONs were created through the guarded selector
access. Verify allowed selectors and reject raw f84-prefixed metadata before
group use. f84/f84r, f116v and every actual reserve remain closed.

All sources and all179admitted selectors have prior project exposure. Root
also knows GDT969's earlier literal/count capacity and GDT928's results; this
is a new fixed model, not a newly blind corpus. The new one-group rule and
cyclic output have not been queried before registration. All pairs are used
for candidate evaluation; there is no additional independent meaning panel.
Report physical-leaf ownership and cross-edition coincidences descriptively.
Independent confirmation capacity here is0; no reserve is opened to remedy it.

## Validation and claim ceiling

Before target execution, check the source AB/BA relation and source-only
fixtures: arbitrary code widths; event boundaries inside words; trimmed
exterior spaces; identical projections under distinct prefix-free codes;
identity offsets; same counts but different cyclic order; a conjugate pair
with incompatible repeated source events; and one-group/uncertain-boundary
intake cases. These are program checks, not an empirical null.

The independent validator must reconstruct paragraph intake and all pair
decisions from the guarded caches without importing the primary runner,
compare direct doubled-string searches with canonical-cycle decisions,
and check every candidate/prediction row and denominator. Every source hash
and saved result is bound. No primary PASS substitutes for an unchecked path.

No suitable countercontrol for the entire research search is supplied, so
there is no significance, posterior probability or expected-guess-count
claim. No independent semantic check is supplied, so no translated musical
term, note name or plant name is confirmed. A negative addresses only the
specified source-copy/channel and literal scope; a positive alone cannot
distinguish music from another cyclic list or explain the original spaces.
