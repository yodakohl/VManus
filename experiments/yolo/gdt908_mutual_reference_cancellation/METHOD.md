# GDT908 — finite mutual-reference cancellation

## Decision before target enumeration

GDT899 left all six full24-record header cases UNKNOWN in two string solvers.
The new falsifier is an exact necessary four-record identity, derived from its
unchanged historical compiler: N03/N09 occur in the MS03/MS09 differing headers
and at opposite MJ09/MJ03 terminal references. No source, compiler, plaintext,
channel, paragraph boundary, target or codeword bound is changed. No timed-out
engine is retried. This is a finite algebraic certificate, not an optimizer repair.

IDEA133's earlier assistant-written reopening text demanded a new observable
contract. That wording is overrestrictive for resolving an UNKNOWN with a new
exact certificate. This continuation follows the user's standing permission
for a genuinely different predeclared falsifier, not an assertion that the user
explicitly chose this proof or that the old clause automatically passed. Keep
the old curation and all UNKNOWN receipts intact; record this scoped decision
explicitly. IDEA139 retains the new proposal and the distinction.

Information gain: an empty necessary domain resolves the full original model
as impossible; a nonempty domain yields explicit possible N03/N09 substrings
and, after the fixed refinement, complete nine-atom/four-record witnesses.
Those witnesses alone leave the61-atom/24-record model UNKNOWN. No four-record
key or retrieved prognosis is a translation. Either way, this finite pass
ends without new corpora, template repairs, relaxed constraints or automatic
solver follow-ons. The user removed the time budget; there is no clock cutoff.

## Theorem covering every header order

Let A,B,C,D be complete outputs for MS03,MS09,MJ03,MJ09 respectively, and let
X=code(N03), Y=code(N09). For every one of the six header permutations,

    A = U X V       B = U Y V
    C ends in Y    D ends in X

HOUSE always precedes N, so |U|>=1. PARTILE and RET follow N in the two MS
traces, so |V|>=2. All codewords are nonempty and pairwise prefix-incomparable;
in particular X and Y are nonempty and neither is a prefix of the other.
C and D have nonempty prefixes before their terminal values. The full model
requires all source outputs distinct, hence A,B,C,D are four distinct strings.
These are necessary conditions, not sufficient interpretation rules.

## Fixed exhaustive procedure

Hash-check SOURCE_PROGRAMS and TARGET_INPUT from899. Verify the four source
traces and the theorem's factor lists directly for all six header orders.
Use the exact existing IT2a259-paragraph panel, deduplicating identical target
strings while retaining all source IDs. Preserve all original characters and
single spaces: cuts may occur at ANY character, not only group boundaries.
All other panels remain below the original24-record capacity; no new data.

For every unordered pair of distinct whole strings A/B, enumerate every
common-prefix cut u>=1 and common-suffix cut v>=2 that leaves nonempty X/Y.
Reject prefix-comparable X/Y. Enumerate proper-suffix whole-string witnesses
D for X and C for Y, excluding A/B and requiring C!=D. Keep every surviving
cut and every possible C/D index. The pair certificate records even empty
pair domains, with cut and rejection-stage counts. No source block or target
window is shortened: X/Y are hypothetical atom values inside whole equations.

If relaxed survivors exist, refine every cut in BOTH A/B source orientations
and all six header orders. Positively split U and V into their exact ordered
HOUSE/MERCURY/SATURN/PARTILE/RET factors and require prefix incomparability
with X/Y and each other. In each whole C, JUPITER occurs once before REF;
its positive length ranges over the exact remaining character budget, REF
is then the complete residual before terminal Y. Check the complete D
expression, all four full equations, distinct whole targets, and all nine
nonempty pairwise prefix-incomparable codewords. Preserve every complete
four-record witness and its full source/target provenance. A/B reversal is
mandatory because MJ03 contains RET before REF and MJ09 does not.

No witness in the relaxed domain, or none after exact four-record refinement,
implies no full24-record solution for every covered header. Any survivor is
only a necessary partial-model fit; no new full solver or meaning claim follows.
Independent validation must rederive the source theorem, target string table,
all pair certificates and surviving cuts/witnesses, without importing the runner.
A small explicit positive nine-code fixture must exercise all six headers and
both source orientations so an always-empty implementation cannot pass.

## Scope and evidence limits

The packet is already exposed odd-leaf discovery from893/899, not held
confirmation. Alternate readings are not independent manuscripts. f84 and
f84r stay sealed; no native image, new selector, mixed TSV or even-leaf body
is opened. GDT899's PARTILE and RET serialization remains a declared compiler
hypothesis, not literal historical wording. An exact exclusion would concern
only that entire source/compiler/channel/target conjunction, not astrology,
conditional prose, or Voynich meaning generally.
