# GDT908 — exact cancellation resolves the fixed899 model

The unchanged GDT899 source/compiler/code/target conjunction has no solution
for any of its six global header orders. A necessary four-record condition
already has an empty domain. This resolves a previously UNKNOWN model; it
recovers no manuscript word, key or translation and does not exclude astrology
or conditional prose generally.

## Finite certificate

| Exhaustively covered object | Count |
|---|---:|
| Original IT2a whole paragraphs / distinct strings |259 /259|
| Unordered pairs of whole paragraphs |33411|
| Nonempty middle cuts with shared prefix and suffix |3577|
| Cuts with prefix-incomparable middle values |3577|
| Cuts whose canonical first middle is a proper suffix of another permitted paragraph |0|
| Relaxed four-distinct-paragraph assignments |0|
| Exact nine-atom/four-record witnesses |0|
| Original header orders excluded by the necessary condition |6|

Let A and B encode MS03 and MS09, and X/Y encode N03/N09. Their frozen
source traces differ only at that house value. Under every header order,
A=UXV and B=UYV, with |U|>=1 because HOUSE precedes N, and |V|>=2 because
PARTILE and RET follow it. The complete MJ03 output must end in Y; MJ09
must end in X. All four outputs must be distinct whole paragraphs.

Every permissible shared-prefix/shared-suffix cut of every paragraph pair
was enumerated at character resolution, including cuts inside written groups
or spaces. In none of3577 cuts is even the canonical first middle a proper
suffix of a third permitted paragraph. Therefore the required two opposite
reference suffixes cannot coexist. Swapping the two source roles cannot
rescue this: both middle values require external suffix witnesses. The exact
four-record refinement is not reached on manuscript data. No general string
solver, key-length cap or timeout is involved in this certificate.

PAIRS.json retains all33411 pair rows, including empty domains and rejection
counts. TARGET_IDS.json binds every exact complete string to its inherited
paragraph IDs. THEOREM.json records the actual U/V atom factors for all six
header orders. RELAXED.json and WITNESSES.json are empty arrays, not missing
or timed-out computations.

## What changed, and what did not

GDT899's original twelve solver receipts remain UNKNOWN and unmodified.
The new mathematical certificate decides their shared fixed model, using
exactly the old24 source traces,61-atom nonerasing prefix code,259-paragraph
IT panel and six header alternatives. The other original panels remain below
24 records; no target was added, edited or resegmented. No new source, raw TSV,
native page, even-leaf body or held data entered.

This is a different predeclared falsifier, derived from the source's mutual
references. The continuation decision in METHOD.md explicitly distinguishes
it from the older assistant-written demand for a changed observable contract;
that old decision was not silently treated as satisfied or deleted.

The source compiler is itself hypothetical: common RET tokens address
prognosis prose externally, and PARTILE includes inherited context. No claim
that those fields are written literally in Voynich follows. The exclusion
belongs to this complete fixed conjunction, not to the whole historical
interpretation family. No corpus, compiler or optimizer repair follows.

## Reproduction

Registration94cd7bf3 was public before target pair enumeration.
Run `python3 experiments/yolo/gdt908_mutual_reference_cancellation/src/run.py`.
Run `python3 experiments/yolo/gdt908_mutual_reference_cancellation/src/validate.py`.
The separate `--fixture-only` runner mode checks source-derived known positive
nine-code examples for all six headers in both A/B orientations without
opening manuscript targets. These are implementation checks, not decipherment.
The primary target enumeration took approximately0.10seconds; that timing
excludes preparation, validation and publication and is not a research result.

Independent source-to-result validation passes: all six theorem factorizations,
the entire259-string/ID table, all33411 pair certificates and3577 cuts, and
the empty suffix-witness domain agree. Twelve separate variable-length positive
fixtures recover the known necessary-domain witnesses in both orientations.
Primary exact-refinement fixture multiplicities were not independently certified;
that stage is unused in the manuscript exclusion. Validation certifies the
finite certificate, not historical identity or manuscript meaning.

Focused staged privacy/scope/binding checks pass. The global audit retains the
existing seven GDT600 binding errors and stale global index group; no unrelated
repository repair or global PASS is claimed.
