# GDT375 — comparator-derived functional-operator roadmap

GDT375 revises the GDT373 roadmap before any new comparator-oracle evaluation.
It registers fifteen comparator-derived attack points as **distinct hypothesis
families**, not subnotes under the older broad families. The full ranked table
retains all twenty-two GDT373 families, giving 37 separately addressable
routes.

The priority order is fixed as requested: state-transition fingerprints;
local valency/predicate heads; REF/anaphora/ellipsis; long-distance
correlatives; NEXT/RESUME; UNTIL/state gating; AND, OR, and polarity; then
pointer/relation, exclusion, comparison, information bottleneck, scope horizon,
and a latent procedural automaton. Older broad routes follow.

## Comparator-first gate

The six frozen CoReMA collections are evaluated leave-one-collection-out.
Held oracle values for `ALTERNATIVE`, `TIME`, `REF`, `CLOSER`, exclusion,
analogy, comparison, and `parent_instruction_ordinal` are not detector inputs.
Source strings and editor English labels are also forbidden. Observable forms
are opaque equality IDs plus record order, local recurrence, lengths, and
cross-record reuse.

A signature may transfer to Voynich only if it improves over both position/
length nuisance and opaque exact-identity baselines, produces positive held
gain in at least four collections, and clears the frozen discrimination and
max-family controls. A failure remains useful calibration and is not repaired
after Voynich inspection.

## Claim ceiling

This registry assigns no Voynich function, gloss, morpheme, POS, language,
plaintext, or translation. It does not score Voynich and does not access f84.
