# GDT739 working protocol

## Target

Re-render exactly the 202 GDT738 position licences for exactly twelve complete
forms. Do not add discovery-only forms or positions.

## Planned comparison

Compare three occurrence renderers:

1. the inherited GDT738 family-oriented whole card;
2. an active host-first renderer using eligible neighbors at distance one or
   two;
3. a distance-three-to-five sensitivity deck that is recorded but never
   spoken.

## Planned decisions

- Scalar rings choose one of quality degree, amount/portion, counted process
  passage, conflict, or open.
- State/result forms separate descriptive mode, result/end mode, favored local
  axis, and broad carrier.
- The nearest ring wins only when its scalar types agree.
- A carrier names only preparation/material/part class.
- An absent or conflicting binder preserves an explicit open slot.

## Required outputs

- exhaustive radius-five neighbor audit;
- one dispatch row for every source patch;
- one profile for every licensed whole;
- twenty representative cached lines covering all twelve forms;
- compact result JSON, executable builder, and independent validator with
  byte-identical replay.

## Non-exports

No component, body, head, unseen form, literal patient, species, lexeme,
plaintext sentence, new page, `f84`, or `f84r` may be exported.
