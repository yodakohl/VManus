# GDT521 compact working book

## Live stack

`exact event > known surface/role > finite compiler > visible form/context >
stem/short-renderer alignment > visible boundary license > short recipe history`

Selected final addition:

```text
0.50 * mean add-0.5 order-five recipe NLL
```

It uses at most four previous components and never a whole surface identity.

## Counts

- old history model: 1,558 surface types, 1,993 histories, 3,284 observed
  transitions, 44 tokens;
- old four-fold: 1,090 rank one, 1,325 top two, 1,387 top three, 1,418 top
  five, rank sum 2,118, deepest 22;
- current 159: 140 rank one, 154 top two, 158 top three/top five, rank sum 189,
  deepest 9;
- one correction (`psheody`), no lost hit, one wrong-to-wrong change (`dyky`),
  nineteen remaining top-one errors.

## Established working contrast

- `psheody → P+SH+E+O+D_ADDR+Y`;
- `shckheody → SH+CH+K+E+O+DY`.

Old terminal inventory: `O+DY` 30 types, `O+D_ADDR+Y` 17, `O+Y` 20;
`OL` 107, `O+L` 6.

## Next route

Build an analogue-transformation atlas for the nineteen residuals. Search old
surface/recipe pairs for the same one-local-change operation:

- inserted or swallowed `a`, `d`, `q`, `ch` and local characters;
- `DY ↔ D_ADDR+Y`, `OL ↔ O+L`, `AIIN ↔ IIN/address pieces`;
- renderer whole ↔ atomic split.

Require a reusable transformation signature, not a target full-form card.
The exact old tails needed for `dsholdaiir` and `saiis` are absent, so those
must stay provisional until an analogue family supplies a real route.
