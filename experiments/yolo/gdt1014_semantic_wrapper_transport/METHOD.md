# GDT1014 method

The entire52case/312member population is fixed in artifacts/PREDICTIONS.json.
Every full original47word dictionary is retained; unlike1013,the quotient uses
all47values because a morphological relation may involve an old word absent
fromf50r. Cargo renaming alone yields sixfamilies and26valid settings,times two
reader paragraphs. No non-cargo value,meaningvariant,source or grammar changes.

For each joint vocabulary,select only whole raw strings matching [a-z]+ for
wrapper analysis. For every attested base and result differing by a literal
prefix or suffix of length1–3,retain that directed pair. Keep a wrapper only
if it has at leasttwo distinct pairs. All such pairs are included; no frequency,
semantic plausibility,convenient prefix or old/new classification selects them.
Nonliteral source groups remain as opaque words in the full grammar and
semantics. There is no extraction/normalization inside uncertainty notation.

L_p(v(a))=v(pa) and R_s(v(a))=v(as). For each samewrapper pair,a=c in terminal
value requires b=d in terminal value. L and R are different function families.
No injectivity,commutativity,extra implicit state or word-identity override is
allowed. Functions are otherwise unconstrained; the test imposes consistency,
not independently known component translations. Unlinked words retain explicit
whole-word aliases. This is a partial writing-law test,not a complete parser.

Primary: unchanged GDT1013 Boolean-location fullworld plus pairwise implications.
Independent: copied GDT1013 integer/boat/STAY fullworld plus unary function
applications,with independently enumerated trimming relations. The preflight
checks exact source difference: only the wrapper hook was added to eachmodel.
Both inherit the prior1152full-semantic differentialfixtures. New81assignment
truth-table cases exhaust a two-pair,three-value function; all52inventories are
independently reconstructed. Every29old saved complete witness is checked as a
fixed lexical/parse fixture,not refitted.28violate the law and1satisfies it;these
are not an exhaustive new search. Original-only precursor:36compatible,0failed,
one recurrentwrapper with unequal known inputs,thus no candidate separation.
The precursor was locally registered before checking but not yet public; this
exposure and the fixed-witness checks precede the new public registration.

All52unconstrained full cases run in both solvers. SAT requires direct wrapper
checking, complete grammar coverage and replay by both frozen993/994/1006full
executors. Every positive is lifted into each of its sixoriginalmembers,with
all47values and bothwholeparagraphs checked. UNSAT closes a case only when both
solvers agree. UNKNOWN or unverified replay remains unresolved. Every312case
gets a table row,including contradictions/unknowns. Previous unconstrained1013
SAT remains unchanged even when this stronger law fails.

Z3limit30seconds;cvc5limit45seconds;processwall150seconds;1GiB;16workers;
fullreplay10seconds. No extension of limits,extra domainenumeration,modelrepair
or post-result wrapper removal. Observe the inclusive02:37UTC checkpoint.

Previously exposed originalZL63group seam and bothuncertainf50rreaders only.
ZL/IT are alternate readings of onephysicalpassage. No newtext,image,reserve,
f84/f84r,f116v or contact. No all-searchnull,significance or independentmeaning.

Reproduce with Python3.12 and versions in requirements.txt; dependencies may
be installed in any chosen virtual environment. From the repository root:

```
python3 experiments/yolo/gdt1014_semantic_wrapper_transport/src/run.py
python3 experiments/yolo/gdt1014_semantic_wrapper_transport/src/validate.py
```

The public commit is bound in artifacts/PUBLIC_REGISTRATION.json; locks reject
changed scientific/input bytes. Results and readable summaries are separate
from the frozen scientific inputs. Precursor registration is intentionally
non-overwriting; its recorded data can be independently reconstructed.
