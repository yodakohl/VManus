# GDT526 artifacts

- `gdt526_result.json` — compact selected policy and all principal metrics.
- `gdt526_model_ladder.tsv` — base, bonus-plus-one, bonus-only, and binary
  variants on the old and both current target decks.
- `gdt526_1558_four_fold_cha_rehearsal.tsv` — complete rotating old-form
  rehearsal.
- `gdt526_159_cha_rerank.tsv` — inherited and family-revised ranks for every
  current form.
- `gdt526_cha_route_atlas.tsv` — the two licensed stem routes, including
  support and bonus.
- `gdt526_candidate_score_atlas.tsv` — all selected candidate scores and
  traces.
- `gdt526_changed_decision_atlas.tsv` — the `chady` and `chap` corrections.
- `gdt526_revised_remaining_top1_error_atlas.tsv` — eleven-form next-work
  queue.
- `GDT526_CHA_STEM_WORKING_BOOK.md` — compact continuation basis.
- `gdt526_validation.json` — 28 independent consistency checks.
