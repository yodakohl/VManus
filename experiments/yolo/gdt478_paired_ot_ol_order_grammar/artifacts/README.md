# Artifacts

- `GDT478_PAIRED_OT_OL_ORDER_GRAMMAR.md`: readable paired grammar and complete
  41-slot OT audit;
- `gdt478_69_paired_order_scope_occurrences.tsv`: all OT and OL slots in source
  order;
- `gdt478_60_paired_order_event_editions.tsv`: complete order-bearing event
  edition;
- `gdt478_5_paired_order_scope_rules.tsv`: observed root×direction rules;
- `gdt478_7_ot_ol_joint_events.tsv`: every event carrying both roots;
- `gdt478_6_page_order_summary.tsv`: page totals;
- `gdt478_result.json`: compact result;
- `gdt478_validation.json`: deterministic validation certificate.

All artifacts are rebuilt by `../src/run.py` and checked by
`../src/validate.py`.
