# GDT478 — paired OT/OL order grammar

Status: `OT_STARTS_NEXT_UNIT__OL_KEEPS_CURRENT_UNIT__PAIRED_ORDER_GRAMMAR_COMPLETE`

All 69 admitted local order-root slots now share one paired state grammar:
`OT=DANACH` starts the next sibling unit, while `OL=FORTSETZEN` keeps the
current unit active. OT has forty forward openings, one between-name bridge and
no backward hold; OL retains GDT477's 9/10/9 directional split.

Seven events contain both roots. OT precedes every OL in all seven, so forms
such as `otol` can be read as two predictable control cards—start the next unit,
then continue it—rather than one learned compound.

Start with
[`artifacts/GDT478_PAIRED_OT_OL_ORDER_GRAMMAR.md`](artifacts/GDT478_PAIRED_OT_OL_ORDER_GRAMMAR.md).
See `METHOD.md`, `REPORT.md`, and `experiment.json` for the complete route.
