# GDT606 artifacts

`mixed_attack_result.json` and `carrier_stability_result.json` are the compact
results. `complete_mappings.tsv` retains all 48×98 assignments;
`held_decodes_*` retains every held chunk for every primary start.
`primary_unit_stability.tsv` and the three all-grid category tables expose the
structural whole-word-category lead. `positional_word_carrier_audit.tsv` shows
why apparent dictionary words fail; `carrier_stable_fragments.tsv` contains
the sole surviving prefix-family trace. `binding_inventory.json` seals source
and artifact hashes, and `gdt606_validation.json` records validation.

`guarded_rows.tsv` and `unit_sequences.json` are retained because they are the
compact, f84/f84r-free reproducibility bridge from the guarded selector query
to every decoded chunk. No sealed selector is present.
