# GDT369 order-preserving geometry-null report

Status: **GDT368_ASSOCIATION_NOT_UNUSUAL_UNDER_ORDER_MATCHED_NULL**.

GDT368's fixed `ACA`×terminal-arm association has CMI 0.666667 bits/row.
Against ordinary within-array permutations it had complete-library maxT
p=.002929, but that null destroyed the arrays' visible clustering.

After preserving both exact array state counts and each array's exact number
of adjacent equal-state pairs:

- fixed-candidate exact tail: 72/2,880, **p=.0250**;
- terminal-arm endpoint max-mask tail: 168/2,880, **p=.05833**;
- sampled all-three-endpoint/max-mask tail: 245/4,096, plus-one
  **p=.06004**;
- identity/reversal-only fixed and endpoint-max tail: 4/32,
  **p=.125**.

Thus gross array order accounts for much of the apparent rarity. The fixed
association does not survive the frozen global order-matched diagnostic at
.05, and its visual-count direction already reverses in f99v/L1. The
major-body endpoint has an endpoint-only adjacency max p=.0423, but this is not
the fixed candidate and does not survive the three-endpoint global comparison;
no alternate row is promoted.

The adjacency null preserves one scalar measure of clustering rather than the
entire physical sequence, while the reversal null preserves the whole sequence
but has only 32 worlds. Both point away from a stable shared geometry code.
GDT368 and GDT369 together close this postselected quantitative-Pharma route.

The pre-score physical-ordinal correction is documented separately. No visual
or formal feature was reselected, no semantic label was assigned, and no f84
source or image was accessed. The result identifies no ownership, object,
plant part, role, word, language, plaintext, meaning, or translation.
