# GDT369 order-preserving geometry null

Status: **POSTEXPOSURE DIAGNOSTIC FROZEN BEFORE RESCORING**.

GDT368 found a maxT-surviving but directionally unstable association between
family trigram `ACA` and the three-bin terminal-arm count. Its within-array
null preserved category counts but destroyed local spatial clustering. GDT369
asks only whether that fixed GDT368 association and the fixed 27-mask library
remain unusual when gross array order is preserved more closely.

No visual state, formal feature, support threshold, endpoint, or candidate is
reselected. The complete GDT368 panel and feature masks are inherited byte for
byte.

## Primary adjacency-matched null

For each endpoint and each physical array, enumerate every unique permutation
of the frozen visual labels that preserves:

1. the exact state count in that array; and
2. the exact number of adjacent equal-state pairs in the linear left-to-right
   sequence.

Take the Cartesian product across arrays. The exact endpoint orbit sizes,
determined from visual labels before rescoring, are:

- major-body count: 1,040 worlds;
- terminal-arm count: 8,640 worlds;
- dominant hue: 120 worlds.

For the fixed `ACA`×terminal-arm candidate, report the exact inclusive local
tail and the exact terminal-endpoint max-mask tail over all 8,640 worlds.

For a common three-endpoint/max-mask diagnostic, draw 4,096 deterministic
uniform worlds independently from each endpoint's full adjacency-matched
orbit and take the maximum CMI across all endpoints and all 27 frozen masks.
This is a Monte Carlo global diagnostic, not an exact p-value.

## Reversal sensitivity

Also enumerate every unique Cartesian product of identity/reversal for each
linear array. This preserves all adjacency and run lengths but has very low
capacity. Report it descriptively for the fixed candidate and endpoint max.

All statistics, conditional-MI definitions, rows, masks, and ordinal direction
diagnostics are inherited from GDT368. No semantic alignment or alternate
feature search is allowed. The result can only say whether GDT368's anomaly is
robust to gross spatial order; it cannot identify an object, role, word,
meaning, or language. No f84 source or image is eligible.
