# V79 four-role selection — apprentice reconstruction

## Selected result

The V78 edition is teachable as an **exemplar-dependent formal notation**, not
as a self-reading semantic code.

- all four roles audit the same 19 statement-internal physical-line crossings;
- all four obtain exactly one generic read-once match, `E180→E181`;
- the other 18 crossings remain ordinary continuations;
- four of those 18 also contain visible owner resets;
- H2, B2 and the left f69 wheel round-trip exactly as form;
- concrete Herbal, Biological and celestial values do not round-trip without
  the master exemplar;
- no new word is added.

Selected status:

`EXEMPLAR_NOTATION_FORMALLY_LEARNABLE__CONCRETE_CONTENT_MASTER_DEPENDENT`

This is a consistency test of the creative ten-page edition, not a
decipherment result.

## The read-once rule passes narrowly

The rule is stated without page, event or word identity:

1. take the last exact card of one physical line and the first exact card of
   the next;
2. require the same statement;
3. require the same exact card;
4. require the same visible owner;
5. require no intervening Close;
6. if all conditions hold, preserve both visible copies but read the first as
   an anticipatory edge copy and emit the second once.

Across the full 19-transition opportunity set the result is:

`TP=1, FP=0, FN=0, TN=18`

The sole match is E180/E181. Thus an apprentice needs no locus list. This
closes the narrow V78 mechanical falsifier. It does **not** prove that the
historical writer intended a catchword. One positive case cannot distinguish
intentional anticipation from local dittography, and ordinary medieval
quire/page catchwords are not the same mechanism. The selected name is:

`LOCAL_ANTICIPATION_CARRY_OR_DITTOGRAPHY__READ_ONCE`

## What the apprentice can do

The selected R3 machine supplies sixteen rules. A workshop learner can:

- copy exact opaque cards from a code sheet/master exemplar;
- retain record, field, statement and physical-line topology;
- keep a statement open across a line;
- reset substance, target and direction at a visible owner break;
- preserve both E180 and E181 while counting one source token;
- keep Astro labels in one local wheel/field namespace;
- verify exact form in both directions.

The representative traces cover:

| unit | visible/formal inventory | result |
|---|---:|---|
| H2 | 24 prose cards | exact formal round-trip |
| H4 | 18 prose cards | exact formal round-trip |
| B2 | 62 visible cards, 61 source positions after read-once | exact formal round-trip, four owner resets |
| f69 left wheel | 28 local slots, 33 opaque groups | exact local-slot round-trip |

R3's combined machine test therefore reconstructs 137 exact formal atoms both
with and without the master (104 visible prose cards plus 33 Astro groups).
But without the master it recovers **0/103** concrete prose source positions
and **0/28** concrete Astro values. With the master, the meanings are retrieved
by lookup, not inferred from the script.

## `ET?` and `PER?`: selected two-layer treatment

The four roles agree on form and disagree only on whether the two historical
word candidates should be spoken during autonomous readback.

### `dcda…`

- Historical admissibility: Fi1 (1414) contains an actual whole-word codebook
  category `et`; therefore `ET?` is allowed under the user's codebook rule.
- Internal result: all 19 positions are equally compatible with a silent
  `FORMAL_LINK_OR_SLOT`.
- Selected operational value: `FORMAL_LINK_OR_SLOT`.
- Optional master-supplied word gloss: `ET? = UND/AUCH?`.

### `b5fcea…`

- Historical admissibility: Fi1 (1414) contains an actual whole-word codebook
  category `per`; therefore `PER?` is allowed under the same rule.
- Mechanical result: nine visible cards become eight source positions after
  the one read-once pair; the generic repair is reversible.
- Internal result: `FORMAL_RELATION_OR_ENTRY_MARK_WITH_ENTRY_BIAS` is simpler
  or tied and requires no semantic label.
- Selected operational value:
  `FORMAL_RELATION_OR_ENTRY_MARK_WITH_ENTRY_BIAS`.
- Optional master-supplied word gloss: `PER? = DURCH/GEMÄSS?`.

This does not contradict the historical source. A period codebook establishes
that the categories existed, not that these two Voynich card identities encode
them. V80 must show the operational value first and the questioned word only as
a rival/master convention.

## Four-role comparison

| role | formal result | semantic result | distinctive contribution |
|---|---|---|---|
| R1 workshop master | PASS; 114 trace rows | no content without exemplar | readable 16-step manual; PER survives narrow mechanical test |
| R2 historical scribe | PASS; 119 trace rows | formal LINK/ENTRY wins internally | distinguishes local duplication from standard catchword practice |
| R3 technical writer | PASS 106/106; 264 trace rows | 0/103 prose and 0/28 Astro without master | deterministic state machine and exact two-way accounting |
| R4 corrector | PASS 14/14; 12 aggregate traces | no independent concrete content | compact ceiling and no-new-word enforcement |

## Selected artifacts

- `V79_SELECTED_MACHINE_MANUAL.tsv` — R3's deterministic sixteen-rule
  manual;
- `V79_SELECTED_FORWARD_BACKWARD_TRACES.tsv` — R3's complete 264-row trace;
- `V79_SELECTED_19_LINE_TRANSITION_AUDIT.tsv` — all 19 opportunities;
- `V79_SELECTED_REPAIR_DECISIONS.tsv` — R2's historical/formal decisions;
- four independent role reports and their validators;
- `V79_VALIDATION.json` — central binding and validation.

## V80 instruction

The canonical third edition must retain:

1. the exact opaque visible form;
2. local image ownership and owner resets;
3. the general read-once rule, explicitly marked single-positive and
   unconfirmed historically;
4. `FORMAL_LINK` and `FORMAL_RELATION_OR_ENTRY` as the autonomous operational
   readings;
5. `ET?` and `PER?` only as historically admissible, master-supplied question
   glosses;
6. every other card as `EXEMPLAR_VALUE_UNKNOWN` unless it is merely a formal
   nonword channel;
7. concrete prose only in an explicit occurrence-bound exemplar layer;
8. local celestial namespaces with no start, direction or f68↔f69 key.

V80 ends the authorized cycle. No additional page or word is licensed here.
