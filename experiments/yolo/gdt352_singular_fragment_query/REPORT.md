# GDT352 report

## Result

**SINGULAR_QUERY_NEGATIVE_WITH_AMBIGUOUS_COMMON_FORM_LEAD**

The overlooked singular query is real as an ownership fact but negative as a
formal anchor. Human sources bind f99r fragment 94 to the sole label in its
one-plant bottom row, `f99r.46`, and independently describe the fragment as
similar to the full plant on f96v. The label has no exact surface occurrence on
f96v in ZL3b, IT2a, or RF1b and no exact source-family occurrence there.

The nearest surface similarity is weak: the query ranks 22/26, 24/26, and
24/26 among formally covered f99r plant labels for the three readings. Its consensus
family ranks 15/26 against f96v groups. This is a counterexample to exact label
or family invariance for the human-nominated referent.

The second catalogue relation, f96v to f100r fragment 116, remains ownership
ambiguous. One of its two fixed candidate loci, `f100r.3`, is `otear` in all
three readings and has family `AQJAC`; f96v contains nearby `ytear`/`yteor`
surfaces and exact family `AQJAC`. This is the strongest descriptive lead, but
it does not identify the owner: the row has five plants and six labels, the
source explicitly questions the offset, and `AQJAC` is common (56 group rows
on 44 non-f84 pages). Within the five formally covered top-row labels,
`f100r.3` ranks first by both surface and family similarity, but the row's
disputed one-position ownership shift prevents that formal result from
selecting it as fragment 116's label.

## Interpretation

This result repairs the source atlas without rescuing the referent route. The
only new singular query fails. The attractive fragment-116 near-pair is exactly
the kind of ownership-ambiguous, common-form coincidence that must remain a
YOLO lead rather than a gloss.

The experiment is post-exposure exploratory because the singular label's
surface was displayed during the audit before scoring. No f84 row was parsed,
retained, displayed, joined, or scored.

## Next route

Do not mine more catalogue proximity rows. A useful acquisition must provide a
new source-bound repeated referent with singular inscription ownership, or an
external readable diagram with an authored slot-to-text mapping and an
independent held folio.
