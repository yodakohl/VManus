# Artifacts

- `gdt352_query_inventory.tsv`: source-bound ownership states.
- `gdt352_scores.tsv`: reading-specific and family-level comparisons.
- `gdt352_source_digests.tsv`: hashes of only the guarded selected rows.
- `gdt352_result.json`: compact decision and provenance.
- `gdt352_validation.json`: independent reconstruction.
