# GDT386 source audit

All machine inputs are compact artifacts previously published as f84-free:

* `gdt169_external_referent_candidates.tsv` — complete source-selected
  Herbal/pharmaceutical/internal-Herbal relation census; its source report
  explicitly rejects f84 before retention.
* `experiments/yolo/gdt351_remaining_referent_label_capacity/artifacts/gdt351_capacity.tsv`
  — the four remaining asserted-same target ownership checks.
* `experiments/yolo/gdt360_existing_annotation_joint_grounding/artifacts/gdt360_annotation_inventory.tsv`
  — guarded non-f84 annotation cases; no formal identity columns are used.
* `experiments/yolo/gdt337_external_homologue_census/artifacts/gdt337_candidate_correspondences.tsv`
  — complete geometry-only external candidate gate matrix.
* `q20ob001_source_panel.tsv` — 170 star-defined Q20 records in each alternate
  reading, restricted to f104–f115. Only unit/page/folio and schema fields are
  retained by GDT386; transcription payload columns are never parsed.

ZL3b, IT2a and RF1b are alternative readings, so Q20 contributes 170 records,
not 510 independent observations.

GDT385's compact result is used only to bind the preceding comparator decision:
one anonymous route passed, but the complete parent-link instrument did not.
No comparator readable role label is transferred.

The audit does not open global annotation/transcription tables, cached images,
or f84 material.
