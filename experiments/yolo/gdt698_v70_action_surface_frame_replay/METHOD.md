# GDT698 method

## Question and fixed scope

Does an unbound occurrence of any action surface used by V70 reproduce an
already observed exact participant frame?

The scope remains the same 479 tokens, 51 lines, three spans and 36 pages.
GDT697 supplies seven microrecords and nine occurrence edges. No transcription,
image or page is opened; f84/f84r remain forbidden.

## Exact frame deck

`src/V71_EXACT_TARGET_FRAME_TEMPLATES.tsv` stores one target-aligned surface
hull for each of C001–C009. The nine templates are:

- `olpcheey ykaiin`, `cthororaiin yteeeor`, `orchey qey`;
- `olkar y qol`, `chcphey qol`;
- `qodar ykaiin`, `qokar olkar qodar`;
- `olkar y qol qol`, `otaiin ydaiin qokamdy`.

For every exact occurrence of the template's final action surface, the same
number of positions is aligned immediately to its left and compared with the
stored surface sequence. Surface equality is the replay gate. The inherited
V69 roles are checked afterward as source provenance; they are not used to
manufacture a match at an unbound occurrence.

This is intentionally narrower than a semantic-class or grammatical replay.
An action word alone, the broad `NOMINAL_BLOCK → ACTION_CLAUSE` shape and the
nearest noun are explicitly insufficient.

## The open `qol` contrast

The only unbound scanned occurrence is f77r.38#9. It is aligned separately
against all three `qol` templates:

- C004 expects `olkar y qol`; the candidate has `ltaiin shedy qol`;
- C005 expects `chcphey qol`; the candidate has `shedy qol`;
- C008 expects `olkar y qol qol`; the candidate has
  `qol ltaiin shedy qol`.

The mismatch counts are 2, 1 and 3. None of the candidate's aligned nominal
positions has a V69 participant role. The superficially similar two-token
nominal block before f77r.38#6 is retained only as a shape-level false friend.

## Decision rule and claim ceiling

Pass requires exactly six action surfaces and ten occurrences, nine templates
covering C001–C009 once, nine exact source matches, zero cross-occurrence
matches, one unbound `qol` held against three failed contrasts, and the complete
479/51/3 V70 freeze with zero new edges, microrecords, meanings or pages.

V71 is an exact-string transfer audit. It does not establish that an action
surface determines its participant frame, and it supplies no default object,
destination, product, plaintext or language claim.
