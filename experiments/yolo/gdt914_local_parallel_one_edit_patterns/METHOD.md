# GDT914 — local parallel single-edit discovery

User requests cheap useful reruns on expanded admissions, otherwise new pattern
checks. GDT787,748,854 already use179 text selectors; current extra visual
admissions do not supply new text to them. No unchanged854/787 rerun. GDT793's
35-selector visual ownership cases do not gain independently annotated matching
owners merely through a larger text inventory. No such semantic rerun selected.

New bounded observable: in four consecutive written groups, do TWO distinct
pairs exhibit the SAME exact single-character edit at the SAME left- or
right-anchored position? GDT003 predicts dictionary squares, GDT851 exact tandem
identity, GDT748 inherited-card neighborhood concordance, GDT787 context-profile
addition. None of those reviewed contracts is this complete local occurrence
predicate. IL008's generic adjacent-distance inference remains closed: no new
distance score or alternative p-value is substituted here. Lexical route screening
is navigation, not novelty proof. Prior whole-project source exposure disclosed.

Before cache access: use unchanged GDT851 SOURCE_ZL3b/IT2a/RF1b.json, all179
allowed selectors and all recorded source kinds, separately. No new raw TSV,
image, page or normalization. f84/f84r remain sealed. Four groups must have
consecutive source indices and three bilateral DEFINITE_SPACE seams. Require
four DISTINCT nonempty raw [a-z]+ forms; retain excluded-window denominators.
This avoids mere repetitions counting as two changed bases. No line crossing.

Enumerate all three fixed positional pairings of the four groups w0,w1,w2,w3:
ADJACENT=(0->1,2->3), PARALLEL=(0->2,1->3), CROSSED=(0->3,1->2).
Directions always follow written order. Each pair must differ by exactly ONE
literal insertion, deletion or substitution; enumerate every alignment, retaining
ambiguous repeated-character positions. A signature is edit-kind, old-character,
new-character, anchor L/R and position. L position counts unchanged characters
before the edit; R position counts unchanged characters after it. Both pairs
must share a signature. Empty old/new denotes insertion/deletion. This is an
ASCII transcription predicate, not an identified glyph, morpheme or meaning.

Count a window once per pairing even if it has several shared signatures.
Preserve all signatures, pair inputs/outputs, source IDs, full lines and reader
concordance. Left/right descriptions may encode the same observed edit; they
are not independent evidence. Consensus requires same locus, starting source
index, four raw groups, pairing and signature in all three readings. Readings
are observations of one manuscript, not replications.

Aggregate signature support by physical leaf, distinct ordered pairs of base
forms and occurrences. A descriptive candidate for later research requires at
least3 physical leaves and2 distinct base-pairs in all-three concordance. This
is a nomination threshold, not significance or confirmation. Rank by leaf count,
base-pair count, occurrences then signature alphabetically; no threshold repair.
Retain every hit even below this threshold. All three pairings are descriptive
comparisons, NOT a calibrated null or negative control.

Question/decision: repeated coordinated edits nominate concrete source passages
for a separately registered functional contrast; absent/isolated patterns stop
this local mechanism and retain inventory only. Neither result refutes general
morphology. No broader windows, two-edit variants or semantic labels follow
within this pass. No relation edge score or meaning promotion. Budget20 minutes
including preparation, execution, independent validation and publication; source
is cached and computation small. No new decoder or control framework.
