# GDT852: physical spacing at two identical letter-sequence locations

GDT850 and851 identify f75v.44 as containing qol chedy at groups7–8 and
qolchedy at11 in ZL3b/IT2a. These are outcome-aware targets; the physical
spacing has not been natively adjudicated in this pass. f75v is text-admitted
but NOT previously visually admitted in the active budget. Admit this one
page before image access:39visual keys/44selectors,11admissions remain.

Fetch only the exact75v-labelled Yale canvas at its native full/full URL.
Keep source URL/hash/native dimensions. Reuse851's lossless admitted source
snapshots for this page only, preserving every reader/group/separator. Root
may use complete page text to locate the fixed line; only T1/T2 are judged.
Native original full-image viewing, no OCR/cropping/enhancement/detection.

Compare the same inner seam between initial sequence and following bench
in T1/T2. Use other within-sequence intervals and ordinary intergroup spaces
on the SAME line as visual scale references. For each target record exact
localization or unresolved, seam category SPACE_LIKE, INTERNAL_LIKE or
UNCERTAIN, and an explanatory native visual note. Separately judge whether
the contrast is LOCAL_SEAM_CONTRAST, WHOLE_SPAN_SCALING_COMPATIBLE,
NO_CLEAR_CONTRAST or UNCERTAIN. A local contrast requires other corresponding
internal intervals to remain broadly comparable; simply narrower wholeword
width does not reject uniform horizontal scaling. These are qualitative
visual judgments, not metrically estimated ink-gap ratios or linguistic gold.

Root knows transcriptions. Save and hash-seal A before giving B only the
original image and neutral locations/approximate original-image coordinates,
plus the observation schema without expected strings. No target form labels,
source contexts or root judgments to B. General prior script exposure is
allowed and disclosed; do not claim two independent physical witnesses.
B must not substitute a nearby easily read pair if localization fails.

Support physical selective-spacing contrast only if both viewers localize
both targets, agree SPACE_LIKE forT1 and INTERNAL_LIKE forT2, and judge
LOCAL_SEAM_CONTRAST. Otherwise keep exact uncertainties/disagreements and
transcription-level claims only. Even support identifies neither authorial
word boundaries nor meaning; it rejects only uniform scaling of one invariant
spacing template as a sufficient explanation of these two forms. Optional
spacing, context-conditioned notation and different constructions remain.
No repeat with new thresholds/targets or extra page after outcomes.

Budget15minutes including preparation/publication, checkpoint after root
judgments. Software validation covers source bytes, sealed A and schema,
not paleography or authorship. B blind observations are the independent
judgment step; no semantic relation score or GDT388 edge is created here.
