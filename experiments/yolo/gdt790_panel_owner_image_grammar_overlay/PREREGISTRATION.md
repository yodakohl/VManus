# GDT790 pre-run working contract

## Scope

Use only the already inspected f77r, f82r and f83r images and their guarded
ZL3b lines. Add an external image-owner layer; do not change token meanings.

## Required output

- one panel owner for every paragraph record;
- one visible component or component zone for every graphical label;
- exact label-token reuse across the complete three-page prose;
- a readable owner-aware rendering of every prose line;
- an explicit compatibility table showing what earlier structures survive.

## Working decision

Keep the overlay if it covers the full three-page layout without overlap and
without requiring a universal visual prefix, approximate label-to-paragraph
mapping or proximity-only word owner. Preserve promising form families as
exploratory whole-form leads even when they are not yet proven.

## Ceiling

No plaintext, confirmed lexeme, free root, sound value, flow direction,
substance, unit or unseen-page prediction is claimed.
