# GDT798 preregistration

Date: 2026-09-04

## Fixed target

The complete target is literal ZL3b `qokaldy` at exactly these inherited-cache
loci: f107v.6, f33r.2, f46r.4, f46v.6, f58r.19, f58r.32, f66r.67, f66r.69,
f76r.24 and f79r.13. The released primary subset is f66r.67, f66r.69 and
f76r.24. No newly opened page or image may enter.

## Fixed hypotheses

- QREF: complete `qokaldy` = `davon` (text-internal back-reference).
- QREM: complete `qokaldy` = `Heilmittel`.
- QAPP: complete `qokaldy` = `anwenden`.
- QSAME: complete `qokaldy` = GDT797's `Bade-/Behandlungseintrag`.
- QOPAQUE: opaque learned text form.

All are whole-form hypotheses. A visual resemblance to `okaldy`, the analyst
handle DAQABBA, and the modern EVA character `q` have zero component credit.

## Fixed decision order

1. Reject an entry/opener reading if the target is never line- or
   paragraph-initial in the ten-line census.
2. Reject portable QSAME if GDT797's complete `okaldy` domain fails to predict
   both released `qokaldy` paragraph hosts.
3. Treat running-only deployment as nondiagnostic if at least 80% of all
   observed q-prefixed types are also running-only.
4. Call TEXT_OR_OTHER concentration transferable only if it survives the
   source-surface qS cohort, a host-cluster null, the nearest `qokaly` whole,
   and removal of f66/f76 from the control pool.
5. Rank QREF/QREM/QAPP descriptively from the full positions and contexts. A C0
   winner needs more independent matched evidence axes than each concrete
   rival; ties retain multiple displays. QOPAQUE always remains live.

The event-level hypergeometric result is diagnostic only. Paragraph and
page-contact results govern because the f66 target repeats within one paragraph.

## Fixed outputs

The pass must emit the ten-line occurrence atlas, two released paragraph hosts,
candidate paragraph readers, qS and q...dy control profiles, exact statistical
tests, three-surface contrast, candidate adjudication, bounded renderer,
scope/guard audit, result JSON and independent validation JSON.

## Forbidden conclusions

No free q/operator, okal, dy, DAQABBA family, reader-vote semantics, neighbour
translation, page identity, new visual owner, confirmed lexeme or plaintext is
permitted. f84 and f84r remain sealed.
