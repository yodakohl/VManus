# Registration

METHOD.md, src/SPEC.json, the frozen403proposal/dossiers, raw admitted source
snapshots, parser/replayer/validator and pre-run synthetic checks are hashed in
PREREG_LOCK.json before consequence execution. Public push must complete first.

This is an exposed exploratory reading frozen for a subsequent finite audit,
not blinded discovery. The known source solution, manual trace, source variants
and anticipated counterexamples are disclosed in METHOD.md. All32variants,
all8hazard diagnostics and all6name permutations are retained. No source,
lexicon, grammar or counterexample is repaired after results.
