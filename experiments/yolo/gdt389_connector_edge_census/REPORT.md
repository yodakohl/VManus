# GDT389 — authorial connector-edge census report

## Outcome

`COMPLETE_CENSUS_ZERO_ELIGIBLE_DIRECTED_EDGES`

The complete frozen 61-page/30-folio frame was directly reviewed on 50 unique
official Yale canvases. Forty-seven pages have no connector candidate. Fourteen
contain ducts, paths, branches, arcs, or connected diagrams, but their visible
endpoints are drawings, figures, pools, stars, apparatus, or regions—not two
singular inscriptions. None reached source-aware two-endpoint localization,
direction review, or edge eligibility.

| page-screen state | pages |
|---|---:|
| `NO_CONNECTOR_CANDIDATE` | 47 |
| `AMBIGUOUS_CONNECTOR` | 14 |
| `CONNECTOR_WITH_FEWER_THAN_TWO_INSCRIPTIONS` | 0 |
| `CONNECTOR_WITH_TWO_OR_MORE_INSCRIPTIONS` | 0 |
| eligible singular directed edge | 0 |

## The 14 retained ambiguities

| pages | visible geometry | why it is not an inscription edge |
|---|---|---|
| f67v2 | connected corner-circle/face networks | inscriptions do not occupy two singular line endpoints; no direction |
| f68r3 | curved star-group-to-moon tail | endpoints are drawings; nearby group/tail writing is not an ordered endpoint pair |
| f75r, f75v | tubes, pools, repeated spout/figure columns | ducts terminate at figures, basins, or pools, not inscriptions |
| f77r, f77v, f78r | segmented bands, branches, conduits | nearby writing labels components; the drawn path does not join the labels |
| f80r, f81v | marginal tubes and enclosed figure/pool arrays | no singular inscription pair can be isolated |
| f82r, f82v, f83r, f83v | tubes, cross-pieces, arches, flow-like drawings | endpoint ownership and direction remain graphical or ambiguous |
| f85r2 | radial/path geometry on part canvases | exact subpanel/endpoints unresolved; no arrowhead or two inscription endpoints |

These pages remain published as ambiguities, not converted into negative
training examples. Radial spokes, catalogue order, spatial proximity, and
ordinary text reading order were excluded exactly as frozen.

## Capacity decision

The required capacity was at least 50 singular directed inscription edges on
five physical folios. The observed capacity is zero edges on zero folios. The
GDT387 parent-link instrument therefore remains locked; no formal projection,
association model, null, or semantic score was run.

This closes the current permitted-frame connector acquisition route. Repeating
the same pages or relaxing “inscription endpoint” to “near a connected object”
would not create new evidence. A future relation experiment requires genuinely
new source-authored edge data or a different externally readable endpoint—not
another reinterpretation of these drawings.

## Provenance and limitations

The census was performed by one AI direct visual reviewer in exploratory YOLO
mode. It is not a human annotation or independent confirmation. The validator
independently checks frame coverage, safe image mapping, hashes, accounting,
and the capacity decision; it explicitly does not claim a second visual review.

No OCR, automated image classification, CLIP/embedding retrieval, captioning,
or Voynich text/formal identity was used. The previously disclosed f84r
metadata-display breach is recorded in `CORRECTION.md`; no f84 image,
transcription, source group, formal identity, prediction, or score was accessed
in this image mapping or review.

Validation: `PASS 55/55`.

## Claim ceiling

GDT389 establishes only a complete geometry-only connector census and its zero
eligible-edge capacity. It establishes no parent relation, reference, syntax,
semantic role, POS, word, meaning, language, plaintext, or translation.
