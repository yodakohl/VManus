# GDT525 compact K-stem working book

## Live rule

Use a two-hop chain only when the old base recipe begins with `K`, the first
edit is right `y -> Y`, the second is inner `e -> E`, both GDT522 bonuses are
positive, and the ordered pair has at least two old carriers. Feature:
`log(1 + pair support)` at weight 1 after GDT524.

The full old pair has six carriers: `cth>cthy>cthey`, `ok>oky>okey`,
`ot>oty>otey`, `qot>qoty>qotey`, `chcth>chcthy>chcthey`, and `k>ky>key`.

## Current K family

| surface | base | intermediate | live recipe |
|---|---|---|---|
| `kcheody` | `kchod` | `kchody` | `K+CH+E+O+D_ADDR+Y` |
| `kechody` | `kchod` | `kchody` | `K+E+CH+O+D_ADDR+Y` |
| `keody` | `kod` | `kody` | `K+E+O+D_ADDR+Y` |

`kcheody` replaces the inherited `K+CH+E+O+Y` working default. GDT515 marked
that default as retained until a better parse. Do not export this K-base rule
to CH-/SH-base `eody` tails.

## Scorecard

- old four-fold: unchanged `1098/1328/1386/1418`, rank sum `2109`;
- inherited current: top1 `144 -> 145`, rank sum `185 -> 184`;
- family-revised current: top1 `143 -> 146`, rank sum `187 -> 183`;
- revised remaining queue: 13 forms.

## Next route

Inspect `chady` and `chap` as one `ch -> cha` intermediate family. Their truth
chains are respectively `a->A_ADDR` plus `dy->DY`, and `a->A_ADDR` plus
`p->P`; the ordered pairs have no old triple carrier, so they need a stem-level
license rather than the K ordered-pair rule.
