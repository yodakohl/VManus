# GDT893 — global source montage under one word code

Authoritative model, inputs, all-optimum criterion and90min decision budget:
`src/SPEC.json`. Source pool14 editions/witnesses, not14 independent traditions.
The source-only intake yields1318 gap-bounded units and153031 normalized tokens.
Text provenance and exclusions stay reversible; no editorial semantic tags enter
matching. Some witnesses are later than the manuscript's usual dating; they are
textual comparators, not claimed physical exemplars or origin evidence.

GDT211/378 tested anonymous roles,838 internal windows,884 one expanded source
passage and887 one fixed medical-record compiler. This distinct discovery seeks
complete exact source runs with a common word dictionary. It neither reruns
those fixed failures nor reuses the unqualified892 control as a pass.

A local source run is admissible exactly when its ordered word-equality pattern
matches the complete target paragraph. Previous-occurrence distances test this
without guessing word values. The second stage maximizes whole-paragraph word
coverage under one globally consistent injective map. It retains all optimal
plaintext projections; the untranslated residual incurs no invented gloss.
Uniqueness is conditional on the finite source pool and this exact-copy model.

No relation/null score or confirmed translation is issued here. Source-window
enumeration and global optimization have independently implemented exhaustive
invented-data oracles. A later independent source/held test would require its own
frozen candidate contract and executable388 gates before promotion.

Computational amendment after initial Python execution began: source-window
enumeration is complete; the identical candidate pool and objective are now
also solved by two separately tested C++ implementations. This changes runtime,
not eligibility, source selection, mappings, objective or all-optimum retention.
The original Python execution is preserved with its actual completeness status.
CANDIDATE_LOCK.json seals the exact input pools for the accelerated execution.
The original ZL3b run and independent enumeration retain all5,167,758 optimum
index sets and agree exactly. Independent RF1b and consensus enumeration also
finished; IT2a enumeration was killed by the operating system before a complete
result. Those actual outcomes are preserved, including unknown IT2a status.

Before the new primary execution, the representation changes to the exact
implicit family {S: frozen candidate constraints hold and weight(S)=W}, where
W must be exhaustively proved maximal. All members remain represented by the
bound input records, constraints and executable membership checker. This does
not enumerate or claim the family's cardinality. For each candidate or word
pair in one optimum, a feasibility query forbids it at weight W. A complete
counterexample disproves universality; exhaustive unsatisfiability proves it.
A timed-out query proves neither. Independent original full-family results
provide additional ground truth for the three already completed panels.
`src/run_cpp.py` freezes computational order and caps before this execution:
ZL3b90s, RF1b360s, consensus90s, IT2a900s; total1500s. All four original panels
remain in scope. No source, target, model, objective or projection criterion
changes. Publication and validation remain inside the original21:00UTC stop.
