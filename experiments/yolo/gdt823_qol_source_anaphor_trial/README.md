# GDT823 — qol as “daraus?”

Status: `C0_SOURCE_ANAPHOR_POSSIBLE_REFERENTS_UNRESOLVED`

One fixed material-source meaning in complete cached paragraphs. See
`WORKING_THEORY.md`, `REPORT.md` and `src/CONTENT_READING.md` for the actual
content and its limits; `src/CANDIDATES.tsv` records confidence and evidence.
No confirmed translation, new admission, image or dictionary promotion.

Reproduce: run `src/run.py --check`, then `src/validate.py --check` with Python3
from this experiment or via their repository-relative paths. Standard library
only; no network/raw mixed TSV reads. Manifest binds reused GDT822 inputs.
