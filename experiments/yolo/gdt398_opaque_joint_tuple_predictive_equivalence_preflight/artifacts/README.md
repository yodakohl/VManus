# GDT398 artifacts

The bounded run produces only `result.json`, `fold_scores.tsv`,
`cluster_summary.tsv`, `stable_merges.tsv`, `counterexamples.tsv`, and
`validation.json`. No source text, semantic annotation, visual annotation, or
sealed-folio content is published here.
