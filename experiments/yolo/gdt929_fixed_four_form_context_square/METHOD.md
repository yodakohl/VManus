# GDT929 — fixed four-form context square
Registered 2026-09-14 before this experiment accesses cached content. Prior project exposure is extensive; no blind discovery claim. Motivation W55: cheey/lsheey confounds l addition and ch/sh change. GDT914 tested consecutive four-word edit patterns, not this fixed four-form context inventory. Old lexical decompositions are not semantic evidence.

Decision/budget: unknown whether all four exact forms have usable occurrences and share unchanged immediate left/right frames. Enumerate cheey, lcheey, sheey, lsheey only in all six exposed GDT915 snapshots, all179allowed selectors. No new image or reserved access; f84/f84r sealed. Budget20minutes including preparation, validation, publication. No new decoder, normalization, gloss or window expansion.

Count every exact raw occurrence, preserve entire line and group ID. A context frame is the exact immediately preceding and following raw group, both ASCII lowercase alphabetic, with contiguous source indices and definite spaces on both seams. Exclude boundary/uncertain frames from matched comparisons, but retain their occurrences. Enumerate every frame occupied by >=2 target forms, separately per reading. Mark all four fixed edges (two l additions; two ch/sh changes). For each edge report whether at least one witness pair occurs on different physical leaves. Full square requires all four forms in one frame; report leaves, not independent-reader replication. No minimum chosen afterward. Cache discovery/evaluation partitions are both already exposed here, not held verification.

If complete square exists, retain it as a structural comparison only. If only edges exist, retain exactly those and do not infer missing edges. If none, stop fixed frame test, no wider windows. Identical neighbors do not guarantee identical syntactic role or referent. No significance without entire-search controls; no human/station/plant meanings without independent binding. No edge packet or scoring nomination.
