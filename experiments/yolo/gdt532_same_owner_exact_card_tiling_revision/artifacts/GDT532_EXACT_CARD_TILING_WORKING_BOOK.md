# GDT532 exact-card tiling working book

## Current change

```text
dsholdaiir = d | shol | daiir
            D_ADDR | SH+OL | DA+IIN+R
            HIER | HALTEN · FORTSETZEN | STUFE II · MARKIEREN

“Hier halten und fortsetzen; Stufe II markieren.”
```

Certificate:

```text
d      = D_ADDR       11 old running events
shol   = SH+OL        18 old running events
daiir  = DA+IIN+R      2 old local events on f70v
daiir  = DA+IIN+R      2 current prose events on f31r and f66r
target = G515-E0366    f66r.58 / F66R_PROSE_02
tail   = G515-E0408    f66r.62 / same owner and block
```

Only candidate rank 6 has an exact full tiling; ranks 1 and 2 have none. The
rank-six override is intentional and its diagnostic cost is retained.

## Residual selectivity

- `dairykodas`: twelve routes but two distinct recipes; still ambiguous.
- `aiicthy`: no candidate-matching exact-card tiling.
- `dalcheeeky`: no candidate-matching exact-card tiling.
- `qef`: no candidate-matching exact-card tiling.

## Edition state

- 159/159 forms still have a default working recipe.
- 154 are candidate rank one; 157 lie within top two.
- `saiis=S+A_ADDR+IIN+S` and `chekchy=CH+K+Y` remain unchanged.
- Four surfaces remain unresolved even though `dsholdaiir` is now a resolved
  non-top-one semantic override.

Next preferred probe: compare the two fully tiled `dairykodas` recipes through
the old `odas`, `kod`, `as`, `dair`, and surrounding f66r.57/f66r.62 cards. Use
no new page.
