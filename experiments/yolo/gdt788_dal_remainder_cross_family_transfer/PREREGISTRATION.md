# GDT788 exploratory registration

This is a throughput-oriented family tournament over the already admitted
cache, not a blind decipherment claim. The primary evidence unit is one of ten
complete X rows, never an occurrence and never an alternate reader.

Primary prediction:

```text
P(Xdal) = P(Xal) + P(Xdar) - P(Xar)
```

Direct-core rival:

```text
P(Xdal) = P(Xal) + P(dal) - P(al)
```

Both must be evaluated after masking the entire raw `al/dal/ar/dar` ending
family, provenance-sieve forms, quarantine forms and the older DAL semantic
lineage. They are compared with Xal and a form-selected learned whole. A model
passes only with at least 7/10 wins against both and a coherent outside-axis
pattern. Otherwise `dal` remains a complete-whole reading and `Xdal` defaults
remain complete-whole cards.

Empty target semantic fields are common `NA` across every model. Boundary
splits and Stolfi never vote semantically. Every observed `*dal` surface must
receive a concrete but replaceable display, confidence, positive evidence,
counterevidence and two distinct semantic rivals. Raw-only forms remain warning
cards. All component, plaintext, substance, phonetic and historical-word
credits stay zero.
