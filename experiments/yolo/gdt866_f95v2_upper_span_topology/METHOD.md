# GDT866: topology of the documented f95v2 upper extension

The second personal orientation recorded a tall horizontally extended form at
the upper left of Yale1006243. That photograph is already admitted as f95v,
with existing explicit f95v2 mapping. GDT859 and861 supplied local upper links,
but did not establish a link that skips a complete separate spatial group.
The candidate here was noted before this question; it is not a held-out image.

## Fixed native comparison

Inspect only the leftmost prominent tall horizontal extension in the first
physical text baseline of the upper text block on original1006243. If the
previous orientation cannot be localized to that fixed target, record
unlocalized/uncertain; do not substitute another line or flourish.

A full-original native viewer describes whether the extended trace is attached
to just one upright (ONE_UPRIGHT_FLOURISH), joins two separate tall uprights
(TWO_UPRIGHT_LINK), or remains UNCERTAIN. Do not infer pen order or chronology.
For a two-upright link, count only complete visibly separated spatial groups
strictly between the groups containing its two endpoint uprights. Low marks
inside either endpoint group do not count. A candidate intermediate group
requires a complete low-writing cluster separated on both sides by visibly
ordinary whole-group-sized gaps, with the relevant endpoints outside it.
Compare local ordinary gaps; do not assign EVA characters or assume linguistic
words. For a one-upright flourish, count0only if this classification is clear.
If group boundaries or either endpoint are ambiguous, uncertainty=true and
count=null; do not convert incomplete visibility into absence.

Each viewer writes localized(bool), endpoint_topology(enum above),
complete_intervening_groups(nonnegative integer or null),
group_boundary_evidence(text), endpoint_description(text), uncertainty(bool),
and note(text). Root A is saved and SHA256-sealed before B receives the image
task. B knows this contract and the old orientation but not A's observation.
Both see the same photo, not independent manuscripts; source exposure is stated.

Both localized, uncertainty=false, TWO_UPRIGHT_LINK and counts>=1 yields
BOTH_VIEWERS_INTERVENING_GROUP_LINK. Both localized and certain with count0
and matching endpoint topology yields BOTH_VIEWERS_NO_COMPLETE_INTERVENING_GROUP.
All other combinations yield UNRESOLVED_OR_DISAGREEMENT. Preserve individual
counts and topology even when the coarse result agrees. These are descriptive
same-photo judgments, with no population estimate or significance test.

## Consequence and limits

A positive would establish a candidate written linkage across a complete
intervening spatial group, motivating future consideration of a nonadjacent
writing relation. It would not establish semantic scope, abbreviations,
ligature identity, authorial directed ownership, glyph value or translation.
A negative or unresolved result leaves the required selective-link anchor
missing in this example; no target swap or wider image campaign follows.
The complementary written expansion required for the shared-abbreviation
hypothesis remains a separate prerequisite even after a positive.

No GDT388 relation packet or relation score is created. No text corpus,
transcription correction, codepoint census, LM001/LM002 leaf rating or TGC001
excluded glyph classification. Root's selector-first TGC001 query for f95v/f95v2
returned0excluded rows. f84/f84r remain sealed; no new visual admission.
Use full original2838x3697 only, no crops, enhancement, OCR or image models.
Image hashes/bytes/dimensions and A seal can be independently checked; software
cannot validate native vision, endpoint tracing or the actual collection time.

Publicly freeze protocol, source and validation code before the target views.
Total budget09:30:36–09:44:00UTC including source feasibility, code, views and
publication. If the target cannot be resolved in that window, stop as unresolved.
Do not preserve a favourable interpretation by extending or changing the target.
