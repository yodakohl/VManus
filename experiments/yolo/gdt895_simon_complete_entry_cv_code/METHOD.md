# GDT895 method

Can every inherited eligible odd complete paragraph correspond to a different
complete Simon dictionary entry under one global lossless consonant/vowel code?
The exact contract is [src/SPEC.json](src/SPEC.json). Source pool independently frozen;
no target fit or held access has started.

The source is an explicitly partial archived **eclectic edition**, originally
based on the1510 print. Its modern editors sometimes use other witnesses and
expand abbreviations. It is neither a diplomatic1510 transcription nor the
complete historical dictionary. All5803 links in21 available letter indices were
fixed before body acquisition. The nearest eligible capture to12August2022 is
fixed for each of1575 titles;4228 lack exact-title capture metadata. No later
capture/source choices will depend on manuscript fit. Failed acquisition and
source exclusions remain explicit. The full modern edition cache stays local.

Retain the complete initial maintext before an explicit Apparatus, Translation
or Commentary marker. Unknown layout, uncertainty, unsupported characters and
editorial duplication are quarantined whole. Preserve inline text, all words,
letter names, Roman numerals and maintext variants; never prepend page titles or
substitute apparatus spellings. The strict parser and independently implemented
validator reconstruct every accepted word sequence. Source witnesses can have
different entry boundaries; only the frozen edition boundary is tested.

The code uses the exact lossless GDT892 CV transformation: six possible inherent
vowels,20 consonant bases, five other vowel modifiers, CARRIER and VIRAMA. Assign
the27 components globally to injective prefix-free codes of one/two literal
lowercase ASCII characters. An inherent CV can contract to one component. Bare
consonants and standalone vowels remain explicitly represented. This creates
complete-entry equations instead of892's unexecuted lexicon/grammar control;
that control remains capacity-stopped and is not claimed passed or repaired.

Every complete source word must encode to its corresponding complete written
group. Cover every declared paragraph; do not maximize a favourable subset.
Whole-word equality in both directions is a necessary consequence, permitting
an exact source-domain rejection before expensive key search. Surviving cases
must share one component key and distinct entry assignment, retaining every
vowel/segmentation/plaintext alternative. Unused key values remain unidentified.

A verified witness establishes conditional compatibility. Rejection requires an
exhaustive result or independently checked necessary-condition certificate.
Uniqueness requires complete counterexample searches; timeouts remain UNKNOWN.
No result rejects missing Simon entries or establishes manuscript language or
meaning. A portable candidate requires a separately fixed held evaluation and
the applicable GDT388 gates before semantic promotion.

Source acquisition/parser/validation/publication budget:21:16–22:46UTC on
9September2026; stop acquisition22:40. The target phase is now capped at30minutes, ending no later than23:15UTC;
primary and independent CV searches each receive300seconds, with10000 observed
models per panel as an UNKNOWN output cap. The
broader arbitrary block-expansion alternative is not an automatic successor.

Source reconstruction uses `src/source_text.py --cache CACHE --output POOL`,
then `src/validate_source.py --corpus CACHE --source-pool POOL --output AUDIT`.
These paths name a local cache supplied by the caller. The original5803 receipts,
CDX response and frozen source/audit hashes accompany SOURCE_LOCK.json.
The receipt replay script reconstructs the original snapshot from byte-identical
archived bodies; later acquisition successes never enlarge this fixed pool.

The independent CV validator first materializes all local codebooks using binary
component-length equations, then exhaustively joins them with distinct source
entries and globally consistent injective prefix-free codes. It compares every
observed model with the direct word-equation search. Unused component codes
remain existentially extendable and unidentified.
