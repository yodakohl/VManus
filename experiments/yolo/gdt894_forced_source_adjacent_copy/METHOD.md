# GDT894: fixed adjacent-copy falsifier

One new law is tested: the conditional GDT893 RF source reading continues
without omitted/reordered words immediately before and after its28-group
anchor. Both entire128-word source horizons are frozen before target access,
clipped only at existing source-unit edges:128 preceding and39 following.
The source location, its sole retained provenance and26-value partial bijection
are fixed in artifacts/PREDICTION.json. No new key values are assigned.

At each fixed offset, membership in the known source vocabulary must equal
membership in the known cipher vocabulary; if known, the frozen map must agree.
Unknown-to-unknown compatibility is a necessary condition only. The test does
not assign their values or check their mutual equality. Any safe contradiction
rejects this exact continuous-copy law. It does not refute GDT893's weaker
paragraph-switching model, its entire source pool, or general source montage.

Only existing admitted f103v text is queried, literal RF1b groups. All28 anchor
IDs and values must replay. Groups are never skipped: uncertain literal forms,
non-prose kinds, invalid line counts, missing numeric lines or nondefinite
crossed separators stop positional evidence from that point outward. Page
edges are unobserved continuation. Each direction starts anew at its own
anchor edge. STA parsing alone is not a literal-group uncertainty gate here;
RF's incomplete uncertainty annotation limits any rejection to this literal
transcription model. No native manuscript certainty is asserted.

Inputs were historically project-exposed, and eligible odd paragraphs entered
GDT893 discovery. The adjacency comparison is therefore a newly frozen logical
falsifier, not an independent untouched held test. No new manuscript image,
sealed f84/f84r, relation score, null p-value or confirmed meaning.

Decision and35min budget20:21–20:56UTC are in docs/joint_reading/PROPOSAL.md.
A contradiction parks exact continuation; no shifts, repairs, looser key or
new corpus. Barrier-only evidence stays unknown. Compatibility alone warrants
no translation claim. GDT884's different fixed morphism failure stays closed.
