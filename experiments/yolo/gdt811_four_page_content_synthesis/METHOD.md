# GDT811 method

Can the complete text and imagery of f17r, f77r, f88r and the three released
f72r panels support explicit referent and phrase scope, rather than unrelated
local word guesses? The exploratory selection history is in PREREGISTRATION.

Use the GDT791 released-page spine for boundaries and line kinds. Query the
mixed alternate-reading table with the exact six source selectors, rejecting
f84 prefixes before retaining rows. Assert one-to-one source agreement.
Emit all178 source loci, not merely a favorable paragraph. Line_kind is an
inherited annotation: RUNNING_PROSE on a ring does not prove prose syntax.

For each complete local inscription, enumerate exact contiguous occurrences
in all running lines of its physical page. Whole multiword inscriptions remain
whole. Alternate support requires the identical complete inscription at its
label locus and the same count of the target sequence in the target line;
occurrence rank indexes repeated matches. This is conservative textual
alignment, not three independent witnesses or a shared pictured referent.

reference_inventory.py separately inventories the seven declared whole forms;
scope_inventory.py separately inventories otchol/next-chol phrase brackets.
Their source design notes govern the exact scope and exclusions. Full text,
exact identity edges, component-free word distributions and manual readings
are kept distinct. No similarity metric supplies a translated word.

The independent validator checks conservation and reconstructs the inventories,
not the correctness of German meanings. GDT388 receives only explicitly
textual edges and must return zero eligible edges. Cross-selector f72r matches
retain both original loci: its single-selector schema also reports their
target as outside the packet page. Neither those schema limits nor the
unsealed status reject an exploratory meaning. No locus is renamed to hide it.
