# GDT686 artifacts

- `TARGET_955_VALUE_HEAD_CENSUS.tsv`: every exact target with value level,
  local context, reader state and global axis policy;
- `SURFACE_VALUE_DISPATCH_SUMMARY.tsv`: totals for `dain`, `daiin` and
  `qodaiin`;
- `DIRECT_AXIS_EVIDENCE_SUMMARY.tsv`: the five exhaustive context classes;
- `QODAIIN_41_CONTEXT_AUDIT.tsv`: all qod-value neighbours and reader
  boundaries, including both same-span split rivals;
- `COMPOSITION_EVIDENCE.tsv`, `HYPOTHESIS_COMPARISON.tsv` and
  `COUNTEREXAMPLE_AUDIT.tsv`: evidence and competing models;
- `V59_51_LINE_READER.tsv`: complete updated 51-line reader;
- `V59_PATCHED_LINES.tsv`: ten changed lines with before/after prose;
- `V59_TARGET_POSITION_DEBT_DELTA.tsv` and `V59_DEBT_SUMMARY.tsv`: eleven
  position changes and aggregate information gain;
- `GDT686_V59_LOCAL_VALUE_READER.md`: compact human-readable patch reader;
- `RESULT.json` and `VALIDATION.json`: machine-readable result and independent
  byte-rebuild validation.
