# GDT751 artifacts

- `Q_BASE_51_PAIR_DECK.tsv`: inherited axes, raw pair-balanced placement and
  contact results for every complete q/base pair.
- `Q_BASE_3761_OCCURRENCE_FEATURES.tsv`: every reader-exact q/base occurrence
  with guarded line and paragraph features.
- `NONQ_PREFIX_160_CONTROL_DECK.tsv`: identical statistics for all other
  one-initial-character complete-pair controls.
- `MATCHED_51_CONTROL_MAP.tsv`: outcome-free one-to-one control selection.
- `PAIR_GROUP_COMPARISON.tsv`: q, matched, all-control and `o`-base summaries.
- `DIRECT_Q_BASE_CONTACTS.tsv`: all 44 reader-exact direct pair relations.
- `OKEEY_10_CARRIER_ENRICHED_CARDS.tsv`: the ten local carrier additions.
- `GDT751_Q_BASE_PAIR_READER.md`: readable result and enriched passages.
- `GDT751_GDT388_Q_BASE_EDGE_PACKET.tsv` and intake JSON: executable relation
  packet, invalid only because formal access is unsealed.
- `RESULT.json` and `VALIDATION.json`: machine result and replay certificate.

The occurrence table is retained because pair balance, paragraph boundaries,
section adjustment and every negative occurrence must remain reproducible.
