# GDT751 — q/base carrier-shell audit

Status: `PARTIAL`

Fifty-one clean `qX`/`X` complete pairs contain a striking inherited renderer
pattern—47 preserve quality/stage and 41 put `PREPARATION` only on `X`—but raw
position does not make q a special entry shell versus 160 other prefix pairs.
The pair relationship itself remains visible in 44 direct contacts at 1.74×
the all-control contact density. Ten GDT750 `okeey` positions directly hosted
by `qokeey` receive the exploratory local reading "heiße Zubereitung an der
End-/Vollstufe". EVA `q` still has no exported value.

See `REPORT.md`, `METHOD.md` and
`artifacts/GDT751_Q_BASE_PAIR_READER.md`.
