# Method

The fixed specification is PREREGISTRATION.md, hash-bound before execution by PREREG_LOCK.json. src/run.py enumerates maximal shared literal sequences. src/validate.py independently reconstructs complete paragraph coverage, candidate coverage and direct matches without importing the runner. No semantic classifier, decoder or held-data test.
