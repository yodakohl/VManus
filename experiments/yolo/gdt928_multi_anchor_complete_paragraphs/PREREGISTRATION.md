# GDT928 — multiple literal anchors in complete paragraphs

2026-09-12; IDEA000199, exposed inventory after user requests larger text-grounded constraints. Current route and926/927 protocols examined. The926-atlas-only preflight finds zero ZL/RF selector pairs sharing two anchors; IT f78r/f80r shares three anchors: one ol chedy qokain at another position, and two nested matches on the same four-word span. Whether the distinct positions belong to the same complete paragraphs is not yet established. This was seen before registration. To test paragraph structure rather than branching next words, include all exact repeats>=3whole groups, including line-terminal matches and repeats with same following words. No reduction to shorter words, no normalization or glosses.

Predecessor boundary:925 failed complete-line multiset equality,926 inventoried divergent next-word contexts,927 audited one seed and paragraph boundaries. None asserts whether two complete paragraphs share two disjoint longer literal passages. The closed HERBAL_PARAGRAPH_ORDINAL_TEMPLATE row says IL019 failed learned construction-order transfer; its referenced report is absent from this checkout and bounded locate finds no tracked path. No feature/classifier/order-stage model is reopened here, and no claim to have read that missing primary. This is a new literal-source overlap census, not a repaired ordinal or anagram decoder.

Only six existing guarded GDT915 JSONs and SPEC179selectors; all exposed, no new/held pages, f84/f84r sealed, no contacts. Separate ZL/IT/RF, not independent manuscripts. Build paragraphs using original P start/end flags on each selector: require a start, an end, no intervening conflicting boundary, and consecutive numeric source loci. Retain every raw group in each paragraph, even uncertainty. An unmarked RF page is not a complete paragraph and supplies no paragraph-order test.

Anchor-bearing lines must independently satisfy926's whole-line literal [a-z]+, consecutive group indices and definite internal seams. All other lines remain in the displayed paragraph but cannot supply anchors. Do not cross line boundaries when finding exact passages. Seed every shared triple containing>=2different words between complete paragraphs on different physical leaves, extend each match maximally left/right within both host lines, deduplicate identical position-pairs. Every resulting maximal match and all its original source IDs are preserved.

A qualifying paragraph pair needs at least two matches with no shared word positions in EITHER paragraph. Enumerate every such match-pair; same-order or reversed-order from the complete-paragraph offsets. If both orders are available, report ambiguous correspondence, not selective alignment. If matches overlap, they count as one possible evidence region, not two steps. No 'independent semantic evidence' credit from disjoint positions. Keep every paragraph pair with>=1match in machine-readable outputs, including nonqualifying and ambiguous ones. Show every qualifying pair with both whole paragraphs and all exact positions/intervening words.

Fixed outputs: denominator paragraphs and anchor-lines by reading, maximal matches, qualifying pair table, all same/reverse/ambiguous correspondences, source-conserved full paragraph packets. No selection of a best plausible story. If no pair qualifies, stop fixed contract; if pairs qualify, they become concrete structural work material without meaning or equivalent-task claims. No automatic statistical/control-corpus followup. Syntactic stage, referent and topic remain unbound.

Budget35min inclusive preparation, implementation, independent verification and publication. Smallest test: indexed triples plus maximal extension; independent exhaustive direct substring matches for every reported pair and independent shared-triple pair coverage. No significance or new score-ready relation packet. Source recurrence is not copied meaning or a decoded grammar.
