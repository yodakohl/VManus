# Artifacts

PARAGRAPHS.json preserves all complete admitted paragraphs; PAIRS.json includes every cross-leaf matched pair, even nonqualifying. PAIR_INDEX.tsv is the compact decision table. RESULT.json holds denominators; VALIDATION.json records independent checks. RF1b lacks paragraph capacity.
