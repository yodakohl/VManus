# GDT740 artifacts

- `TYPED_104_RING_EVIDENCE.tsv`: exhaustive typed nearest-ring evidence,
  including the one non-binding scalar-conflict cue.
- `SELECTED_103_CONTACT_ATTACHMENT.tsv`: every role-bearing contact with
  geometry, full frame, exact/raw recurrence, practical units, retained roles,
  direction priors, and attachment decision.
- `ORDERED_PAIR_100_RECURRENCE.tsv`: one row per distinct selected ordered pair.
- `TARGET_95_ATTACHMENT_ADJUDICATION.tsv`: complete formerly-specific target
  audit with relays, boundaries, conflicts, and manual reasons.
- `TARGET_202_RENDERER_PATCH_V2.tsv`: corrected occurrence renderer over every
  licensed position.
- `FORM_12_ATTACHMENT_PROFILE.tsv`: per-whole attachment and renderer totals.
- `PASSAGE_20_ATTACHMENT_REVIEW.tsv`: 23 focal patches on twenty cached lines;
  surrounding values are labeled as a cellwise audit, not clauses.
- `GDT740_ATTACHMENT_READER.md`: compact human-readable reader.
- `GDT740_GDT388_EDGE_PACKET.tsv`: six repeated direct contacts in executable
  intake schema; deliberately invalid/not score-ready.
- `RESULT.json`: scope, counts, interpretation, claim ceiling, and hashes.
- `VALIDATION.json`: independent checks, edge-intake result, and byte replay.
