# GDT740 method

## Question and boundary

When GDT739 assigns a local quality, amount, process, state orientation, or
broad carrier to one of twelve complete wholes, does that neighbor behave like
an attached microentry host or merely a nearby field?

The target is exactly GDT739's 202 occurrence patches. The 95 formerly specific
occurrences form the adjudication deck. Inputs are published GDT739 windows,
GDT734 cached cell-unit geometry, and the inherited guarded 179-page projection.
No page, image, or transcription is added. `f84` and `f84r` remain forbidden.

## Complete ring and binding decks

The builder reconstructs every GDT739 scalar-axis, favored state-axis, and
broad preparation/material/part contact. A conflicted scalar ring contributes
evidence for keeping the dimension open but supplies no selected axis.

This yields 104 typed ring-evidence rows. One row, `G739-N00627`, is only a
non-binding rival in an open scalar conflict. The role-bearing binding deck has
103 contacts on 90 targets. Both artifacts are published so the distinction
cannot disappear in a summary.

## Geometry and recurrence

For every selected contact the artifact records signed offset, manuscript-order
two- or three-cell frame, practical-unit layers, the intervening cell at radius
two, and exact/raw recurrence over the guarded cache.

The primary repeat requires target and host to be exact under the
alternate-reader map. Radius-two full-frame recurrence additionally requires
the middle token to be exact. A raw ZL3b repeat that fails the alternate-reader
gate remains a control, not an exact repeat.

All target and host cells are single-cell practical units; no contact shares an
existing bound span. Every radius-two middle cell emits its own unit; 35 are
known, six unknown, two strict-head forms, and none another GDT738 target. No
exact radius-two full frame repeats.

## Attachment dispatcher

Direct contacts are attachment-eligible working hypotheses. Exact recurrence
raises confidence. Unique direct contacts are split by the formal occurrence-
role expectation H1/H2→right and H3/H4→left; this changes the confidence label,
not the renderer licence.

Direct adjacency is not absolute. The manual passage audit can stop a role at
an explicit closure boundary, prevent different flanks from donating axis and
carrier to one phrase, or retain only the carrier where a neighboring value
owns its own axis.

Radius two is held by default. Two enumerated relay hypotheses survive:

- `qokeey–okeey–lain` at `f116r.17`, where the middle field continues the hot
  axis;
- `pcheol–sholoiin–cthor` at `f52v.7`, where the middle field continues the
  broad material carrier.

They remain occurrence-scoped manual working theories. Neither has a shared
formal span or repeated full frame.

## Carrier, flank, and result repair

A composite carrier survives only when one host itself carries the selected
combination. Components from separate flankers are not unioned. Axis and
carrier supplied from opposite flankers are likewise not fused into one
microentry.

GDT739 had eight state/result overrides: two labeled strong local and six
endpoint best-fit. The passage audit finds only `f103r.47` locally coherent as a
process/closure-to-result frame. The other seven return to descriptive state.
This is a manual occurrence correction, not a global meaning for any form.

## Historical priors

The historical table reuses targeted GDT735 witnesses. Wellcome MS.542 shows
name or named part before quality and degree in inspected descriptive entries.
MS.307 and MS.683 show multi-field dose frames; MS.683 additionally shows
later product formation. Pal.lat.1234 shows rubric scope; MS.404 shows
number-before-material in a
vernacular recipe. These are qualitative architecture weights, not frequency
estimates and not evidence for a Voynich word or syntax.

## Renderer and claim ceiling

The complete-whole field class and ordered level survive. An axis or carrier
is spoken only from a retained contact/relay. Conflicts and boundaries open the
slot. The passage artifact's semicolon-separated surrounding display is a
cellwise audit, not a sentence or implied attachment.

The six repeated exact contacts are also emitted in GDT388 packet format. The
executable intake correctly returns `INVALID_PACKET`: selection used formally
accessed semantic text and lacks sealed acquisition, external capacity,
held-folio allocation, independent review, and mobile-null evidence.

No head, body, component, sound, glyph value, unseen form, ingredient, species,
disease, cure, unit, lexeme, language, or plaintext clause is inferred.
