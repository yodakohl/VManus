# Sidequest Pass 831: eighth compact workshop grammar

The eighth edition replaces the abstract pronoun value `Y=DIES` with the noun
already used by the fluent edition:

> `Y = POSTEN`

This changes 60 exact cards and 124 events. All 60 Y-bearing statements now
name a POSTEN; only f11r H3-S001 required a wording change from ANSATZ to POSTEN.

The benefit is immediate. `OK+Y` is “Posten ansetzen”, `SH+Y` is “Posten
halten”, `T+Y` is “Posten bearbeiten”, and `CHD+Y` is “Posten umsetzen”. Grade
cards now say “kurz/länger/voll den Posten …” without a hidden noun supplied by
the editor.

The full inventory remains 39 components, 173 cards, 381 events, and 116
statements. The six narrow exceptions and the compact 24-recipe / 30-surface
prediction deck are preserved and recomputed.

The preceding component inventory had counted one terminal neighbor inside Y,
giving 61/125. The explicit recipe audit corrects that bookkeeping: DY remains
a separate licensed close, so the Y scope is 60/124.

Next, repeat the hidden-word audit. If POSTEN has disappeared from the hidden
layer, attack the next highest recurrent content addition rather than adding
new pages prematurely.
