# GDT773 — `ol` capacity-equalized composition audit

Status: `PASS`. The independent validator completes 6,507 checks, verifies all
eight source locks, and byte-replays all 20 runner artifacts plus `REPORT.md`.

The experiment reuses the fifteen already masked `ol` occurrences from GDT772
on eleven lines and ten physical folios. All five pure readings receive
identical structural capacity. The formal topology score selects the
quantifiable nominal head as the best invariant fallback: **6** penalty points
versus partitive `von` **10**, field/sequence **13**, measure/unit **15**, and
directional `aus` **24**.

The practical reader is more specific than one fixed word. Its explicit
record-field dispatch renders five amount/value→content cases as `Ansatz:`, two
content→amount cases as `Menge:`, and eight remaining cases as field or sequence
connections (`:`, `;`, `und`, `und dann`). After its fixed complexity cost, this
composite scores **2**, versus **6** for the best pure practical model
(field/sequence). Every occurrence receives a visible working default.

Alongside the mechanical 11-line reader, a manually punctuated record display
preserves all fifteen `ol` outputs while exposing inherited uncertainty in the
other 78 tokens: 33 lack a structural type, 29 have display-only defaults, and
three are not reader-exact. This polished display changes no score or meaning.

These are replaceable structural and rendering defaults, not recovered
plaintext or a confirmed meaning of `ol`. No new page, image, OCR, transcription,
`f84`, or `f84r` is opened.

Run:

```bash
python3 experiments/yolo/gdt773_ol_capacity_equalized_composition_audit/src/run.py
python3 experiments/yolo/gdt773_ol_capacity_equalized_composition_audit/src/validate.py
```

See `PREREGISTRATION.md`, `METHOD.md`, and `experiment.json`.
