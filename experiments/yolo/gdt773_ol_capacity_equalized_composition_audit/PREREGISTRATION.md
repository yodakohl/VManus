# GDT773 preregistration — five concrete readings and one record operator

Date: 2026-09-03

Status before execution: `REGISTERED_UNSCORED`

## Fixed material

1. Use exactly the fifteen GDT772 `ol` cases on eleven lines and ten physical
   folios; do not add or remove a case after scoring.
2. Reuse GDT772's target-independent immediate roles and written order.
3. Use the five pure candidates `VON`, `AUS`, `NOMINAL_HEAD`,
   `FIELD_SEQUENCE`, and `MEASURE_UNIT`.
4. Give every pure candidate identical left/right binding capacity.
5. Keep the 376-exact-occurrence global census and sixteen known amount
   contacts descriptive; the fifteen-case deck is intentionally enriched and
   cannot estimate a global rate by itself.
6. Render every target occurrence with a concrete default even where the
   underlying operator remains provisional.

## Fixed comparisons

- Capacity equality must hold in every case and every leave-one-folio-out
  fold.
- The formal topology layer uses the authored support/neutral/contradiction
  costs and winner gates independently of the practical German surface.
- Pure-model workshop cost uses only the authored `0..3` case ratings.
- The contextual operator follows the ordered dispatch rules, not a
  post-result minimum chooser, and pays its declared complexity cost.
- Its concrete output contract is fixed as five amount/value→content cases
  rendered `Ansatz:`, two content→amount cases rendered `Menge:`, and eight
  field/sequence cases rendered as `:`, `;`, `und`, or `und dann`.
- A directional `AUS` fit requires a source/result direction independently
  visible across the target; none may be invented from fluent German.
- A measure/unit reading is weakened, not forbidden, by the sparse global
  amount-contact rate.

The exact 5/2/8 surface wording supersedes the earlier shorthand
"`von`/nominal `Ansatz`/quantity colon according to direction". This is a
display clarification made before the final recorded replay: it changes no
case membership, topology, branch assignment, fit rating, or complexity cost.

## Non-scoring display addition

After the score design, an eleven-row manual record-display specification was
added solely to make the inherited non-`ol` prose readable and to expose its
uncertainty. It must preserve every selected `ol` direction and cannot alter
capacity, topology, fit costs, winner gates, or any outcome class. The
mechanical token ledger remains the reproducible source of token accounting;
the polished reader is explicitly not a translation.

## Outcome classes

- `PURE_READING_SELECTED`: one pure reading has the unique lowest adjusted
  cost and supplies all fifteen defaults.
- `CONTEXTUAL_RECORD_OPERATOR_SELECTED`: the predeclared operator covers all
  fifteen once and beats every pure adjusted total after complexity cost.
- `TIE_RETAIN_MULTIPLE`: two or more lowest adjusted totals tie.
- `NO_USABLE_MODEL`: coverage, capacity equality or replay fails.

The formal invariant-fallback decision and the practical renderer decision are
reported separately. A formal pure-model winner does not prevent the
contextual operator from winning the practical comparison.

## Ceiling

The winner is a concrete working renderer policy, not deciphered plaintext.
No page, image, OCR, transcription, `f84` or `f84r` may be opened. No substring
or EVA-to-Latin value is exported.
