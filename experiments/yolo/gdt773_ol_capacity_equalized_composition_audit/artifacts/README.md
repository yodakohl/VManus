# GDT773 artifacts

- `OL_15_OBSERVABLE_CASES.tsv`: fifteen target-independent `ol` contexts and
  their `A/C/0` topology.
- `BASE_CAPACITY_AUDIT.tsv` and `CAPACITY_LEAVE_ONE_FOLIO_OUT.tsv`: all five
  pure readings receive identical local binding masks, including every folio
  holdout.
- `CANDIDATE_CASE_SCORE.tsv`, `CANDIDATE_SCOREBOARD.tsv`,
  `LEAVE_ONE_FOLIO_OUT.tsv`, `GATE_AUDIT.tsv`, and
  `FOCAL_SELECTION_DELTA_AUDIT.tsv`: formal five-way topology comparison and
  its acquisition-neutrality and robustness audits.
- `OL_FIVE_WAY_PRACTICAL_FIT.tsv` and `PRACTICAL_MODEL_SCOREBOARD.tsv`: the
  separate concrete-reader comparison, including the contextual operator's
  fixed complexity cost.
- `INDEPENDENT_READER_DISAGREEMENT.tsv` and `READER_PRIMARY_COUNTS.tsv`: both
  manual reading profiles without averaging away their nine disagreements.
- `OL_CONTEXTUAL_DEFAULTS.tsv`: one visible output and strongest rival for all
  fifteen `ol` occurrences.
- `GDT773_11_LINE_TOKEN_DEFAULTS.tsv` and `GDT773_11_LINE_READER.tsv`: complete
  93-token ledger and its 91-unit mechanical working display.
- `GDT773_11_LINE_POLISHED_RECORD_READER.tsv`: non-scoring register-style
  presentation that preserves every `ol` direction while exposing inherited
  untyped, display-only, and nonexact non-`ol` material.
- `HISTORICAL_COMPOSITION_BRIDGE.tsv`: period and near-period construction
  analogues; these grant no Voynich identity credit.
- `GLOBAL_AMOUNT_CONTACT_CHECK.tsv`: sixteen known amount contacts among 376
  cached reader-exact `ol` occurrences.
- `GDT773_OL_WORKING_DICTIONARY.tsv`: the replaceable invariant fallback and
  contextual renderer, with evidence and counterevidence.
- `RESULT.json`: machine-readable decisions, counts, source locks, scope, and
  claim ceiling.
- `VALIDATION.json`: 6,507 independent checks and byte-identical replay of all
  twenty runner outputs plus `REPORT.md`.
