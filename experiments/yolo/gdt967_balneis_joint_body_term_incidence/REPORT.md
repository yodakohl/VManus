# GDT967 — both fixed shared bath-content models contradicted

The complete32-record,13-family source matrix has no joint embedding in the
fixed71 literal IT2a paragraphs under either declared carrier model. Enumeration
finished; neither the120-second limit nor the16,000-family cap was reached.
This rejects the specific lossless count-preserving hypothesis. It does not
identify any target word, exclude balneology or establish the manuscript language.

Registration088396515 was pushed to main before target carrier counting.
The independently reviewed source fixes166 occurrences and all416 counts.
The initial source draft's seven occurrence corrections and record-boundary error
are disclosed in src/SOURCE_REVIEW.md; all preceded registration and fitting.

## Actual complete consequences

| Model | All literal strings | Full-panel observation classes | AQUA-compatible classes | Complete13-family assignments |
|---|---:|---:|---:|---:|
| Complete word group | 1,209 | 481 | 1 | 0 |
| Contiguous piece within a group | 4,546 | 1,417 | 5 | 0 |

The source requires an AQUA carrier with paragraph-count distribution
0:1,1:4,2:13,3:6,4:6,5:2 among its32 chosen paragraphs. All71 target paragraphs
are available, but a single dictionary must supply all joint signatures using
32 different paragraphs. Separate feasible columns do not suffice.

For complete groups, `ol` is the only carrier with sufficient AQUA count
capacity. All40 individually eligible STOMACH classes contradict its joint
AQUA/STOMACH requirements. Thus no choices for the other11 families can rescue
this model. `ol` is not adopted as water: it is a failed conditional candidate.

| Piece considered for AQUA | Individually eligible STOMACH classes tested | Joint two-column survivors | Three-column AQUA/STOMACH/HEAD survivors |
|---|---:|---:|---:|
| aii | 253 | 0 | 0 |
| hee | 253 | 8 | 0 |
| hy | 253 | 2 | 0 |
| p | 253 | 11 | 0 |
| qot | 253 | 0 | 0 |

Of1,265 piece-model two-column combinations,1,244 fail the exact joint counts.
The21 remaining profile pairs each face all315 individually eligible HEAD
classes:6,600 fail joint signature capacity and15 reuse a class without enough
distinct literal carriers. All6,615 branches are contradictory. The remaining
10 columns need no deeper enumeration because they cannot repair those failures.
These are exhaustive necessary contradictions within the registered domain,
not selected local mismatches or repeated solver restarts.

## Tables and reproducibility

- artifacts/SOURCE_PREDICTIONS.tsv: every source record and all13 predicted counts.
- artifacts/SOURCE_EQUIVALENCE.json: all31 distinct source signatures; XI andXXIV
  have identical selected counts and cannot be distinguished by this test.
- artifacts/WHOLE_GROUP_CANDIDATES.tsv and WITHIN_GROUP_PIECE_CANDIDATES.tsv:
  every observation class, every member string, its entire71-paragraph count
  vector, all individually eligible meanings and every single-column deficit.
- artifacts/*_DEPTH*_CONSEQUENCES.tsv: every visited partial assignment, all
  required/observed deficits, corresponding source records and available target
  paragraph IDs. No failed branch is replaced by a favorable example.
- artifacts/TARGET.json and SCOPE.json: complete eligible paragraph contents,
  source IDs and every rejected paragraph's original eligibility reason.
- artifacts/WHOLE_GROUP.json and WITHIN_GROUP_PIECE.json: complete class domains,
  exact search order, all first-variable branch coverage, status and stop reasons.

src/summarize.py was added after the run solely to expand the recorded
contradictions into tuple-count tables. It does not change the registered search,
source, target panel or outcome. Run src/run.py, then src/summarize.py, then
src/validate.py from the repository root. The first-witness and family-display
branches have no actual manuscript witness in this result.

## Limits, ambiguity and decision

All18 selectors f75r–f83v were fixed before fitting. IT2a supplies79 complete
paragraphs,71 fully literal under unchanged GDT928 rules;8 remain outside the
exact domain. ZL3b supplies78 complete paragraphs but only3 literal, so it has
insufficient capacity for32 distinct records. RF1b supplies no complete paragraph
boundaries. Wider nonliteral/boundary-dependent scope is unknown, not contradicted.
The three transcriptions are alternate readings of one manuscript.

The source's XI/XXIV collision remains an inherent identity ambiguity; equal
full-panel carrier vectors are observation equivalences, not synonym evidence.
No full candidate survives, so there is no surviving lexicon to claim as unique.
All9 physical leaves were previously exposed; independent confirmation capacity
is0. There is no appropriate control of the entire source/model search and no
independent meaning test: no significance, confirmed organ name or translated word.
The test covers partial explicit term incidence only, not full paragraph syntax,
implicit mentions, paraphrase, named references or effect polarity. A more flexible
model is not licensed merely to repair this failure.

Decision: close both exact all32-record carrier models. Preserve GDT888/GDT913
and earlier experiments unchanged. Any next experiment must state a different
content consequence and justify its writing assumptions before implementation.

Independent validation passes all11 grouped checks. It reconstructs the entire
source and six-cache intake, every carrier vector and single-column domain,
both exhaustive searches using independent tuple signatures, and all7,926
visited-prefix table rows with source/target IDs. Twenty-one small brute-force
cases exercise12SAT/9UNSAT outcomes, injectivity, count overflow, witness
construction and stopping limits. Positive manuscript-witness and runtime-limit
paths were not exercised by the actual result. VALIDATION.json binds the
validator and result hashes; validation is computational, not semantic.
