# GDT744 artifact map

- `MICROFIELD_202_CHANNEL_DISPATCH.tsv` — complete target, boundary, anchor,
  sensitivity, confidence, field-card and claim-ceiling deck.
- `FORM_CHANNEL_RECURRENCE_CENSUS.tsv` — complete 12-target by seven-channel
  matrix with full-boundary and W3-only recurrence counts.
- `UNRESOLVED_CONTENT_SLOT_CANDIDATES.tsv` — 42 exact-reader unknown cells in
  template-backed fields; literal identities remain open.
- `PASSAGE_20_MICROFIELD_READER.tsv` — twenty distinct examples with manual
  coherence and information-gain assessments.
- `GDT744_HISTORICAL_MICROFIELD_READER.md` — human-readable templates and
  passage cards.
- `GDT744_GDT388_MICROFIELD_EDGE_PACKET.tsv` — deliberately ineligible
  formal-context relation packet.
- `GDT744_GDT388_EDGE_INTAKE.json` — executable invalid/not-score-ready intake.
- `RESULT.json` — machine-readable scope, counts, sensitivities and hashes.
- `VALIDATION.json` — independent recomputation, edge intake and byte-replay
  certificate.
