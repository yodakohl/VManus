# GDT623 artifacts

Manual compact inputs:

- `SOURCE_PROVENANCE.tsv`: official source URLs, hashes, and claim limits.
- `HISTORICAL_LAYOUT_OBSERVATIONS.tsv`: same-line, forward-block, nested-part,
  and re-anchoring mechanisms in readable comparators.
- `VISUAL_OBSERVATIONS.tsv`: manual image comparisons without automatic image
  labels.
- `VISUAL_ROLE_AUDIT.tsv`: folio-by-folio root and reproductive-role readings
  for the eleven inspected Herbal images.
- `ANCHOR_EVIDENCE.tsv`: concrete plant-quality supports, counterexamples, and
  rejected distant attachments.
- `CARRIER_SPECS.tsv`: exact carrier search definitions and horizons.
- `ALTERNATE_READING_AUDIT.tsv`: ZL3b/IT2a/RF1b stability checks.

Generated evidence:

- `FAMILY_COUNTS.tsv`, `SECTION_FAMILY_COUNTS.tsv`: exact, q-prefix, and
  exploratory substring counts.
- `SOURCE_QUADRANT_COUNTS.tsv`, `ORIENTATION_FREQUENCY_COMPARISON.tsv`,
  `MARGINAL_AXIS_COMPARISON.tsv`: source distribution and all orientation
  rankings.
- `HEADER_REPEAT_AUDIT.tsv`, `INITIAL_SHELL_AUDIT.tsv`: Herbal heading forms
  and entry-initial shell enrichment.
- `CARRIER_EVIDENCE.tsv`, `CARRIER_FAMILY_SUMMARY.tsv`: local repeated-carrier
  contacts.
- `STATE_WORD_AUDIT.tsv`: exact `chody`, `shody`, and `shedy` nearest-code
  counts, including section/language slices.
- `SUFFIX_AUDIT.tsv`: productive `-or`, `-os`, and `-dal` endings and shared
  bases.
- `BINDING_RULES.tsv`: executable attachment priority derived from historical
  layouts.
- `WORKING_DICTIONARY_V2.tsv`: short current defaults with strength and caveat.
- `CONCRETE_READINGS_V2.tsv`: twenty rendered spans; every unknown surface
  remains in angle brackets.
- `RESULT.json`: canonical synthesis and hashes.
- `VALIDATION.json`: deterministic replay, counts, provenance, and privacy
  checks.

No manuscript image bytes are committed.
