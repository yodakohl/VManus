# GDT849 — two e-position discovery grid

Decision note before query: GDT632/633 already record outer e/ee examples,
including f29r.1 cheecthy and f82v.36 sheecthey. GDT651 gives four CKH shells
and six endings; it lacks outer ee and includes d-bearing endings. None of
these checked primary reports supplies the complete joint36-cell raw grid
or a within-prefix/kernel comparison of the two lengths. Route-check used
"two independent e runs cheecthy sheecthey checkhy checkhey coupled e length grid"
and "cheecthy sheecthey cheeckhy sheeckhy two run coupling". Other ranked hits
address single runs, semantic cards or unrelated relation capacity, not this
exact two-position inventory. Ranking is navigation, not a scientific gate.

Question: does the admitted written inventory offer enough joint variation
at the two literal e positions to study their relationship? Exactly
{ch,sh}+e{0,1,2}+{cth,ckh}+e{0,1,2}+y, 36 whole forms.
All cells, including zeros, are retained separately for ZL3b/IT2a/RF1b.
These are alternative readings of one manuscript, not independent witnesses.

Use GDT845's179selectors and source columns through GDT829's guarded query.
All source kinds; preserve complete raw matches, separators and metadata.
Literal whole equality excludes bracket/brace/entity/inline metadata forms;
in particular GDT847 shows metadata-bearing groups can contain otherwise
plain words. Counts here are deliberately not annotation-normalized word
counts. No new images, pages, OCR, phonemes, meanings or relation packets.
f84 and f84r remain sealed before payload materialization.

Output every cell's occurrence count and physical-folio set. For each reading
and fixed prefix×kernel, tabulate a3×3 outer/inner count matrix and expected
row_total×column_total/stratum_total. Empty strata have all expected counts0.
Report the number of observed lengths at each position and whether both
margins vary. Calculate within-stratum mutual information in natural-log units
and its occurrence-weighted mean across the four strata. Zero cells contribute0;
empty strata contribute0. These are descriptive exposure/coupling summaries,
not p values, a fitted language model or proof of independence/dependence.
Section, hand, folio and position remain possible confounds.

If neither position varies jointly within fixed prefixes/kernels, stop this
comparison for capacity. If joint variation exists, preserve the full matrix
as a candidate for later nuisance-controlled study; no inference of morphology
or semantics from a large descriptive coupling. Either outcome excludes an
automatic decoder, enlarged e-range, new endings or post-hoc rescue family.

Time budget: preparation10min; after public registration, acquisition,
validation, concise report and publication target10min. At either checkpoint
stop expansion and reassess. The protocol knows predecessor examples but not
the new exact raw joint census. No confirmatory or held-sample claim.
