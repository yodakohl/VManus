# Artifacts

SOURCE_VALIDATION.json records the independent source-capacity reconstruction.
TESTS.json records invented-fixture integration checks. Fitting will add 48
restart files, six selected files and FIT_LOCK.json before any truth evaluation.
RESULT.json and VALIDATION.json then record recovery and independent replay.
