# GDT837 — registered, recovery unscored

The fixed SCG source passes capacity: 129120 discovery / 192991 held words in
9859 / 13828 verified source sentences. All 22 active literal, 4 suffix and 8
wholeword rules have discovery support. Held novelty includes 20871 composed
form occurrences and 8254 unambiguous composed lemma occurrences. An independent
source audit passes. No real initialization, search or recovery evaluation has run.

The predeclared comparison uses the unchanged GDT836 engine with and without
mandatory wholeword precedence: three opaque keys, eight paired starts per key,
48 fits in total. All six discovery selections must be locked before evaluating
held text. Recovery and incremental constraint benefit have separate criteria.
See METHOD.md, PREREGISTRATION.md and src/SPEC.json.

SCG is new as a ciphertext control, but its ITTB train subset was used as a
GDT832 reference. The current frozen reference is native Monarchia. Source-sentence
boundaries and nominal role counts are supplied; ITTB u/v spelling is retained.
GDT836's Questio stop remains unchanged. No Voynich reading or target fit follows
from preparation or software checks; GDT616 and CDA001 remain closed.
