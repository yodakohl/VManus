# GDT897 — closed with unresolved stereographic cases

The whole-constellation interpretation remains unresolved for LEFT. No geometric
identity or translation was recovered. The fixed searches are closed at their
registered budgets; no further optimizer, source, box or projection change is
selected.

## Manuscript and source scope
Two independent native inventories agree on29definite LEFT stars and59MIDDLE
stars in already admitted Yale1006196. An additional tiny LEFT mark remains
unresolved; both complete29/30alternatives retain the same29whole-ink boxes.
Matched boxes use their union. Faces, text and the right radial diagram are
excluded by the predeclared object/field rule.

All48FORMED and48FULL Ptolemaic constellation units were frozen from Verbunt and
van Gent2012/CDS J/A+A/544/A31. Literal integer-arcminute coordinates are used,
without proper-motion, error, brightness or subset adjustments. Modern HIP
identities play no role. MIDDLE59 exceeds every whole source unit(max45), so
cardinality excludes that complete-field conjunction independently of projection.

## Exact result and remaining uncertainty
The primary gnomonic/projective invariant search exhausts all5count-compatible
LEFT source cases under both globalorientations with no bijection. Independent
search reproduces C07:FORMED; its other4cases time out. Those4primary exclusions
are not independently reproduced. Final homogeneous denominators must all be
positive; arbitrary pole-crossing projectivities were never included.

The initial stereographic interval relaxation was uninformative: every target
quadruple allowed every sign. Its15validated bijections are not leads. Before
any continuous fit, one published fixed Bernstein precision amendment retained
all source coordinates and boxes. Two independent integer kernels agree on
every control-net sign range. It supplies7083strict LEFT29 and8999strict LEFT30
quadruple signs; remaining tuples retain uncertainty. No subdivision was used.

Under those refined constraints, all4LEFT29source cases remain UNKNOWN in both
implementations and both orientations. LEFT30/C07:FULL is UNSAT in the primary
search; independent search times out, so that exclusion is not independently
reproduced. No refined bijection was returned and no continuous fit was run.
UNKNOWN is not a contradiction and is not a negative result.

## Validation and reproduction
Initial source/vector/tensor validation, independent Bernstein parity, frozen
code/provenance/box preservation and result validation pass. The result validators
explicitly report PASS_WITH_UNREPRODUCED_EXCLUSIONS. See the corresponding JSON
artifacts for every case and orientation.

Initial freeze:8f600c72; precision freeze:2fd54a53, both public before their
comparisons. Reproduce with src/prepare.py, src/validate_geometry.py --packet
artifacts/SEARCH_PACKET.json.gz, src/run.py and src/run_independent.py. Then run
src/prepare_refined.py, src/run_refined.py, src/run_refined.py --independent,
src/validate_results.py and src/validate_refined.py. Use repository-relative
script/argument paths when invoking from the repository root. Search caps are
120seconds per case/orientation; time-sensitive UNKNOWN results may differ by
hardware and cannot be strengthened into exclusions.

The root inventory repeat-view timestamp23:45:00 was manually rounded within
the23:44–23:47native-inspection interval; actual seal timestamp and bytes remain
preserved. Full-ink boxes define this conditional model's position allowance,
not historical drawing error. No new page, held transfer, star-name binding,
word meaning or general astronomical interpretation follows.
