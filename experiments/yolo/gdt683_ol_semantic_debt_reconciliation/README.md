# GDT683 — OL semantic debt reconciliation

Status: `PASS_374_BILATERAL_OL_BASE__64_MAJORITY_WITH_RIVAL__25_OVERRIDES__V57_ZERO_OL_DEBT`

GDT683 repairs the split between GDT664's published practical stem card
`ol = Grundansatz` and the stale structural meta-gloss that the later token
renderer inherited. All 463 admitted exact ZL3b `ol` positions are routed:
374 retain the portable working card in both alternate readers, 64 use it only
as a majority default with an explicit rival, and 25 receive local boundary or
reader-rival renderers. V57 replaces the
six visible semantic-debt positions with four bilateral free `Grundansatz`
readings, one one-reader majority default with an explicit rival, and one
locally bound `cheopol` powder compound.

See `REPORT.md`, `METHOD.md`, `artifacts/` and `experiment.json`.
