# GDT783 preregistration — `chsky` majority-variant fields

## Frozen cohort

The cohort has exactly three physical loci. `f86v5.15@12` is the masked GDT781
target. `f25r.2@3` is admitted by ZL3b/RF1b `chsky` against IT2a `chrky`;
`f103r.37@7` is admitted by ZL3b/IT2a `chsky` against RF1b `chsty`. Readers are
alternate readings, not independent observations. Target letters and their
variants receive no semantic credit.

## Frozen evidence order

The target slot is removed before field construction. Majority-complete
neighbor wholes are read at R3 in both external fields. The target line is a
separate R5 sensitivity only. GDT754 sanitation precedes the GDT734 clean-pool
vote; GDT768 may change display wording but not axes. All four GDT781 analogs
must be audited, and the source-composed `chky` card must not vote.

## Frozen candidates and selection

The candidate deck contains exactly six readings: HOT quality, HOT|DRY
quality, hot-dried material, preparation heat-stage, dry preparation and a
heating process. The score formula is fixed in the runner and emitted on every
score row. `FINAL_SELECTION_SPEC.tsv` separately freezes the practical
exploratory policy: retain the parent HOT|DRY card while it remains possible,
but publish HOT-only as the minimum portable core and dissent, with HOT C1 and
DRY C0.

## Frozen outputs and failure conditions

Required shapes are three physical loci, 28 non-target aligned slots, four
analog audits, three historical comparators, six candidates, one revision, one
target patch, two unlicensed external readers and 376 renderer rows. The run
fails if a target enters a neighbor field, a reader is counted separately, the
GDT754 block disappears, any of the 109 inherited GDT782 columns changes, a
new token is consumed, renderer totals differ from 270 contextual/106 fallback
/230 consumed, or any forbidden claim becomes nonzero.

The GDT388 relation packet is acquisition-only and is expected not to meet
capacity, holdout or mobile-null score-ready gates.
