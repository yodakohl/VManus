# GDT906 artifacts

`BINDINGS.json` seals unchanged inputs and prospective primary implementation.
`PLAN.json.gz` is the complete exact partition of the 2,240,785 surviving GDT905
mask rows into 98,083 segmentation classes and 439,399 vowel cases. Its 7.2 MB
compressed size is needed to preserve every equivalent mask and literal unit.
`SCOPE.json` binds that plan and records reused/outstanding case counts.
`PROGRESS.json`, if present, is an operational checkpoint, not a final verdict.
Completed primary and independent case receipts will be packed for publication.
No reference-cache or private work-directory paths are stored in artifacts.
