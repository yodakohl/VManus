# GDT906 complete CV key enumeration

Registration: 2026-09-10, before preparing the full case plan or running new key
cases. The user explicitly removed the GDT905 time budget and requested a
solution. This continuation removes elapsed-time and witness-count limits only.

The unresolved question is whether any complete observed key for the same 49
whole paragraphs passes the frozen grammar. The reference, grammar, 12–24 raw
 group scope, literal 20-character alphabet, six inherent vowels and 27-component
injective prefix code are exactly GDT905/GDT892. No new source, held paragraph,
exception or linguistic objective is introduced. GDT892 control capacity remains
insufficient; its recovery control has not passed.

Use the independently validated GDT905 mask sets. Group masks only when the
ordered segmentation of every sorted unique raw word and inherent-vowel bitset
are identical, preserving literal codeword identities and every equivalent mask.
Each allowed vowel in each group is one case. The exact case partition and input
hashes are retained. Unused full-code assignments may be represented by one valid
completion because the endpoint is the observed key/plaintext. Every represented
mask must permit extension to all 27 components, including unused singletons.

Reuse a GDT905 COMPLETE case only under identical bindings and matching masks.
Carry all its complete lexical assignments, including the two rejected by CFG.
Restart every UNKNOWN case from its root: GDT905 did not save a branch frontier.
Enumerate all remaining cases using the frozen exact table CSP with infinite
deadline, no witness limit, up to 28 primary workers and durable atomic receipts.
Retain every lexical assignment and its full-paragraph grammar verdict.

An independent implementation verifies the partition, rebuilds lexical tables
from hash-verified reference indexes, and uses sound propagation plus exhaustive
Z3 model blocking to establish the exact key set of every case. It independently
reencodes candidates and evaluates the complete feature grammar. Four CPU slots
are reserved for that validation. Infrastructure interruption leaves explicit
outstanding cases, never a negative scientific result. No clock forces closure.

A grammar-accepted key is an exploratory full-model candidate for consequence
review, not an established translation or Latin identity. Complete zero closes
this exact construction. Multiple survivors must all be retained; none may be
selected by attractive prose. Alternate transcriptions are dependent readings of
one manuscript. Confirmed meanings remain zero without independent binding.
