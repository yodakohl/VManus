# GDT779 route declaration — sanitized residual V99R7 exact wholes

Declared: 2026-09-03, after GDT778 was published and before the GDT779 runner
was written.

## Fixed cohort and occurrence-free rule

Start from all 376 final GDT778 renderer rows and retain only its 181 generic
fallbacks. A raw candidate has a complete `right_surface` present in the fixed
V99R7 complete-word registry. This produces 99 rows/76 forms. A candidate is
selected exactly when `right_reader_exact=1`; every occurrence of a qualifying
form is kept. The expected selected cohort is 50 rows/44 forms on 49 loci, 33
page labels and 24 physical folios. The 49 rejected raw rows/36 forms are an
exactness audit, not a source from which to hand-pick survivors.

The fallback predicate is a real dispatch condition, not merely a reporting
filter. Across the full 376-row parent, the final 44-form deck has 63
reader-exact matches: 50 unresolved fallbacks and 13 already contextual rows.
Those 13 rows—covering eight forms—are a protected shadow cohort and must not
be overwritten. The selected rule is exactly inherited fallback AND fixed
complete surface AND right-reader exact.

The broader control keeps the original 76 raw card-bearing forms. Across all
376 rows it must contain 179 matches: 99 fallback plus 80 contextual. Of its
127 reader-exact matches, 50 are the selected fallbacks and 77 are protected
contextual rows. The final 44-form deck itself must have 68 raw parent matches,
63 reader-exact matches and the same selected 50.

Selection may use neither target occurrence ID nor page, folio, locus,
frequency, neighbours, edit distance, semantic family, prefix, suffix or body.
Occurrence IDs are output fingerprints only.

## Frozen meaning and sanitization rule

The authored 44-card deck freezes one short German working default, two rivals,
confidence, positive evidence and counterevidence for each complete surface.
Forty-one cards have V99R7 `unconditional_global_export_allowed=1`. Three
forms—`daiin`, `kaiiin` and `chcthy`—have an inherited context-bound card and
receive the new scope `EXACT_OL_PLUS_COMPLETE_WHOLE_ONLY`; this does not change
their global source license.

Six old cards contain literal patients inherited from the retired head model:
`cheeor`, `cheor`, `lkedy`, `loeees`, `olshdy` and `qokar`. Their old strings
remain visible only in provenance and are replaced by patient-free whole
defaults. `qockhey` is separately source-composed in GDT754; its V99R7 prose is
forbidden and only the later GDT755 complete-whole candidate may seed the new
short value. No other default is inferred from a substring.

`chcphy` and `okeeey` are the two GDT734 cards originally selected through a
unique full-card composition route. They may run only as inherited complete
wholes and must be labelled `COMPOSITION_DERIVED_WHOLE__NO_COMPONENT_EXPORT`;
their old derivation grants no current component meaning.

The five disjoint provenance classes are frozen at 32 direct inherited whole
cards, six patient sanitizations, three new exact-`ol` scopes, two
composition-derived complete-whole cards, and one later GDT755 replacement.

## Renderer, passage and consumption rule

Precedence is GDT779 exact whole, then unchanged GDT778. Every selected row is
an inherited fallback, so the selected complete whole directly replaces
`Ansatz-/Zubereitungsposten`. The whole right token is consumed once. No
same-row takeover is expected and any inherited or cross-row token collision is
fatal. Consumption must rise from 155 to 205.

All 50 target rows must change. Because two targets share `f75r.26`, the
passage layer should contain 49 distinct locus patches. Contextual coverage
must rise 195 to 245 and fallback count fall 181 to 131.

## Claim ceiling

GDT779 is an aggressive practical renderer pass, not a decipherment claim. It
may publish only replaceable meanings of complete right words inside exact
`ol + whole` spans. It assigns no free value to an EVA character, prefix,
suffix, body or sound and confirms no lexeme, plaintext, specific substance,
disease, treatment, historical abbreviation or recipe. No new page, image,
OCR, transcription, `f84` or `f84r` may be opened.
