# GDT861 — two exact-locator native comparisons

GDT859 establishes local visible AB upper contact at f56r.1, separately from
raw source segmentation. GDT860 finds additional literal mentions at f100r.12
(@168;, ZL/RF) and f114r.1 (@167;, RF). These are exposed text locators, not
established physical glyphs. Root's prior guarded TGC001 exclusion check found
no excluded record on either selected page. New page admissions are registered
here before acquisition: f100r Yale1006248 and f114r Yale1006272, exact source
URLs/dimensions in SPEC. No other page, crop, OCR or enhancement.

Question: at each registered text locator, can native viewers localize a
candidate entity region and see an extended upper connection reaching a
separate neighboring written form? This is a bounded discovery comparison,
not a blind population test or a test of pair semantics. Neither the same
literal code nor matching appearance establishes exact glyph identity.

Root acquires only both original JPEGs after public GO, saves runtime images
and SOURCES.json with page/canvas_id/url/cache_filename/sha256/bytes/width/height.
Root supplies VIEWER_A.json then A_SEAL.json (SHA256 and sealed_at_utc) before
VIEWER_B.json. Both views use full original native images only. A/B may know
fixed source locators and published GDT860 contexts; record actual exposure
honestly. B must not read A's classification before its own record. Both are
machine-authored views of the same two photographs, not independent sources.
A's seal checks immutability, not independently proved collection chronology.

Observation schema: targets T100 and T114, each localized boolean, connection
EXTENDED_UPPER_LINK / NO_EXTENDED_UPPER_LINK / UNCERTAIN, alignment LOCUS_ONLY /
CANDIDATE_ENTITY_REGION / UNCERTAIN, note; also overall note. Extended upper
link means a visible upper trace reaching a separate adjacent form, rather
than only a tall form's internal crossbar. Describe localization and the
candidate neighbor in prose. Full-page native physical line counting and published GDT860 source context
are permitted, with possible line/index mismatch disclosed. No contact
elsewhere on a page qualifies as an entity match. No prescribed raw-group
index mapping; confusion
between line or candidate region must remain UNCERTAIN. LOCUS_ONLY cannot
establish an entity-specific positive or negative classification.

Per-target fixed result: both localized, CANDIDATE_ENTITY_REGION and agreeing
EXTENDED_UPPER_LINK => BOTH_VIEWERS_LOCAL_UPPER_LINK; analogously agreeing
NO_EXTENDED_UPPER_LINK => BOTH_VIEWERS_NO_LOCAL_UPPER_LINK. Any other combination
=> UNRESOLVED_OR_DISAGREEMENT. Report both targets and all notes without
selective omissions or pooling. A resolved no-link target defeats only the
universal claim that every corresponding localized candidate has this visible
link under these observations; it does not reject code identity or allography.
Positive cases establish local appearance only, not a general code-pair rule.
No pen-motion, stroke chronology, physical glyph identity, word or meaning.

Runner consumes recorded observations; it performs no autonomous software
vision. Independent validator checks original image bytes/hashes/dimensions,
source identity against SPEC, frozen page admissions, observation schemas,
A seal and independently recomputes per-target descriptors. Missing sources
or observations prevent completion; never substitute another page. Synthetic
controls cover agreement, no-link, disagreement and locus-only uncertainty.
Frozen runner --check verifies byte-identical result replay.

Budget15min08:15:21–08:30:21UTC including public registration and publication.
Root owns native judgments, globals and git. Implementation agent acquires
and views no images. Frozen code/protocol precede image acquisition.
