# Sidequest V12 `Y–AIIN–Y` reference-versus-index protocol

Date: 2026-08-21

Status: frozen before reading V12 outcomes. Speculative theory generation, not
a GDT result or translation.

## Scope

The target is the sole exact three-card path recurring across two fixed prose
pages:

```text
f10r.6  Y -> AIIN -> Y    Herbal-A field tail
f83r.3  Y -> AIIN -> Y    Biological-B field head, before LCHE-COMMIT
```

Use only the ten fixed sidequest pages, with guarded f84-free source-native
contexts. Do not add pages, mine substrings, use edit/phonetic similarity or
import a semantic label from one register into the other. `f84` and `f84r`
remain sealed.

Each independent agent receives one unchanged background from
`SIDEQUEST_FOUR_AGENT_BACKGROUNDS.md` and may not read sibling V12 reports.

## Required audit

1. Reconstruct both complete physical lines, fields and paragraph records.
2. Inventory every exact Y and AIIN occurrence on the seven fixed prose pages.
3. Identify possible antecedents and downstream consequences without choosing
   them from glyph similarity.
4. Compare the repeated path with matched three-card paths, repeated cards,
   physical position and wrapper/renderer controls.
5. Determine whether the two outer Y cards have independently distinguishable
   left/right operands or merely repeat one formal identity.
6. Test whether `AIIN` binds a prior/current value, indexes a checklist slot,
   marks a dyadic frame, or is ordinary recurring content.
7. Produce a teachable forward writing rule and a controlled reverse reading.

## Frozen candidate architectures

- `SHARED_ACTIVE_REFERENCE`: both Y nodes receive or invoke one active value;
- `PAIRED_EQUAL_VALUE`: two arguments explicitly receive equal amounts/status;
- `DYADIC_RELATION_FRAME`: Y and Y are two anonymous endpoints linked through
  AIIN, without an equality claim;
- `INDEXED_CHECKLIST_FRAME`: the path instantiates a repeated formal coordinate;
- `ORDINARY_FORMULAIC_PROSE`: a recurrent phrase with no special notation;
- `RENDERER_OR_SEGMENTATION_ARTIFACT`;
- `NOT_DISTINGUISHABLE_WITH_TWO_OCCURRENCES`.

Historical analogies such as *idem*, *ut supra*, *ana*, paired accounts or
recipe measures may be considered only as functional comparators. No card may
be read phonetically or assigned one of those words.

## Selection rubric

| criterion | points |
|---|---:|
| exact reconstruction of both occurrences and full records | 20 |
| all-occurrence consistency for Y and AIIN | 20 |
| antecedent/operand and downstream-consequence analysis | 15 |
| renderer, placement and matched-form controls | 15 |
| fit to the assigned historical workshop perspective | 10 |
| executable forward rule and controlled reverse reading | 10 |
| contradictions and fixed-page predictions | 10 |

Maximum: 100. One architecture must win, including the frozen
`NOT_DISTINGUISHABLE` option. Equal amount may win only if both records contain
independently supported symmetric operands; recurrence of `Y` alone is not
sufficient.
