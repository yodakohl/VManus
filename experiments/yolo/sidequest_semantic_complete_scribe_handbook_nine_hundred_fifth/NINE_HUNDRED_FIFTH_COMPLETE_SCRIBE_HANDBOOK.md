# Vollständiges Schreiberhandbuch

Diese Arbeitsfassung benutzt eine kleine produktive Kartenmaschine: 36 Bedeutungswurzeln, 12 Hilfszeichen, 8 Kartenmuster, 15 Rendererregeln und 38 Funktionsallographen.
Zwei Diagrammwerte bleiben lokale Ganzwörter; alles andere wird aus Wurzelrezept, Slotrolle und beabsichtigter Allographenfunktion gelesen.

## Zwölf Arbeitsschritte

1. **OWNER** — Nimm Bild, Becken, Station oder Diagrammplatz als stillen Besitzer.
2. **REGISTER** — Wähle WHAT für Herstellung, HOW für Anwendung und WHEN für Bedingung.
3. **ORDER** — Setze OT/OL/OS, wenn Folge, Fortsetzung oder Zusatz ausdrücklich markiert werden soll.
4. **OPERATION** — Wähle Handlungskerne wie OK, K, CH, CHD, CHK, LSH, P, SOLK oder T.
5. **MATERIAL** — Füge Teil, Ansatz, Portion, Maß oder Nachgabe mit HO/OR/AIN/AIIN/AN ein.
6. **ADDRESS** — Gib Quelle, Zielstelle, Lauf oder Durchlass mit AR/AL/AIR/CKH/L an.
7. **GRADE** — Setze E/EE/EEE für kurz, lang oder vollständig.
8. **STATE** — Setze bereit, kühl, halten, ruhen oder Stufe mit CTH/R/SH/SHED/IIN.
9. **REFERENT** — Setze Y für den laufenden Posten; wähle dessen funktionalen Allographen.
10. **CLOSE** — Füge DY nur als lizenzierten Schrittabschluss hinzu; nacktes dy kann ein Y-Allograph sein.
11. **RENDER** — Erhalte die Wurzelreihenfolge und wende q-, Rahmen-, Grad- und Endallographen an.
12. **READ_BACK** — Lies Wurzeln atomar zurück und erweitere sie mit Bildbesitzer zur flüssigen Werkstattanweisung.

## Acht Kartenmuster

- **WHOLE_LEXICON** — Eine verschmolzene oder lokale Ganzkarte direkt aus dem Werkstattdeck lesen.
- **CLOSING_INSTRUCTION** — Beliebige Nutzlast plus DY; den lokalen Schritt nach Ausführung schließen.
- **ORDERED_INSTRUCTION** — OT/OL/OS zuerst; danach die folgende Nutzlast als Folge oder Fortsetzung lesen.
- **OPERATION_INSTRUCTION** — Mindestens ein Handlungskern; übrige Kerne geben Stoff, Ort, Grad oder Posten an.
- **TRANSFER_OR_PATH** — L/AIR/CKH führt einen Posten von Quelle, durch Lauf oder Durchlass, zur Zielstelle.
- **STATE_OR_GRADE** — Zustand oder Grad ohne eigene Handlung; am aktiven Posten halten oder prüfen.
- **ARGUMENT_OR_ADDRESS** — Menge, Stoff, Quelle oder Zielstelle für die benachbarte Handlung bereitstellen.
- **REFERENT_OR_LABEL** — Den aktiven Posten oder eine lokale Diagrammadresse benennen.

## Symbolkern

- `AIIN` = **MASS**; sichtbarer Hinweis `aiin`.
- `AIN` = **PORTION**; sichtbarer Hinweis `ain`.
- `AIR` = **LAUF**; sichtbarer Hinweis `air`.
- `AL` = **ZIELSTELLE**; sichtbarer Hinweis `al`.
- `AN` = **NACHGABE**; sichtbarer Hinweis `an`.
- `AR` = **QUELLE**; sichtbarer Hinweis `ar`.
- `CH` = **ENTNEHMEN**; sichtbarer Hinweis `ch`.
- `CHD` = **UMSETZEN**; sichtbarer Hinweis `chd/ched`.
- `CHK` = **WAERMEN**; sichtbarer Hinweis `chk/chek`.
- `CKH` = **DURCHLASS**; sichtbarer Hinweis `ckh`.
- `CTH` = **BEREIT**; sichtbarer Hinweis `cth`.
- `DA` = **ZWEITE**; sichtbarer Hinweis `da`.
- `DY` = **SCHLIESSEN**; sichtbarer Hinweis `dy`.
- `E` = **KURZ**; sichtbarer Hinweis `e`.
- `EE` = **LANG**; sichtbarer Hinweis `ee`.
- `EEE` = **VOLLSTAENDIG**; sichtbarer Hinweis `eee`.
- `HO` = **TEIL**; sichtbarer Hinweis `cho/sho`.
- `IIN` = **STUFE**; sichtbarer Hinweis `iin`.
- `K` = **ZUGEBEN**; sichtbarer Hinweis `k`.
- `L` = **LEITEN**; sichtbarer Hinweis `l`.
- `LD` = **FESTBINDEN**; sichtbarer Hinweis `ldd`.
- `LSH` = **SPUELEN**; sichtbarer Hinweis `lsh`.
- `O` = **ARBEITSGANG**; sichtbarer Hinweis `o`.
- `OK` = **ANSETZEN**; sichtbarer Hinweis `qok/ok/chok`.
- `OL` = **FORTSETZEN**; sichtbarer Hinweis `ol/chol/qol/ls/sol`.
- `OR` = **ANSATZ**; sichtbarer Hinweis `or`.
- `OT` = **DANACH**; sichtbarer Hinweis `qot/ot`.
- `P` = **EINBRINGEN**; sichtbarer Hinweis `p`.
- `R` = **KUEHL**; sichtbarer Hinweis `r`.
- `S` = **PROBE**; sichtbarer Hinweis `s`.
- `SH` = **HALTEN**; sichtbarer Hinweis `sh`.
- `SHED` = **RUHEN**; sichtbarer Hinweis `shed`.
- `SOLK` = **SAMMELN**; sichtbarer Hinweis `solk/qolk`.
- `T` = **BEARBEITEN**; sichtbarer Hinweis `t`.
- `TALAM` = **BEISEITESTELLEN**; sichtbarer Hinweis `talam`.
- `Y` = **POSTEN**; sichtbarer Hinweis `y/chy/chey/dy/shy/sy`.
- `A_ADDR` = **STELLE**; sichtbarer Hinweis `a`.
- `AM_ADDR` = **GEGENFELD**; sichtbarer Hinweis `am`.
- `D_ADDR` = **TEILSTELLE**; sichtbarer Hinweis `d`.
- `D_LABEL` = **PHASE**; sichtbarer Hinweis `d`.
- `S_ADDR` = **STERNBEZUG**; sichtbarer Hinweis `s`.
- `S_LABEL` = **PHASENZEICHEN**; sichtbarer Hinweis `s`.
- `CHEO` = **AUSZUG**; sichtbarer Hinweis `cheo`.
- `WHOLE[cheey|shey]` = **LANG HALTEN**; sichtbarer Hinweis `cheey/shey`.
- `NONE` = **LOKALES GANZWORT**; sichtbarer Hinweis `memorize`.
- `CFH` = **AUSPRESSEN**; sichtbarer Hinweis `cfh`.
- `OS` = **DAZU**; sichtbarer Hinweis `os`.
- `RESUME_CARD` = **DAVON**; sichtbarer Hinweis `schol`.

## Funktionsallographen

- `AIIN` + BARE_CONDITION_MEASURE → `aiin`.
- `AIIN` + OPERATIONAL_MEASURE → `daiin`.
- `AL` + EMBEDDED_TARGET → `cheal`.
- `AL` + DEFAULT_TARGET → `dal`.
- `AR` + BARE_CONDITION_SOURCE → `ar`.
- `AR` + ACTIVE_SOURCE → `char`.
- `CHD+DY` + DIRECT_TARGET_CLOSE → `dchdy`.
- `CHD+DY` + DEFAULT_TRANSFER_CLOSE → `schedy`.
- `CHD+Y` + CONDITION_STATE_TRANSFER → `chdy`.
- `CHD+Y` + MARKED_STATION_OPEN_TRANSFER → `chedchy`.
- `CHD+Y` + DEFAULT_OPEN_TRANSFER → `chedy`.
- `CHK+EE+Y` + MATERIAL_LONG_WARM → `cheeky`.
- `CHK+EE+Y` + APPARATUS_LONG_WARM → `chkeey`.
- `NONE` + MOISTURE_STAGE_WHOLE_WORD → `daiial`.
- `NONE` + WEATHER_CLASS_WHOLE_WORD → `iokeeor`.
- `OK+CHD+DY` + CHAIN_FINAL_TRANSFER_CLOSE → `okchedy`.
- `OK+CHD+DY` + STANDALONE_TRANSFER_CLOSE → `qokchdy`.
- `OK+OL` + PRODUCTION_CONTINUATION_START → `okchol`.
- `OK+OL` + APPLICATION_CONTINUATION_START → `qokol`.
- `OK+Y` + CONDITION_CURRENT_START → `choky`.
- `OK+Y` + ARTICLE_INITIAL_CURRENT_START → `qokchy`.
- `OK+Y` + DEFAULT_CURRENT_START → `qoky`.
- `OL` + ATTACHED_OR_RING_CONTINUATION → `chol`.
- `OL` + TRANSFER_CONTINUATION → `ls`.
- `OL` + GENERAL_CONTINUATION → `ol`.
- `OL+Y` + CONDITION_CURRENT_CONTINUATION → `choly`.
- `OL+Y` + APPLICATION_CURRENT_CONTINUATION → `qolchey`.
- `OT+CHD+DY` + COMPACT_FOLLOW_CLOSE → `otchdy`.
- `OT+CHD+DY` + CHAIN_FINAL_FOLLOW_CLOSE → `otchedy`.
- `OT+CHD+DY` + STANDALONE_FOLLOW_CLOSE → `qotchedy`.
- `OT+Y` + APPLICATION_FOLLOW_REFERENT → `otchey`.
- `OT+Y` + PRODUCTION_FOLLOW_REFERENT → `qotchy`.
- `SH+EE+Y` + GENERAL_LONG_HOLD → `cheey`.
- `SH+EE+Y` + MARKED_LONG_HOLD → `sheey`.
- `Y` + MATERIAL_OR_QUALITY_REFERENT → `chey`.
- `Y` + STATE_OR_BODY_REFERENT → `chy`.
- `Y` + ECHOED_CURRENT_REFERENT → `dy`.
- `Y` + BARE_CURRENT_REFERENT → `y`.
