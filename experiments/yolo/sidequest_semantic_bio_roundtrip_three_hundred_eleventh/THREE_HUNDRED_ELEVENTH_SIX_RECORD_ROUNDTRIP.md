# Lehrlings-Rundlauf der sechs Biological-Records

Vorwärts: Kurzhandlung + offen/geschlossen → Kartenidentität → Komponentenrezept + Grad + optionaler Selektor → kanonische Form. Rückwärts: jede registrierte Oberfläche → genau eine Kartenidentität → Kurzhandlung + Scope.

## B1 — 66 Karten

- E101 **[OWNER-RESET]**: `qokedy` ⇄ kurz einwirken [TERMINAL] → `qokedy` (CANONICAL)
- E102: `okaiin` ⇄ bemessen [NONCLOSE] → `okaiin` (CANONICAL)
- E103: `kair` ⇄ Beckenlauf [NONCLOSE] → `kair` (CANONICAL)
- E104: `okal` ⇄ dorthin einsetzen [NONCLOSE] → `okal` (CANONICAL)
- E105: `sar` ⇄ davon [NONCLOSE] → `char` (REGISTERED_ALLOGRAPH)
- E106: `ol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E107: `kain` ⇄ Portion [NONCLOSE] → `chkain` (REGISTERED_ALLOGRAPH)
- E108: `olkain` ⇄ weiterer Anteil [NONCLOSE] → `olkain` (CANONICAL)
- E109: `al` ⇄ dorthin [NONCLOSE] → `al` (CANONICAL)
- E110: `ol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E111: `rol` ⇄ Anschluss [NONCLOSE] → `rol` (CANONICAL)
- E112: `dl` ⇄ Zusatz [NONCLOSE] → `dl` (CANONICAL)
- E113: `olor` ⇄ derselbe Ansatz [NONCLOSE] → `cholor` (REGISTERED_ALLOGRAPH)
- E114: `ol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E115: `sheckhal` ⇄ Kurze Zielpassage [NONCLOSE] → `sheckhal` (CANONICAL)
- E116: `daiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (REGISTERED_ALLOGRAPH)
- E117: `qokeedal` ⇄ Langer Zieleinsatz [NONCLOSE] → `qokeedal` (CANONICAL)
- E118: `daiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (REGISTERED_ALLOGRAPH)
- E119: `chckhy` ⇄ durchleiten [NONCLOSE] → `chckhy` (CANONICAL)
- E120: `schedy` ⇄ überführen [TERMINAL] → `dchedy` (REGISTERED_ALLOGRAPH)
- E121: `qol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E122: `sshkchdy` ⇄ übertragen [TERMINAL] → `sshkchdy` (CANONICAL)
- E123: `chedy` ⇄ überführen [NONCLOSE] → `chdy` (REGISTERED_ALLOGRAPH)
- E124: `ol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E125: `shedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)
- E126: `qolchedy` ⇄ weiterführen [TERMINAL] → `olchedy` (REGISTERED_ALLOGRAPH)
- E127: `qokain` ⇄ Anteil zugeben [NONCLOSE] → `okain` (REGISTERED_ALLOGRAPH)
- E128: `shckhy` ⇄ durchleiten [NONCLOSE] → `chckhy` (REGISTERED_ALLOGRAPH)
- E129: `dl` ⇄ Zusatz [NONCLOSE] → `dl` (CANONICAL)
- E130: `ral` ⇄ Zielmarke [NONCLOSE] → `ral` (CANONICAL)
- E131: `qokchdy` ⇄ neuen Posten einsetzen [TERMINAL] → `qokchdy` (CANONICAL)
- E132: `chey` ⇄ dies [NONCLOSE] → `chey` (CANONICAL)
- E133: `ol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E134: `cheky` ⇄ kurz wärmen [NONCLOSE] → `cheky` (CANONICAL)
- E135: `ol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E136: `shedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)
- E137: `qokedy` ⇄ kurz einwirken [TERMINAL] → `qokedy` (CANONICAL)
- E138: `qokedy` ⇄ kurz einwirken [TERMINAL] → `qokedy` (CANONICAL)
- E139: `chckhy` ⇄ durchleiten [NONCLOSE] → `chckhy` (CANONICAL)
- E140: `qoky` ⇄ einsetzen [NONCLOSE] → `choky` (REGISTERED_ALLOGRAPH)
- E141: `lsho` ⇄ Waschgang [NONCLOSE] → `lsho` (CANONICAL)
- E142: `qokey` ⇄ kurz einwirken lassen [NONCLOSE] → `okey` (REGISTERED_ALLOGRAPH)
- E143: `lshedy` ⇄ Waschgang [TERMINAL] → `lshedy` (CANONICAL)
- E144: `lshedy` ⇄ Waschgang [TERMINAL] → `lshedy` (CANONICAL)
- E145: `chedy` ⇄ überführen [NONCLOSE] → `chdy` (REGISTERED_ALLOGRAPH)
- E146: `qolky` ⇄ Weiterweg [NONCLOSE] → `qolky` (CANONICAL)
- E147: `lchedal` ⇄ Zielabführung [NONCLOSE] → `lchedal` (CANONICAL)
- E148: `qol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E149: `otar` ⇄ Folgequelle [NONCLOSE] → `otar` (CANONICAL)
- E150: `ytey` ⇄ Kurzteil [NONCLOSE] → `ytey` (CANONICAL)
- E151: `okchedy` ⇄ vorigen Posten überführen [TERMINAL] → `okchedy` (CANONICAL)
- E152: `qokal` ⇄ dorthin einsetzen [NONCLOSE] → `okal` (REGISTERED_ALLOGRAPH)
- E153: `okeey` ⇄ länger einwirken lassen [NONCLOSE] → `okeey` (CANONICAL)
- E154: `qol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E155: `cheedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (CANONICAL)
- E156: `sal` ⇄ dorthin [NONCLOSE] → `al` (REGISTERED_ALLOGRAPH)
- E157: `teol` ⇄ kurz weiterführen [NONCLOSE] → `teol` (CANONICAL)
- E158: `dchdy` ⇄ Transfer [TERMINAL] → `dchdy` (CANONICAL)
- E159: `ly` ⇄ Auffanggefäß [NONCLOSE] → `ly` (CANONICAL)
- E160: `dsheol` ⇄ Kurzhalt [NONCLOSE] → `dsheol` (CANONICAL)
- E161: `oiiin` ⇄ Arbeitsstufe [NONCLOSE] → `oiiin` (CANONICAL)
- E162: `olkeedy` ⇄ lange sammeln [TERMINAL] → `olkeedy` (CANONICAL)
- E163: `tedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)
- E164: `cheky` ⇄ kurz wärmen [NONCLOSE] → `cheky` (CANONICAL)
- E165: `shckhedy` ⇄ durchlassen [TERMINAL] → `shckhedy` (CANONICAL)
- E166: `chal` ⇄ dorthin [NONCLOSE] → `al` (REGISTERED_ALLOGRAPH)

## B2 — 62 Karten

- E167 **[OWNER-RESET]**: `dchedy` ⇄ überführen [TERMINAL] → `dchedy` (CANONICAL)
- E168: `qolchedy` ⇄ weiterführen [TERMINAL] → `olchedy` (REGISTERED_ALLOGRAPH)
- E169: `qokain` ⇄ Anteil zugeben [NONCLOSE] → `okain` (REGISTERED_ALLOGRAPH)
- E170: `dy` ⇄ dies [NONCLOSE] → `chey` (REGISTERED_ALLOGRAPH)
- E171: `qokeedy` ⇄ lange einwirken [TERMINAL] → `qokeedy` (CANONICAL)
- E172: `qokal` ⇄ dorthin einsetzen [NONCLOSE] → `okal` (REGISTERED_ALLOGRAPH)
- E173: `lcheckhy` ⇄ Abführpassage [NONCLOSE] → `lcheckhy` (CANONICAL)
- E174: `lched` ⇄ Abführung [NONCLOSE] → `lched` (CANONICAL)
- E175: `qokeey` ⇄ länger einwirken lassen [NONCLOSE] → `okeey` (REGISTERED_ALLOGRAPH)
- E176: `lcheckhedy` ⇄ Trennabzug [TERMINAL] → `lcheckhedy` (CANONICAL)
- E177: `qokaly` ⇄ Zieleinsatz [NONCLOSE] → `qokaly` (CANONICAL)
- E178: `solkaiin` ⇄ Sollsammlung [NONCLOSE] → `solkaiin` (CANONICAL)
- E179: `chckhy` ⇄ durchleiten [NONCLOSE] → `chckhy` (CANONICAL)
- E180: `qokaiin` ⇄ bemessen [NONCLOSE] → `okaiin` (REGISTERED_ALLOGRAPH)
- E181: `qokaiin` ⇄ bemessen [NONCLOSE] → `okaiin` (REGISTERED_ALLOGRAPH)
- E182: `octheol` ⇄ Fortsetzung vorbereiten [NONCLOSE] → `octheol` (CANONICAL)
- E183: `chkeey` ⇄ Langwärmen [NONCLOSE] → `chkeey` (CANONICAL)
- E184: `ldy` ⇄ abziehen [TERMINAL] → `ldy` (CANONICAL)
- E185: `oteey` ⇄ Langfolge [NONCLOSE] → `oteey` (CANONICAL)
- E186: `qokal` ⇄ dorthin einsetzen [NONCLOSE] → `okal` (REGISTERED_ALLOGRAPH)
- E187: `sheckhy` ⇄ Kurzpassage [NONCLOSE] → `sheckhy` (CANONICAL)
- E188: `qoky` ⇄ einsetzen [NONCLOSE] → `choky` (REGISTERED_ALLOGRAPH)
- E189 **[OWNER-RESET]**: `dshedy` ⇄ Frischwasser zugeben [TERMINAL] → `dshedy` (CANONICAL)
- E190: `sotaiin` ⇄ Folgemaß [NONCLOSE] → `otaiin` (REGISTERED_ALLOGRAPH)
- E191: `qokar` ⇄ Quelleinsatz [NONCLOSE] → `qokar` (CANONICAL)
- E192: `shedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)
- E193: `solshedy` ⇄ Folgeabsetzen [TERMINAL] → `solshedy` (CANONICAL)
- E194: `qokeey` ⇄ länger einwirken lassen [NONCLOSE] → `okeey` (REGISTERED_ALLOGRAPH)
- E195: `qoky` ⇄ einsetzen [NONCLOSE] → `choky` (REGISTERED_ALLOGRAPH)
- E196: `ls` ⇄ Auslass [NONCLOSE] → `ls` (CANONICAL)
- E197: `cheey` ⇄ Klarlauf [NONCLOSE] → `cheey` (CANONICAL)
- E198 **[OWNER-RESET]**: `okain` ⇄ Anteil zugeben [NONCLOSE] → `okain` (CANONICAL)
- E199: `char` ⇄ davon [NONCLOSE] → `char` (CANONICAL)
- E200: `okain` ⇄ Anteil zugeben [NONCLOSE] → `okain` (CANONICAL)
- E201: `qokeedy` ⇄ lange einwirken [TERMINAL] → `qokeedy` (CANONICAL)
- E202: `lchy` ⇄ Abführgut [NONCLOSE] → `lchy` (CANONICAL)
- E203 **[OWNER-RESET]**: `cheey` ⇄ Klarlauf [NONCLOSE] → `cheey` (CANONICAL)
- E204: `qcthey` ⇄ kurz vorbereiten [NONCLOSE] → `qcthey` (CANONICAL)
- E205: `qokeey` ⇄ länger einwirken lassen [NONCLOSE] → `okeey` (REGISTERED_ALLOGRAPH)
- E206: `lcheey` ⇄ Klarabzug [NONCLOSE] → `lcheey` (CANONICAL)
- E207: `daiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (REGISTERED_ALLOGRAPH)
- E208: `chey` ⇄ dies [NONCLOSE] → `chey` (CANONICAL)
- E209: `qokeeedy` ⇄ Volleinsatz [TERMINAL] → `qokeeedy` (CANONICAL)
- E210: `lchedy` ⇄ abführen [TERMINAL] → `lchedy` (CANONICAL)
- E211: `lar` ⇄ Quellabzug [NONCLOSE] → `lar` (CANONICAL)
- E212 **[OWNER-RESET]**: `tshey` ⇄ Folgeklarlauf [NONCLOSE] → `tshey` (CANONICAL)
- E213: `qokeedy` ⇄ lange einwirken [TERMINAL] → `qokeedy` (CANONICAL)
- E214: `cheal` ⇄ dorthin [NONCLOSE] → `al` (REGISTERED_ALLOGRAPH)
- E215: `lchedar` ⇄ Quellabführung [NONCLOSE] → `lchedar` (CANONICAL)
- E216: `ches` ⇄ teilen [NONCLOSE] → `ches` (CANONICAL)
- E217: `aiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (CANONICAL)
- E218: `oteey` ⇄ Langfolge [NONCLOSE] → `oteey` (CANONICAL)
- E219: `qokaiin` ⇄ bemessen [NONCLOSE] → `okaiin` (REGISTERED_ALLOGRAPH)
- E220: `okey` ⇄ kurz einwirken lassen [NONCLOSE] → `okey` (CANONICAL)
- E221: `pchedy` ⇄ Zuführung [TERMINAL] → `pchedy` (CANONICAL)
- E222: `rsheal` ⇄ Kurzhalt am Ziel [NONCLOSE] → `rsheal` (CANONICAL)
- E223: `daldy` ⇄ Zielschluss [TERMINAL] → `daldy` (CANONICAL)
- E224: `qokeedy` ⇄ lange einwirken [TERMINAL] → `qokeedy` (CANONICAL)
- E225: `rshedy` ⇄ Vollwaschung [TERMINAL] → `rshedy` (CANONICAL)
- E226: `qoteedy` ⇄ lange Folgestufe [TERMINAL] → `qoteedy` (CANONICAL)
- E227: `qokeedy` ⇄ lange einwirken [TERMINAL] → `qokeedy` (CANONICAL)
- E228: `lochedy` ⇄ Abführung [TERMINAL] → `lochedy` (CANONICAL)

## B3 — 86 Karten

- E229 **[OWNER-RESET]**: `olkeedy` ⇄ lange sammeln [TERMINAL] → `olkeedy` (CANONICAL)
- E230: `qotal` ⇄ danach dorthin [NONCLOSE] → `otal` (REGISTERED_ALLOGRAPH)
- E231: `chkeedy` ⇄ Langwärmen [TERMINAL] → `chkeedy` (CANONICAL)
- E232: `chey` ⇄ dies [NONCLOSE] → `chey` (CANONICAL)
- E233: `daiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (REGISTERED_ALLOGRAPH)
- E234: `chey` ⇄ dies [NONCLOSE] → `chey` (CANONICAL)
- E235: `lchedy` ⇄ abführen [TERMINAL] → `lchedy` (CANONICAL)
- E236: `qokaiin` ⇄ bemessen [NONCLOSE] → `okaiin` (REGISTERED_ALLOGRAPH)
- E237: `qotal` ⇄ danach dorthin [NONCLOSE] → `otal` (REGISTERED_ALLOGRAPH)
- E238: `dar` ⇄ davon [NONCLOSE] → `char` (REGISTERED_ALLOGRAPH)
- E239 **[OWNER-RESET]**: `schedy` ⇄ überführen [TERMINAL] → `dchedy` (REGISTERED_ALLOGRAPH)
- E240: `chedchy` ⇄ Postentransfer [NONCLOSE] → `chedchy` (CANONICAL)
- E241: `qokal` ⇄ dorthin einsetzen [NONCLOSE] → `okal` (REGISTERED_ALLOGRAPH)
- E242: `olchedy` ⇄ weiterführen [TERMINAL] → `olchedy` (CANONICAL)
- E243: `qokaiin` ⇄ bemessen [NONCLOSE] → `okaiin` (REGISTERED_ALLOGRAPH)
- E244: `chedy` ⇄ überführen [NONCLOSE] → `chdy` (REGISTERED_ALLOGRAPH)
- E245: `qokeedy` ⇄ lange einwirken [TERMINAL] → `qokeedy` (CANONICAL)
- E246: `lchedy` ⇄ abführen [TERMINAL] → `lchedy` (CANONICAL)
- E247: `qoky` ⇄ einsetzen [NONCLOSE] → `choky` (REGISTERED_ALLOGRAPH)
- E248 **[OWNER-RESET]**: `pchedal` ⇄ Zielzuführung [NONCLOSE] → `pchedal` (CANONICAL)
- E249: `otedy` ⇄ Kurzfolge [TERMINAL] → `otedy` (CANONICAL)
- E250: `shecthedchy` ⇄ Vorbereitungstransfer [NONCLOSE] → `shecthedchy` (CANONICAL)
- E251: `qoky` ⇄ einsetzen [NONCLOSE] → `choky` (REGISTERED_ALLOGRAPH)
- E252: `chedy` ⇄ überführen [NONCLOSE] → `chdy` (REGISTERED_ALLOGRAPH)
- E253: `chary` ⇄ Quellposten [NONCLOSE] → `chary` (CANONICAL)
- E254: `sor` ⇄ Ansatz [NONCLOSE] → `chor` (REGISTERED_ALLOGRAPH)
- E255: `shedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)
- E256: `qokaiin` ⇄ bemessen [NONCLOSE] → `okaiin` (REGISTERED_ALLOGRAPH)
- E257: `chkain` ⇄ Portion [NONCLOSE] → `chkain` (CANONICAL)
- E258: `shcthey` ⇄ kurz vorbereiten [NONCLOSE] → `qcthey` (REGISTERED_ALLOGRAPH)
- E259: `qokedy` ⇄ kurz einwirken [TERMINAL] → `qokedy` (CANONICAL)
- E260: `okair` ⇄ Laufeinsatz [NONCLOSE] → `okair` (CANONICAL)
- E261: `sheedy` ⇄ Langabsetzen [TERMINAL] → `sheedy` (CANONICAL)
- E262: `lchedy` ⇄ abführen [TERMINAL] → `lchedy` (CANONICAL)
- E263: `lo` ⇄ Abzug [NONCLOSE] → `lo` (CANONICAL)
- E264 **[OWNER-RESET]**: `qokchedy` ⇄ vorigen Posten überführen [TERMINAL] → `okchedy` (REGISTERED_ALLOGRAPH)
- E265: `qokeedy` ⇄ lange einwirken [TERMINAL] → `qokeedy` (CANONICAL)
- E266: `shedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)
- E267: `qokshedy` ⇄ Einsatzabsetzen [TERMINAL] → `qokshedy` (CANONICAL)
- E268: `dal` ⇄ dorthin [NONCLOSE] → `al` (REGISTERED_ALLOGRAPH)
- E269: `lchedy` ⇄ abführen [TERMINAL] → `lchedy` (CANONICAL)
- E270: `qokaiin` ⇄ bemessen [NONCLOSE] → `okaiin` (REGISTERED_ALLOGRAPH)
- E271: `shcthy` ⇄ bereit [NONCLOSE] → `checthy` (REGISTERED_ALLOGRAPH)
- E272: `dal` ⇄ dorthin [NONCLOSE] → `al` (REGISTERED_ALLOGRAPH)
- E273: `sy` ⇄ dies [NONCLOSE] → `chey` (REGISTERED_ALLOGRAPH)
- E274: `saiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (REGISTERED_ALLOGRAPH)
- E275: `shedal` ⇄ am Ziel absetzen [NONCLOSE] → `shedal` (CANONICAL)
- E276: `shecthy` ⇄ Kurzvorbereitung [NONCLOSE] → `shecthy` (CANONICAL)
- E277: `chey` ⇄ dies [NONCLOSE] → `chey` (CANONICAL)
- E278: `tal` ⇄ dorthin [NONCLOSE] → `al` (REGISTERED_ALLOGRAPH)
- E279: `shcthy` ⇄ bereit [NONCLOSE] → `checthy` (REGISTERED_ALLOGRAPH)
- E280: `dalchdy` ⇄ Zieltransfer [TERMINAL] → `dalchdy` (CANONICAL)
- E281: `qotchedy` ⇄ Folgetransfer [TERMINAL] → `otchedy` (REGISTERED_ALLOGRAPH)
- E282: `lchedy` ⇄ abführen [TERMINAL] → `lchedy` (CANONICAL)
- E283: `tchedy` ⇄ überführen [TERMINAL] → `dchedy` (REGISTERED_ALLOGRAPH)
- E284: `qokchdy` ⇄ neuen Posten einsetzen [TERMINAL] → `qokchdy` (CANONICAL)
- E285: `cheedar` ⇄ Quelltransfer [NONCLOSE] → `cheedar` (CANONICAL)
- E286: `chldaiin` ⇄ Sollabsetzung [NONCLOSE] → `chldaiin` (CANONICAL)
- E287: `chedy` ⇄ überführen [NONCLOSE] → `chdy` (REGISTERED_ALLOGRAPH)
- E288: `qokain` ⇄ Anteil zugeben [NONCLOSE] → `okain` (REGISTERED_ALLOGRAPH)
- E289: `checthy` ⇄ bereit [NONCLOSE] → `checthy` (CANONICAL)
- E290: `chealror` ⇄ Zielbereitung [NONCLOSE] → `chealror` (CANONICAL)
- E291 **[OWNER-RESET]**: `solkeedy` ⇄ lange sammeln [TERMINAL] → `olkeedy` (REGISTERED_ALLOGRAPH)
- E292: `qoteedy` ⇄ lange Folgestufe [TERMINAL] → `qoteedy` (CANONICAL)
- E293: `qokeey` ⇄ länger einwirken lassen [NONCLOSE] → `okeey` (REGISTERED_ALLOGRAPH)
- E294: `qokedy` ⇄ kurz einwirken [TERMINAL] → `qokedy` (CANONICAL)
- E295: `sol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E296: `cheeety` ⇄ Vollteil [NONCLOSE] → `cheeety` (CANONICAL)
- E297: `qokedy` ⇄ kurz einwirken [TERMINAL] → `qokedy` (CANONICAL)
- E298: `qoky` ⇄ einsetzen [NONCLOSE] → `choky` (REGISTERED_ALLOGRAPH)
- E299: `saiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (REGISTERED_ALLOGRAPH)
- E300: `schedair` ⇄ Weiterlauf [NONCLOSE] → `schedair` (CANONICAL)
- E301: `otchedy` ⇄ Folgetransfer [TERMINAL] → `otchedy` (CANONICAL)
- E302: `qokeedy` ⇄ lange einwirken [TERMINAL] → `qokeedy` (CANONICAL)
- E303: `chedain` ⇄ Anteilstransfer [NONCLOSE] → `chedain` (CANONICAL)
- E304: `chedy` ⇄ überführen [NONCLOSE] → `chdy` (REGISTERED_ALLOGRAPH)
- E305: `qotedaiin` ⇄ Kurzsoll [NONCLOSE] → `qotedaiin` (CANONICAL)
- E306: `otaiin` ⇄ Folgemaß [NONCLOSE] → `otaiin` (CANONICAL)
- E307: `otedy` ⇄ Kurzfolge [TERMINAL] → `otedy` (CANONICAL)
- E308: `ldy` ⇄ abziehen [TERMINAL] → `ldy` (CANONICAL)
- E309: `soiiin` ⇄ Arbeitsstufe [NONCLOSE] → `oiiin` (REGISTERED_ALLOGRAPH)
- E310: `checthy` ⇄ bereit [NONCLOSE] → `checthy` (CANONICAL)
- E311: `chety` ⇄ Teil [NONCLOSE] → `chety` (CANONICAL)
- E312: `otaiin` ⇄ Folgemaß [NONCLOSE] → `otaiin` (CANONICAL)
- E313: `olsaly` ⇄ Zwischenziel [NONCLOSE] → `olsaly` (CANONICAL)
- E314: `shedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)

## B4 — 47 Karten

- E315 **[OWNER-RESET]**: `qokeedy` ⇄ lange einwirken [TERMINAL] → `qokeedy` (CANONICAL)
- E316: `qolchey` ⇄ Weiterposten [NONCLOSE] → `qolchey` (CANONICAL)
- E317: `qokeey` ⇄ länger einwirken lassen [NONCLOSE] → `okeey` (REGISTERED_ALLOGRAPH)
- E318: `qokedy` ⇄ kurz einwirken [TERMINAL] → `qokedy` (CANONICAL)
- E319: `chedy` ⇄ überführen [NONCLOSE] → `chdy` (REGISTERED_ALLOGRAPH)
- E320: `otal` ⇄ danach dorthin [NONCLOSE] → `otal` (CANONICAL)
- E321: `otchey` ⇄ das nächste [NONCLOSE] → `otchey` (CANONICAL)
- E322: `qokeey` ⇄ länger einwirken lassen [NONCLOSE] → `okeey` (REGISTERED_ALLOGRAPH)
- E323: `qoky` ⇄ einsetzen [NONCLOSE] → `choky` (REGISTERED_ALLOGRAPH)
- E324: `tol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E325: `shedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)
- E326: `qokylddy` ⇄ befestigen [TERMINAL] → `qokylddy` (CANONICAL)
- E327: `dain` ⇄ Einlage [NONCLOSE] → `dain` (CANONICAL)
- E328: `chedy` ⇄ überführen [NONCLOSE] → `chdy` (REGISTERED_ALLOGRAPH)
- E329: `qokeedy` ⇄ lange einwirken [TERMINAL] → `qokeedy` (CANONICAL)
- E330: `shckhedy` ⇄ durchlassen [TERMINAL] → `shckhedy` (CANONICAL)
- E331: `shckhedy` ⇄ durchlassen [TERMINAL] → `shckhedy` (CANONICAL)
- E332: `saiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (REGISTERED_ALLOGRAPH)
- E333: `cheeky` ⇄ länger bearbeiten [NONCLOSE] → `cheeky` (CANONICAL)
- E334: `sheey` ⇄ Langhalt [NONCLOSE] → `sheey` (CANONICAL)
- E335: `qokedy` ⇄ kurz einwirken [TERMINAL] → `qokedy` (CANONICAL)
- E336: `shedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)
- E337: `oldy` ⇄ fertig [TERMINAL] → `oldy` (CANONICAL)
- E338 **[OWNER-RESET]**: `saiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (REGISTERED_ALLOGRAPH)
- E339: `cheky` ⇄ kurz wärmen [NONCLOSE] → `cheky` (CANONICAL)
- E340: `okeeol` ⇄ Langfortsetzung [NONCLOSE] → `okeeol` (CANONICAL)
- E341: `okain` ⇄ Anteil zugeben [NONCLOSE] → `okain` (CANONICAL)
- E342: `chdy` ⇄ überführen [NONCLOSE] → `chdy` (CANONICAL)
- E343: `sol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E344: `lkedy` ⇄ weiter abziehen [TERMINAL] → `lkedy` (CANONICAL)
- E345: `lchedy` ⇄ abführen [TERMINAL] → `lchedy` (CANONICAL)
- E346: `qokol` ⇄ Weiter einsetzen [NONCLOSE] → `qokol` (CANONICAL)
- E347: `shedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)
- E348: `or` ⇄ Ansatz [NONCLOSE] → `chor` (REGISTERED_ALLOGRAPH)
- E349: `chey` ⇄ dies [NONCLOSE] → `chey` (CANONICAL)
- E350: `qockhey` ⇄ Kurzdurchgang [NONCLOSE] → `qockhey` (CANONICAL)
- E351: `dairydy` ⇄ Laufschluss [TERMINAL] → `dairydy` (CANONICAL)
- E352: `qokain` ⇄ Anteil zugeben [NONCLOSE] → `okain` (REGISTERED_ALLOGRAPH)
- E353: `shey` ⇄ Klarlauf [NONCLOSE] → `cheey` (REGISTERED_ALLOGRAPH)
- E354: `kain` ⇄ Portion [NONCLOSE] → `chkain` (REGISTERED_ALLOGRAPH)
- E355: `chckhal` ⇄ Zielpassage [NONCLOSE] → `chckhal` (CANONICAL)
- E356 **[OWNER-RESET]**: `solkey` ⇄ Kurzsammlung [NONCLOSE] → `solkey` (CANONICAL)
- E357: `lchedy` ⇄ abführen [TERMINAL] → `lchedy` (CANONICAL)
- E358: `qolkain` ⇄ weiterer Anteil [NONCLOSE] → `olkain` (REGISTERED_ALLOGRAPH)
- E359: `dal` ⇄ dorthin [NONCLOSE] → `al` (REGISTERED_ALLOGRAPH)
- E360: `skar` ⇄ Quellausguss [NONCLOSE] → `skar` (CANONICAL)
- E361: `shedy` ⇄ kurz absetzen [TERMINAL] → `cheedy` (REGISTERED_ALLOGRAPH)

## B5 — 11 Karten

- E362 **[OWNER-RESET]**: `otchdy` ⇄ Nachtransfer [TERMINAL] → `otchdy` (CANONICAL)
- E363: `qokchdy` ⇄ neuen Posten einsetzen [TERMINAL] → `qokchdy` (CANONICAL)
- E364: `shedal` ⇄ am Ziel absetzen [NONCLOSE] → `shedal` (CANONICAL)
- E365: `dal` ⇄ dorthin [NONCLOSE] → `al` (REGISTERED_ALLOGRAPH)
- E366: `cheol` ⇄ weiter [NONCLOSE] → `cheol` (CANONICAL)
- E367: `lol` ⇄ Weiterabzug [NONCLOSE] → `lol` (CANONICAL)
- E368: `chdal` ⇄ Zieltransfer [NONCLOSE] → `chdal` (CANONICAL)
- E369: `aiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (CANONICAL)
- E370: `sol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E371: `daiiin` ⇄ Endstufe [NONCLOSE] → `daiiin` (CANONICAL)
- E372: `chedy` ⇄ überführen [NONCLOSE] → `chdy` (REGISTERED_ALLOGRAPH)

## B6 — 9 Karten

- E373 **[OWNER-RESET]**: `solkeey` ⇄ Langsammlung [NONCLOSE] → `solkeey` (CANONICAL)
- E374: `qekey` ⇄ kurz bearbeiten [NONCLOSE] → `qekey` (CANONICAL)
- E375: `raly` ⇄ Endposten [NONCLOSE] → `raly` (CANONICAL)
- E376: `ol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E377: `daiin` ⇄ Sollmaß [NONCLOSE] → `aiin` (REGISTERED_ALLOGRAPH)
- E378: `ol` ⇄ weiter [NONCLOSE] → `cheol` (REGISTERED_ALLOGRAPH)
- E379: `dain` ⇄ Einlage [NONCLOSE] → `dain` (CANONICAL)
- E380: `chey` ⇄ dies [NONCLOSE] → `chey` (CANONICAL)
- E381: `ldalor` ⇄ Endziel [NONCLOSE] → `ldalor` (CANONICAL)
