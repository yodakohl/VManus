# GDT926 — repeated literal contexts with divergent continuations

2026-09-12. User explicitly requests text-grounded recurrent contexts instead of new freely assigned glosses. This is an exposed candidate inventory, not a translation test or a significance study.

Decision note: GDT914 tests coordinated character edits;915 terminal-r/l phrase covariance;803 selected neighbour brackets;925 exact whole-line multiset equality. Those contracts do not inventory branching continuations after longer identical written contexts. This is not a shortened-window repair of925's anagram hypothesis: word order is preserved, no multiset equality or reordered-line claim is tested. All prior failures remain unchanged. Unknown: which long literal anchors have divergent following words on several physical leaves, and whether the same occurrences survive alternate readings. If adequate anchors exist, inspect their entire texts before proposing meanings; if none meets repeated-branch support, report the actual lower-capacity inventory without lowering the predeclared tier.

Inputs: only six guarded GDT915 SOURCE_DISCOVERY/EVALUATION JSONs and its SPEC.json,179 previously admitted selectors. Both old partitions already exposed; their names do not make them independent confirmation now. No raw mixed table, new image, source-language text or reserved page. Assert every metadata page belongs to the old allowed selectors and neither f84 nor f84r is accessed. Alternate readings ZL3b/IT2a/RF1b are one manuscript, not independent trials.

Eligibility: complete P lines, all groups literal [a-z]+, every internal seam DEFINITE_SPACE on both sides and consecutive source indices. Reject the entire line on any defect. Preserve all words. Primary ZL3b; same census separately IT2a/RF1b. Every contiguous anchor of length>=3 ending before the line end is considered, with at least2distinct whole forms. No upper length cutoff other than line length. An anchor must occur on>=2physical leaves and have>=2different immediately following whole forms. Keep all such anchors, not only strongest examples. Multiple occurrences on a line remain distinct.

Two declared levels: INVENTORY as above; REPEATED_BRANCH if at least two distinct following forms each occur with this exact anchor on>=2physical leaves. Repeated branches are capacity only, not evidence of causal/semantic equivalence. All continuation remainders through line end, full host lines and source group IDs are retained. For shortlisted contexts also include the immediately previous/next cached P line on that same selector as context, explicitly not assumed to be one paragraph or sentence. No continuation crosses a rejected seam or line boundary in the search.

Rank the primary candidate worklist deterministically: repeated-branch tier first, longer anchor, more distinct anchor words, more supporting leaves, more occurrences, lexical order. Nested anchors are flagged through exact occurrence-set relations, not counted as independent confirmations or silently discarded. Top10 are navigation only; publish the full primary/alternate candidate census and all occurrences. Assess top10 primary occurrences in each alternate reading by same page/locus and unique matching anchor+next-word; multiple matches remain ambiguous. No alternate index matching pretends identical word segmentation.

Report branch sizes/leaf sets, transcriber dependence, all exact lines and remaining ambiguity. No invented word meaning; no claim that repeated text denotes same object, experiment, cause or condition. No statistical p-value or project-wide search control. No formal GDT388 relation packet is nominated.

Smallest adequate implementation: in-memory exact n-gram dictionary on cached lines plus independent direct scan of all exported anchors and separate complete prefix-trie candidate inventory. Budget40min including preparation, implementation, validation and publication. At budget limit stop expansion; no weaker anchor length, relaxed seam, invented normalization or extra data. Main deliverable: concrete complete-context worklist showing where meanings could later make different textual predictions and where capacity is missing.
