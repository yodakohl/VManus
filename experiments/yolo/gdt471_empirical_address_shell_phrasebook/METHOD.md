# GDT471 method

## Question

Can the 89 label-derived patterns be reduced to a small empirical phrasebook by
replacing only their genuinely learned spans with named slots? Which concrete
function frames recur across pages or owner classes, and should owner-family
substrings be treated as part of the same productive layer?

## Template construction

Hide exact whole-label identity and apply the unchanged 44-channel GDT466
selector to every source form. Preserve every selected surface stem and its
order. Replace each maximal uncovered span, from left to right, with
`{NAME_1}`, `{NAME_2}`, and so on. Record four parallel representations:

- literal surface template;
- component recipe template;
- German working-meaning template;
- PREFIX/INTERNAL/SUFFIX plus name-slot topology.

Group only exact representations. Count source forms, physical pages and owner
classes for every group. Component templates may merge different calibrated
surface renderings of the same old component sequence; topology alone never
inherits a component meaning.

## Mutation and family-marker separation

Rebuild the template after the synthetic `x` insertion for all 89 GDT470
probes. The function template passes only when surface template, component
template, meaning template, topology and exact channel signature all remain
unchanged.

Independently replay all eighteen owner-family substring markers. Report their
loss or retention, but never let a family marker raise function-template
familiarity. This preserves the difference between a productive technical
frame and a memorized name-family clue.

## Intake ranks

The ranked worksheet uses this order: exact label; recurrent exact function
template across owner classes; recurrent exact template across pages; local
recurrent exact template; singleton exact template; known component template
under another calibrated rendering; known slot topology only; whole-name
default; unseen topology.

## Claim boundary

No page, function channel, component meaning, spelling prediction, object name,
plaintext, language or confirmed lexeme is added.
