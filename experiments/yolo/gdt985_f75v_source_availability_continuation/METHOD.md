# GDT985 — a conditional continuation obligation, not a new gloss winner

## Decision before implementation

Selection began 00:00 UTC on 20 September 2026. Inclusive preparation,
implementation, validation and publication checkpoint: 00:40 UTC. The user’s
longer ten-hour research block continues. No extension of this local model at
its checkpoint.

Positive baseline: the complete f75v.38–42 paragraph preserves repeated whole
forms, including alternating cheey/sheey and several ol references. Spaces,
directed composition, whole-form residuals and entry context remain real
constraints; this trial does not replace them with an alphabet. GDT982 gives
an explicit, unconfirmed same-portion B→A→B versus B→A reading of .39.
W56/W58/W59 preserve the entire paragraph and warn that participant identity
and unread intervening actions cannot be inferred from gloss coverage.

Unknown: if the same portion really continues into .40–42, and each source
reference checks its current availability, what changes of location must the
still-unread written groups express? This is an abductive content obligation,
not a claim that the source-reference construction is already read.

The smallest adequate test is the entire three-line continuation, every
written group retained, with one shared two-state partial function per unknown
whole form. It may expose a contradiction, multiple compatible completions,
or a forced transition at a particular whole. No complete word translation,
whole paragraph translation, or observed motion follows from a function table.

Do not build a general decoder or expand to arbitrary outside paragraphs whose
participant identity and source constructions have not been read. In particular,
GDT903's different common-endpoint action contract is not reopened. GDT984's
weak name-code projection stop remains separate and unchanged.

## Selection review

IDEA369's optical f80r sentence supplies eleven new values and no internally
repeated word. Its three geometric constraints are consequences of those new
values, not observations identifying a mirror. After reading GDT943's complete
context and GDT796, retain it as an exploratory draft without a geometry solver.
IDEA368's f17r.5 sowing draft was compared with GDT768/813 and its full .4–6
paragraph: the paragraph supplies no repeated whole within itself, so extending
it would add thirteen or fourteen more nearly unconstrained values. No botanical meaning is
refuted, and no fixed sowing test was run. Both remain possible raw readings.
The f75v continuation is selected for repeated-word coupling, not claimed
independent evidence or a lower false-positive rate.

## Fixed conditional hypotheses

GDT982 stays byte-identical. Its THEN_REPEAT and THEN_ONCE branches finish .39
with the one portion at B; NOT_ONCE finishes at A. NOT_REPEAT is already
contradicted under its old contract and is not repaired. DESCRIPTION makes no
current-location assertion and is not scored as a failed process.

The following are NEW assumptions, not necessary consequences of the old glosses:

1. The same indivisible portion persists through the whole .40–42 continuation.
   No fresh lot, split, refill from hidden stock, or unstated reset is allowed.
2. Every whole `ol` / `or` in this continuation is a source-availability check
   for that portion at A / B, respectively. It does not move it. This interprets
   the old “from A/B” as an actual-current-source construction. A description
   of possible sources or a different participant is a substantive rival.
3. `qol` (portion) and `sheedy` (liquid) preserve location. Their old unconfirmed
   meanings remain assumptions, not independent noun identifications.
4. Every other exact whole has one globally shared deterministic partial
   function {A,B}→{A,B,undefined}. All nine tables are initially allowed. These
   are deliberately permissive effect summaries: identity, either transfer,
   swap, reset, tests, and undefined combinations. An undefined applied step
   fails. A table is NOT the complete meaning of its word.
5. No word is dropped. Repeated words use the same table. Functions compose in
   literal written order across the two line breaks. There is no silent effect
   at a line break. The model owes a real phrase grammar before any lexical use.

This projection knows only location; it says nothing about chemistry, flow rate,
quantity, identity of pictured containers, or event arguments. A table that fits
could correspond to many verbs or a larger construction. The rest of the complete
paragraph (.38 and the already modelled .39) remains printed; .38 is not newly
claimed understood. Preparation on .39 initializes the old conditional account.

## Prospective consequence and rivals

The already exposed IT sequence at the .41/.42 break is `or sheolo | ol`.
Under assumptions 1–5, sheolo must map B to A. Its A input is not fixed by that
single occurrence, so strict return, reset-to-A and swap may be indistinguishable.
This anticipated local fact is not a fresh holdout discovery. The test asks
whether the entire continuation admits such a shared assignment without
contradicting its other repetitions, and reports EVERY word's exact feasible
tables for each old candidate, rather than picking that one span alone.

Source-check versus static-source-description remains unchosen. Global A↔B
renaming is a gauge. THEN_ONCE/THEN_REPEAT have the same input location and are
necessarily indistinguishable here; the test must explicitly report that.
The existing NOT_REPEAT failure is inherited, not counted anew.

## Scope and analysis

Use only GDT928's existing complete paragraph cache and GDT915's six safe source
JSON snapshots. Retain all three original readings of all five physical lines.
IT2a supplies the primary continuation if all three lines remain anchor_eligible.
ZL/RF raw differences, entities, source flags and uncertain spaces are retained;
no `o l`→`ol` or entity normalization is allowed. A reading lacking a complete,
fully eligible continuation is NO_CAPACITY, not a negative or independent test.
No fresh text/image admission, f84/f84r/f116v/reserves remain closed. All target
lines and the anticipated bridge were read BEFORE this registration. Physical
selection leaves=1; independent confirmation capacity=0; the alternatives are
readings of one manuscript. No significance or score-ready relation claim.

For each eligible reading and initial location, solve the whole constraint
system, then ask for each unknown form and all nine functions whether a complete
assignment exists, and similarly for both possible final locations. Preserve
one full grounded witness for every positive query; report UNKNOWN if timed out.
Z3 is the runner, with 10 seconds per query and a 120-second batch checkpoint.
A separately written finite-domain constraint propagator/backtracker verifies
every query and all witnesses without importing the runner. Synthetic sanity
checks include repeated-word contradictions, line-break continuity and the
single intervening-word bridge. They check software, not manuscript meaning.

## Decisions fixed in advance

- UNSAT entire continuation: reject this NEW same-portion/source-check contract
  for that inherited candidate, not its individual words or old GDT982 decision.
- SAT with forced B→A for sheolo: retain `sheolo carries a return-to-A effect`
  as a CONDITIONAL_ROLE_LEAD, alongside exact remaining tables and all debts.
  Do not call it “return” translated or prefer the process over description.
- SAT without forced effect: report the actual alternatives; do not repair rules.
- Missing eligible text or unresolved computation: capacity/unknown, never PASS.

Stop after this one complete continuation. A subsequent meaning test needs an
explicit whole construction binding source, participant and effect in another
complete context. A bare occurrence or an unrelated global source-reference
census does not pay that debt. Record the result, validator and selection notes.
