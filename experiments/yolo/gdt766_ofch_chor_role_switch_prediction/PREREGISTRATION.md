# GDT766 source-registered comparison

## Question and fixed cohorts

Use the inherited guarded cache to enumerate (a) every reader-exact complete
surface containing the literal EVA sequence `ofch`, retaining the strict
`ofch*` prefix subset, and (b) the exact complete surfaces `chor`, `pchor`,
`schor` and `lchor`. EVA similarity is a selector only and supplies no Latin,
sound, character or component value.

The source decks register 25 observed `ofch`-containing whole-form defaults,
four complete-`chor` role candidates, seven family models, six derivation
quarantines, five whole-line renderers and one local default for every token in
those lines. Concrete flower, leaf, amount and process readings may survive as
replaceable working choices even when they remain C0.

## Comparisons

- Compare the 22 exact strict-prefix occurrences other than `ofchy` with all
  exact non-`ofch*` occurrences having the same section and the same global
  exact complete-form frequency. Use the full pool without sampling and report
  the unweighted mean of the 22 pool rates. Line-first and paragraph-start-line
  are outcomes and are not matched.
- Compare `pchor` with `chor` on line-first and true-paragraph-opener geometry
  by a two-sided Fisher exact test.
- Count exact immediate `chor` contacts with the registered complete state and
  value wholes.
- Treat same-line `ofch`/reproductive co-presence as an acquisition candidate,
  not a semantic edge; run the executable GDT388 packet gates.

The registered family score is

```text
2*geometry + 2*context + family_bridge + prior_anchor + portability
- 2*contradiction - circularity
```

It orders working models and is not a probability.

## Renderer boundary

Every registered token receives a local default. Structural role, portable
whole-form description and bold concrete reading remain separate. Inserted
German articles, relations and verbs cannot become lexical evidence. Weak
details remain visible as `möglicherweise` or as named rivals instead of being
silently converted into fluent syntax.

No result may export a value for `ofch`, `chor`, `q`, `l`, `p`, `s` or any
other substring; infer an unseen word; identify a substance or unit as fact;
or claim plaintext. No new page, image or transcription is opened. `f84` and
`f84r` remain forbidden.

## Reproduction targets

The registered expected counts are 43 exact `ofch`-containing occurrences in
25 forms, 25 strict-prefix occurrences in thirteen forms, 191 exact four-`chor`
occurrences split 176/10/3/2, four same-line contacts, ten
`ofchedy|qofchedy` occurrences, five exact lines and 46 tokens. The validator
must independently reconstruct these cohorts, rerun the control pools and
relation packet, enforce zero component export and byte-rebuild every declared
artifact.
