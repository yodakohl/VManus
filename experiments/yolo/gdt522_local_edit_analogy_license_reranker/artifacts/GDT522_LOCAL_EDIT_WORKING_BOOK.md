# GDT522 compact working book

## Live stack

`exact event > known surface/role > finite compiler > visible form/context >
stem/short-renderer alignment > visible boundary > short recipe history >
nearest local-edit analogy`

Selected local addition:

```text
signature = visible block/position -> atom block/position
p = add-0.5 conditional probability within the visible condition
bonus = support/(support+2) * (1.1 + log(p))
final = GDT521 - 0.40 * bonus
```

Only the shortest deletion route to an old base is used; target is the big
form; one-to-three visible and zero-to-three atom insertions are allowed.

## Counts and result

- 1,558 old forms;
- 1,081 local signatures, 3,493 unique pair/signature relations and 585
  visible conditions;
- 49 visible-but-null signatures;
- old four-fold: rank one 1,096, top two 1,327, top three 1,386, top five
  1,418, rank sum 2,113, deepest 22;
- current 159: rank one 142, top two 154, top three/top five 158, rank sum 187,
  deepest 9;
- current: two corrections, zero losses, 17 remaining top-one errors.

## Current corrections

- `dcheol → D_ADDR+CH+E+O+L`, via `dcheo+l→L` (12/12);
- `dyky → D_ADDR+Y+K+Y`, via `yky+d→D_ADDR` (32/47).

`psheody → P+SH+E+O+D_ADDR+Y` remains correct because left `p→P` is
conditionally unambiguous (15/15), rather than being drowned by more frequent
inner `o` insertions.

## Strong null licenses

- left `q→NULL`: 75/84;
- left `ch→NULL`: 29/88;
- inner `ch→NULL`: 22/67;
- inner `e→NULL`: 19/117;
- inner `d→NULL`: 16/56;
- left `d→NULL`: 12/47.

These are renderer/composition behavior, not word meanings.

## Next route

Apply the learned local/null edit inventory inside candidate alignment even
when deleting the visible block does not leave a separately known old surface.
First target: `qef`, whose truth needs `q` to carry no atom but whose residual
`ef` is not an old base. Then test two compatible local edits for the remaining
known-neighbour cases. Do not add target full-form exceptions and do not open
another page.
