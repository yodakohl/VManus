# GDT987 prospective protocol after986results

The earlier source, its published986results and complete exposed target panel
are known. This follow-up freezes a finite enumeration and its derived word-start
constraint before target fitting by that procedure. It is a computational proof
attempt on the same mathematical content hypothesis, not a newly blinded test.

Freeze DECISION.md, METHOD.md, finite.py, run.py, fixtures.py, validate.py, and all
inherited inputs with PREREG_LOCK.json. Publish the registration before running
run.py. The source-only fixture output is identified and bound before fitting.
Every148literal case receives the same main settings; all166source-uncertain
cases remain accounted. Alternative-value queries and independent replay limits
are fixed in METHOD.md. No source or word-seam change, restart vote, selective
paragraph or timeout-to-fit conversion is allowed.

Root owns execution and both implementations. The authorized bounded producer
supplies other raw proposals without reviewing this experiment; it is neither
blind confirmation nor an external expert. All previous exposure is retained.
f84/f84r are sealed; f116v and reserves remain closed. Zero confirmed words,
independent confirmation capacity, or significance are claimed at registration.
