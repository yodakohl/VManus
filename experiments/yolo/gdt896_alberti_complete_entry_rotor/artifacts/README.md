# GDT896 artifacts

SOURCE_PROJECTION binds every retained projected source string by hash and
records all whole-entry exclusions. Reconstruct the unchanged895 source pool
using its published source acquisition/replay instructions. No source bodies
or manuscript packet are acquired by896. MECHANISM_SOURCE binds the historical
printed pages used for the channel definition.

Run src/run.py with --source-pool, --source-lock, --source-audit, --target-packet
and --output. The lock/audit are895's final public artifacts; target is the
exact893 odd-only packet bound in SPEC.json. --source-only writes only the
projection metadata and never opens target data. Run src/validate.py using
the same input arguments, --result and --output. All inputs are checked before
target decompression. See METHOD for budgets, full coverage and claim limits.

Commit compact, reproducible results here. Large exhaustive tables require an explicit retention justification.
