# Artifacts

- `gdt391_complete_unit_frame.tsv`: the 180-locus complete-unit frame.
- `gdt391_pre_normalization_freeze.json`: selection, orders, normalization
  states, access ceiling, and capacity gate frozen before visual normalization.
- `gdt391_pre_normalization_freeze_validation.json`: independent source-frame
  reconstruction and freeze validation.
- `gdt391_image_manifest.tsv` / `gdt391_review_image_hashes.tsv`: the safe
  21-page / 20-canvas review mapping and image hashes.
- `gdt391_page_review.tsv`: complete source-aware page-review accounting.
- `gdt391_normalized_object_relations.tsv`: all 180 conservative normalized
  relation cases with opaque page-local object IDs where singular.
- `gdt391_eligible_relation_packet.tsv`: the four matched eligible positives.
- `gdt391_capacity_gates.tsv`, `gdt391_access_log.json`, `gdt391_result.json`,
  and `gdt391_validation.json`: capacity, access, result, and independent
  artifact/accounting validation.

Commit compact, reproducible results here. Large exhaustive tables require an explicit retention justification.
