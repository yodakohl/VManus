# GDT749 artifact map

- `TARGET_17_FIXED_DECK.tsv`: sixteen GDT748 recurrent complete forms plus the
  fixed `qochey` diagnostic and all excluded discovery coordinates.
- `TARGET_OCCURRENCE_AUDIT.tsv`: all 1,684 target occurrences with exactness,
  discovery flag, whole-neighbor axes and written line.
- `REFERENCE_DISTRIBUTION_SCORES.tsv`: all 781 target-to-known-whole raw
  distribution comparisons.
- `KNOWN_46_LEAVE_SELF_CALIBRATION.tsv`: decisive 46-whole calibration showing
  why raw global ranks cannot confirm meanings.
- `TARGET_OUTSIDE_ROLE_CENSUS.tsv`: baseline-adjusted seventeen-form working
  decisions and all positive/counterevidence.
- `QOCHEY_HYPOTHESIS_SPLIT.tsv`: explicit DRY/MIDDLE, END and HOT/END rivals.
- `GDT749_OUTSIDE_FRAME_READER.md`: compact human reader with outside examples.
- `GDT749_GDT388_OUTSIDE_EDGE_PACKET.tsv` and
  `GDT749_GDT388_EDGE_INTAKE.json`: executable relation-gate evidence.
- `RESULT.json` and `VALIDATION.json`: machine summary and replay certificate.

The occurrence audit and comparison census are retained because they expose
every discovery exclusion and the failed attractive global classifier.
