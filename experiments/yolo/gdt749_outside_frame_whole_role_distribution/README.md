# GDT749 — outside-frame whole-role distribution

Status: 1,311 reader-exact outside occurrences; the global distribution
classifier fails its known-whole self-calibration. Four roles retain weak,
base-rate-adjusted outside compatibility; `qochey` shifts toward an end/transition
role and `okechy` remains polarity-open.

Start with [REPORT.md](REPORT.md). Reproduce with:

```bash
python3 experiments/yolo/gdt749_outside_frame_whole_role_distribution/src/run.py
python3 experiments/yolo/gdt749_outside_frame_whole_role_distribution/src/validate.py
```
