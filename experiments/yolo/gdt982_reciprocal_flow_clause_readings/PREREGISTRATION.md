# GDT982 — exposed exploratory readings, fixed follow-up census

Freeze the five complete trial lexicons, METHOD, source bytes, runner and
validator before the new all-chey census. Publish registration before running.
The seed line, several old countercontexts and one outcome-aware native image
view are already known. No blind semantic confirmation is claimed.
The local NOT_REPEAT failure is anticipated by the written model; its actual
finite execution is a consistency check, not a surprising corpus discovery.
Run every declared model and every fixed frame. No post-result connector,
endpoint, identity, tail scope, paragraph or eligibility change. Separate
local consistency, external construction recurrence and meaning confirmation.
