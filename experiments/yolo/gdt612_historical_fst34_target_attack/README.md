# GDT612 — simplified FST34-inspired decoder pilot

Status: `HEURISTIC_DECODER_INVALIDATED__ORACLE_TRUTH_RANKS_LAST__ZERO_STABLE_TARGET_OUTPUTS`

This developmental pilot fits a simplified hard-bucket implementation inspired
by GDT609's 34-role historical abbreviation prior to the GDT605/GDT606 98-unit
stream. It is not an execution or falsification of the exact GDT609 FST. An
independent reimplementation of the executed objective ranks the planted truth
last of seven keys: `1.914535` versus `3.114952–3.386643` for six wrong fitted
keys. The synthetic control also leaves five truth items unobserved in train.
The objective/control pair is therefore invalidated before target semantics.

The archived target keys yield zero restart-stable primitive outputs, unit
outputs, held word spans, or concrete meanings. Their large language-order
scores are circular diagnostics: real and destroyed fits use different
candidate inventories, and all 18 dominant real outputs are reference words
injected as connector/wholeform candidates.

The one retained target-side structural observation is MHG `y`: suffix role in
6/6 starts, but split 3× `oy` versus 3× `ex`; this is not a value assignment.

See `REPORT.md` for the complete result and `FULL_REPRODUCTION.md` for the clean
scratch-directory rerun. The compact independent audit is:

```sh
python3 experiments/yolo/gdt612_historical_fst34_target_attack/src/run.py
```
