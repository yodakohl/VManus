# GDT974 — declared exploratory two-candidate consequence

15 September 2026, 14:41 UTC. This is an **exploratory post-selection audit**, not a blinded prediction. Root saw both complete four-word headers and their length10 first words in GDT973 before deriving the following contradiction. The subsequent machine check is to make that exact logical consequence exhaustive and reproducible; neither prior exposure nor the GDT973 result is erased. No significance inference is possible.

## Decision note and fixed scope

GDT973's registered necessary-form screen leaves exactly two IT2a paragraphs under R2: f104r16–18 and f114r32–33. It did not test shared internal payload codes. Both complete candidates must now be checked. R1 and R0 remain closed, and R2/source/numeric/word-boundary rules remain unchanged. Live route/composition and ideas/route-check were reviewed in GDT973; the claim-bearing GDT973 primary and frozen R2 primary are the immediate dependencies. This is a narrower mandatory consequence, not a repeated failed route or new decoder.

Smallest adequate test: load the complete GDT973 prediction list; select every R2 NECESSARY_FORM_ONLY row; record selection denominator and both candidates. Inspect the entire first group with its known R2 structure. No body, source values, other paragraph, new page or reserve is used. Inclusive budget14:41–14:49UTC includes preparation, two independent checks, reports and publication. If both contradict, close unchanged R2 in GDT973's eligible panel. If any first word is not length10, mark this particular test UNKNOWN rather than inventing a different alignment. If a length10 word satisfies this necessary repetition, retain it as unparsed, without a full code claim.

## Exact consequence and source-only example

R2's first word is the concatenation

`H(GRADES@00) H(A) H(NUM) H(d_a) H(B) H(NUM) H(d_b) H(C) H(NUM) H(d_c)`

when the three positive grade numerals each have one digit. More digits add components. Every component is nonempty. Thus a ten-character first word forces exactly ten components, all of length1, and exactly one digit per grade. Its zero-based characters2,5,8 (one-based3,6,9) must be identical because they all realize the single globally shared NUM component. This is necessary even before injectivity, ordering, prefix freedom or numeric values are checked. A failure cannot be repaired by choosing a different key or grade values within unchanged R2.

Positive logical fixture: word `abnde nfgn h` without its visual separators is generated explicitly by the runner from ten components `[a,b,n,d,e,n,f,g,n,h]`; its three NUM slots are n. The different grade-digit components are consistent with a possible one-digit ordered-grade assignment and may be extended to a full57-component prefix-free code using other initial letters. The fixture checks this repeated-slot implication, not a complete historical account. Changing only the middle n to x must fail. All complete source examples already passed their writer checks in GDT973.

No new meaning, confirmed word, independent leaf confirmation, RF/ZL replication or plant/material name follows. Both candidate leaves were already project-exposed; f84/f84r, f116v and every reserve remain outside access. GDT973 remains a successful necessary-form screen with two survivors under its original conditions even if this additional consequence rejects both.
