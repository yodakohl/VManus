# GDT536 — `aii` renderer square and `aiicthy` closure

Status: `PASS_AII_RENDERER_SQUARE_aiicthy_CLOSURE`

This experiment completes the old `aiin/saiin/saii` analogue square, reads the
missing fourth corner as `aii=AIIN`, joins it to exact `cthy=CH+T+Y`, and
selects `aiicthy=AIIN+CH+T+Y` as the final unresolved working composition.

Run:

```bash
python3 experiments/yolo/gdt536_aii_renderer_square_aiicthy_closure/src/run.py
python3 experiments/yolo/gdt536_aii_renderer_square_aiicthy_closure/src/validate.py
```

See `REPORT.md` for the result and `METHOD.md` for the bounded construction.
