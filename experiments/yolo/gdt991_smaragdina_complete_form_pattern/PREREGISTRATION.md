# Fixed before new target matching

Bind SOURCE.json, METHOD.md, DECISION.md, model, runner, independent reverse and
validator, source-pattern predictions and pre-run fixture receipt in PREREG_LOCK.
Publish the registration before the first new match. Input is the entire fixed
GDT990 inventory; perform new pattern jobs on all1,980unretained cases with the
same caps. Preserve all other cases. No post-fit case selection, target repair,
source rewrite or additional matcher round. Journals retain each completed job.

The whole repeated (root,slot) sequence is the falsifier. Every positive has a
whole alignment; a successful original factorization is separately labelled.
All uncorroborated negatives, timeouts, errors, pattern-only positives and source
uncertainties remain distinct. Group equal source predictions before execution.
The90%stop and inclusive06:24UTCcheckpoint are fixed as in METHOD/DECISION.
No significance, code uniqueness or independently confirmed lexical meaning.
