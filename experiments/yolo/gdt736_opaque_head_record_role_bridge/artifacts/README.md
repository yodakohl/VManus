# GDT736 artifacts

- `OPAQUE_1166_OCCURRENCE_CONTEXTS.tsv` — complete guarded occurrence-level
  context and role rendering.
- `HEAD_RECORD_ROLE_PROFILE.tsv` — target and full-scope position, paragraph,
  reader, and standalone-proxy census for H1–H4.
- `BODY_CONTROLLED_POSITION_CONTRAST.tsv` and
  `PAIR_POSITION_BY_SECTION.tsv` — body- and section-resolved placement checks.
- `ROLE_AXIS_TESTS.tsv` — raw, strict-reader, and stratified role-axis tests.
- `HEAD_BODY_AFFINITY_PROFILE.tsv` and `HEAD_PAIR_BODY_COSINE.tsv` — complete
  24-body vectors and all six pairwise similarities.
- `RECORD_ROLE_2X2_GRID.tsv` — selected formal architecture and aggressive
  pharmaceutical working roles.
- `GLYPH_CLASS_AND_READER_AUDIT.tsv` — STA provenance and explicit unobserved
  physical-shape fields.
- `BODY_ROLE_DICTIONARY_V2.tsv` — all 24 revised body defaults, confidence,
  evidence, rivals, and the one inherited Herbal-only `air` override.
- `OPAQUE_96_CONCRETE_ROLE_GRID.tsv` — one structural and one aggressive
  working rendering for every registered form.
- `CORRECTED_ROLE_TRANSLATION_EXAMPLES.tsv` — 24 repaired audit spans.
- `HISTORICAL_RECORD_MODEL_COMPARISON.tsv` — eight ranked architectures.
- `RESULT.json` and `VALIDATION.json` — machine-readable result and independent
  byte-replay certificate.

The 1,166-row table is retained because it is the minimum audit trail for the
paragraph, position, reader, body-control, and renderer claims. It contains no
image bytes or new transcription.
