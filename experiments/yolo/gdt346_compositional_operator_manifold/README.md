# GDT346 — compositional operator manifold

This experiment freezes GDT345's fair coordinate marginals and asks whether a
sparse, training-environment-transferable pair graph improves complete held
operator recovery on unseen state×operator combinations. See `METHOD.md`.

Result: **LOCAL_COMPATIBILITY_WITHOUT_TRANSFER**. The coupling-destruction
null detects real local cross-coordinate structure, dominated by interactions
with the canonical wrapper plus a recurrent RIGHT_FAMILY↔DY edge. However,
neither graph repays its frozen topology selector, top-5 recovery does not
improve, held-section transfer fails, and Herbal A supports neither the foreign
nor a locally selected graph after cost. Under the frozen decision rule, the
active conclusion is **MARGINAL_TRANSITION_SMOOTHING_ONLY** and this internal
operator-manifold route stops. No semantic alignment, PAGE_HOST factorization,
tuple merge, or f84 access occurred.
