# Fixed type-level union law

Compile each complete paragraph independently. Let C_p be exactly its written
cargo inventory and H_p every locally resolved PAIR/COPY edge. Let H be the union
of both positive edge sets. Every existing state of p must avoid an unattended
co-located pair from H whose two endpoints both belong to C_p. Keep all other
physical,reference,grammar and final-goal requirements. No cross-paragraph
reference resolution or individual identity is introduced.

The fixed source population has a three-cargo star at qokedy. A third edge
would make any first crossing impossible. After independently checking this
source property for every member and inverse map,use the20case quotient with
canonical center C. Primary symbolic safety adds locally activated C-G/C-W
edges and forbids positive G-W;independent integer/boat model imposes the same
rule independently. Both retain every local PAIR/COPY cause and all states.
Every SAT dictionary is replayed by both unchanged underlying executors with
an external direct union check. The original paragraph is also replayed under
the added assertions for every lifted member,so no one-way inference suffices.

All29saved original1013witnesses are checked as a fixed pre-exposed population.
They are examples,not the complete new search. Every20case and312member remains
in the final table,including UNKNOWN. Repeated restarts or sixfold cargo-name
symmetry do not provide uniqueness. Literal wolf/goat/cabbage labels are unbound.
