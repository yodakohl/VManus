# Fifth compact workshop grammar

The mixed technical register now contains 33 productive semantic cores:

`OK` ANSETZEN; `OT` DANACH; `OL` WEITER; `CH` ENTNEHMEN; `SH` HALTEN; `CTH`
BEREITEN; `T` ANWENDEN; `K` ZUGEBEN; `L` LEITEN; `CHD` UMSETZEN; `P`
EINFÜLLEN; `CHK` WÄRMEN; `SHED` STEHENLASSEN; `LSH` SPÜLEN; `R` KÜHLEN;
`CFH` AUSPRESSEN; `AIR` WASSER; `OR` ANSATZ; `HO` ZUTAT; `CKH` DURCHLASS;
`SOLK` SAMMELSTELLE; `O` VORGANG; `S` PROBE; `AIN` PORTION; `AIIN`
SOLLMASS; `IIN` STUFE; `AL` ZIELSTELLE; `AR` QUELLE; `E` KURZ; `EE` LANG;
`EEE` VOLL; `Y` DIES/current item; licensed `DY` SCHLUSS.

Three values are bound rather than freely productive: `AN` in its after-addition
frame, `DA=ZWEI` only before STUFE, and `LD=BEFESTIGEN` only before SCHLUSS.

Three complete command cards remain memorized: `os`, `dchol/schol`, `talam`.

Coverage: all 173 cards / 381 events / 116 statements rebuild; core33 touches
170 cards / 377 events and fully composes 167 cards / 374 events. The remainder
is six cards / seven events. Seventy-six unseen surface predictions stay
outside the observed edition.
