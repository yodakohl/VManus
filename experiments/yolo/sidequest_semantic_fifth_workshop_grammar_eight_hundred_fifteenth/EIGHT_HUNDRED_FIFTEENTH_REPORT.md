# Sidequest Pass 815: fifth workshop grammar

The complete prose edition is rebuilt after resolving the four former local
singletons. `CFH=AUSPRESSEN` and `S=PROBE` join the productive core;
`DA=ZWEI` and `LD=BEFESTIGEN` become bound components.

The 39-component inventory now has exactly:

- 33 productive cores;
- 3 bound components;
- 3 memorized whole commands;
- 0 local singleton values;
- 0 recurrent wall-strip values.

Core33 fully composes 167/173 card types and 374/381 events. The six-card
remainder is `ykan`, `daiiin`, `qokylddy`, `os`, `dchol/schol`, and `talam`.
The first three are already semantically read through their bound values. Only
the final three still lack any internal recipe.

Next, attack `os` first. Read its exact position in H1-S001 against the
candidates FACH, ORT, WERKZEUG, TRENNSCHRITT, and a copied source label. The
winning meaning must make the whole plant sentence more coherent.
