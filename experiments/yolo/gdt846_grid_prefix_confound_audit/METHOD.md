# GDT846: prefix-grid exposure and confounding audit

GDT845 fixed all72cells and found a descriptive ee+d deficit under bare/o,
but not qo. All8primary ee+d hits are sectionS, already exposed. Unknown:
does the contrast retain informative comparisons within section/hand or folio,
or arise from different exposure? This is an outcome-aware follow-up, not held
confirmation. No new raw query, page, visual admission, semantic edge or image.

Freeze four partitions before reaggregation: CORE only, SECTION, SECTION_HAND,
and physical FOLIO. All additionally stratify prefix, k/t and ch/sh. Unknown
hand @ is a separate category, not assigned to a known hand. Keep all three
readings separate, ZL3b primary. The exact72words and source rows stay unchanged.
Collapse e0/1 versus e2 solely for the fixed e2-by-d2x2. For every stratum retain
(a,b,c,d)=(e2d1,e2d0,e0/1d1,e0/1d0). Expected a is (a+b)(a+c)/n. Report both
all-strata and informative-strata sums. Informative means all four margins
positive; zero cells remain allowed. Record informative strata, their source
folios and supported e2/ee+d events. Do not interpret uninformative strata,
whose expected count can mechanically equal the observation, as confirmation.
Report MH descriptive odds as sum(a*d/n)/sum(b*c/n), with infinity/undefined
explicit; no p value or confidence claim from these dependent exposed data.

Also count exact shared strata in which qo and bare, or qo and o, each have
an informative2x2. These are capacity counts, not authorial text relations.
No later section subset, pooling, threshold rescue, e-range or suffix change.

If finer conditioning removes the descriptive separation, retire the prefix
interpretation. If few informative cross-prefix strata remain, retain only
sparse local support and seek genuinely independent data, not another fit.
Only if a broadly supported contrast persists should a new held test be
considered, with its own feasible data plan. All outcomes leave meanings open.
Budget10min total; checkpoint after aggregation. Validator independently
reconstructs2x2 entries from original hit rows and checks all accounting.
