# GDT846 — descriptive direction survives, direct comparisons remain sparse

**Retain the written-form contrast; do not promote a general prefix rule.**
The ee+d underrepresentation under bare/o and opposite qo direction persist
under the frozen section, section+hand and folio partitions. However, only
one jointly informative exact folio/core stratum remains for qo versus bare,
and one for qo versus o. This is exposed-data description and capacity, not
independent confirmation or a causal test.

| ZL3b conditioning | bare observed/expected | o | qo |
|---|---:|---:|---:|
| k/t × ch/sh only |1/5.889 |1/3.891 |6/4.020 |
| plus section |1/6.160 |1/4.073 |6/4.906 |
| plus section and hand |1/4.751 |1/4.035 |6/4.573 |
| plus physical folio, informative strata only |0/1.525 |1/2.976 |5/3.819 |

All rows additionally stratify prefix. The last row deliberately excludes
uninformative strata, so its observed totals differ from the eight original
hits. A stratum must have positive e2/non-e2 and d/non-d margins; individual
cells may be zero. With folio conditioning, supported e2 counts are4,6,6 for
bare,o,qo respectively; informative stratum counts3,5,6 on3,5,4folios. Some
folios contribute multiple k/t×ch/sh strata and are not independent samples.
The scalar direction is the same in both alternate transcriptions, but the
supported samples differ and remain very small. They are not replications.

## Exact paired support is the decisive limitation

For primary ZL3b, with section+hand conditioning there is one jointly
informative qo/bare stratum and two qo/o strata. On exact physical folios:

| Comparison | Sole shared stratum | qo [a,b,c,d] | Other [a,b,c,d] |
|---|---|---|---|
| qo vs bare | k,ch,f113 |[0,1,5,3] |[0,2,1,2] |
| qo vs o | k,ch,f104 |[1,0,6,5] |[0,1,1,1] |

Here a=e2d1,b=e2d0,c=e0/1d1,d=e0/1d0. The qo/bare shared folio has no ee+d
hit in either prefix. The qo/o comparison has just one ee+d hit versus one
e2-without-d hit. It is a concrete local source contrast, not a robust
cross-folio prefix experiment. The larger separate-prefix summary draws on
different informative supports, so its opposite odds cannot be read as a
well-controlled direct prefix effect.

A copied prefix-linked form, different lexical inventory, unmodelled physical
position or a genuine spelling dependency remain possible. Section/hand
conditioning does not make those rivals disappear. No additional subgroup,
ending, longer e run, classifier, p value or interval was selected here.
The appropriate next observation is source verification of the concrete
f104 contrast, not another global regression or automatic broader fit.

## Source and validation

All2,398 GDT845 hit rows were reused; no new source query, image or selector.
The independent validator parses suffixes differently from the runner, checks
every contingency table, each of the four full-coverage partitions and paired
support. It validates source inventory and arithmetic, not meaning. Frozen
protocol e68832ab preceded this aggregation; GDT845 outcomes were already known.
Route navigation also returned IL010, which concerns another directional
adjacency statistic and does not license reopening its failed hand-transfer
route. No claimed morphology, number, writer identity or translation.

A parallel retrospective source audit is GDT847; its correction explains the
old829 versus current816e0/1count difference without modifying GDT845 bytes.
The current conditioning audit uses the unchanged GDT845 exact raw contract.
