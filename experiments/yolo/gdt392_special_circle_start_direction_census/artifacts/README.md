# Artifacts

- `gdt392_array_frame.tsv`: complete 45-array review frame.
- `gdt392_image_manifest.tsv`: safe allow-listed canvas mapping.
- `gdt392_review_image_hashes.tsv`: cached review-image provenance.
- `gdt392_pre_image_freeze.json`: outcomes, exclusions, gates, and order frozen
  before focused review.
- `gdt392_pre_image_freeze_validation.json`: independent source-frame and
  freeze validation.

Commit compact, reproducible results here. Large exhaustive tables require an
explicit retention justification.
