# GDT969 — complete generated square-root result traces

The selected contract is ROOT_TRACE_DECISION.md together with the exact23-row
programme table in ROOT_EXTRACTION_GENERATED_PROGRAM_REVIEW.md, both in the
existing ten_hours_20260915 source dossier and hash-bound here. The separate
timing correction preserves the erroneous manually estimated minute. The
metadata preflight was executed at11:55:14UTC before any numeric target fit.

Every n=100..9999 supplies a complete trace. INPUT,HIGH,ROOT,SQUARE,SUB,DOUBLE,
APPEND,QUOTIENT,CHOOSE,MUL,SUB,APPEND,SQUARE,SUB,APPEND,MOD,SQUARE,MOD,MOD,ADD,
MOD,MOD,CHECK are the23 ordered operation occurrences. The source programme
TSV contains every concrete numeric prediction before target fitting.
The fixed source-generating algorithm is in src/run.py:trace. Its CHOOSE
enumerates all ten possible next digits. Arithmetic checks are exact.

The source is Fibonacci's integer-root algorithm and explicit proof procedure,
not a claimed literal copy of his prose or table layout. Estimated trial,
selected digit, decimal append, aggregate product, residual and proof outputs
are fully prescribed. Every generated numeric result is written under this
hypothesis; inferred outputs are not relabelled historically attested fields.
Source spelling/table conflicts and larger examples' prerequisite calls remain.

Each numeric group equals one nonempty opcode prefix plus a canonical decimal
result. Ten digit codewords are distinct and have one positive common widthk.
Thirteen opcode strings are distinct and nonempty, shared globally. Prefix
overlap is permitted and disambiguated by the fixed programme grammar. CHECK
has no numeral and occupies one full group. There is no alternative format.

For each fixed eligible paragraph, enumerate every input100..9999 and width
1..min(len(group)-1) over the22numeric groups. Larger widths cannot leave a
nonempty opcode even for one decimal digit. Given input and width, all suffix
lengths are determined; there is exactly one prospective opcode/digit split.
Check nonempty prefixes, repeated opcode identity, distinct opcodes, consistent
digit assignments and distinct digits in that order. Record every n/k case
and its first contradiction position, or its complete exact candidate. The
first contradiction is sufficient for exclusion; it is not a claim that later
fields were independently tested. A zero width bound is an exhaustive length
contradiction. Missing digits have all injective completions over literal[a-z]
of the same width, counted exactly; none is preferred or claimed recovered.

Retain all single-paragraph candidates in all three editions even when a panel
lacks the minimum three-leaf capacity. Join all triples on three distinct
physical leaves with three different inputs. Require identical width and
opcode map, mutually consistent injective digit maps. Do not collapse different
numeric readings. All triples and unused-code completions remain alternatives.
The complete eligible intake itself supplies all further unchanged-key uses;
there is no additional independently held target set. All source IDs and full
target groups are published after registration.

The unchanged GDT928 loader covers the six guarded GDT915 caches/179admitted
selectors. Preflight found14complete23-group frames:6ZL3b,8IT2a,0RF1b.
One ZL3b frame onleaf114 and three IT2a frames onleaves24,103,114 meet the fixed
literal/definite-seam rule. Other23-group frames remain transcription/segmentation
unknown for this test; all other complete lengths remain outside this fixed
format, not evidence against arithmetic. No newly opened data or image.

The target run has a total120s wall cap including intake, all singles and joins.
At a cap preserve completed n/k cases and mark remaining scope COMPUTATION_UNKNOWN.
No automatic runtime, decoder or programme repair. The inclusive work checkpoint
13:00UTC includes source preparation, registration, validation and publication.

All material has previous project exposure. The old latitude preflight exposed
aggregate length histograms, and predecessors exposed their old examples. No
independent meaning confirmation, no whole-project search null and no significance
claim. A conditional exact calculation reading would remain a hypothesis about
intended content. No confirmed numeral, operator or plant name follows solely
from model consistency. ZL3b/IT2a/RF1b are alternative transcriptions of one
manuscript. f84,f84r,f116v and actual reserves stay closed.
