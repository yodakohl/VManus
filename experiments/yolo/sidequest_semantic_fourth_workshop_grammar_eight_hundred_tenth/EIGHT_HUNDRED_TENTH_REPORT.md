# Sidequest Pass 810: fourth workshop grammar

The complete 173-card / 381-event / 116-statement prose edition is rebuilt with
the new core31. No recurrent strip remains.

The 39 component entries now divide into:

- 31 productive cores;
- 1 bound variant (`AN`);
- 4 owner-local singletons (`CFH`, `S`, `DA`, `LD`);
- 3 memorized whole commands (`OS`, `RESUME_CARD`, `TALAM`).

Core31 touches 170 cards / 377 events and fully composes 165 cards / 372 events.
The non-core remainder is therefore exactly eight cards / nine events. The
teaching manual needs sixteen short rules, while 70 prediction-input rows
deduplicate to 69 unique unseen forms outside the observed text.

This is now a concrete mixed system of technical abbreviations and learned
whole cards: most meanings come from reusable components, but exact surface
packing and the tiny residue are still memorized from the workshop exemplar.

Next, attack the four singleton components in this order: `CFH` in `cfhy`, `S`
in `ches`, `DA` in `daiiin`, and `LD` in `qokylddy`. Compare them to existing
core roles and promote only if the full local sentence remains simpler.
