# GDT873 — one retrospective matched-position comparison

Decision budget:2026-09-08 07:55–08:25UTC, including implementation, validation
and publication. One count pass; no automatic control repair or broader model.
The motivating GDT872 result is already known. This is retrospective discovery
follow-up, not an unseen confirmation or a semantic translation test.

## Unknown and consequences

GDT872 has distal CH-side response composition41/51forward versus36/66backward
in raw ZL, afterboth CH and SH anchors. Its strict reading diagnostic does not
repeat that direction. GDT751 shows that another apparent prefix-position effect
was reproduced by matched controls; it does not test these four anchors.
GDT807 tests other targets and unordered paragraph ecology. This comparison asks
whether comparable positions inside the very same paragraphs reproduce this
particular directional density profile. If so, stop anchor-specific extrapolation.
A distributed residual could motivate a later qualified question, but selects
no state/operator meaning. Formula and fine-position effects may remain.

## Fixed data and statistic

Reuse only the SHA-bound GDT872 guarded runtime ATLAS and LINE_RECORDS projections.
Regenerate them with its registered runner when absent. No new source acquisition,
page, image, form inventory or widened lag. Four anchors/18responses stay fixed.
Use rawpositions, exactwhole eligibility, traversed-gap rules, same-page bounds,
and strict source paragraph IDs from872. Here only within-paragraph lags3–8 count.
Primary ZL and all-traversed-lines raw-identical diagnostic views remain separate.

A center must be an exact raw one-fragment whole and have at least one eligible
offset in BOTH directions. For each center retain exact forward/backward eligible
lag bitmasks. Each opportunity is+1 for a CH-side response,-1 for SH-side,0otherwise.
The endpoint D is forward mean minus backward mean. Report its four components
F_CH,F_SH,B_CH,B_SH separately; D is response density, not CHshare among hits.
This new endpoint must not be presented as GDT872's original primary statistic.

Controls are ALL exactraw nonanchor centers in the same sourceparagraph, with:
- same exclusive rawline-position bin: FIRST2_ONLY,LAST2_ONLY,BOTH_EDGE,MIDDLE;
- same paragraph-line bin: FIRST_ONLY,LAST_ONLY,BOTH_EDGE,INTERIOR;
- exactly equal forward AND backward eligiblelag masks.

First/last overlap is its own bin; shortlines get no arbitrary precedence.
Exclude allfour anchor surfaces from controlcenters. Other response words may be
centers, because the center itself is never a response at lag3–8. Keep every raw
group in positional counting. Pool membership never uses the response outcome.

For each anchor i withcontrols, C_i is the equal-center mean over its complete
controlpool. Report D_i-C_i and its equal-anchor mean, plus individual and physical-
folio summaries. Reused controlcenters and overlapping windows are not independent
replications. Compare D on the SAME matched anchors with C; also show all eligible
anchors before matching. Do not compare allanchors against a selected matchedpool.

Always publish coverage and all descriptive values. Operational adequacy requires
at least25matched anchors,10physicalfolios, and50percent of direction-eligible
anchors, separately perview. These are declared coverage checks, not power or
significance gates. If insufficient, stop withlimited coverage; do not coarsen bins
or masks, expand lags/pairs, change readingcriteria, or start a classifier.
No p-values, confidence claims, semantic winner or mappedGDT388 relation score.

## Validation and outputs

Use a fixed synthetic case with analytically counted matching centers and rawlag
responses. Independently audit full matching pools, all offset masks, component
counts, equal-anchor weighting, physicalleaf grouping and the coverage decision.
Publish compact anchor/control-cell profiles, summary, source receipt and validator.
