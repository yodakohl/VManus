# GDT348 source audit

GDT348 introduces no new corpus and no new Voynich score. Its only control
sources are the already published, frozen GDT172 and GDT173 observation/oracle
layers. System A is an injective lexical-ID control, System B an explicit
factorial distributed control, and B2 an irregular human-grown distributed
table. The systems share a medieval medical graphematic source schedule,
variable records/lines, registers, hands, and unchanged literal escape.

The sealed oracle fields are appropriate here because the scientific question
is instrument capacity: whether the exact authored coordinate changes can
carry the already frozen GDT347 potentials when the blind surface parser did
not recover them. Oracle use makes this an upper-bound calibration, not a
blind replication.

No f84 source, row, image, transcription, metadata, or payload is an input.
