# GDT633 artifacts

`OUTER_LITERAL_E_RUN_OCCURRENCES.tsv` and
`CONCRETE_TRANSLATIONS_V5.tsv` are the complete 257-fused and
268-expression working editions. `OUTER_LITERAL_E_RUN_LATTICE.tsv`,
`OUTER_E_FIXED_BODY_LADDERS.tsv`, and `EE_RIVAL_CONTEXTS.tsv` isolate the
outer `e/ee` slot. `INNER_CTH_E_RUN_LADDERS.tsv`,
`INNER_E_PAGE_COEXISTENCE.tsv`, and `INNER_E_SHARED_CONTEXTS.tsv` keep the
inner `y/ey/eey` form ladder separate.

`O_HEAD_DISTRIBUTION.tsv`, `O_CTH_SAME_REMAINDER_CONTEXTS.tsv`, and
`O_CTH_SHARED_CONTEXTS.tsv` carry the `o+CTH = preparation/batch` comparison.
`E_BINDING_VS_HEATING.tsv` contains occurrence-normalized, rather than raw
contact-row, heat/cold counts. `CONTROLLED_INTERFIX_TYPE_PAIRS.tsv` contains
the 13 E, ten O, and five E-versus-O fixed-body contrasts.

`SAME_PAGE_MICROEDITIONS.tsv`, `ATOMIC_MEANING_CANDIDATES.tsv`,
`CANDIDATE_SCOREBOARD.tsv`, `PREDICTION_DECK.tsv`, and
`WORKING_DICTIONARY_V10.tsv` are the compact human-readable outputs.
`LITERAL_E_RUN_BACKGROUND.tsv` records the manuscript-wide literal e-run
comparison on the same guarded page panel. `RESULT.json` and
`VALIDATION.json` bind the build and independent replay.

No image or newly released page is included. f1r is excluded; f84 and f84r
remain forbidden.
