# GDT752 preregistration — q/base outer-microfield role audit

## Frozen decks

- q deck: all 44 GDT751 reader-exact direct q/base contacts, twelve pair types;
- control deck: all 42 direct contacts materialized from GDT751's fixed 160
  non-q one-initial-character pairs, without semantic or outcome matching;
- renderer review deck: all thirteen `qokeey/okeey` contacts and the fixed ten
  GDT751 `okeey` carrier cards;
- no new page, image, transcription, OCR, f84, or f84r.

## Frozen construction

Each pair member receives only the outward half-field away from its partner.
The partner is a boundary and never an anchor. Radius is five. Line edges,
strict initial heads, known left-prior/right-current close fields, and repeated
pair surfaces stop the span. Only clean reader-exact W2/W3 whole cards provide
axes. GDT744 channel precedence remains unchanged.

## Frozen decisions

- exact support: prefix side has quality/stage, base side has PREPARATION, the
  reverse is absent, and both outward fields are complete;
- exact reverse: base side has quality/stage, prefix side has PREPARATION, the
  hypothesized direction is absent, and both fields are complete;
- evidence in both directions is ambiguous, not support;
- broad carrier results are sensitivity only;
- radius-five-censored patterns are leads only;
- a GDT751 `okeey` preparation card stays spoken only under exact complete
  support at that same occurrence.

## Claim boundary

Pair geometry may alter occurrence-local working cards. It grants zero value
to EVA q or any substring and identifies no literal preparation, lexeme,
ingredient, language, plaintext, or unseen form. Structural field tags remain
separate from English or German word translations.
