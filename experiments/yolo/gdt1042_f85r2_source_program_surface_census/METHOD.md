# GDT1042 — complete f85r2 spatial surface inventory

Registered 2026-09-26 before the current target query. Exploratory description,
not a meaning test or a reserved confirmation. No new decoder or word gloss.

## Decision note and predecessor

The user requests a renewed multi-feature Laufenberg/housebook comparison and
then a text-pattern check. Prior image-program comparison survives in
VOYNICH_ACTIVE_STATE.md, section "f85r2 and the Laufenberg medical-regimen image
program". Its original report, F85B001 and IL019 report files are absent from
the worktree; their retained state/ledger claims are identified as such.
GDT884's available primary analytically excludes its fixed introductory text
under its exact STA morphism. None is rerun or overturned. The old source-role
root bridge and paragraph-order scoring stay closed.

What remains useful here is a complete, meaning-neutral inventory showing
whether the four spatial blocks actually share whole written forms, repeated
local strings and minimal surface variations. This can qualify or remove a
proposed text-family link without assigning a season, element, humor or language.
The external source comparison is being renewed separately from primary images,
including conflicting Herbst/Winter attribution. It cannot choose favorable
text pairs or rotate blocks after seeing a score.

Smallest adequate work: query this already exposed page only, retain every
native group, produce exhaustive tables and independently replay counts. No
new images or pages, morphology learner, segmentation, significance or search
over source meanings. Budget: 50 minutes including preparation, run, validation
and publication; reassess without automatic expansion if exceeded.

## Data contract

Use source_separator_transcription.tsv ONLY through vmanus-exp query-tsv,
selector page, allow f85r2. Explicit columns: source_group_id, edition, locus,
page, source_group_index, source_group_count, paragraph_start, paragraph_end,
left_separator, right_separator, ivtff_group_raw. The source is mixed and the
guard must reject f84 prefixes before other fields are materialized.
Query all f85r2 groups, retain any outside the four blocks separately, not
silently discarded. Text admission is GDT631 PAGE_ALLOWLIST. No target image
access is added. f84/f84r remain closed; f116v not admitted.

Spatial blocks are fixed by the prior catalogue: N lines2–6, E7–11, S12–17,
W18–23. No ordinal reading order across blocks. ZL3b, IT2a and RF1b are alternate
readings, analyzed separately, not independent manuscripts. Other editions in
the guarded extract are retained but not primary counts. Full source group
strings and uncertain separators are preserved without splitting or cleaning.

## Frozen descriptive outputs

1. Every line and group with block, raw text and within-line position;
   counts of groups and distinct complete forms per reader/block.
2. All exact forms occurring at least twice in that reader's four blocks,
   listing every location; report within-block and cross-block recurrence.
3. For every unordered pair of blocks, list the complete intersection of
   exact form sets. No winner, significance or semantic selection.
4. All repeated contiguous native-group bigrams and trigrams within a line;
   boundaries include recorded separator flags. Same surface sequence across
   different separator flags is explicitly uncertain, not a certified phrase.
5. All pairs of clean literal ASCII-letter whole forms at Levenshtein distance
   exactly1, no transposition operation, among the four blocks; exclude marked
   entities/uncertainty from this auxiliary family table only. List every
   occurrence, block membership and first/interior/last position. These are
   surface neighbors, not inferred morphemes or semantic contrasts.
6. Complete first/last group and separator inventory for every line, so
   position patterns are visible without selecting a favorable ending.

## Interpretation and dependencies

If exact shared forms or constructions exist, retain their full inventory as
candidate links for a future independent meaning contrast. If none exist,
there is no exact literal construction link at this scope. Neither outcome
discriminates seasons from life ages or humors: those meanings remain unbound.
Single-edit edges alone do not demonstrate a productive paradigm. Repeated
forms have no rarity claim without an appropriate whole-search control.
The predecessor's spatial ownership, native grouping and edition differences
are assumptions; inherited seasonal glosses are not inputs. All this page is
previously exposed development material, with zero independent confirmation
capacity in this experiment. Historical f84 exposure incidents do not confer
access and no sealed data enters the run. Zero confirmed translated words.
