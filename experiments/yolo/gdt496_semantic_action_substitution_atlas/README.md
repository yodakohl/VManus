# GDT496 — semantic action-substitution atlas

Status: `EIGHTEEN_DIRECT_AND_NINE_CONTEXT_SAFE_DEFAULTS__ALL_242_OBSERVED_REMAINDERS_MATCH`

GDT496 reads the 27 GDT495 Tier-A cards horizontally. It removes only the
variable action slot and compares the remaining portable and owner-local
meaning trace against all 95 observed same-register head cells and all 242
underlying events.

All remainders match. Eighteen target phrases remain direct and
self-contained; nine active-argument phrases receive the simpler editorial
default "das zuvor Genannte" because their old family events inherit multiple
different argument roots.

Start with `REPORT.md` and the complete readable atlas in `artifacts/`.
