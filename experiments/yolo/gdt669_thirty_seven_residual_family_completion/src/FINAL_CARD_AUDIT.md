# GDT669 final card audit

## Verdict

The synthesized deck has the exact 37-surface first-frontier order and covers
165 released-panel positions. The stem sheet has 56 distinct roles. Every
declared role sequence reconstructs its ZL surface exactly and in order; both
learned aliases use a sole exact-surface namespace.

The requested architecture is exact:

- 33 cards use only the inherited 54 roles;
- `oytor` uses `OY_PREP_BASE` and `okytaiin` uses `OKY_HOT_PREP_BASE`;
- `lkchdal` and `eeckhy` are the two learned reader-alias wholes.

The audit found and corrected one wording-direction defect in `tcheodal`. No
composition or scope correction was otherwise required.

## Productive family spine

The high-frequency roots remain decomposed rather than promoted to redundant
whole stems:

- `shear`, `shek`, and `shedaiin` use `SH_MOIST+E_MIDDLE+...`; naked `sh`
  remains the free moistening/soaking operation.
- `dalar` and `dals` preserve `D_MEASURE+AL_RAW_I+...`, agreeing with the
  larger `dal-` measure/raw-material family.
- naked `ro` is `R_ROOT+O_PREP`, the same narrow root-preparation spine visible
  in the released `roaiin`, `rodaiin`, `rodam`, `roiin`, `roiiin`, `rol...`,
  `rom`, and `ror...` families.
- `oeees` is `O_PREP+EEE_LONG_OR_FINAL+S_TERM_SPECIES`, parallel to released
  `oeeeody` and `oeeeos`; `oeee` is not made into an opaque new word.

The endings remain consistent: `-d` closes, `-y` supplies base/close,
`-dy` finishes, terminal `-s` supplies charge/species, and the quantity/form
tails retain their distinct values.

## OY and OKY extensions

The extensions repair otherwise illegal internal `y`, rather than weakening
the global Y scopes.

- The guarded panel has naked `oy` 12 times, plus `oychey` and the new
  `oytor`. `OY_PREP_BASE+T_COLD+OR_PORTION` preserves the shared “Ansatz in
  Grundform” core.
- Naked `oky` occurs 89 times and the new `okytaiin` once.
  `OKY_HOT_PREP_BASE+T_COLD+AIIN_III` preserves “heißer Ansatz in Grundform.”
  The extension remains LOW because only one longer compound is presently
  licensed.

Neither extension licenses an internal `Y_REFERENCE` or
`Y_START_OR_CLOSE` elsewhere.

## Reader-alias evidence

All 37 target loci were compared across ZL, IT2a, and RF1b. Exactly two ZL
targets have the same differently spelled target card in both alternate
readers:

- f51r.7: ZL `eeckhy`; IT2a and RF1b `chckhy`. The inherited consensus meaning
  is “Arzneikompositum: trocken, Gradanfang.” The ZL card correctly stays
  `LEARNED_EECKHY_WHOLE`; it does not pretend that ZL contains initial `ch`.
- f105v.13: ZL `lkchdal`; IT2a and RF1b `lkeedal`. Its practical default may
  inherit the consensus `l+k+ee+d+al` reading, but ZL correctly stays
  `LEARNED_LKCHDAL_WHOLE`; it does not pretend that ZL contains `ee`.

At f82r.32 both alternate readers split `shedaiin` as `she daiin`. That split
concatenates to the same ZL spelling and exactly confirms
`SH_MOIST+E_MIDDLE+D_MEASURE+AIIN_III`; it is not an alias. Other differences
either preserve the target or lack two-reader agreement.

## Initial `ol-` correction

`olkair` correctly follows the inherited GDT664/GDT668 initial-`ol*` rule:

`O_PREP+L_WOOD+K_HOT+AIR_FRACTION_II`

Its default, “zweite erhitzte Holzdrogenfraktion im Ansatz,” preserves that
wood/preparation reading. It is not misclassified as initial `OL_MATERIAL`.

## Applied wording correction

The draft `tcheodal` prose started with measuring and then moved backward to
cooling and drying. Its composition visibly orders cold → dry → middle grade →
preparation → measure/raw material. The final wording row is therefore:

```tsv
tcheodal	abkühlen, bis zur Mittelstufe trocknen, ansetzen und Rohdroge I abmessen	T_COLD+CH_DRY+E_MIDDLE+O_PREP+D_MEASURE+AL_RAW_I	abgemessener kalt getrockneter Rohstoff-I-Ansatz	MEASURE
```

This applied editorial repair does not change the role sequence.
