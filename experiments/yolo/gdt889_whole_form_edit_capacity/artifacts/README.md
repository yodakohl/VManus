RESULT.json contains all fixed words, exact forced message classes and a shared
received-string spanning forest. VALIDATION.json independently checks the classes
by all-pairs edit distance and BFS. No semantic labels or new manuscript input.
