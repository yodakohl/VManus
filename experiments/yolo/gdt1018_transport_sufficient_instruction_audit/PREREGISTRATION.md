# Registered finite test

Freeze DECISION/METHOD, both checkers, all source hashes and the complete saved
population before executing the new predicate on manuscript candidates.
The known two FULL15 counterbranches disclosed in RAW480 are prior exposure.
No new search, alias refitting, source repair, class removal or exception is
allowed. Test all declared witnesses/settings/member lifts; preserve failed
fixed readings and old existential positives as different claims.

Outcomes: SUFFICIENT if every locally eligible choice succeeds; INSUFFICIENT
if a reached fixed command/assertion/goal fails or an open command has no
eligible load; INVALID if the bound legacy parse/reference contract is broken;
UNKNOWN only for bounded resource failure. A surviving saved dictionary proves
existence under the new law. Absence among saved dictionaries leaves the full
extension space OPEN and supplies no original dictionary exclusion.

Each row retains original setting, reader, source whole-word assignments/parse,
exact checked prefix counterexample or constructive complete consequences,
old existential status and independent confirmation capacity0. Both alternate
readers belong to one physical f50leaf and all physical leaves were exposed.
There is no held-leaf confirmation or whole-search null in this experiment.
f84 and f84r explicitly sealed; f116v unadmitted; all reserves closed.

Public registration required before target predicate run. Inclusive checkpoint
04:10UTC; no automatic follow-up solver. Implementation fixtures may run before
publication only on synthetic texts, with target census still unopened.
