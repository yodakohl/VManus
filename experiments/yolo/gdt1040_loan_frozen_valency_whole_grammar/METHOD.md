# GDT1040 method: complete fixed valency before new glosses

The exact contract is src/SPEC.json and the unchanged IDEA528 proposal. This
test asks a necessary question before constructing47 new denotations: can every
whole group receive one globally fixed category and participate in the six
declared grammar productions? A positive type assignment is not a translation.

Seven known-to-this-hypothesis whole forms retain their guessed values. Three
are nouns, three nullary state/modal assertions, and qokeedy is a unary event
predicate. Every other exact form may be NOUN, unary predicate or binary
predicate. No new nullary assertions are allowed. All predicate arguments must
be following nouns; all nouns must be consumed. Text is a nonempty sequence of
clauses. Lines carry no additional sentence boundaries. Each complete source
unit starts and ends with no unpaid argument slots.

The capacity test deliberately relaxes argument sorts and individual identities
to NOUN. A satisfiable assignment still needs denotations, typed referents and
initial custody. An unsatisfiable result under this broader necessary condition
rules out the more constrained offered reading without inventing any meaning.

Primary implementation enumerates all3^r category assignments for r recurrent
unknown forms within each unit. With those assignments fixed, all singleton
forms can be handled by a dynamic program over1/2/3-group clause partitions.
A singleton's chosen category has no other occurrence to constrain; each path
therefore determines one exact global category assignment. Every complete path
and every recurrent assignment is counted. The declared million-assignment cap
would produce UNKNOWN_CAP, never an exclusion.

An independently written validator uses one category variable per exact form
and a remaining-argument state at every position. A clause head opens0/1/2
slots; each next noun consumes one. It checks every complete unit, the joint
shared-form problem, and the primary assignment/prefix tables. It does not
import or execute the primary algorithm. Both are computational checks, not
independent manuscript witnesses or meaning judgments.

All models: original72-group report projection, complete72-group diplomatic ZL
unit, complete71-group IT unit. Source extraction copies exact native groups
and IDs from the already admitted cached selected unit only. RF has no whole
record with its own paragraph flags. Raw uncertainties remain exact forms;
no source normalization, aliases or group splitting. The projection is retained
separately and never silently substituted for either diplomatic branch.

The only partial diagnostic is the longest contiguous prefix consisting of
complete clauses. There are no skipped tokens, resets at errors, new production
or repaired reading. Ties choose the lexicographically smallest recurrent
category assignment (-1,1,2 in sorted form order), then smallest clause-length
sequence. Every following group stays in ALL_GROUPS as UNPARSED. This diagnostic
does not turn an unsuccessful whole account into a successful local reading.

Root's exact qoky conflict forecast is in DECISION and predates the algorithm.
The source is historically exposed; no blind selection or significance claim.
The separate producer's legacy sealed-row incident is documented in the scope
record. None of its payload entered this source, code or forecast; no claim
that all team activity was sealed-data-free is made. No reserve use is licensed.

Run src/run.py --controls before public freeze using invented symbols only.
Target run and independent validator require PREREG_LOCK and a public commit
receipt. A result closes only this grammar or preserves syntax capacity; it
does not select the seven old meanings or reopen GDT979's different code.
