# GDT523 compact working book

## Live stack

`exact event > known surface/role > GDT522 finite candidate > light
atom-conditioned path-null edit`

Selected addition:

```text
pure left visible insertion relative to selected renderer alias
global null odds > 0 and first-base-atom null odds > 0
feature = support/(support+2) * visible insertion width
final = GDT522 - 0.25 * feature
```

## Result

- old four-fold: 1,096 rank one, 1,327 top two, 1,387 top three, 1,418 top
  five, rank sum 2,111, deepest 22;
- current 159 unchanged at 142/154/158/158 and rank sum187;
- no selected top-one change, 17 current errors remain;
- `qopchy` rank4→3 and `qopchey` rank9→8 in the rotating old deck.

## q split

- global left `q→NULL`: 75 null / 9 other pair relations;
- before `OK`: 33/1; before `OL`:11/0; before `OT`:21/1;
- before `O`:4/7, therefore blocked;
- before `E`:1/0, therefore mechanically allowed but thin.

`qef` truth path: `qe=>e~E`, visible q-null feature0.974 under the selected
edit-only mode. It remains benchmark rank2. Combined weight0.85 makes it rank1
but degrades old rank one1,096→1,089 and rank sum2,113→2,123; do not promote
that setting.

## Next route

Compose two compatible GDT522 local edit routes while keeping each individual
signature visible. Start with `kchody`, `kechody`, `keeol`, `ld`, `okedals` and
the closure/split families. Require reusable two-step operations, not target
whole-form cards. Keep the current pages closed.
