# GDT379 F1 orthogonal behavior

This experiment freezes the exposed GDT378 `CMP_FUNCTION_04` identity lead as
anonymous candidate `F1` and tests only previously unused behavioral
consequences. See `METHOD.md` for the prospective contract. All semantic labels
remain unassigned and f84 is excluded.
