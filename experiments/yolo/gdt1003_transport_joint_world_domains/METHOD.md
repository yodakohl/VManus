# Full joint world constraints and exhaustive shared-value queries

Use exactlyGDT1002 P0-3 and P2-4, the only overlapping systems not syntactically
excluded. Source groups, flags, provenance, all47old values,17productions and
four retained993variants are unchanged. All complete paragraphs are owned.
One existing terminal value per new whole spelling is shared across each pair;
synonyms remain allowed. No morphological, glyph, source or reference repair.
The words queried are the complete shared-new inventories: okaiin for0+3;
dcheor,s,shodol for2+4. Every terminal in the fixed grammar/lexicon union is
queried, including values incompatible with obvious local syntax.

A coherent solution jointly chooses one common dictionary, both whole parses,
one of the four retained variants common to both, and physically complete paths
satisfying the unchanged993reference, availability, capacity, assertions, final
voyage and whole-goal semantics. No syntax sample is later excused or silently
retargeted. All cargoes explicitly named anywhere in a paragraph belong to its
initial set; a future name cannot be erased because it is inconvenient at the
end. Hypothetical danger statements do not become actual state changes.

The primary Z3 encoding uses exact word-position clause coverage, Boolean bank
variables and a source-order first-named-cargo recurrence. The independent cvc5
encoding uses boundary flow, integer banks, an explicit boat position and a
pending-STAY record. Both preserve the full mandatory-clause scope. Different
source reader flags remain uncertain as in997; raw alternatives/entities are
not normalized. No freshly blind or independent meaning test is asserted.

Only the following proven bounded reductions are used. All paragraphs have
30–36groups; mandatory scope costs26. A PAIR needs two distinct named cargoes,
6extra groups and at least3voyages of at least2groups to get both cargoes and
agent from L to R. A coherent PAIR paragraph therefore needs at least38groups.
COPY requires PAIR. Thus both are impossible here under the full semantics,
without changing the grammar. The boat stays with the agent under every original
transition. The primary omits a pending-STAY assertion after a trip transporting
a different cargo because that trip leaves the saved cargo's location unchanged;
the independent encoding retains it. STAY's immediate nonempty-load and bank
assertions, all OUT/RETURN/OPPOSITE directions, EXCLUDING's alternative allowed
loads, FIRST, OTHER, RESULT, FINAL and final-goal constraints remain active.

The retained variants always have exclude=EXCLUDING, first=FIRST and copy=FIRST.
other=OTHER/FIRST and there=GOAL/CURRENT are shared binary choices. Without PAIR,
OTHER mode cannot resolve OTHER_CARGO; FIRST mode still needs a prior explicit
cargo. Every reference uses the original source-order introduction rule. A later
explicit cargo belongs to the initial material set but does not retroactively
become the first previously named referent.

## Fixed query population and limits

For each system first ask existence. Then, irrespective of that answer, query
every(shared-word,terminal-value)pair. Each SAT answer must carry its complete
alias map, full parses, common variant and old-executor coherent replay. Each
UNSAT excludes that marginal value only in this fixed model. A solver limit
is UNKNOWN. These are marginal value domains; arbitrary combinations of values
from different successful witnesses are not asserted jointly compatible.

Separately enumerate up to64distinct complete tuples of all shared-new values
per system. Block the shared tuple across all remaining maps/parses/variants
before the next query. Exhaustion establishes all possible shared tuples only
within this model; reaching64or a query limit leaves alternatives unresolved.
Full dictionaries/parse ambiguities are not exhaustively counted, even if a
shared tuple is unique. For each tuple witness replay allfourvariants to expose
any surviving reference ambiguity. Keep every failed variant as well as success.

Primary per-query limit5seconds, two parallel systems; independent per-query
limit5seconds, two parallel system batches. A system has a600second total worker
budget, with unstarted query rows filled as UNKNOWN_BATCH_LIMIT. A tuple loop
stops at its first limit; marginal queries remain individually accounted for.
No automatic extension beyond the20:09UTC inclusive checkpoint. Independent
limits remain explicit; a negative unverified by the other finite encoding is
not presented as independently settled.

## Validation and interpretation

Before target execution,20complete synthetic programs compare both new
encodings with the unchanged993executor. They include legal crossings, wrong-bank
returns, unloaded STAY, future named cargo, loaded returns, references, repeated
crossings, FINAL violations and source/current-bank distinctions. Complete
shared-code grammar behavior was separately checked in1002; no older output is
called a new blind control.

The validator checks the entire fixed query population, source and code hashes,
all word/production ownership, every saved SAT dictionary and its selected
variant using the independent993bit replayer plus994binding check. For all
primary negative probes it runs the separate cvc5 query. Any contrary SAT/UNSAT
is an error. Shared-tuple exhaustion also gets an independent query blocking
every saved tuple. Implementation checks do not establish source identity.

A positive gives a concrete, complete shared hypothetical reading and its
remaining value domains, suitable for a later component-semantic proposal.
A verified exclusion closes this exact pair or value. Neither yields a confirmed
translation. The word meanings, content grammar, initial inventory and genre are
assumptions; name permutations, synonyms, variant equivalence and missing word
formation remain. All four leaves and83 were historically exposed; one member
of each selected pair has uncertain source flags. Independent meaning capacity0;
no reserves, f84/f84r, f116v, new image or contact. No whole-search null or
significance claim. GDT993/997positives and1002five-way closure remain unchanged.
