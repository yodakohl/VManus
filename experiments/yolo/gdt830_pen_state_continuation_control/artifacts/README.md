# Artifacts

FEATURES.tsv will contain all fixed-window measurements and explicit quality
failures. QUERY_SCORES.tsv retains the fixed candidate comparisons. MODEL.json
records the calibration-only nuisance regression. RESULT.json and VALIDATION.json
report the control result and the actual verification coverage. No native
images, decrypted text or disputed-block scores are produced here.
