# GDT898 — conditional complete-calendar code is impossible

The fixed CHD February reading has28 distinct mnemonic strings. The complete
f69v inscriptions14 and18 each read `okeod`, as one raw group, in ZL3b, IT2a
and RF1b. An injective prefix-free nonempty code on source characters induces
an injective map on entire strings: compare first codewords, use prefix freedom
to force equality, cancel and induct. Therefore no bijection of the28 complete
source cells to the28 inscriptions can satisfy this model. This excludes all56
cyclic start/direction choices without requiring a certified native order.

Independent extraction from the frozen source HTML and selector-guarded replay
of all100 raw group rows reproduce both premises. Independent proof validation
passes. No decoder search, fitted ordering, new Voynich page or held data was used.

This is conditional on the edited mnemonic fields of Copenhagen GKS79 f7v,
not an exact diplomatic transcription. Two observers confirm February and its28
rows; faint letters/abbreviations and extra red ink after day9 remain unresolved.
The projection retains lowercase ASCII letters from the mnemonic field, removing
editorial stars, hyphens, stops and whitespace; it does not expand modern feast
names. Raw fields and source hashes are preserved in SOURCE_INPUT.json.

The native target observer confirms a compatible complete radial layout, but
locus order is catalogue-assisted and not independently certified glyph by glyph.
Root saw that proposed order before making its own receipt. Neither limitation
is concealed; the negative proof needs only the complete membership and duplicate
inscriptions. The three editions remain alternative readings of one manuscript.

The collision was noticed after receipt exposure, before any solver work. The
local initial protocol existed beforehand; firstpublic publication is with this
result. This is a deductive audit, not a prospectively blinded statistical result.
Only the fixed source/complete-cell/injective-prefix-code conjunction is excluded.
Calendar content, other historical versions and non-uniquely-decodable writing
systems are not generally refuted. No meaning is established; no automatic repair.

Reproduce the mathematical result and artifact agreement with the manifest's
run and validate commands. For independent primary-source replay, supply the
unchanged CHD HTML (SHA256 in SOURCE_INPUT.json):

```bash
python3 experiments/yolo/gdt898_cisiojanus_complete_cycle_compositional_code/src/validate_injective_cycle.py --source-html CHD_HTML --artifacts experiments/yolo/gdt898_cisiojanus_complete_cycle_compositional_code/artifacts --output INDEPENDENT_REPLAY.json
```

Primary source: https://www.manuscripts.org.uk/chd.dk/cals/gks79cal.html;
original: https://img.kb.dk/ha/manus/gks79/gks790014v.jpg.
