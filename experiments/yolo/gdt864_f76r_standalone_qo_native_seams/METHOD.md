# GDT864 — two fixed f76r qo seams

Local source-fidelity audit, not a new model test: f76r.21 source index4
qo|olain and f76r.30 index5 qo|qokey on admitted Yale1006210. GDT863 outcomes
and full contexts are already exposed. SOURCE_LINES copies exactly the six
published GDT863 occurrence records before new native seam inspection; no
expanded GDT851 source access. Exact line/index/follower controls selection.
Previous whole-image orientation is disclosed, not held out.

Native A then sealed A before B, full original only. Targets T21/T30 each
record localized boolean, seam SPACE_LIKE/INTERNAL_LIKE/UNCERTAIN,
manual_alignment_note, neighbor_whole_group_seams_note,
within_group_gaps_note and note. Compare target gap with immediate left/right
neighboring whole-group seams and gaps within adjacent groups. Whole-image
line counting and published contexts are allowed; uncertain alignment must
be unlocalized or UNCERTAIN. Do not infer exact glyph identity from matching
spelling or silently change source indices. Root supplies both viewer records,
overall note and excluded_ink_target_not_inspected=true; A_SEAL records byte
SHA256 and sealed_at_utc. B may know target contexts/outcomes but not A's rating.
Both inspect one photograph, not independent manuscript evidence. Seal checks
unchanged record bytes, not independently verified collection chronology.

TGC001 excluded f76r.5|C004 is not either target; no glyph inspection or
classification there. No crops, image enhancements, OCR, new admission or
image acquisition. Implementation agent does not view the image.

Per target: both localized and SPACE_LIKE => BOTH_VIEWERS_SPACE_LIKE;
both localized and INTERNAL_LIKE => BOTH_VIEWERS_INTERNAL_LIKE; all other
combinations => UNRESOLVED_OR_DISAGREEMENT. Both target descriptors SPACE_LIKE
=> BOTH_FIXED_SEAMS_LOCAL_SPACING_SUPPORTED; otherwise
LOCAL_SPACING_NOT_CONFIRMED_FOR_BOTH. Retain every result with no target swap,
no follow-up relaxation, and no reinterpretation of uncertainty as absence.
Support concerns local spacing only, not authorial word boundaries, a function
word, prefix meaning, semantics, stroke order or false starts generally.

Independent validator checks exact published source-record parity, image
bytes/hash/dimensions against existing provenance, native schema and A seal,
and independently recomputes descriptive decisions. Software does not verify
manual alignment or native vision. Controls cover agreement/internal/uncertain
and localization failure. Frozen result --check byte replay required.

Budget08:43:59–08:53:59UTC on2026-09-06 including root publication. Freeze
METHOD/SPEC/code/SOURCE_LINES before new target native inspection. Root owns
observations/globals/git. No broad corpus, model or image extension follows.
