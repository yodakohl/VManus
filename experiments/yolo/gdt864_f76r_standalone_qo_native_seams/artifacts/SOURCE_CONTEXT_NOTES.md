# Exposed context and source differences

The exact target pairs agree at the fixed source indices in all three readings,
as retained in the six complete GDT863 occurrence records in SOURCE_LINES.json.
The surrounding text is not forced into agreement:

- f76r.21 begins ZL `qokch[s:?]dy`, RF `qokch@206;dy`, IT `qokchsdy`.
  IT later has one `otshedy` group where ZL separates `ot` / `shedy` and RF
  separates `ot` / `{ch'}edy`. Those downstream splits do not change the fixed
  qo source index4 or immediately following olain atindex5.
- f76r.30 has ZL/IT `shcthy` versus RF `shcth@222;` at source index3.
  The fixed qo and qokey remain source indices5 and6. Full contexts and raw
  entities are preserved; no entity equivalence or new transcription is made.

These are observations on already published source records, not an expanded
corpus search. The native match must use the surrounding visible arrangement
with uncertainty rather than infer physical alignment from index agreement.
