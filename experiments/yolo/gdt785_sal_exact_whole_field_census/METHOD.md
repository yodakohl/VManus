# GDT785 method — exact `sal` whole-field census

## Question

Across all 33 reader-exact complete-word occurrences of `sal` in the inherited
179-page cache, what single concrete default best survives position, immediate
state/value frames, amount controls, same-head controls, exact superforms and
the already published `chorcholsal` prediction?

## Inputs and boundary

The token, cross-reader and line sources are reconstructed only through
GDT782's guarded `./vmanus-exp query-tsv` loader and its explicit 179-page
allow-list. `f84*` and `f84r*` remain forbidden. GDT736 supplies the formal H2
record-role comparison; GDT759/GDT760 the quantity controls; GDT762 the clean
state-contact and matched-frequency controls; GDT777 the fused/split control;
GDT784 the long-whole prediction. GDT735/GDT784 historical rows are
architecture comparators only.

No new page, image, OCR or transcription is opened. `sal` is treated as a
complete written whole. The EVA characters `s`, `a`, and `l` receive no sound,
Latin-letter or independent semantic value.

## Method

1. Reconstruct every raw and reader-exact `sal`; publish all 33 exact contexts
   with line/paragraph position, two neighbours on each side and target-free
   functional axes.
2. Compare its line-edge rate with (a) every exact token, (b) all 76 forms with
   25--45 exact occurrences, (c) the 68 clean recurrent complete forms in the
   same GDT762 frequency band, and (d) the 350 exact H2 occurrences.
3. Count repeated immediate frames. Preserve `sal shol` and `sal raiin` only
   if each is reader-exact twice. Keep state labels and the target meaning
   separate.
4. Check the GDT762 polarity row, all 96 GDT759 amount pairs, bare
   `sal ain/aiin/aiiin`, and GDT760's one amount-expression contact.
5. Census all reader-exact complete surfaces containing the literal string
   `sal`. Report standalone, prefix, suffix and internal-string classes, plus
   an all-trigram productivity rank; string recurrence licenses at most a
   tentative non-exporting core echo.
6. Score eight prewritten semantic roles. The score is a transparent
   throughput weight, not a probability. Render the selected one-word default
   unchanged in all 33 occurrences; only German word order may vary in the
   twelve practical examples.
7. Revisit `chorcholsal` without changing its written-whole boundary. A
   successful `sal` role may strengthen the internal working explanation but
   may not split or export any EVA substring.

## Decision rule and claim ceiling

Select the highest diagnostic score only if it also explains both repeated
frames, is compatible with the quantity null, and needs no EVA-letter or sound
mapping. Retain any concrete rival that fits the same contexts but makes no
distinctive prediction.

GDT785 may assign `sal` a C1 portable nominal role and one C0 replaceable
one-word display. It may publish a C0 non-exporting core echo inside longer
surfaces and revise the explanation—but not necessarily the display—of
`chorcholsal`. It confirms no plaintext word, language, sound, glyph value,
specific substance, salt, seed, unit, plant, disease or cure.
