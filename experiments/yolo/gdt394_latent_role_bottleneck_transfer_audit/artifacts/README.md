# Artifacts

`gdt394_pre_score_freeze.json` binds the complete model family, dimensions,
folds, decoder, null, and gates before scoring. Final artifacts retain compact
domain/model scores, held-fold scores, null summaries, counterexamples, result,
and independent validation.
