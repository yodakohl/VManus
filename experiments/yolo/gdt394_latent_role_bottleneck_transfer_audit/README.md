# GDT394 — latent-role bottleneck transfer audit

Status: `FROZEN_BEFORE_SCORING`

Comparator-only audit of whether the anonymous GDT385/GDT387 role coordinate
is a portable one-dimensional compression, rather than impossible conditional
information absent from its own source features.

See `METHOD.md`, `SOURCE_AUDIT.md`, and `experiment.json`.
