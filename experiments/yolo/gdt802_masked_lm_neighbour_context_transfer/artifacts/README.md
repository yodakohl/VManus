# GDT802 artifacts

- `GDT802_4137_MASKED_NEIGHBOUR_ATLAS.tsv`: exact target-to-line joins and
  immediate complete neighbours.
- `GDT802_SPARSE_RIDGE_METRICS.tsv`: physical, stem, context and mixed model
  scores across three populations and three holdout schemes.
- `GDT802_RAW_IDENTITY_AUDIT.tsv`: transparent exact-identity and rarity
  comparator.
- `GDT802_SHARED_CONTEXT_COEFFICIENTS.tsv`: opaque recurrent context candidates;
  this is not a dictionary.
- The remaining compact tables record folds, capacity, predictions, the
  `daiin` correction, echo sensitivity, null diagnostics and adjudication.
