# GDT472 — every one of the 107 learned-address cards now has a template mode

## Result

The complete dictionary has one explicit assignment for every old label:

| assignment mode | labels | transferable after a form is supplied |
|---|---:|---:|
| learned function frame plus name slot(s) | 89 | 89 |
| general full-function zero-name template | 16 | 16 |
| exact-package-only full-function card | 2 | 0 |

All eighteen complete formula cards replay their exact old reading. Sixteen are
also reproduced completely with exact identity hidden: their general channels
cover every character and return the same ordered recipe and working phrase.

## The two package cards are now impossible to overgeneralize

`ykyd` retains the exact reading
`POSTEN · GEBEN · POSTEN · HIER` from `yky|d = Y+K+Y+D_ADDR`. With exact
identity hidden, the reader recovers only `yky=Y+K+Y`; final `d` becomes an
opaque drug-name residue. The whole four-part reading therefore belongs to the
exact `yky|d` package, not to a free final-`d` suffix rule.

`yddy` retains `POSTEN · HIER · POSTEN` from the overlapping package
`y|d|dy = Y+D_ADDR+Y`. The general directional reader finds no free channel at
all in this surface. Its two populated arms remain evidence for the exact card,
not a rule that can be copied onto another spelling.

Both cards have runtime exact-label rank 0, but are excluded from all future
surface, component and topology lookup maps. This preserves their useful
working meanings without making them productive stems.

## Complete empirical inventory

The 105 transferable labels form:

- 87 exact surface templates;
- 85 component/meaning templates;
- 20 slot topologies.

The sixteen zero-name surfaces and component templates are all individually
distinct. They therefore do not inflate the old recurrence result: fourteen
surface templates still recur over 32 labels, and fifteen component templates
over 35. Their four zero-name topologies do add useful structural coverage.
Fifteen of twenty topologies now recur and cover 100/105 transferable labels.

## Reader behavior

Exact `otainy` returns `DANACH · ANTEIL · POSTEN`, marks itself as a general
zero-name template and remains transferable. Exact `ykyd` returns its complete
four-part phrase but also reports `EXACT_PACKAGE_ONLY`, `transferable=NO` and
its GDT463 dependency. An unseen `otexeeon` still matches the four-owner
`ot{NAME}` frame, while an unmatched `zxqv` still becomes a whole owner-bound
plant name.

Validation passes 73/73 checks with a byte-identical rebuild. Four future-page
slots remain unreleased. No page, channel, meaning, spelling prediction,
occurrence, individual name, plaintext, language or confirmed lexeme is added.
