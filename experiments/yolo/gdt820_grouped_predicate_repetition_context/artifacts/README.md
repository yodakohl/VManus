# Artifacts

Core: `CONTEXTS.tsv` retains172 loci, all input origins and raw ZL metadata.
`BLOCKS.tsv` holds nine complete P streams and one complete C record.
`SOURCE_GROUPS.tsv` retains4734 whole raw groups in516 reader-loci;
`GROUP_COMPARISON.tsv` accounts for fragment losses and source-native lines.
`FULL_READER.md` shows group content, not leading locus-placement annotations
(those remain in CONTEXTS). All3readings retained; source groups are not words.
`REPETITIONS.tsv` preserves67 exact raw pair/readings, not67 independent events.
`RESULT.json` stores core counts, guard provenance and claim limits.

POST annex: `ASCENT_TRIALS.tsv` has12 displays/four records/14 exact raiin;
`ASCENT_RESULT.json` explicitly separates the two records added outside the core
and limits coverage. Unknown groups/opaque entities remain literal placeholders.

`VALIDATION.json`: independent source/annex reconstruction,7 synthetic checks,
7 core and2 annex negative mutations. No semantic or image validation.
