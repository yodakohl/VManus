# GDT665 — 148 residual family completion

Status: `PASS_994_TARGET_POSITIONS__V42_CONCRETE_RECIPE_REGISTER`

GDT665 gives concrete, replaceable readings to the 148 surfaces exposed by the
V41 one-hole frontier. It closes all 156 starting lines, distinguishes
initial/internal `s-` (seed) from absolute terminal `-s` (charge/species), and
adds a 47-row scoped stem model plus thirty manually smoothed recipe passages.

See `REPORT.md` for the interpretation, `METHOD.md` for the construction rules,
and `artifacts/RESULT.json` for exact dimensions and hashes.
