# GDT665 artifacts

`RESULT.json` and `VALIDATION.json` are the compact machine-readable entry
points. `TARGET_DECISION_DECK.tsv` contains all 148 defaults and rivals;
`FRONTIER_148_COMPLETIONS.tsv` contains all 156 closed starting lines;
`STEM_MODEL_V42.tsv` holds the 47 scoped stem roles; and
`MANUAL_PASSAGE_AUDIT.tsv` compares thirty manual recipe readings with the
automatic token-aligned channel.

The exhaustive occurrence, reader, dictionary, and coverage tables are kept
because they reproduce all 994 replacements, four accepted local reader joins,
the inherited practical revisions, the 189 newly complete lines, and the next
frontier without reopening sealed pages.
