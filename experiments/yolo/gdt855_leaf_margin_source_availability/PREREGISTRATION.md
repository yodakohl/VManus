# GDT855 preregistration

Freeze exact metadata-only columns/selectors/allow-values and old32+19+9exposure subtraction before queries. Root publishes and sends GO. No outcome/image access or new admission.
