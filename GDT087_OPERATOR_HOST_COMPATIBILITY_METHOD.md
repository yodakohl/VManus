# GDT087 — operator/host compatibility across matched O/Y hosts

Status: **YOLO formal grammar discovery**

Use the source-native HPR2 inventory and retain every PAGE_HOST pair
`o+TAIL / y+TAIL` for which each base has at least two manuscript occurrences.
This criterion is fixed without reference to wrapper outcomes.  Hold out one
physical folio at a time.

For each wrapper event `q`, `d`, `s`, and the pooled constructional wrapper
family `{ch,che,sh}`, predict binary presence from:

1. a `TAIL × register` baseline; and
2. the same baseline plus PAGE_HOST base `o/y`.

The base-specific estimate is shrunk toward the baseline with
`lambda in {1,4,16,64,256}`.  Report the full grid, the selected raw-gain
lambda, its five-way selector-paid gain, register contributions, and all
aggregate cells.  `NONE` is scored as a descriptive complement, not an
operator hypothesis.

This is parser-dependent formal compatibility, not a claim that wrappers or
PAGE_HOST pieces are linguistic morphemes.  GDT003 remains the controlling
negative result for general transformation algebra.  No semantic endpoint is
used.  f84r is rejected before analysis and remains sealed.
