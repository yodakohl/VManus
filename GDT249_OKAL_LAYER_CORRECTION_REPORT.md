# GDT249 — `okal` layer correction

## Result

**GDT248_OKAL_Q13_STARS_LEAD_WITHDRAWN_SOURCE_GROUP_PAGE_HOST_LAYER_CONFLATION**

The GDT248 `okal` ranking was wrong at the representation layer. The exact
f82r label/prose equality remains real, but HPR2 parses both occurrences as:

```text
okal -> PAGE_HOST ok + RIGHT_FAMILY al
```

GDT248 instead looked up the distinct PAGE_HOST `okal`, whose source forms
include an additional rendered layer (for example `okalal`). That produced the
spurious four-Stars-folio concentration.

The correct PAGE_HOST `ok` has 834 published parser-independent window
occurrences on 69 folios across six sections (B 289, S 334, H 139, T 59, P 10,
C 3). It is not a narrow q13↔Stars address. The proposed `okal` content-address
lead is therefore withdrawn.

## What survives

- GDT247's exact source-group equality at f82r.36 and f82r.6;
- its interpretation as a shared rendered group tuple crossing label/prose;
- `okaly` as a cross-register formal host and `olky` as a Currier-B-wide formal
  host, both still context-unstable and unglossed.

What does not survive is treating the visible source group as automatically
identical to PAGE_HOST. This is precisely why HPR2 layers must remain separate.

## Translation consequence

The corrected result favors distributed construction: the reusable f82r form
combines a ubiquitous host with a right-side rendering family. It supplies no
opaque content key and no q13↔Stars lexical bridge.

## Claim ceiling

Layer correction and exact rendered-tuple reuse only. No referent, function,
word, language, plaintext, or translation is established. No f84 input was
read or retained and no f84 result was scored.
