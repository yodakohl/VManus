# GDT236 — translation-frontier architecture checkpoint

## Leading theory

The best current generator is a **hybrid page-conditioned technical record
compiler**.  It is more specific than “abbreviated language,” but less specific
than a decipherment:

```text
PAGE := PAGE_PROFILE + REGISTER_INVENTORY + RECORD+
RECORD := ENTRY_STATE? + FIELD+ + PHYSICAL_CLOSE?
FIELD := WRAPPER? + FRAME? + OPAQUE_CONTENT + RIGHT_RENDERER? + CHECKPOINT?
GRAPHICAL_LABEL := TRANSFERRED_LABEL_PREFIX? + OPAQUE_REGISTER_RESIDUAL
```

The opaque content component may eventually prove lexical, abbreviated,
code-like, or distributed across the tuple.  Current diagnostics cannot choose
among those realizations.  What they do reject is the easy reading in which a
frequent exact PAGE_HOST or a stripped label residual is already a portable
object word.

## What is actually recovered

| layer | current status |
|---|---|
| page/register-conditioned inventories | strong structural prior |
| physical line as reset record/utterance-like unit | supported structure |
| wrapper/frame/right/DY/B3 system | formal record/rendering compiler |
| GDT233 graphical-label prefixes | partial transferred label-register compiler |
| q13 field lattice | complete abstract hypothesis scaffold; not semantic identification |
| PAGE_HOST | recurrent opaque identity; no transferable dictionary |
| stripped label residual | register-bound opaque material; no object-class transfer |
| source language/plaintext | unidentified |

GDT235 is the decisive new boundary.  On 703 family-covered human label rows,
held-folio exact residual lookup scores `.483` while the matched section
baseline scores `.721`.  On held-out q13/B, exact family, residual, and prefix
make zero correct coarse-object predictions on every covered row.  Thus the
GDT233 prefix is useful as compiler evidence, but its remainder is not a
portable object dictionary.

## Correction to the older executable theory

GDT181's f57/f77 four-quality decoder is no longer an executable semantic
fragment.  GDT182 showed that its fit is not above the searched feature
multiplicity; GDT184 removed the four-element interpretation of f57 R2; GDT185
rejected the direct 17-position bridge.  The page-local pattern remains a
descriptive candidate, but the active generator now contains **zero semantic
source-unit assignments**.

## Translation state

The manuscript is partially parsed at the level of document mechanics:
register, record, field, graphical-label rendering, and closure are plausible
layers.  q13 has a complete 701-field role lattice and a leading
therapeutic/hydraulic practical-record *document hypothesis*, but every field
still carries alternatives and every source identity is opaque.

The next advance cannot come from choosing a prettier substring.  It requires
one of six frozen discriminators in `gdt236_prediction_registry.tsv`, led by a
new independently owned referent repeated on two physical folios.  That target
will distinguish exact-host, stripped-residual, and full-tuple content models
without rewriting the compiler.

## Conclusion

**HYBRID_RECORD_COMPILER_LEADS_CONTENT_CHANNEL_UNRESOLVED.**

This is useful progress toward translation because it says where a future key
must act: beneath a transferable record/label-rendering layer, but not as a
simple global exact-host or exact-residual dictionary.  It does not identify a
word, semantic role, diagram value, sound, language, plaintext clause, or
translation.  No f84 input was read or retained, no f84 result was scored, and
no prediction targets f84.
