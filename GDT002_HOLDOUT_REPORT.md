# GDT002 holdout report

Status: **NOT RUN — f84r FORMAL PAYLOAD REMAINS UNOPENED**.

No role-world prediction was frozen. The source-only discovery atlas retained
zero identifiable hypotheses: its strict visual links were isolated one-group
labels and the only repeated family expressions did not distinguish roles.
Three bounded attempts to acquire a relation contrast then stopped before any
formal comparison:

- the first CONTACT/GAP panel failed its per-folio capacity gate on f89;
- the f88/f102 extension stopped at an uncertain editorial/visible unit
  boundary before review;
- the complete-array replication passed all censuses and obtained unanimous
  source-free calls, but f100v/L1 contained 0 CONTACT and 4 CLEAR_GAP, failing
  the preregistered every-array gate.

Because the discovery beam is empty and the visual augmentation gate failed,
opening f84r would invite postselection rather than test a frozen joint world.
`gdt002_frozen_holdout_prediction.json` therefore records an explicit
no-prediction and non-opening decision. `gdt002_holdout_results.json` records no
scores and no solver run. The existing f84r row-count/hash commitment remains
intact; its exact formal payload was not exported, inspected for GDT002,
joined, or scored.

This is an experimental outcome, not a translation result. It establishes no
semantic role, word, POS, sound, language, plaintext, meaning, or translation.
