# GDT261 — exposed f83r adjacent-paragraph transfer method

GDT260 nominates an attached-label component neighborhood concentrated in the
physically following paragraph. The only independent-folio case in the
published attachment inventory whose next locus is a paragraph start is
f83r.46 → f83r.47. Its human assertion is hedged: the label may be attached to
the right end of a tube. Its surface `olsaiin` and the f83r prose were already
exposed in GDT201/GDT222, so this is a falsifying sensitivity, not prospective
validation.

No f83r-specific all-reading STA-member projection exists among the safe
compact inputs used here. The sensitivity therefore operates at the visible
source-display layer and tests exactly three fixed representations:
`ol`, `saiin`, and full `olsaiin`. `ol` is the previously registered all-reading
GDT002 module; the other two are its literal remainder and whole group. Every
same-length within-token display window on the 25 published f83r prose lines is
positive at Hamming distance at most one. The target paragraph is mechanically
f83r.47–.49, bounded by paragraph starts f83r.47 and f83r.52.

Conditional on the number of positive page lines, enrichment in the three
target lines uses an exact hypergeometric tail. A factor of three covers the
fixed representations. This endpoint is not directly commensurate with
GDT260's STA-member test and cannot confirm it; a failure is nevertheless a
useful check against a robust visible component→adjacent-paragraph rule.

All inputs are published f84r-free derivatives. The GDT257 process-level
breach remains disclosed and no new f84r access occurs.
