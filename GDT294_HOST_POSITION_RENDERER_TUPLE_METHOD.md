# GDT294 — host-position renderer tuple

## Question

GDT293 established strong held-folio completion of the same-group renderer
tuple from exact opaque host identity.  GDT294 asks whether the remaining
variation has a reusable host-by-field-position structure, or instead needs a
larger host-local exception table.  This is the full-tuple successor to
GDT286/GDT291's wrapper-only position result.

## Frozen population and models

Use the eight f84-free native panels and only events whose exact host recurs on
another physical folio.  Predict the exact joint tuple
`wrapper|frame|inner-D|right|DY|B3` in held-folio folds with Dirichlet-1/2
smoothing and prior mass 11.  Nest these fixed models:

1. `LAYOUT_CONTEXT`: section, Currier, hand, register, within-field position,
   record/field ordinal buckets, physical group position, host length;
2. `BOUNDARY_CONTEXT`: add physical line close, paragraph close, and known
   label renderer;
3. `EXACT_HOST`: add opaque PAGE_HOST identity;
4. `HOST_X_POSITION`: add exact host by within-field-position counts;
5. `HOST_X_RECORD_SLOT`: add exact host by within-field position, record and
   field buckets, and physical group position.

The primary effect is `EXACT_HOST - HOST_X_POSITION`.  The record-slot
increment is fixed secondary evidence.  No host glyph, substring, neighboring
host, meaning, or target-folio outcome history is available.

## Frozen null and decision

After held-folio probability vectors are frozen, permute renderer outcomes
within exact `folio × host × section × Currier × hand × register × record
bucket × field bucket × physical group position × host length × line close ×
paragraph close × label renderer` strata.  This retains the exact host and
baseline opportunity while destroying within-field-position alignment.  Use
64 worlds and standardized max-eight over panels with positive variance.

Run prior 5/22 and held-section/held-hand Voynich sensitivities.  Call
`HOST_POSITION_RENDERER_TUPLE_SUPPORTED` only if the primary gain is positive,
at least 60/91 folios are positive, held-section and held-hand gains are
positive, and corrected `p <= .05`; otherwise call
`HOST_POSITION_RENDERER_TUPLE_WEAK_OR_LOCAL`.

## Claim ceiling and seal

At most this identifies a host-specific positional renderer distribution.  It
cannot establish a productive morphological rule, lexical class, word, code
value, language, meaning, plaintext, or translation.  Only the published
f84-free native event inventory is read.  No f84 row may be opened, parsed,
retained, joined, or scored.
