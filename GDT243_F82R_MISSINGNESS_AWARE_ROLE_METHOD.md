# GDT243 — f82r missingness-aware abstract-role projection

## Question

Can any GDT176 abstract role-like class be retained on the 51 GDT241 fields
when every unparsed physical line is allowed an unknown field count?

## Frozen uncertainty model

Use the three corrected GDT242 paragraphs.  A covered line contributes its
exact GDT241 field count.  Each unparsed prose line contributes between one
field and the maximum source-group count recorded in any of ZL3b/IT2a/RF1b.
For every covered field, enumerate every feasible aggregate number of missing
fields before and after it.  Apply the unchanged GDT224 implementation of the
GDT176 readable-recipe position/length model.

Retain an abstract class only when every feasible coordinate gives the same
abstract class.  Otherwise emit `UNRESOLVED_MISSINGNESS_SENSITIVE`.  These are
role analogies, not semantics.
