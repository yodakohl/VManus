# GDT116 — Q20 OPEN/BODY wrapper-channel map

Status: `INHERITED_POST_GDT115_LOCALIZATION`

GDT115 established an anonymous OPEN-compiler → BODY-compiler link under a
same-page exact-length null.  Its largest visible subcomponent was wrapper →
wrapper.  GDT116 localizes that inherited channel without changing the panel,
parser, held-folio split, nuisance baseline, or sealed-material policy.

For each of seven HPR2 wrappers (`q,d,s,ch,che,sh,t`), fit every one of the 49
OPEN-wrapper → BODY-wrapper scalar predictions.  Ridge is fixed at 1000 because
all 312 nested GDT115 block fits selected 1000; it is not retuned.  Each held
folio baseline already has record shape and the other records' BODY profiles.

Use 4,096 same-page/exact-OPEN-member-count permutation worlds, shared across
all cells.  Report local and max-49 p-values.  Also predeclare the identity
composite (each OPEN wrapper predicts its same BODY wrapper) and enumerate all
7! wrapper-output bijections as a descriptive mapping diagnostic.  The best
bijection is postselected and cannot establish grammar or meaning.

ZL3b is primary; IT2a/RF1b are reading sensitivities.  f84r is excluded before
retention and receives no query or prediction.  No wrapper meaning, word,
morpheme, POS, sound, language, plaintext, meaning, or translation is assigned.
