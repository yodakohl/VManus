# GDT044 — OKAM terminal-M attribution

## Question

GDT037 nominated `OKAM` as a mixed nonclosure B/S host. GDT038 showed that
its wrapper/state mixture changes by section. This pass asks whether its
strong apparent late placement is specific to exact `OKAM`, or inherited from
the independently established terminal-M positional system.

Only fully retained GDT016 physical lines in Herbal-B and S/B are scored.
Exact `OKAM` is tested unconditionally and then within terminal-M,
terminal-AM, and terminal-OK...M parent pools. Outcomes are physical line end
and membership in the final open field. Placement is permuted exactly inside
each physical line while the parent pool and number of OKAM forms are fixed.

Only six of sixteen GDT038 OKAM occurrences lie on fully retained lines; the
other ten remain reported context observations but are not treated as known
physical endpoints.

This is formal attribution only. No function, meaning, word, morpheme, POS,
sound, language, plaintext, or translation is assigned. f84r remains sealed.
