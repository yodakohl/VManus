# Risky predictions

1. At frozen locus `f1r.1`, require finite `wrong_source_bits > actual_source_bits` under the exact wrong-form rule in `reverse_generation.tsv`; a non-finite score, equality, or reversal kills this prediction.
2. At frozen locus `f1r.2`, require finite `wrong_source_bits > actual_source_bits` under the exact wrong-form rule in `reverse_generation.tsv`; a non-finite score, equality, or reversal kills this prediction.
3. At frozen locus `f1r.3`, require finite `wrong_source_bits > actual_source_bits` under the exact wrong-form rule in `reverse_generation.tsv`; a non-finite score, equality, or reversal kills this prediction.
4. At frozen locus `f1r.4`, require finite `wrong_source_bits > actual_source_bits` under the exact wrong-form rule in `reverse_generation.tsv`; a non-finite score, equality, or reversal kills this prediction.
5. At frozen locus `f1r.5`, require finite `wrong_source_bits > actual_source_bits` under the exact wrong-form rule in `reverse_generation.tsv`; a non-finite score, equality, or reversal kills this prediction.
6. At frozen locus `f75r.1`, require finite `wrong_source_bits > actual_source_bits` under the exact wrong-form rule in `reverse_generation.tsv`; a non-finite score, equality, or reversal kills this prediction.
7. At frozen locus `f75r.2`, require finite `wrong_source_bits > actual_source_bits` under the exact wrong-form rule in `reverse_generation.tsv`; a non-finite score, equality, or reversal kills this prediction.
8. At frozen locus `f75r.3`, require finite `wrong_source_bits > actual_source_bits` under the exact wrong-form rule in `reverse_generation.tsv`; a non-finite score, equality, or reversal kills this prediction.
9. At frozen locus `f75r.4`, require finite `wrong_source_bits > actual_source_bits` under the exact wrong-form rule in `reverse_generation.tsv`; a non-finite score, equality, or reversal kills this prediction.
10. At frozen locus `f75r.5`, require finite `wrong_source_bits > actual_source_bits` under the exact wrong-form rule in `reverse_generation.tsv`; a non-finite score, equality, or reversal kills this prediction.
