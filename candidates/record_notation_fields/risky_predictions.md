# Risky predictions

1. At frozen locus `f1r.1`, require a future independent conditional reverse scorer for this exact exported decoder to return finite actual and wrong-form costs with `wrong_source_bits > actual_source_bits`; absence, equality, or reversal kills this prediction and the candidate.
2. At frozen locus `f1r.2`, require a future independent conditional reverse scorer for this exact exported decoder to return finite actual and wrong-form costs with `wrong_source_bits > actual_source_bits`; absence, equality, or reversal kills this prediction and the candidate.
3. At frozen locus `f1r.3`, require a future independent conditional reverse scorer for this exact exported decoder to return finite actual and wrong-form costs with `wrong_source_bits > actual_source_bits`; absence, equality, or reversal kills this prediction and the candidate.
4. At frozen locus `f1r.4`, require a future independent conditional reverse scorer for this exact exported decoder to return finite actual and wrong-form costs with `wrong_source_bits > actual_source_bits`; absence, equality, or reversal kills this prediction and the candidate.
5. At frozen locus `f1r.5`, require a future independent conditional reverse scorer for this exact exported decoder to return finite actual and wrong-form costs with `wrong_source_bits > actual_source_bits`; absence, equality, or reversal kills this prediction and the candidate.
6. At frozen locus `f75r.1`, require a future independent conditional reverse scorer for this exact exported decoder to return finite actual and wrong-form costs with `wrong_source_bits > actual_source_bits`; absence, equality, or reversal kills this prediction and the candidate.
7. At frozen locus `f75r.2`, require a future independent conditional reverse scorer for this exact exported decoder to return finite actual and wrong-form costs with `wrong_source_bits > actual_source_bits`; absence, equality, or reversal kills this prediction and the candidate.
8. At frozen locus `f75r.3`, require a future independent conditional reverse scorer for this exact exported decoder to return finite actual and wrong-form costs with `wrong_source_bits > actual_source_bits`; absence, equality, or reversal kills this prediction and the candidate.
9. At frozen locus `f75r.4`, require a future independent conditional reverse scorer for this exact exported decoder to return finite actual and wrong-form costs with `wrong_source_bits > actual_source_bits`; absence, equality, or reversal kills this prediction and the candidate.
10. At frozen locus `f75r.5`, require a future independent conditional reverse scorer for this exact exported decoder to return finite actual and wrong-form costs with `wrong_source_bits > actual_source_bits`; absence, equality, or reversal kills this prediction and the candidate.
