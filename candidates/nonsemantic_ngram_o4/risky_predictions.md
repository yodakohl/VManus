# Risky predictions

1. Freeze this decoder and require the mapping/record state used at the hash-selected occurrence bucket 01 to recur without a new exception; any incompatible occurrence kills this prediction.
2. Freeze this decoder and require the mapping/record state used at the hash-selected occurrence bucket 02 to recur without a new exception; any incompatible occurrence kills this prediction.
3. Freeze this decoder and require the mapping/record state used at the hash-selected occurrence bucket 03 to recur without a new exception; any incompatible occurrence kills this prediction.
4. Freeze this decoder and require the mapping/record state used at the hash-selected occurrence bucket 04 to recur without a new exception; any incompatible occurrence kills this prediction.
5. Freeze this decoder and require the mapping/record state used at the hash-selected occurrence bucket 05 to recur without a new exception; any incompatible occurrence kills this prediction.
6. Freeze this decoder and require the mapping/record state used at the hash-selected occurrence bucket 06 to recur without a new exception; any incompatible occurrence kills this prediction.
7. Freeze this decoder and require the mapping/record state used at the hash-selected occurrence bucket 07 to recur without a new exception; any incompatible occurrence kills this prediction.
8. Freeze this decoder and require the mapping/record state used at the hash-selected occurrence bucket 08 to recur without a new exception; any incompatible occurrence kills this prediction.
9. Freeze this decoder and require the mapping/record state used at the hash-selected occurrence bucket 09 to recur without a new exception; any incompatible occurrence kills this prediction.
10. Freeze this decoder and require the mapping/record state used at the hash-selected occurrence bucket 10 to recur without a new exception; any incompatible occurrence kills this prediction.
