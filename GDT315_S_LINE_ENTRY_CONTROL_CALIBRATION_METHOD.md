# GDT315 — `s` line-entry control calibration

Freeze GDT314 unchanged on Voynich.  For every other already frozen
GDT278 control, construct exact opaque
`PAGE_HOST × frame × inner-D × right × DY × B3` cells with the identical
eligibility rule: at least two `s` and two non-`s` events, each on at least two
physical folios.  A panel is powered at at least five cells, 100 events, five
folios, and 20 `s` events.

The powered controls are arbitrary local codebook, compositional technical
notation, hybrid shorthand, IForal graphematic, Latin-15c graphematic, Latin
medical graphematic, and Latin scholastic graphematic.  Run the unchanged
ridge-10 exact-cell versus cell-plus-physical-line-start LOFO instrument,
including the same fixed-crossfit alignment diagnostic.  Do not rescale or
tune any panel.

Report raw gain, cell/register-matched delta, positive coefficient folds,
positive folios, and Voynich rank.  `s` is called Voynich-enriched only if it
ranks first on both held gain and matched delta; it is called non-specific if
at least two controls equal or exceed its held gain.  Otherwise report mixed.

This calibrates a formal surface-choice behavior only.  Control parser cells
are observation-layer analogues, not proof of common linguistic function.  No
meaning, sound, language identity, plaintext, or translation is assigned.  No
f84 row may be opened, parsed, retained, joined, or scored.
