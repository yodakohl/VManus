# GDT071 — behavior-class exact-host ablation

Status: **YOLO postselected robustness audit**

Take the nine `INTERESTING_EXPLORATORY` behavior-class rows from GDT069.
Rebuild their fold-safe memberships exactly.  For every exact PAGE_HOST that
appears in at least two feature-positive loci, remove every locus containing
that host and recompute the folio×human-unit conditional effect.  Also report
explicit `d` and `ok` deletions.

This tests whether a structural class lead is merely an exact-host effect.
It does not remove the original outcome/threshold postselection and therefore
cannot confirm an external association.  No semantic class, role, gloss, word,
morpheme, POS, sound, language, plaintext, meaning, or translation is assigned.
f84r remains sealed.
