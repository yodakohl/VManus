# GDT316 — fresh-surface `q` post-DY generalization

Exclude every exact surface used by a GDT303 `NONE->q` pair and every surface
in the prospective GDT306 panel.  On the remaining corpus, freeze every exact
opaque `PAGE_HOST × frame × inner-D × right × DY × B3` cell with at least two
`q` and two non-`q` events, each class on at least two physical folios.  The
score-blind panel has 36 cells, 450 events, 137 `q` choices, and 82 folios.

In leave-one-folio-out folds, compare ridge-10 exact-cell priors against exact
cell plus the immediately preceding physical group's DY-closure indicator.
Report held log2 loss, fold coefficient direction, folio/section contributions,
and an 8,192-world fixed-crossfit cell/register alignment diagnostic.  The
diagnostic is not called an exact retrained null.

Call `Q_POST_DY_EXTENDS_TO_FRESH_SURFACES` only if held gain and matched delta
are positive, at least 65 of 82 fold coefficients are positive, at least two
of B/H/S contribute positive held gain, and diagnostic p is at most .05.

At most this establishes a `q`-choice post-DY tendency across new exact cells.
It assigns no morpheme, POS, meaning, sound, language, plaintext, or
translation.  No f84 row may be opened, parsed, retained, joined, or scored.
