# GDT068 — PAGE_HOST behavior-profile external-information test

Status: **YOLO exploratory, archived outcomes are hypothesis-generation only**

Test whether source-native PAGE_HOST classes defined by their distributional
record behavior predict archived local visual/content axes better than raw or
host strings.  Use only exact annotated loci for which every PAGE_HOST occurs
on at least two non-f84 physical folios in the prose inventory.  In every held
folio fold, rebuild host profiles after removing that folio.

Profiles contain no literal host identity.  Predeclared variants are:

- SELF: wrapper, inner-D, O/OT frame, RIGHT_FAMILY, DY, B3, and position;
- SELF+NEIGHBOR: SELF plus preceding/following compiler states;
- SELF+NEIGHBOR-NOPOS: the previous profile without position;
- RAW_CHAR3, PAGE_HOST_CHAR3, and COMPILER_ONLY baselines.

Use the same low-capacity five-neighbour, section/Currier/hand/kind/unit/length
nuisance scaffold as GDT059.  Axes are correlated and results are descriptive
rankings.  Profiles are formal constructions, not semantic classes.  No role,
gloss, word, morpheme, POS, sound, language, plaintext, meaning, or translation
is assigned.  f84r remains sealed.
