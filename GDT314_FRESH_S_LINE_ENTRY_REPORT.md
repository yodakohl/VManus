# GDT314 — fresh-surface `s` line-entry generalization

Status: **S_LINE_ENTRY_EXTENDS_TO_FRESH_SURFACES**.

Every exact surface used by GDT303 `ch->s` or `d->s` is excluded. The remaining panel contains 15 cells, 344 events, 35 `s` choices and 78 folios.

The cross-fitted exact-cell plus line-start model changes held log loss by +0.041502 bits/event. The cell/register-matched held line-start delta is +0.287; raw `s` rates are 31.4% at line start and 4.7% elsewhere.

Line-start coefficients are positive in 78/78 folio folds; 61/78 folios have positive local gain. Powered-section gains are B=+0.0694, H=+0.0498, S=+0.0238. The fixed-crossfit alignment diagnostic is p=0.000122055413.

This is the first surface-disjoint extension of the `s` physical-entry tendency. It still conditions on cells already known to admit `s`; it does not predict a new license.

## Claim ceiling

Fresh-surface physical-line-entry tendency only; no morpheme POS meaning sound language plaintext or translation. No f84 row was opened, parsed, retained, joined, or scored.
