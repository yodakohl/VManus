# GDT050 — KAIIN construction-family decomposition

GDT047 left `KAIIN` as the only residual besides OKAIR with positive
leave-folio enrichment against both broad controls. GDT048 showed why a whole
host must be tested inside its construction family. This pass applies that
test to `KAIIN`.

From the GDT016 strict group inventory, skip f84r before parsing and retain
non-DY, non-terminal-M groups in Herbal A, Herbal B, Stars/Recipe B, and other
Currier B. Parse the longest member of `AIIN/AIR/AIN/AR/AL` from the right.
Keep every observed outer wrapper because KAIIN occurs both bare and under the
established carrier family.

Three exact two-sided Fisher tests are frozen:

1. base `K` versus all other bases in target (HB+S/B) against controls;
2. `AIIN` versus the other four right members within base K; and
3. exact `K+AIIN` versus every other cell in the construction panel.

KAIIN remains a privileged residual only if at least one family-conditioned
test supports target enrichment. Failure attributes the broad-rank lead to
ordinary base/right-family prevalence, not to a content meaning.
