# GDT135 — section-B adjacent field-architecture transfer

Status: `FROZEN_POSTSELECTED_BEFORE_TARGET_ARCHITECTURE_EXTRACTION`

## Motivation and chronology

GDT134 prospectively rejected manuscript-general transfer of the GDT132 raw
string trace.  Its already-exposed section decomposition nevertheless found a
small `COMPILER12` lead concentrated in section B.  GDT135 is an explicitly
postselected YOLO follow-up.  It cannot validate that observed effect.  It asks
a new question before the target compiler endpoint is extracted: does the
final field of one ordinary section-B continuation predict the *entry and
closure architecture* of the immediately following continuation field?

## Frozen panel

Use exactly the 69 section-B, continuation-to-continuation rows already listed
in `gdt134_general_continuation_inventory.tsv` (17 pages, nine physical
folios).  Every f84 page and all Q20 training folios are excluded by the GDT134
guarded selection.  No visual, semantic, lexical, or historical-language
field enters selection.

## Frozen target

Parse the next line's first HPR2 field only after this freeze.  The target is
the fixed 18-dimensional compiler architecture:

- first-cell wrapper: `NONE/q/d/s/ch/che/sh/t` (8);
- first-cell frame: `NONE/O/OT` (3);
- first-cell RIGHT-present, inner-D, DY, and B3 flags (4);
- field closure: `DY/B3/OPEN` (3).

The four field-count indicators are deliberately excluded.  Exact target
first-field group count, already public in GDT134, is an opportunity variable
in the reference and null.

## Frozen predictors and evaluation

Use fixed HPR2 definitions, SHA-32 trigrams, ridge `lambda=1000`, and physical-
folio leave-one-out refitting.  The reference contains source/target group
opportunities, source member count, final-field group/host/raw lengths,
source-line position and parity.  Compare exactly three additions from the
source final field:

1. `COMPILER12` — primary;
2. `HOST_CHAR3` — stripped PAGE_HOST control;
3. `RAW_CHAR3` — raw string-statistical control.

Report total, per-folio, source-line-parity, and the four fixed target-block
contributions.  Block contributions are descriptive partitions, not extra
hypotheses.

## Frozen null and gates

Refit every fold in 4,096 shared representation-permutation worlds.  The exact
null preserves:

`page × exact source-group count × final-field group count × PAGE_HOST length
× raw-token length × exact target first-field group count`.

The coarse diagnostic preserves:

`page × exact source-group count × exact target first-field group count`.

The exact null must retain at least 30 swappable records.  A transferable
section-B architecture lead requires `COMPILER12` gain above zero, gain above
both string controls, positive gain on at least six of nine held folios,
positive gain in both nonoverlapping source-line parity subsets, and exact
max-three `p<=.05`.  Insufficient exact capacity is reported, not repaired.

## Claim ceiling

A pass would establish only a section-B formal adjacent-field transition at
the frozen HPR2 resolution.  It would not establish a record meaning, heading,
recipe, semantic role, word, morpheme, POS, sound, language, plaintext,
translation, or content gloss.  This is postselected hypothesis generation,
not confirmation.  No f84 material may be retained, parsed, joined, or scored.
